# RTM — Requirements Traceability Matrix (001-remote-workflow-engine)

Generated by the validator (Gate 7.5) directly via trace.py's scan()/analyze()/build_matrix() module functions.
Note: this repo's trace.py ships no --rtm CLI flag (the role-contract command `trace.py --rtm` does not exist here; a plugin/tool version gap across all 21 prior iterations, none of which produced an rtm.md). Reported as needs_clarification rather than silently invented.

| REQ | title | tiers reached | real:true verified |
|---|---|---|---|
| REQ-001 | Execute Claude-generated workflow JS files unmodified (100% API compatibility) | architecture, design, tasks, impl, verification | ✅ |
| REQ-002 | Workflow semantics: nesting, concurrency caps, budget accounting | architecture, design, tasks, impl, verification | ✅ |
| REQ-003 | Real agent execution per `agent()` via Claude Agent SDK | architecture, design, tasks, impl, verification | ✅ |
| REQ-004 | Multi-model routing (model aliases → configured providers via gateway) | architecture, design, tasks, impl, verification | ✅ |
| REQ-005 | MCP Streamable HTTP interface for run control | architecture, design, tasks, impl, verification | ✅ |
| REQ-006 | Lifecycle: suspend / resume / stop with durable state | architecture, design, tasks, impl, verification | ✅ |
| REQ-007 | Per-agent observability via MCP tools | architecture, design, tasks, impl, verification | ✅ |
| REQ-008 | Web dashboard for runs and agents | architecture, design, tasks, impl, verification | ✅ |
| REQ-009 | Sync-upload local skills (recursion-guarded) | architecture, design, tasks, impl, verification | ✅ |
| REQ-010 | Claude Code client plugin (install + guidance skill, conflict-free) | architecture, design, tasks, impl, verification | ✅ |
| REQ-011 | Deployable on local and remote Linux | architecture, design, tasks, impl, verification | ✅ |
| REQ-012 | OAuth 2.0: engine is its own authorization server, federating to Google as IdP (un-deferred, v15 Slice B) | architecture, design, tasks, impl, verification | ✅ |
| REQ-013 | Per-workflow work folder & per-run workspace | architecture, design, tasks, impl, verification | ✅ |
| REQ-014 | Named workflow registry (save, list, invoke by name; `workflow(name)` compat) | architecture, design, tasks, impl, verification | ✅ |
| REQ-015 | Execution modes: cron schedule, one-shot timed, resident user-triggered | architecture, design, tasks, impl, verification | ✅ |
| REQ-016 | Non-Anthropic models run the full agent harness (tool loop + MCP + skills) via the SDK gateway | architecture, design, tasks, impl, verification | ✅ |
| REQ-017 | MCP tools provisioned server-side (registry), referenced by name, explicitly injected | architecture, design, tasks, impl, verification | ✅ |
| REQ-018 | Secrets for providers / MCP via a server-side store, never workspace-reachable | architecture, design, tasks, impl, verification | ✅ |
| REQ-019 | Hooks are explicitly unsupported (uploaded user hooks removed) | architecture, design, tasks, impl, verification | ✅ |
| REQ-020 | SDK gateway path bounds a hung agent LLM call (timeout + retries) | architecture, design, tasks, impl, verification | ✅ |
| REQ-021 | No ambient host-project CLAUDE.md / auto-memory leaks into agent context (workRoot isolation) | architecture, design, tasks, impl, verification | ✅ |
| REQ-022 | Recursively list a run's workspace artifacts with size + sha256 | architecture, design, tasks, impl, verification | ✅ |
| REQ-023 | Fetch a windowed, size-capped, realpath-contained chunk of a workspace file | architecture, design, tasks, impl, verification | ✅ |
| REQ-024 | Bound the HTTP request-body size (413) | architecture, design, tasks, impl, verification | ✅ |
| REQ-025 | Seed files into the run workspace before agents start, with `.claude` RCE strip | architecture, design, tasks, impl, verification | ✅ |
| REQ-026 | Run-workspace retention: manual purge (terminal-only) + opt-in TTL GC | architecture, design, tasks, impl, verification | ✅ |
| REQ-027 | `issue_report` MCP tool files a structured GitHub issue | architecture, design, tasks, impl, verification | ✅ |
| REQ-028 | GitHub token via server-side secret, never workspace-reachable | architecture, design, tasks, impl, verification | ✅ |
| REQ-029 | Structured, agent-consumable issue body + labels for the solve-flow | architecture, design, tasks, impl, verification | ✅ |
| REQ-030 | Bounded, typed-error GitHub API call (never hangs or crashes the engine) | architecture, design, tasks, impl, verification | ✅ |
| REQ-031 | `issue_get` reads a single issue | architecture, design, tasks, impl, verification | ✅ |
| REQ-032 | `issue_list` finds issues (for the solve-flow to pick up work) | architecture, design, tasks, impl, verification | ✅ |
| REQ-033 | `issue_comments` reads an issue's replies | architecture, design, tasks, impl, verification | ✅ |
| REQ-034 | `issue_comment` posts a reply to an issue | architecture, design, tasks, impl, verification | ✅ |
| REQ-035 | `issue_report` de-duplicates instead of spamming | architecture, design, tasks, impl, verification | ✅ |
| REQ-036 | `issue_report` auto-enriches from a runId | architecture, design, tasks, impl, verification | ✅ |
| REQ-037 | Provider-aware SDK routing + Anthropic dual auth (API key or subscription) | architecture, design, tasks, impl, verification | ✅ |
| REQ-038 | `openrouter` first-class provider + model passthrough | architecture, design, tasks, impl, verification | ✅ |
| REQ-039 | `models_list` unified, normalized, cross-provider catalog | architecture, design, tasks, impl, verification | ✅ |
| REQ-040 | `models_list` filtering to narrow the result | architecture, design, tasks, impl, verification | ✅ |
| REQ-041 | `workflow()` nesting supports N levels up to a configurable depth cap | architecture, design, tasks, impl, verification | ✅ |
| REQ-042 | ancestor-cycle guard on nested `workflow()` | architecture, design, tasks, impl, verification | ✅ |
| REQ-043 | total-descendant cap per run | architecture, design, tasks, impl, verification | ✅ |
| REQ-044 | cross-depth invariants preserved (shared budget + resume-safe journal) | architecture, design, tasks, impl, verification | ✅ |
| REQ-045 | each `agent()` node records the composite frame it ran in | architecture, design, tasks, impl, verification | ✅ |
| REQ-046 | each nested `workflow()` call is recorded as a composite-boundary node | architecture, design, tasks, impl, verification | ✅ |
| REQ-047 | `workflow_status` exposes enough to reconstruct the live call-tree (DAG) + drill to logs | architecture, design, tasks, impl, verification | ✅ |
| REQ-048 | pure `buildDagModel` reconstructs a run's call tree from its status | architecture, design, tasks, impl, verification | ✅ |
| REQ-049 | dashboard renders cards → live DAG → agent log | architecture, design, tasks, impl, verification | ✅ |
| REQ-050 | phase timeline with timestamps + current step | architecture, design, tasks, impl, verification | ✅ |
| REQ-051 | per-agent timing (started / ended / duration) | architecture, design, tasks, impl, verification | ✅ |
| REQ-052 | authoritative `onTerminal` hook fires once per terminal transition | architecture, design, tasks, impl, verification | ✅ |
| REQ-053 | durable on-completion chaining (run A completes → start run B) | architecture, design, tasks, impl, verification | ✅ |
| REQ-054 | run-admission counter bounds concurrent top-level runs | architecture, design, tasks, impl, verification | ✅ |
| REQ-055 | a terminated run's DAG (frames, phases, per-agent detail) survives a restart | architecture, design, tasks, impl, verification | ✅ |
| REQ-056 | Host/Origin allowlist on the HTTP server (DNS-rebinding / CSRF defense) | architecture, design, tasks, impl, verification | ✅ |
| REQ-057 | webhook ingress `POST /hooks/:id` fires a pre-bound workflow, HMAC-verified | architecture, design, tasks, impl, verification | ✅ |
| REQ-058 | webhook management tools with generate-once, hashed-at-rest secrets + bind safety | architecture, design, tasks, impl, verification | ✅ |
| REQ-059 | persisted journal read-back (resume-after-restart replays instead of re-running everything) | architecture, design, tasks, impl, verification | ✅ |
| REQ-060 | a run interrupted by a crash/restart is resumable, not permanently failed | architecture, design, tasks, impl, verification | ✅ |
| REQ-061 | a registered workflow's purpose is queryable (description + phases + full detail) | architecture, design, tasks, impl, verification | ✅ |
| REQ-062 | a workflow's DAG is inspectable BEFORE running it (static skeleton) | architecture, design, tasks, impl, verification | ✅ |
| REQ-063 | compressed request bodies + a typed, actionable too-large error | architecture, design, tasks, impl, verification | ✅ |
| REQ-064 | content-addressed blob store with byte-verify + per-namespace refs | architecture, design, tasks, impl, verification | ✅ |
| REQ-065 | assemble a run workspace from a CAS `seedManifest` (efficient seed) | architecture, design, tasks, impl, verification | ✅ |
| REQ-066 | every filed issue carries a version (repro / version / severity / analysis / log) | architecture, design, tasks, impl, verification | ✅ |
| REQ-067 | a dashboard page that DISPLAYS issues (open, per-issue status, resolved) | architecture, design, tasks, impl, verification | ✅ |
| REQ-068 | GitHub tag webhook (HMAC-verified) records an update request on a new version tag | architecture, design, tasks, impl, verification | ✅ |
| REQ-069 | privilege-separated updater: engine writes a flag, a systemd unit does the git/build/restart | architecture, design, tasks, impl, verification | ✅ |
| REQ-070 | fully-automatic apply, observable applied version, safe-fail (never leave the service down) | architecture, design, tasks, impl, verification | ✅ |
| REQ-071 | n8n-style graph view: trigger source node → agent boxes → edges, Morandi theme | architecture, design, tasks, impl, verification | ✅ |
| REQ-072 | composed workflows drawn as distinct Morandi-tinted, labeled frames | architecture, design, tasks, impl, verification | ✅ |
| REQ-073 | clickable agent box → harness detail (model, prompt, tools, skills, live status) | architecture, design, tasks, impl, verification | ✅ |
| REQ-074 | home cards: running / registered / other, with description + mini graph preview | architecture, design, tasks, impl, verification | ✅ |
| REQ-075 | each card shows average success rate + average execution time | architecture, design, tasks, impl, verification | ✅ |
| REQ-076 | system resource metrics (CPU / memory / disk) via a read-only MCP tool + dashboard | architecture, design, tasks, impl, verification | ✅ |
| REQ-077 | process metrics: the engine process + host Top-N processes + system-wide process stats | architecture, design, tasks, impl, verification | ✅ |
| REQ-078 | enriched model catalog: capability, stability, in/out modalities, cost level 0–10 | architecture, design, tasks, impl, verification | ✅ |
| REQ-079 | every new / changed MCP tool schema precisely self-describes params, options, and effects | architecture, design, tasks, impl, verification | ✅ |
| REQ-080 | engine-pull `seedRef:{repoUrl,sha}` behind a fail-closed egress allowlist (SSRF-safe) | architecture, design, tasks, impl, verification | ✅ |
| REQ-081 | raw HTTP body-streaming blob upload (context-free, past the JSON-RPC cap) | architecture, design, tasks, impl, verification | ✅ |
| REQ-082 | server-side seed manifest ref (the manifest itself never transits the caller) | architecture, design, tasks, impl, verification | ✅ |
| REQ-083 | provisioned secrets are redacted AT CAPTURE in the transcript | architecture, design, tasks, impl, verification | ✅ |
| REQ-084 | `asset_push` `kind` schema tells the truth about what materializes | architecture, design, tasks, impl, verification | ✅ |
| REQ-085 | optional `scriptSha256` integrity guard on `workflow_run({script})` | architecture, design, tasks, impl, verification | ✅ |
| REQ-086 | protected surfaces require a per-caller principal; every action is attributed to it | architecture, design, tasks, impl, verification | ✅ |
| REQ-087 | workflow ownership: only the creating principal can edit/deregister; others may run/read | architecture, design, tasks, impl, verification | ✅ |
| REQ-088 | registered workflows bind their default harness params at registration (not buried in script text) | architecture, design, tasks, impl, verification | ✅ |
| REQ-089 | D-BIND fail-closed: non-loopback callers without a valid principal are refused; loopback exempt | architecture, design, tasks, impl, verification | ✅ |
| REQ-090 | a workflow declares its tunable-parameter contract, discoverable without reading the script | architecture, design, tasks, impl, verification | ✅ |
| REQ-091 | per-run overrides are validated against the contract; locked configuration is unreachable from the caller | architecture, design, tasks, impl, verification | ✅ |
| REQ-092 | registered harness defaults actually take effect at run time (repairs the REQ-088 wiring gap) | architecture, design, tasks, impl, verification | ✅ |
| REQ-093 | `effort` is a real end-to-end parameter, not a documented no-op | architecture, design, tasks, impl, verification | ✅ |
| REQ-094 | a user-supplied `appendPrompt` attaches at a fixed position after everything the author controls | architecture, design, tasks, impl, verification | ✅ |
| REQ-095 | problem reports are bound to a specific workflow and filterable by it | architecture, design, tasks, impl, verification | ✅ |
