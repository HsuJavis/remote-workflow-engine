# Gate 8 architecture-consistency review — Quality-dimensions lens (v27, RE-REVIEW #4 pass)

- **Lens:** Quality-dimensions expert — Observability / Replaceability / Consumability / Self-sustainability
- **Iteration:** v27 (REQ-131..136, REQ-140, REQ-141; ARCH-122..131, ADR-049..056, INV-V27-1..9)
- **Compared:** `02-architecture.md` §"v27 slice" (lines 3318–3850, as amended at v27b and v27h) against `06-impl-log.md` IMPL-221..281 and the files those entries list (plus `src/dashboard/lib/status.js`, on no IMPL `files:` line but a key in `static-assets.ts:22`).
- **Tree:** `18b9c03`, working tree clean. Every line number below is from that tree. Supersedes the `ced73ff` version of this file (preserved in git history at `1149073`).
- **Scope of change since the prior pass, measured:** `git diff --stat ced73ff..HEAD -- src/ package.json tsconfig.json vitest.config.ts` → exactly **five** files — `lib/connection.js` (+6/−1), `ui/home.js` (+6/−1), `ui/run.js` (+16/−2), `ui/system.js` (+6/−1), `ui/workflow.js` (+11/−2) — plus five test files. Every server-side file this lens verified at `ced73ff` (`server.ts`, `mcp-facade.ts`, `run-manager.ts`, `agent-executor.ts`, both stores, `static-assets.ts`, `dashboard.ts`, `dashboard-page.ts`) is byte-identical, so those "verified consistent" lines are carried at unchanged code and not re-argued; the five changed files were re-read in full.
- **Method:** each BF closure re-opened on disk, not quoted from the IMPL row; the reducer reproduced with `node` against the shipped `lib/connection.js` (all-fail tick 1 from `live` → `degraded`/1; tick 2 → `offline`/2; `{ok, degraded}` → `degraded`; two 404 ticks → `offline`; `{}` from `offline` → `live`; `worstOf({a:'bogus'})` → `ok`); the agent panel's failed-fetch projection reproduced with `node` against `lib/agent.js` (see QD-O5); `lang === 'zh'` copy sites re-counted (17); `ls tsconfig*.json` and `package.json:8,11` re-checked.
- **Verdict:** `consistent: no` — **13 numbered deviations, composed as: 2 NEW/RESIDUAL since the BF-1..BF-6 repairs (`b124430`..`35e00fc`) — 1 MEDIUM, 1 LOW — plus 11 CARRIED — 2 MEDIUM (one of them UPGRADED from LOW on corrected evidence), 9 LOW — of which two enter this lens's numbered list from the reviewer's §9 for the first time (D3-3, D3-5).** 0 HIGH. QD2-O1 (the prior pass's one MEDIUM) is CLOSED for the two views it named and re-filed as a residual at its unswept third site (QD3-O1). Two declared-and-owed items (TASK-215/216) are listed in their dimension sections but NOT counted, per RE-REVIEW #1's "declared-and-owed is not violation" precedent. Findings without an ARCH/INV/ADR anchor are uncounted observations.

Severity key (unchanged): **HIGH** = an architecture decision's chosen control does not deliver the property the decision was taken for; **MEDIUM** = a stated `api:`/boundary/invariant clause is not what shipped, with an operator-visible or test-visible consequence; **LOW** = a clause deviation whose consequence is bounded or cosmetic.

---

## 0. Closures re-verified at `18b9c03`

RE-REVIEW #2 sent back BF-1/BF-2/BF-3; RE-REVIEW #3 sent back BF-4; the implementer then closed BF-5 and BF-6 on its own sweep. Each is re-opened here at file:line; none is taken on the repair's word.

| ID | Repair | Holds? | Evidence at `18b9c03` |
|---|---|---|---|
| BF-1 (≡ F-1 / QD-O1 residue) | IMPL-277 `b124430` | **YES** | `src/dashboard/lib/connection.js:39` `const status = consecutiveFails >= 2 ? 'offline' : 'degraded';` — `prev.status` is read nowhere in the function (only `prev.consecutiveFails`, `:34`); `:27-28` `live` iff `worstOf === 'ok'`; `:30-32` any non-unanimous mix → `degraded`, counter reset. **Reproduced:** `nextConnection({status:'live',consecutiveFails:0}, {results:{a:'fail',b:'fail'}})` → `{status:'degraded', consecutiveFails:1}`; a second identical tick → `{status:'offline', consecutiveFails:2}`. Lock: `tests/unit/dashboard-lib-connection.test.js:54-59` (the renamed case asserts `degraded` + `consecutiveFails 1`). ARCH-124 `api:` as amended v27h is satisfied on every arm. |
| BF-2 (≡ QD2-O1, the two named views) | IMPL-278 `b124430` | **YES — for `home.js` and `run.js`** (see QD3-O1 for the third site) | `src/dashboard/ui/home.js:231` `if (!body \|\| body.degraded \|\| !Array.isArray(body.running)) return;` ahead of any grid write (`HomeView.running` is always an array, `dashboard.ts:120`); `src/dashboard/ui/run.js:475` `if (!dagBody \|\| dagBody.degraded \|\| !Array.isArray(dagBody.cells)) return {};` ahead of the second fetch (`:477`) and every repaint. Locks: `val-198-shell-and-home.test.ts:262` (card still present two ticks after `/api/home` is intercepted), `val-200-swimlane.test.ts:215` (node-cell count unchanged after `/dag` is intercepted). |
| BF-4 | IMPL-279 `2552230` | **YES** | `src/dashboard/ui/system.js:78` `if (res.status !== 'ok')` — the one-token form the review required; `:72` `res` is `getJSON`'s `{status, body}` (`poll.js:51`), so the classifier's verdict is tested, not re-derived; the `else` branch (`:79`) paints the explicit `無法取樣` component. Lock: `val-202-ported-tabs.test.ts:130-166` intercepts `/api/system` with the whole-route shape `{runs:[], degraded}` and asserts panel text contains `無法取樣`, `.sys-table tr` count 0, `pageerror` empty across two ticks, and `.rwe-connection[data-status]` = `degraded`; the non-regression half (`:104-121`) now waits for `.sys-table tr`, not just `#system-panel`. |
| BF-5 | IMPL-280 `86617d7` | **YES** | `src/dashboard/ui/run.js:512` `renderLegend(state.shell.legend, payload, viewRes.status === 'ok' ? viewRes.body : null, state.lang)`. Lock: `val-200-swimlane.test.ts:255-283` (`.run-summary` absent, legend text free of `undefined`, no `pageerror`). |
| BF-6 | IMPL-281 `35e00fc` | **YES — for the `undefined` text it named** (see QD3-O1 for what the same lines still do) | `src/dashboard/ui/workflow.js:326` `const payload = dagRes.status === 'ok' ? dagRes.body : { cells: [], edges: [], warnings: [], lanes: [], current: null };` and `:329` `renderLegend(shell.legend, payload, viewRes.status === 'ok' ? viewRes.body : null, lang)`. Lock: `val-199-workflow-detail.test.ts:272-299`. |
| Prior closures (QD-C1/AC-1, QD-O2/AC-5, QD-O3/AC-6, QD-R1/AC-7, QD-S1/AC-8, AC-9, OWN-1, the v27h induced drifts) | v27g/v27h | **YES** | None of the files those closures live in changed since `ced73ff` (diff above); the `ced73ff` verification stands. |

**The class, in one sentence, for the reviewer's §10:** six fixes across four rounds have now closed "a non-`ok` body reaches a render function" at every `onTick`-level site, but the population the class actually spans is *every consumption of a `getJSON` result*, and two consumers outside the `onTick` guards still take a degraded result and paint something plausible from it — `workflow.js:326-328` (an empty figure, QD3-O1) and `agent-panel.js:234-266` (a panel of zeros, QD-O5).

---

## 1. Observability — transparency of internal state

The owner's stated first motive for v27 is observability (02-architecture.md §"Altitude split": 「以觀測性為主」), so this dimension is graded as the requirement. The engine-side seams are present and unchanged (see "verified consistent"). The connection TAG is now truthful on every arm (BF-1) and two of the three poll-tick views keep their last-known render on a degraded tick (BF-2). What remains is the same class in the two places the file lists did not reach.

### QD3-O1 — MEDIUM — NEW/RESIDUAL — On the workflow-detail page, a degraded `/dag` tick still repaints an EMPTY swimlane over the live one; BF-6 removed the `undefined` text from these lines and left their empty-figure fallback in place
- **Violates:** ARCH-125 `api:` (`getJSON` "classifies every response … a 200 body carrying a `degraded` string is `degraded`, never an exception and **never rendered as data**") — the same clause BF-2 was raised on, whose own title reads 「a degraded body is never rendered as data on the Workflows home **or a run's swimlane**」 (IMPL-278); ARCH-125 `note:` ("listeners are delegated … a tab left open for days" — the page an operator leaves open IS the workflow-detail page); the NFR-ownership paragraph ("connection / degraded honesty → ARCH-124 … fed by ARCH-125's one `getJSON` classifier"). Precedent: QD2-O1 → BF-2 (RE-REVIEW #2 §8) adjudicated this exact consequence — "a confident empty state beside a truthful degraded tag" — as blocking-class for `home.js` and `run.js`.
- **Evidence:**
  - `src/dashboard/ui/workflow.js:318` — `paintSelected` fetches `/dag` and `/api/runs/:id` itself (state-dependent, so `app.js`'s `bodies` map never sees them and `onTick`'s AC-4 guard at `:357` cannot cover them).
  - `:326` — on `dagRes.status !== 'ok'` the fallback is a synthesized `{cells:[], edges:[], warnings:[], lanes:[], current:null}`; `:328` hands it to `paintSwimlane`, which does `svgEl.replaceChildren()` (`run.js:219`) and `layer.replaceChildren()` (`run.js:222`) and then draws zero lanes, zero edges, zero cells; `:329` hands the same payload to `renderLegend`, which `replaceChildren()`s the legend (`run.js:354`) and, with `view === null`, appends nothing. Net: the figure, its lane headers and its legend are wiped for that tick, with no marker of any kind — no 「無法取樣」, no `predictedLayoutUnavailable`, nothing.
  - The BF-6 test's own comment concedes it: `tests/acceptance/val-199-workflow-detail.test.ts:268-270` — 「`paintSwimlane`'s own internal `Array.isArray` guards already neutralize `:319`'s malformed `payload` for the swimlane itself (**an empty repaint either way**)」 — and the case (`:272-299`) asserts only that `.run-summary` is absent and the legend text lacks `undefined`; it never asserts a node cell survives, so the empty repaint ships CI-green under a test titled for this route's degrade.
  - IMPL-281's sweep table marks `workflow.js:326` as "guarded — verdict" — guarded against the `undefined` text, yes, but the three guard shapes now in the tree are not equivalent: BF-2 **bails** (last-known render stays, `home.js:231`, `run.js:475`); BF-4 paints an **explicit** degraded component (`system.js:79`); `:326` synthesizes a **plausible empty state** with no marker — the one shape ARCH-125's clause forbids.
  - Before BF-6 the same lines produced the same empty repaint (`dagRes.body || {…}` → a truthy degraded object → `paintSwimlane`'s `Array.isArray` → `[]`), so this is not a regression BF-6 introduced; it is BF-2's class at the one `paintSwimlane` call BF-2's file list did not name.
- **Failure scenario:** an operator watches a running workflow on `/dashboard/workflow/<name>` (the page the run chips and history table live on); the store read for `/dag` fails for one tick → the tag flips to 部分異常 (correct: `paintSelected` returns `dagRes.status` at `:330`, and `worstOf` reports it) and, in the same paint, every lane header, node and edge disappears and the legend goes blank; the next healthy tick repaints. Beside the empty figure the history table and run chips (fed by `/api/runs`, which was `ok`) stay fully populated — a page that says "this run has no structure" next to a table that says it has nine runs.
- **Why MEDIUM and not HIGH:** the tag is truthful, so it is the operator's page, not their signal, that lies for a tick — the same bound QD2-O1 carried. Fix shape is already in the tree twice: either bail before any DOM write when `dagRes.status !== 'ok'` (BF-2's form: `if (dagRes.status !== 'ok') return { [dagUrl]: dagRes.status, [viewUrl]: viewRes.status };` ahead of `:326`, last-known figure stays), or paint an explicit marker (BF-4's form). One real-browser case on val-199 asserting `[data-node-cell]` count is unchanged after `/dag` is intercepted — the `val-200:215` recipe pointed at the workflow route.

### QD3-O2 — LOW — NEW/RESIDUAL — On the run page, a degraded `/api/runs/:id` tick clears `#run-usage` and silently swaps every node's row-2 model from APPLIED to DECLARED
- **Violates:** ARCH-125 `api:` (`run.js` — "the legend row and the right-aligned run summary"; `#run-usage` is a C2 anchor this layer must keep meaningful); ARCH-125 `note:` ("REQ-134 row 2 … the APPLIED model rides on the run view's own `agents[]`" — `run.js` banner, `:33-38`); ADR-046 / INV-V26-6 ("never a silent zero") read for structure rather than money.
- **Evidence:** `src/dashboard/ui/run.js:513` `renderUsageBox(state.shell.usage, viewRes.body && viewRes.body.usage, state.lang)` — a degraded body has no `usage`, so `renderUsageBox` runs `usageEl.replaceChildren()` (`:375`) and returns (`:376`): the four-column token line and cost vanish for that tick. `:502` `agentsById = new Map(((viewRes.body && viewRes.body.agents) || []) …)` → empty → `:313` `model = (rec && rec.model) || (declared.model && declared.model.default)` falls back to the DECLARED default on every cell, rendered identically to an applied model (`:321`). BF-5's test (`val-200:255-283`) asserts `.run-summary` only. IMPL-280's own note says the legend keeps "no summary rather than old summary retained"; it does not mention `#run-usage` or row 2.
- **Why LOW:** the swimlane itself survives (BF-2's guard at `:475` is on the `/dag` body, which was `ok`); the usage row is a second rendering of a number the legend also carries; the row-2 swap is honest by the banner's own fallback rule when the applied model is genuinely unknown, just not when it is transiently unfetched. Bounded to one tick. Same fix as QD3-O1's bail form, or: pass `viewRes.status === 'ok' ? viewRes.body : state.lastView` and keep the last-known view.

### QD-O5 — MEDIUM (UPGRADED from LOW on corrected evidence) — CARRIED (§9) — A failed or degraded agent-detail fetch renders a panel of confident zeros and a false sentence, because `openAgentPanel` synthesizes a record with `tokens:{input:0,output:0}` and never reads `res.status`
- **Violates:** the NFR-ownership paragraph ("*cost honesty* → … ADR-046 / INV-V26-6 unchanged: **never a silent zero**"); the Altitude-split paragraph ("the slide-in panel IS the agent's inspectability"); ARCH-125 `api:` (`agent-panel.js` — "six stat cards, … the red `detail` block"); ARCH-125 `api:`'s `getJSON` clause ("never rendered as data").
- **Evidence (corrected — the prior pass wrote "six stat cards of `—`", which is wrong for the Tokens card and the three tag columns):**
  - `src/dashboard/ui/agent-panel.js:233` fetches; `:234` `const body = res.body || {};` — `res.status` is never read anywhere in the function; `:238` synthesizes `record = { …, state: '', provider: '', model: '', tokens: { input: 0, output: 0 } }` when `body.record` is absent, i.e. on a 404 (`{error:'Agent not found: …'}`, `server.ts:594-597`), on a degraded body, and on a network `fail` alike.
  - **Reproduced with `node` against the shipped `lib/agent.js`** (`panelModel(record, null, [], false, now, 'zh')` with that exact synthesized record): stats = `["Model: — — — · —", "Tokens: in 0 · out 0 · cache read 0 · cache write 0", "Cost: —", "Timeout: —", "Effort: —", "Activity: —"]`; `systemPromptNote` = **`無 system prompt 紀錄`**. `render()` then paints `工具 0` / `技能 0` / `MCP 0` (`:188-190`, `harness` is `null` → `0`).
  - So a failed fetch renders **four fabricated zeros** (the Tokens card) and **one false statement** (`無 system prompt 紀錄` — `lib/agent.js:62-70`'s own comment says absence must mean UNKNOWN, "never 'not applied'"; here the record was never fetched, and the sentence asserts a fact about it). The server's error text (`shaped.error.message`, `server.ts:594`) and `body.degraded` are both dropped.
- **Why the upgrade, stated plainly:** the reviewer dispositioned this LOW twice (RE-REVIEW #1 §9, #3 §9) on this lens's own prior wording, which described the consequence as six dashes — an honest empty. The reproduction shows it is not an empty: it is INV-V26-6's silent-zero class on the one surface the architecture calls "the agent's inspectability", plus a sentence that is false on the failed path. That is MEDIUM by this lens's severity key (an `api:` clause not what shipped, with an operator-visible consequence). It is the same class BF-2/4/5/6 closed — a non-`ok` result projected into a render — at the one consumption site outside the `onTick` population BF-4's invariant enumerated. Rating is the lens's; routing is the reviewer's.
- **Fix shape:** read `res.status` at `:234`; on non-`ok`, render the existing panel chrome with the server's `error`/`degraded` text in the red `detail` block (the block already exists, `:201-207`) and NO stat cards — never a synthesized record. One real-browser case on val-201 with `/api/runs/:id/agents/:agentId` intercepted.

### QD-O4 — LOW — CARRIED (§9) — A 404 is classified as a connection failure; two 404 ticks paint the nav tag "Offline" while the engine is up
- **Violates:** ARCH-124 `api:` (`offline` "only after ≥ 2 consecutive UNANIMOUS-`fail` ticks (REQ-131 says 連續失敗 — one transient miss during a self-update restart must not paint the whole team's tabs red)") — the clause's semantics are connectivity; a 404 is a successful round-trip.
- **Evidence:** `src/dashboard/lib/connection.js:46` (`status < 200 || status >= 300 → 'fail'`). **Reproduced:** two ticks of `{a: classifyResponse(404,{}), b: classifyResponse(404,{})}` → `'offline'`. Reachable from the shipped page via the legacy `/dashboard/<runId>` route (`app.js:443-445`) for a GC'd or mistyped run: `/api/runs/<id>/dag` and `/api/runs/<id>` both answer 404 (`server.ts:604` and the dag route's own not-found arm) → unanimous fail → 離線 on tick 2; the run view bails (BF-2, `run.js:475`), so the page stays on whatever painted first — for a never-found run, an empty shell with no "not found" text.
- **Status:** unchanged since `ef0a400`; §9 debt. Concur with LOW.

### QD-D3-3 — LOW — CARRIED (§9 D3-3; enters this lens's list) — A view that throws inside `onTick` freezes the connection tag at its last value with no recovery, because `app.js:379` awaits the view unguarded and the tick's already-computed classifications are dropped
- **Violates:** ARCH-125 `api:` (`getJSON` "never an exception … and feeds `nextConnection`") read with ARCH-124's stated property (`connection.js:3-4`: "`live` requires an `ok` THIS tick and therefore cannot go stale by itself") — true of the reducer, false of the wiring around it; the "design the observable seam" obligation this lens carries (a silent failure is a design defect).
- **Evidence:** `src/dashboard/ui/app.js:373-377` computes `results[url]` for every route of the visible view; `:379` `const extra = await view.onTick(…)` has no `try/catch`; a rejection skips `:383-384` (`nextConnection` + `updateConnectionTag`) and `:386`; `scheduleTick`'s `.finally` (`:394`) re-arms the timer regardless, so the page keeps polling forever with a tag that reports the tick BEFORE the fault. The rejection surfaces only as an unhandled-promise console error — no journal line, no tag change, no marker. This is the mechanism that kept BF-4's throw invisible in the browser ("nav tag frozen `live`", RE-REVIEW #3 §5).
- **Why LOW at HEAD:** with BF-4/5/6 landed no view at HEAD throws, so the arm is latent; the reviewer deliberately kept it out of BF-4's scope. Counted rather than left as an observation because it is the one place every view's failure converges and it converges on silence. The design panel's agreed shape needs no reducer change: `try { … } catch { results['view:' + view.name] = 'fail'; console.error(...) }` around `:379`.

### Declared deviation — owed to TASK-215, NOT counted
- **ARCH-122** (`02-architecture.md` ARCH-122 `api:`/`note:`, amended v27h) describes the served body as it IS built (client-built by `app.js:455`) and names `src/dashboard-page.ts:92-152` — a pre-v27 `<header>`/`<main>` with `Runs`/`Issues` links, `#home-*`, `#system-panel`, `#models-panel`, `#issues` — as a fossil "removed by the TASK-A follow-up". Verified at `18b9c03`: the fossil is still emitted (`dashboard-page.ts:92-152`, island at `:153`); TASK-215 is `status: draft` (`03-tasks.md:1837-1838`). **Observability weight, in the architecture's own words:** the dead body is a *boot-failure mask* — when `app.js` 404s, is blocked, or fails to parse, the browser keeps the retired dashboard on screen and it reads as 「healthy engine, nothing running」, and "the connection tag cannot correct it, because the code that paints the tag is the code that failed to load". Live in the tree today; priced by `state.yaml`; counted by `sh .sdlc/trace` as a LOW 未實作 row. Not double-counted here.

### Observations (uncounted — no ARCH clause decides them, or unreachable at HEAD)
- **D3-1 — an empty tick reports `live` from any prior state.** Reproduced: `nextConnection({status:'offline', consecutiveFails:5}, {results:{}})` → `{status:'live', consecutiveFails:0}` (`worstOf({})`'s `'ok'` seed, `connection.js:10`). Unreachable at HEAD only because of `app.js:382`'s `Object.keys(results).length > 0`, which no design row names; reachable the moment REQ-142's visibility gate attaches. Identity-on-empty is the agreed shape.
- **D3-2 — an unrecognised status token ranks `ok`.** Reproduced: `worstOf({a:'bogus'})` → `'ok'` (`RANK[status]` is `undefined`, `connection.js:12`). No producer at HEAD; fail-closed direction is `RANK[status] ?? RANK.fail`.
- **D3-4 — footer freshness on a failed tick.** `app.js:386` `updateFooterClock()` runs after every tick "whether or not it changed anything"; on a degraded tick where BF-2's guards (correctly) skipped the repaint, `Updated HH:MM:SS` still advances over a table from the previous tick. README-owned (IMPL-255), not ARCH-owned; the reviewer holds it as D3-4.
- **D3-6 — architecture text lags BF-1.** ARCH-124 `api:` still opens `connection.js`'s clause with "`worstOf(perRoute) → status` … The tag is `worstOf(perRoute)`" — after BF-1 the tag is a function of `worstOf` AND the streak (`fail` is never a tag state); and "a 200 whose body carries a `degraded` **string**" where `classifyResponse` tests key presence (`'degraded' in body`, `:48`). Architecture lane; not a code deviation.
- **`predictedLanes` fails silently.** `src/dashboard.ts:296` returns `[]` on `!derived.ok` with no journal line, whereas the DAG route's equivalent arm logs `dashboard_api_degraded` (`server.ts:557`). ARCH-131/ADR-055 deliberately give absence one meaning; the asymmetry is noted only. Unchanged.
- **Prefix mismatch fails closed with a journal-only witness.** `src/agent-executor.ts:651-654` — when the gateway echoes something other than the composed prompt, `stripFirstSegment` yields `prompt: ''` and the only witness is `harness_prompt_prefix_mismatch`; the persisted descriptor carries no field saying the prompt was withheld. Fail-closed is the right default. Unchanged.
- **INV-V27-5's lock is Chrome-conditional.** `val-198:85-86` — `itReal` calls `ctx.skip()` when no browser is present; the repo's own verification ran with Chrome (IMPL-273, IMPL-278 13/13). Recorded so nobody reads a skip as the lock holding. Same caveat applies to every BF lock above.

### Verified consistent (Observability)
- ARCH-124 connection clause as amended v27h, now on every arm (post-BF-1): worst-of (`connection.js:26`), un-debounced recovery (`:27-28`), mixed tick → `degraded` with counter reset (`:30-32`), unanimous-fail streak with `status` reporting THIS tick (`:34-40`); reproduced above.
- ARCH-125 `api:` "never rendered as data" at every `onTick`-level site: `home.js:231`, `run.js:475`, `workflow.js:357` (bail), `system.js:78` (explicit component), `models.js:55` (shape), `issues.js:80,102` (deliberate reason-text read). The 16-site table in IMPL-281 was walked; its two remaining gaps are QD3-O1 (a site it marks guarded whose fallback is an empty figure) and QD-O5 (a site it marks safe whose fallback is a synthesized record).
- ARCH-130 (4): `dashboard_api_degraded` at both degrade catches (`server.ts:615`, `:1103`) and at the two `PREDICTED_OVERLAY_UNAVAILABLE` arms (`:524`, `:557`, `:565`) with the closed reason set; the FALLBACK arm does not log. Unchanged code.
- ARCH-125 v27b amendment: warnings rendered as TEXT via `warningText` (`run.js:355-359`, `lib/strings.js:56-71`); `?limit=500` + 「顯示 N / 共 N+」 marker (`agent-panel.js:233`, `:214-221`); 2 KB per-row clip with expand (`:37`, `:96-111`).
- ARCH-123: `dashboard_asset_missing` logged once at module load (`static-assets.ts:56-58`). ARCH-127: `usage_backfill` / `usage_backfill_failed` (`run-manager.ts:884`, `:880`). Unchanged code.
- Agent altitude: the panel is a projection of the RECORD on the success path — `record.provider`/`transport`/`proxyModel` on the model line (`lib/agent.js:28-34`), hung-vs-progressing from `lastActivityAt` (`:47-57`), `effortApplied` on both branches (`:38-42`), the system-prompt FACT sentence distinguishing "applied, N bytes" from "no record" (`:62-70`); the tool-call sequence is inspectable through the same facade call on both transports (`server.ts:592`, MCP `run_agent_log`). The failed path is QD-O5.

---

## 2. Replaceability — decoupling and pluggability

v27's replaceability decision is a *boundary*: `src/dashboard/lib` is the pure, vitest-imported core holding "every decidable behaviour in the client", and `src/dashboard/ui` is "left with nothing but wiring" (ARCH-124 `note:`, ARCH-125 `note:`). The LLM-backend seam is untouched by this slice (no gateway file on any v27 `files:` line) and remains config-selected. `lib/` purity holds (post-AC-7; `lib/connection.js` is the only `lib/` file that changed and it stayed pure). The boundary findings below are unchanged, plus one sharpened from the reviewer's D3-5.

### QD-R3 — MEDIUM — CARRIED (§9, disposition LOW; kept at this lens's rating, unchanged evidence) — The "one string table in two languages" decision shipped as a five-key table plus literals in nine files
- **Violates:** ARCH-124 `api:` (`strings.js` — `STR = { zh, en }` and `t(lang, key)`; key-parity test); ARCH-125 v27b amendment ("REQ-131 forbids literals scattered across the view"); Decision rationale — v27b, "Warning spelling" ("REQ-131 requires every visible string to come from ONE string table in two languages").
- **Evidence (re-counted at `18b9c03`):** `src/dashboard/lib/strings.js:16-31` holds five keys. Outside it: **17** `lang === 'zh'` copy sites — `lib/runlist.js:21,62`, `lib/agent.js:55,56,65,69`, `ui/home.js:71`, `ui/agent-panel.js:105,188,189,217`, `ui/run.js:246,367,390`, `ui/workflow.js:180,182,183`; four private per-file tables — `ui/app.js:28-39 LABELS`, `ui/home.js:16-35 LABELS`, `ui/workflow.js:42-45 COLUMNS`, `lib/status.js:13-16 CTA`; two English-only fallbacks — `ui/app.js:198`, `:414`; and one zh-only literal that BF-4 made the System tab's degraded render — `ui/system.js:29 UNAVAILABLE = '無法取樣'`, now asserted by `val-202:154` in one language only. The ported tabs' remaining untranslated copy (`models.js:56,58`, `issues.js:24,70,72`) belongs to REQ-137/138/139, outside this closure.
- **Consequence (why this lens keeps MEDIUM):** the ARCH-124 key-parity lock guards 5 of the page's strings; a missing translation in any of the other nine files ships CI-green; swapping or adding a language is an edit across ten files with no lock. Nothing changed since RE-REVIEW #1 filed it as non-blocking debt; this pass does not ask for it to block.

### QD3-R1 — LOW — NEW to this lens (≡ §9 D3-5, sharpened) — The one classifier's verdict is dropped at the view boundary, so 16 consumption sites re-derive "is this really ok?" by hand in three different guard shapes; IMPL-281's proposed helper reaches 12 of them and structurally cannot reach the other 3
- **Violates:** ARCH-125 `api:` ("**one** `getJSON(url)` that classifies every response as `ok | degraded | fail`" — one classifier, whose answer then has to be re-decided at every call site); ARCH-125 `note:` ("it may not decide anything a pure function could decide"); Decision rationale — v27 "The server/client mirror pairs" ("the mirror class shrinks instead of growing" — a contract copied by hand at 16 sites is the mirror class in another form).
- **Evidence:** `app.js:373-377` computes `results[url]` (the verdict) and `bodies[url]` (the body) and at `:379` passes **only `bodies`** to the view; `results` stays local. So `home.js:231`, `run.js:475`, `workflow.js:357` — the three `bodies`-map consumers — never see the verdict and re-derive it as `body.degraded || !Array.isArray(...)` (`classifyResponse`'s `'degraded' in body` rewritten by hand, `connection.js:48`). The 12 sites that call `getJSON` directly hold the verdict and test it in two other shapes (`res.status !== 'ok'` at `system.js:78`, `run.js:512`, `workflow.js:326,329`; shape checks at `models.js:55`, `issues.js:80,102`, `run.js:488,495,502,513`, `workflow.js:327`, `agent-panel.js:234`). IMPL-281's own closing recommendation — an `okBody(res, fallback)` helper in `poll.js` — correctly excludes `issues.js`'s deliberate reason-text reads, but a helper over `res` cannot serve the three `bodies` consumers at all, because the view contract `onTick(container, bodies, ctx)` (DES-206) hands them no `res`. Six sites of one defect across six rounds (BF-1..BF-6) is the measured cost of that dropped verdict.
- **Why LOW:** at HEAD every site is guarded; the deviation is that the guard is a convention re-typed 16 times, not a seam, and the next view module (REQ-137/138/139's closures) inherits the obligation with nothing to enforce it. Fix shape, for the next design touch: pass `results` alongside `bodies` (one extra parameter on the DES-206 contract) so the three map consumers can test `statuses[url] === 'ok'`, and land IMPL-281's helper for the direct-fetch sites; then a single grep (`\.body\b` outside `okBody`/`issues.js`) flags the next hand-rolled guard.

### QD-R2 — LOW — CARRIED (§9) — Decidable logic lives in `ui/*.js`, and IMPL-249 excluded those files from the coverage denominator on ARCH-124's premise that none does
- **Violates:** ARCH-124 `note:` ("after v27 that is *every decidable behaviour in the client* — the DOM layer above it (ARCH-125) is left with nothing but wiring"); ARCH-125 `note:` ("it may not decide anything a pure function could decide"); ADR-049 Consequences ("they are small, pure and unit-tested").
- **Evidence (decisions and formulas with no `lib/` home, hence no unit tier), refreshed:** `src/dashboard/ui/workflow.js:50-55` `triggerLabel`, `:62-75` `predictedPayload` (including the "agents absent on every phase = unavailable" rule), `:227-229` `nameFilteredRuns`, `:233-238` `resolveSelectedRunId`; `src/dashboard/ui/home.js:44-50` `formatDuration` (a second copy of `lib/runlist.js:54-63`'s private formatter, exported and imported by `run.js:88` — a `ui→ui` dependency on a pure formatter), `:54-60` `fmtLastRunAt`, `:62-74` `metaLine`; `src/dashboard/ui/run.js:126-128` `toSwimlaneCell`, `:139-151` `cellClassName`/`edgeClassName`, `:311-314` the REQ-134 row-2 join rule; `src/dashboard/ui/agent-panel.js:46-50` `fmtClock`, `:76-80` `eventKindCategory`; `src/dashboard/ui/system.js:38-43` `fmtBytes`. IMPL-249: "`src/dashboard/ui/{…}.js` are EXCLUDED from the UT/IT line-coverage denominator". Unchanged.

### QD-R4 — LOW — CARRIED (§9) — The nine-column history table is a two-file mirror pair
- **Violates:** Decision rationale — v27, "The server/client mirror pairs" ("an eighth mirror must not appear silently"; "the mirror class shrinks instead of growing").
- **Evidence:** `src/dashboard/ui/workflow.js:42-45` `COLUMNS` (zh/en header arrays) must stay in positional lockstep with `src/dashboard/lib/runlist.js:70-83` `historyRow` (value array); nothing ties the two lengths or orders together. Unchanged.

### Declared deviation — owed to TASK-216, NOT counted
- **ADR-049 as amended v27h** records that `tsconfig.json` carries `DOM`/`DOM.Iterable`/`allowJs` (IMPL-229) and that the server/client compile boundary is to be restored by an inverse program `tsconfig.server.json` run in `typecheck` AND `build`. Verified at `18b9c03`: `ls tsconfig*.json` → one file; `package.json:8,11` are a bare `tsc --noEmit`; TASK-216 is `status: draft` (`03-tasks.md:1846-1847`). The ADR says so itself ("the property is UNGUARDED until TASK-B lands"). Priced by `state.yaml`; a LOW 未實作 trace row. Not double-counted here.

### Observations (uncounted)
- ARCH-123's `api:` enumerates 13 client keys and ARCH-124's `api:` opens "Five files"; the tree serves 20 client files (`static-assets.ts:18-26`) and `lib/` holds eight. The v27h housekeeping (6) already names the `lib/` count; the real guard is the closed-both-ways test (`tests/unit/static-assets.test.ts`), unchanged and green. Arch prose lags the tree; no code deviation.
- The `theme-init.js` key literals are the one accepted, text-tested mirror of `lib/theme.js` (DES-201). Consistent.

### Verified consistent (Replaceability)
- ADR-049: plain ESM served from the map, `vitest` imports `src/dashboard/lib/*.js` directly; `tests/fixtures/dashboard-wire.ts` types every literal against the route types and is imported by the `.js` tests. Unchanged.
- ARCH-124 purity (post-AC-7, re-checked after BF-1): `lib/connection.js` reads no clock, no DOM, no `fetch`; `now`/`lang` are parameters everywhere (`runlist.js:70`, `agent.js:72`).
- ARCH-124 amendment: no `accentVars()` JS mirror; the ramp is CSS-native over `--rwe-hue`.
- Decision rationale — v27 "mirror pairs": `SWIMLANE_BOX` has one home (`lib/swimlane.js`); the server emits logical `col`/`row` only (`run.js:126-128` maps them client-side).
- ARCH-127/128 seam: the manager owns live, the store owns at rest, joined only in `listSummaries()` (`run-manager.ts:823-886`); `InMemoryRunStore` parity. Unchanged code.
- Agent altitude: v27 touches no gateway; `_invokeOnce` dispatches through the config-chosen `GatewayClient`; the descriptor decoration is gateway-agnostic (`agent-executor.ts:646-690`; ARCH-129 `note:` "the split belongs to the executor, not to a gateway"). Unchanged code.

---

## 3. Consumability — interface friendliness and integration cost

Every v27 wire delta is typed (`AgentLogView`, the four `RunSummary` fields, `HarnessDescriptor.systemPrompt`), the same facade call serves both transports (`server.ts:592`), and the disclosure control takes a served body as its subject (AC-1). No consumability-facing file changed this round. What remains is the paging cap the architecture assumed and never built, and the generated tool-surface document.

### QD-C2 — LOW — CARRIED (§9) — The HTTP agent-detail `limit` is uncapped, and the MCP schema does not advertise the paging the response's `hasMore` implies
- **Violates:** ARCH-125 `note:` ("the agent panel requests `?limit=500` (the engine's own cap decides)") — no engine cap exists.
- **Evidence:** `src/server.ts:588-590` `limit = limitParam > 0 ? limitParam : undefined` passes any positive value through; `src/mcp-facade.ts:706` `const cap = a.limit ?? 50;` with no upper bound; `src/tool-specs.ts:616` advertises `run_agent_log` as `{runId, label}` only, so a cold model that receives `hasMore: true` (`:709`) has no advertised way to fetch more. Unchanged.

### QD-C3 — LOW — CARRIED (§9) — The generated tool-surface document's `run_agent_log` row does not show the v27 fields; the MCP tool's own description names `systemPrompt` but not `record`
- **Violates:** ARCH-131 / the v27 API-contracts table (the `record` and `harness.systemPrompt` delta is "one facade call, two transports, same delta"; "breaking for any caller that read it"); IMPL-241's own recorded, unwaived DoD gap.
- **Evidence:** `.sdlc/features/001-remote-workflow-engine/v24-tool-surface.md:22` truncates the observed body inside `harness`; `grep -c '"record"\|"systemPrompt"'` on that file → **0**. `src/tool-specs.ts:610-615` — the description states the `systemPrompt` FACT rule but never mentions `record`; `outputSchema` is the empty repo-wide `OUT`, so no machine-readable output shape names either field. Unchanged.

### Observations (uncounted)
- **Two degraded envelopes, one pinned.** The inner catch answers `{runs:[], degraded}` (`server.ts:616` via `buildDashboardModel`); the dispatcher's promise-rejection net answers `{degraded}` alone (`server.ts:1104`). All five BF locks inject the inner shape; `classifyResponse` handles both (`'degraded' in body`), so the client is unaffected. Reachability of the outer shape under normal operation is still unknown; observation-grade.
- **The DAG payload has no declared server type.** `server.ts` builds it as `Record<string, unknown>`; the fixture types it locally (`dashboard-wire.ts` `DagPayloadFixture`), so ADR-049's `satisfies` lock does not reach that route. Unchanged.
- **Cross-repo check.** The contracts table owes "one Gate 5 read of the plugin repo + one release-note line" for `rwe-mcp` callers of `harness.prompt`. Outside this lens's code scope; not verified here.
- **The BF locks pin the client's degrade contract to one server shape.** Five real-browser cases now inject `{"runs":[],"degraded":"…"}` verbatim; if `buildDashboardModel`'s degraded envelope ever changes key set, ADR-054's disclosure test catches the server side and these five catch the client side — a good coupling, noted so the next author does not "simplify" one without the other.

### Verified consistent (Consumability)
- ADR-054 / INV-V27-7 (post-AC-1): key-set rows for every endpoint the v27b budget rule names, asserted against served bodies; the nested `describe.phases[]` row is deferred by ADR-054's own text. Unchanged.
- ARCH-131: `record` on the success branch only (`mcp-facade.ts:713`); `phases[].agents` unconditional, joined by ordinal. Unchanged.
- ARCH-129 / ADR-050 / INV-V27-2: content never captured — strip (`agent-executor.ts:651`) → `redact()` (`:685`) → `capPrompt` (`:687`); `systemPrompt {agentType, bytes}` present iff non-empty (`:666`); the wire prompt to the model unchanged (`composePrompt`, `:580`; dispatched at `:601`); proven against the response BODY on both transports; tool description updated (`tool-specs.ts:614-615`). Unchanged.
- ARCH-127 / ADR-052: the four `RunSummary` fields omitted together (`run-manager.ts:269-279` `summarizeUsage`); `agentCount` omitted for the legacy cohort (`:869`). Unchanged.
- Agent altitude: structured, typed I/O — `AgentLogView` is one declared type consumed by MCP and HTTP alike; the panel VM is a pure function of it (`lib/agent.js:72-92`).

---

## 4. Self-sustainability — closed-loop autonomy and lifecycle

v27 adds no new service and no new state; its self-sustainability decisions are the one-time bounded backfill, `no-store` on client bytes so a self-update cannot strand a stale client, immutable caching for the vendored fonts, and long-lived-tab hygiene. Three of four hold at unchanged code; the fourth is the carried listener/blob finding, plus the accepted AC-5 residual. One cross-reference: QD-D3-3 (§1) is also this dimension's finding — a throwing view leaves the page polling forever behind a frozen tag with no recovery path, which is the opposite of self-healing.

### QD2-O2 — LOW — CARRIED (§9) — The three ported tabs fetch their own endpoint inside `onTick` in addition to the one `app.js` already fetched for them, so each tick on a visible ported tab is two requests to the same route and the `bodies` contract is bypassed
- **Violates:** ARCH-125 `note:` ("Per-view fetch scoping is the polling budget: the visible view's endpoints only, on the one timer"); ARCH-125 `api:` (`app.js` "owns **one** … timer"; the amendment: "still ONE timer, still the per-view fetch set").
- **Evidence:** `src/dashboard/ui/app.js:368-377` fetches `endpointsFor(view.name)` (`/api/system` etc.) and stores the body; `src/dashboard/ui/system.js:72`, `models.js:52`, `issues.js:99` — each `onTick` ignores the `bodies` parameter and calls `getJSON` on the same route again. BF-4's guard (`system.js:78`) is on the view's OWN second fetch; its returned status (`:83`) overwrites `app.js`'s `results['/api/system']` via `Object.assign` (`:380`) — the two verdicts agree, so the tag is unaffected. IMPL-272 recorded this as "Accepted, not fixed".
- **Consequence:** the stated polling budget is 2× on those tabs (same-origin, in-process — harmless in load); the "one timer, one fetch set" seam REQ-142's visibility gate is meant to attach to has a second fetch path it will not gate. The sweep is the same three-line change per file (read `bodies[url]`, drop the `getJSON`) — and, after QD3-R1's contract change, `statuses[url]`.

### QD-S2 — LOW — CARRIED (§9, ≡ F-7) — Every route mount adds window-level listeners and a blob URL that are never released; the view contract has no teardown
- **Violates:** ARCH-125 `note:` ("listeners are delegated on the stable wrappers — a tab left open for days must not accumulate one handler per node per 3-second rebuild"; NFR ownership: "page weight and long-lived-tab hygiene → ARCH-125").
- **Evidence:** `src/dashboard/ui/run.js:115-118` — `initZoomable` registers `mousemove`/`mouseup`/`resize` on `window`, each closing over the zoom element; it runs on every `render()` (`:435`), and `workflow.js` runs it twice per mount (`:119`, `:172`). `app.js:402-419` `mountLazy` does `container.replaceChildren()` with no unmount hook (the view contract is `render`/`onTick` only), and the language toggle remounts the whole app (`:301`). `workflow.js:248,274` revoke the diagram blob URL only when replaced, never on navigation away. Per-tick rebuilds are fine (old nodes and their delegated listener are collected); the leak is per navigation/remount. Unchanged.

### Verified consistent (Self-sustainability)
- ARCH-127 / ARCH-128: `BACKFILL_PER_TICK = 25` (`run-manager.ts:285`, `:825-841`); a zero-record legacy row is memoised so it is not re-walked forever; a failed heal is retried, never memoised (`:876-881`); `backfillUsage` is a no-op unless terminal and usage-less, re-checked inside both stores; one `LEFT JOIN` per list, never N+1. Convergence holds in-process (the single-writer caveat is RE-REVIEW #1's F-6, a missing sentence, not a contradiction). Unchanged code.
- ARCH-123 (post-AC-8): woff2 → `public, max-age=31536000, immutable`; JS/CSS → `no-store` so a self-update restart cannot leave a cached old client against a new `/api/*` (`static-assets.ts:47-49`; `server.ts:1312` writes `entry.cache` verbatim); `readStaticAsset`'s cache is bounded by the closed 26-key set. Unchanged code.
- ARCH-125 amendment: one self-rescheduling `setTimeout` armed in `tick().finally` with a generation guard against stale loops (`app.js:389-400`); independent fetches run concurrently (`:373-377`).
- ARCH-130 (2)/(4): a derivation fault degrades to an empty overlay plus a warning and a journal line, never a 500 (`server.ts:504-566`); the pre-approved once-per-`(runId, reason)` flood guard is not built and was not required. Unchanged code.
- INV-V27-3: every byte from the engine's origin; the shell's only inline `<script>` is `type="application/json"` (`dashboard-page.ts:153`); CSP at `server.ts:1332` matches ARCH-130 (3) byte-for-byte. Unchanged code.
- INV-V27-8: the `src/**` guards and the external-host guard walk `.js` and `.css`. Unchanged.
- Agent altitude: memory metabolism, tool-liveness probes and prompt self-calibration are not in this closure and no v27 ARCH row claims them; the run-level self-healing that is in closure (the usage backfill) converges by construction.

---

## Summary table

| # | Sev | Status | Dimension | Anchor | Where |
|---|---|---|---|---|---|
| QD3-O1 | MEDIUM | **NEW/RESIDUAL** (BF-2's class at its unswept third site; BF-6 left the fallback) | Observability | ARCH-125 `api:` "never rendered as data"; QD2-O1→BF-2 precedent | `ui/workflow.js:326-329` (+ `val-199:268-270`'s own concession) |
| QD-O5 | MEDIUM (**upgraded** from LOW, corrected evidence) | CARRIED (§9) | Observability | NFR ownership "never a silent zero" (ADR-046/INV-V26-6); Altitude split; ARCH-125 `api:` | `ui/agent-panel.js:233-241`, `:238` synthesized `tokens:{0,0}`; `lib/agent.js:62-70` |
| QD-R3 | MEDIUM | CARRIED (§9 says LOW) | Replaceability | ARCH-124 `strings.js`, ARCH-125 v27b amendment | `lib/strings.js:16-31` vs 17 copy sites / 4 private tables / 2 en-only fallbacks / 1 zh-only degraded literal (`system.js:29`) |
| QD3-O2 | LOW | **NEW/RESIDUAL** (BF-5's unswept siblings on the same fetch) | Observability | ARCH-125 `api:`/`note:` (run summary, row-2 APPLIED model) | `ui/run.js:513`, `:502` |
| QD3-R1 | LOW | NEW to this lens (≡ §9 D3-5, sharpened) | Replaceability | ARCH-125 `api:` "one `getJSON`"; ARCH-125 `note:`; mirror-pairs rationale | `ui/app.js:379` drops `results`; 16 guards in 3 shapes; DES-206 contract |
| QD-D3-3 | LOW | CARRIED (§9 D3-3; enters this lens's list) | Observability / Self-sustainability | ARCH-125 `api:` "feeds `nextConnection`"; ARCH-124 "cannot go stale" | `ui/app.js:379`, `:383-386`, `:394` |
| QD-O4 | LOW | CARRIED (§9) | Observability | ARCH-124 `offline` semantics | `lib/connection.js:46`, `ui/app.js:443-445`, `server.ts:604` |
| QD-R2 | LOW | CARRIED (§9) | Replaceability | ARCH-124 note, ARCH-125 note, ADR-049 | `ui/workflow.js:50-75,227-238`, `ui/home.js:44-74`, `ui/run.js:126-151,311-314`, `ui/agent-panel.js:46-80`, `ui/system.js:38-43`; IMPL-249 |
| QD-R4 | LOW | CARRIED (§9) | Replaceability | v27 rationale "mirror pairs" | `ui/workflow.js:42-45` vs `lib/runlist.js:70-83` |
| QD-C2 | LOW | CARRIED (§9) | Consumability | ARCH-125 "engine's own cap decides" | `server.ts:588-590`, `mcp-facade.ts:706`, `tool-specs.ts:616` |
| QD-C3 | LOW | CARRIED (§9) | Consumability | ARCH-131 contract; IMPL-241 caveat | `v24-tool-surface.md:22`; `tool-specs.ts:610-615` |
| QD2-O2 | LOW | CARRIED (§9) | Self-sustainability | ARCH-125 `note:` polling budget | `ui/app.js:368-377` + `ui/models.js:52`, `ui/system.js:72`, `ui/issues.js:99` |
| QD-S2 | LOW | CARRIED (§9 ≡ F-7) | Self-sustainability | ARCH-125 long-lived-tab hygiene | `ui/run.js:115-118,435`, `ui/workflow.js:119,172,248,274`, `ui/app.js:301,402-419` |

**Counts:** 13 violations — **0 HIGH, 3 MEDIUM, 10 LOW.** Composition: 2 new/residual since the BF repairs (QD3-O1 MEDIUM, QD3-O2 LOW) + 11 carried (QD-O5 MEDIUM upgraded on corrected evidence; QD-R3 MEDIUM at this lens's rating, §9 disposition LOW; 9 LOW, of which QD3-R1/D3-5 and QD-D3-3 enter this lens's numbered list from the reviewer's §9). Delta vs the `ced73ff` pass of this lens: 10 → 13, driven by (a) one residual re-filed at its third site, (b) two §9 rows now counted with an ARCH anchor, (c) one BF-5 sibling. **Closed since `ced73ff`:** QD2-O1 for `home.js`/`run.js` (BF-2), plus BF-1 (F-1 / QD-O1 residue) and the BF-4/5/6 chain — all re-verified in §0 by file:line and by execution. Two declared-and-owed items (TASK-215 fossil shell; TASK-216 inverse tsconfig) uncounted. Eleven uncounted observations, including D3-1/D3-2/D3-4/D3-6.

**Invariants verified consistent at `18b9c03`:** INV-V27-1 (`run-manager.ts:823-886`; lock `usage-live-equals-fold.test.ts` on the named shape, AC-9 — unchanged code), INV-V27-2 (`agent-executor.ts:651-687`; live both-transport lock — unchanged code), INV-V27-3 (`dashboard-page.ts:153`, `server.ts:1332` — unchanged code), INV-V27-4 (`dashboard.ts:292-296`, `mcp-facade.ts` — unchanged code), INV-V27-5 (`val-198`, AC-6 — Chrome-conditional, see observation), INV-V27-6 (`run.js:99-120`; transform on the wrapper — unchanged lines), INV-V27-7 (served-body key sets, AC-1 — unchanged), INV-V27-8 (guards walk `.js`/`.css` — unchanged), INV-V27-9 (`dag-masking-auth.test.ts:328-338` exclusion-form parity — unchanged).

**Suggested routing (not decisions):** QD3-O1 → Gate 6, the same round as any BF-7 the reviewer opens: BF-2's bail form ahead of `workflow.js:326` (or BF-4's explicit-component form), plus one val-199 case asserting `[data-node-cell]` count is unchanged after `/dag` is intercepted — the `val-200:215` recipe on the workflow route. QD-O5 → the reviewer's call whether the corrected evidence moves it from §9 to a blocking row; if it moves, the fix is `res.status` read at `agent-panel.js:234` and the server's error/degraded text in the existing `detail` block, with one val-201 interception case. QD3-O2 → fold into whichever of the two is dispatched (same file as BF-5, same fetch). QD3-R1 and QD-D3-3 → the next design touch on DES-206 (pass `statuses` beside `bodies`; guard `:379`), not this loop. The eight remaining carried items stay where RE-REVIEW #3 §9 put them; nothing in this pass changes their evidence except line numbers.
