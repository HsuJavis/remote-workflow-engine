# SDLC progress state — resume relies on this file. Update after each action.
project: remote-workflow
feature: 001-remote-workflow-engine
mode: new                # new | modify | refactor
safety_class: QM         # Gate 1.5 triage: developer tooling, no safety/cyber-physical impact → QM (no safety work products)
iteration: v27
current_stage: review  # v27m GATE 3+4 SEND-BACK REPAIR (2026-09-14, designer): both halves of the RE-REVIEW #4 send-back are closed - BF-8 (design) is DES-206's positive degrade rule, amended in place at iter v27m and inherited by DES-205 in the same edit; BF-7 (impl) applies it at ui/workflow.js's paintSelected (commit db12573), falsified by reverting the guard and re-verified in real Chromium. Stage stays `review` on purpose - a Gate 8 send-back returns to the reviewer, it does not rewind the workflow to Gate 5 (same routing the v27l design send-back used at 24797c5). See gates.design.note.
  # PRIOR: # v27 GATE 5 DEFECT-QUEUE REPAIR (2026-09-12, verifier) — closed defect-queue items (1) UT-249 and (6) IT-170 (see gates.tests.note); items (2)-(5) re-confirmed already resolved; val-193 C2 re-point re-verified correct; trace 1617/41 byte-identical; regression 2798/2830 (2 pre-existing failures outside this pass's scope, flagged). Next: Gate 6 (implementer) continues TASK-208/209/211/212. PRIOR: v27c GATE 5 DELTA PASSED (2026-09-12, verifier, test-first RED) — the CSS-ownership delta's deferred test half is recorded: UT-255/UT-256 (new) trace the TASK-214 F-pattern locks (dashboard-class-contract.test.ts green 13/13, TASK-214's own; dashboard-no-design-values.test.ts red 3/7, the SLICE's final green per ordering rule 4); VAL-206/207/208/209 amended in place (iter v27c) with a new SPEC_ROWS-consuming case each (DES-209's promised real-tier oracle, `tests/helpers/spec-rows.ts`), plus 6 dashboard-spec.ts rows rewritten/deleted where the ORACLE (not the impl) was wrong. trace: 1601/50, byte-identical gap set to the 1599/50 pre-edit baseline (0 broken links, 0 orphans, +2 items only). Two known run-scenario gaps (val-200 is-running/is-queued/is-failed, val-201 .detail-block) flagged for Gate 6/7 rather than engineered here. Next: Gate 6 (implementer) re-runs the Sprint A closure. PRIOR: v27c GATE 3+4 DELTA PASSED (2026-09-12, designer) — the routed dashboard.css ownership gap is closed: TASK-214 (new, owns the stylesheet end-to-end) + DES-209 (new, the class contract/substrate/spec table) + 6 TASK and 5 DES amendments in place at iter v27c. Next: Gate 6 (implementer) re-runs the Sprint A closure with TASK-214 FIRST (ordering rule 6). The tests this delta implies have no gate in this invocation — TASK-214 carries the F-pattern in its dod. PRIOR: impl  # v27b GATE 5 DELTA PASSED (2026-09-12, verifier, test-first RED) — the TESTS half of the scoped delta (impactIds REQ-133/134/140) is done: 7 existing items amended in place (UT-238, UT-244, IT-168, IT-169, IT-092, VAL-207, VAL-212), 0 new ids, sh .sdlc/trace 1591/67 byte-identical (0 broken links, 0 orphans). Next: Gate 6 (implementer) turns TASK-196..213 green over the FULL Sprint A closure (REQ-131..136/140/141) — see gates.tests.note for the full delta detail. PRIOR: v27b GATE 3+4 DELTA PASSED (2026-09-11, designer) — next: the TESTS half of the same scoped delta (impactIds REQ-133/134/140): UT-238 rewritten to 14 cases with the masked axis and the :34 cast GONE, UT-244 + warningText, IT-168 (flip + stabilization predicate + live key-set + PARITY_EXCLUDED + describe parity scoped to phases + cell AND lane anchors), IT-169 (two real producers, pin-HIGH FALLBACK recipe, the log-line spy), IT-092 re-traced to REQ-100 as a GREEN guard on a comment sentinel, VAL-207/212 (auth-ON Chromium showing agent NAMES; VAL-side p95). PRIOR: v27b GATE 2 DELTA PASSED (2026-09-11, architect) — next: the design+tests half of the same scoped delta (impactIds REQ-133/134/140), then impl->review over the full Sprint A closure. PRIOR: v27 GATE 5 PASSED (2026-09-11, verifier, test-first RED) — 34 new work items (UT-230..249/252..254 (20), IT-165..170 (6), VAL-206..212/205 (8)) written test-first across every in-closure DES-191..208 and every REQ-131..136/140/141; each item run once and confirmed red for the stated reason (whole-file import failure for 9 brand-new modules — static-assets.ts, dashboard/lib/{theme,strings,connection,swimlane,runlist,agent}.js, dashboard/ui/poll.js — or genuine behavioural red against an existing module: deriveLanes/predictedLanes missing from dashboard.ts, listSummaries missing from RunManager, stripFirstSegment missing from params/resolve.ts, the real REQ-136 body still carrying the systemPrompt verbatim, no /static/dashboard/* route, no data-theme/CSP/lanes on the wire, no tab UI in real Chromium). A minority (UT-231, UT-232 half, UT-236 half, UT-249 half, IT-168/169/170 partial) are Mode-C green-by-construction — an already-true invariant or a brand-new self-contained guard with nothing yet to violate — recorded green, not forced red, each with its reason stated in 05-tests.md. Test infra landed as part of this gate (not implementation): vitest.config.ts's `include` widened to collect `.test.js` (TASK-196's own prerequisite — a `.js` test never collected reads as a silent pass); tests/helpers/require-browser.ts + tests/helpers/client-corpus.ts + tests/fixtures/dashboard-wire.ts. Known, disclosed, accepted: tests/fixtures/dashboard-wire.ts imports the not-yet-existing `AgentLogView` from src/types.ts, so `npx tsc --noEmit` is RED repo-wide on this one missing export until Gate 6 (DES-192's own "tsc IS the first test"; vitest/esbuild is unaffected). Scope reduction disclosed, not silently narrowed: DES-208's full ~41-assertion STAYS/MOVES/RETIRES disposition table is NOT completed at this gate — only the clientCorpus() helper, its own anti-vacuity test, and one genuine positive-anchor case per touched file are written; the full sweep is flagged as Gate-6-adjacent follow-up. Filename correction (housekeeping): TASK-196 names `val-197-diagram-pan.test.ts`; the real file is `val-197-diagram-drag-pan.test.ts` — wired against the real name. Test-first observation, not an owner_decision: DES-196's `deriveLanes` signature line omits the `status` param the boundary text requires; tests call `opts:{masked,status}`, left for Gate 6 to reconcile literally. ADR-051's owner_decision:pending is untouched (architect's own marker, not duplicated here) — every test encodes decision (a) exactly as DES-197 specifies. sh .sdlc/trace: 1562/67 -> 1591/67 (identical gap COUNT; REQ-131..136/140/141's own gaps flipped from 未實作+未驗證 to 未實作+未真實驗證 — DES-191..208 now has 未驗證 coverage — the intended Gate 5 signal, not a new defect); 0 new broken links (3, unchanged from git HEAD), 0 new orphans (2, unchanged). Full regression + gate_check detail in this dispatch's own report. Next: Gate 6 (implementer) — 18 TASK-196..213 to turn every red item green. PRIOR: v27 GATE 3+4 PASSED (2026-09-11, designer) — TASK-196..213 + DES-191..208 landed for the closure REQ-131..136/140/141; ARCH-124/125 amended in place. Next: Gate 5 (test-first). PRIOR: v27 GATE 2 PASSED (2026-09-11, architect) — ARCHITECTURE LANDED for the dispatched closure REQ-131..136/140/141 (8 REQs). 02-architecture.md gains the v27 slice: ARCH-122..131 + ADR-049..056 (18 items), 4+1 mermaid views, data architecture (NO schema change), the interface/API contract table, INV-V27-1..8 and the Decision rationale; ARCH-120 amended in place and bumped to iter v27 (the swimlane replaces the DAG layout it rendered; zoom/pan/fit + C2 anchors survive). Synthesised from the pre-run lens panel (.panel/architecture/{adversarial,quality-dimensions}.r1.md — round 1 only, stances complementary, no r2 warranted). Self-check: trace 1508/49 -> 1526/49 with the gap SET byte-identical (0 new, 0 broken links, 0 orphans); solid_check PASS (68 modules, 0 mid, 10 pre-existing low unclaimed-file warnings); dashboard_check unchanged except one NEW mid of the known erDiagram-cardinality false-positive class (documented in the section housekeeping note). 1 owner_decision pending (ADR-051: reverse the auth mask over the predicted overlay, or scope REQ-133/134 acceptance to auth.enabled:false). OUT OF THIS DISPATCH CLOSURE and carrying NO ARCH row: REQ-137/138/139 (Models/System/Issues tabs) and the two kind:nfr rows REQ-142/143 — their attachment points are named in the v27 section scope paragraph. Standing user directive in notes below: PAUSE for user architecture review before Gate 3. PRIOR(v27 Gate 1): v27 GATE 1 PASSED (2026-09-11, orchestrator) — REQ-131..143 written with the Round v27 clarification log (3 rounds / 12 owner-answered questions). Gate 1.5 triage: QM, no safety work products. Next: /sdlc-run for Gate 2->8. PRIOR(v26): v26 GATE 8 RE-REVIEW #1 PASSED (2026-09-11, reviewer) — ITERATION CLOSES. send_back=[], blocking_findings=[]. All 13 blocking findings of the 2026-09-10 pass re-verified CLOSED on disk at file:line (4 HIGH H-1..H-4 + 7 MID M-1..M-7 in code; 2 MID A-1/A-2 by architecture amendment). Reviewer-run evidence: npx tsc --noEmit clean; npx vitest run 376 files / 2641 tests passed, 0 failed (4 NEW integration files, no weakened assertion); sh .sdlc/trace 1492 items / 24 gaps with the gap SET byte-identical to the previous pass (0 new); solid_check PASS (58 modules, 0 mid); module_check dormant; dashboard_check 0 high / 6 mid / 1 low, all 6 mids re-diagnosed individually as checker false positives (erDiagram cardinality tokens + one apostrophe in sequence-message text); 0 未真實驗證 / 0 未驗證 / 0 斷鏈 / 0 孤兒; 0 unanswered owner_decision: pending. arch_consistent=FALSE but NOT blocking — 1 MID residual (R-1: run-guard.ts:45 foldUsage skips the no-tokens failed usage event before reading data.unmapped, so the at-rest fold undercounts a failed call's unmapped subtypes on the snapshot-less fallback read path while foldUsageFromRecords counts them; ARCH-111's NAMED surface run_result.meta.unmappedMessages is satisfied on the terminal path, proven by IT-162 through a real facade, and no money/budget path is affected) + 1 MID carried pre-v26 TDD gap (IMPL-082) + 49 LOW, every one enumerated in 07-review.md §0/§2/§7. Routed out: R-1's four-part seam -> next impl touch; REQ-124's nested-lane clause -> orchestrator/requirements. PRIOR: v26 GATE 8 SEND-BACK REPAIR, ARCHITECTURE half (2026-09-11, architect): both architecture-tagged blocking findings closed in 02-architecture.md — A-1 (the v26 body still published the overruled PRICE_UNKNOWN refusal and a nullable costUSD, and still specified applied:true for the openrouter effort arm) and A-2 (ADR-048's Action line authorised five allowlist members while six shipped). 15 sites amended in place with dated `- **amended (2026-09-11 …)`  bullets on the EXISTING headings — ADR-038 heading+note+marker, ADR-047 marker, ARCH-118 api (costUSD number + unpriced, addUsage/foldUsage signatures, meta.budgetEnforceable), the logical-view PU node relabelled (not deleted — deleting it silently adopts REQ-127's forbidden option (i)), both ER costUSD rows + a new bool unpriced on each, ARCH-116 provenance note, the run_start-admission and models_list contract rows, ARCH-117's openrouter arm + its two stale Gate-7.5 claims, ADR-048's Action line — plus two cross-half consistency fixes for the impl repair (ARCH-116 catalogFetchedAt is per-ROW per H-4; ARCH-113 gains contract:'v1'|'v2' per M-3) and the housekeeping Gate 4 / the Gate 8 review had already routed to this stage (ARCH-114 deriveAgentRecords + nested-lane rule, D-6 caps 'unknown', D-7 additionalProperties). ZERO new ARCH/ADR IDs, zero new fields, zero code changes (one comment-only touch retiring the guard's own FLAGGED-for-Gate-8 comment in tests/unit/no-skeleton-surface.test.ts, named in ADR-048's amendment). iter: deliberately NOT bumped — these items already carry this iteration's number and no v27 exists (precedent e8fb8a7 + the impl half of this same send-back); verified inert against trace.py's drift rule. ARCH-117's direct-fetch effortBodyFields clause left verbatim per the review's anti-collision fence. sh .sdlc/trace -> 1492 items / 24 gaps, byte-identical gap SET to the impl half's and to Gate 8's own measurement, 0 new. Owner decisions: none deferred (0 pending markers repo-wide). Both halves of the send-back have now landed; gates.review is deliberately untouched — Gate 8 re-reviews impl + architecture together. PRIOR: v26 GATE 8 SEND-BACK REPAIR, impl half (2026-09-11, implementer): all 11 impl-tagged blocking findings closed — H-1 (client.ts wireEffort unified with the SDK transport, one PROVIDER_CAPS read), H-2 (direct-fetch retryable/detail, terminalHttpFailure + retry-loop break), H-3 (AgentRecord.detail added, redact-then-cap order fixed, deriveAgentRecords restart-survival gap found and closed), H-4 (models_list/GET /api/models read ModelBook.snapshot(), catalogFetchedAt real), M-1 (usage-projection gap counter via the existing unmapped wiring), M-2 (unmapped on the failed branch), M-3 (v1FallbackGraph deleted, deriveExpectedGraph gains contract:'v1'|'v2'), M-4 (LayoutCell tokens/costUSD/unpriced, DAG cell renders them, VAL-193 extended and run green against real Chromium), M-5 (UNDECIDABLE_SHAPE deleted from the union/catalog/advertisement, converse-direction catalog lock added), M-6 (ipc/protocol.ts agentResult.spent typed {usd,tokens}), M-7 (no-skeleton-surface.test.ts prose corrected to six). A-1 (the [architecture]-tagged finding) is OUT of this scope — routed to the architecture repair agent per its own tag; this run's H-1 fix was written to match ARCH-117's preserved direct-fetch clause. Full regression: 2641 passed / 0 failed / 26 pre-existing skips over 376 files (npm test); npx tsc --noEmit clean. sh .sdlc/trace -> 1492 items / 24 gaps — the SAME 24 Gate 8's own review measured, 0 new. Stage moves to verification for the send-back's own re-run; Gate 8 re-reviews both impl and architecture halves together once both land. PRIOR: v26 GATE 7.5 ROUND 6 (2026-09-09, validator): PASSED — the REQ-127/REQ-129 impact closure is real-green on this round's own measurements; next stage is review (Gate 8), which stays BLOCKED by two owner_decision markers (VAL-186 REQ-126 effort, VAL-187 the misnamed production alias). PRIOR: v26 GATE 6.5+7 (2026-09-09, verifier): PASSED — simplify (2 fixes, 5 named-and-rejected), full regression 2534 passed / 0 failed / 26 pre-existing skips over 367 files, 52 v26 items flipped red->green, 8 new items written to close the coverage bar, determinism_check exit 0 for the first time on this tree. See gates.verification.note. PRIOR: v26 GATE 6 (2026-09-09, integrator): PASSED — see gates.impl.note. PRIOR: v26 GATE 5 (2026-09-08, verifier, test-first RED): PASSED — 59 new work items (UT-172..204, IT-141..155, E2E-010, VAL-171..180) written test-first across every DES-170..189 (DES-190 is doc-only, no test) and every REQ-121..130, plus 3 shared fixture modules (tests/fixtures/expected-graph-fixtures.ts — 14 hand-written (script,expected) pairs; pre-v26-terminal-snapshot.json — a REAL pre-v26 snapshot engine-generated at Gate 5, commit 525ade4; v26-public-shapes.ts). Every item run once and confirmed red for the stated reason (whole-file import failure for 3 brand-new modules + TypeError-not-a-function for new exports on 11 existing modules + genuine behavioural red against 14 existing modules); 6 appended-to files (no-retired-surface.test.ts, authoring-guide.test.ts, guide-examples-register.test.ts, val-007-observability.test.ts, val-018-dashboard-browser-ui.test.ts) re-run in full, pre-existing cases still pass. 4 green-by-construction invariants (CallKey byte-identity, a v25 journal replay, the PRICE_UNKNOWN grep, PROVIDER_CAPS totality-shape) held green per Mode C, not forced red. trace: 1413 items / 65 gaps = the unchanged v26-Gate-3+4 baseline (39 pre-gate + 26 "task not implemented" rows, expected until Gate 6); 0 broken links, 0 orphans, every new item traces cleanly. VAL-172/173/175/176/178/180 (REQ-122/123/125/126/128/130) are status:blocked/result:not-run — each needs a real revoked credential, the deployed box, a real OpenRouter key, a --detailed_debug LiteLLM capture, or the REQ-117-derived disqualified-cold-model protocol — recorded honestly per the v24 VAL-128 precedent, not faked. Next: Gate 6 (implementation) — 25 TASK-171..195 to turn every red item green. PRIOR: v26 GATE 3+4 (2026-09-08, designer): PASSED — TASK-170..195 in 03-tasks.md and DES-170..190 in 04-design.md, synthesized from the pre-run two-group design panel (r1+r2), with the ADR-047(b) ledger amendment executed in 04-design.md. trace: 1354 items / 65 gaps = the 39-gap pre-gate baseline + exactly 26 low-severity "task not implemented" rows (one per new TASK, expected until Gate 6); 0 broken links, 0 orphans. BOTH v26 owner_decision markers (ADR-038, ADR-047) are DECIDED at eaf6546 and this gate minted NO new one. Next: Gate 5 (test-first RED) — the design names ~60 externally-anchored cases and 10 real-tier paths. PRIOR: v26 GATE 2 (2026-09-08, architect): PASSED — ARCH-110..121 + ADR-037..047 written into 02-architecture.md from the two-group lens panel (.panel/architecture/*.r1/*.r2), synthesized not re-debated; trace 39 gaps = the pre-gate baseline exactly (0 broken links, 0 orphans; the 20 未實作/未驗證 rows are REQ-121..130 awaiting Gates 3–7). TWO owner_decision markers are PENDING and block Gate 8 only: ADR-038 (PRICE_UNKNOWN refusal for a USD-only budget over an unpriced model — deviates from REQ-127's literal) and ADR-047 (trigger-started runs carry no budget; D-V2h is an unimplemented 'Resolved'). Next: Gate 3+4 (tasks+design). PRIOR: v26 GATE 1 (2026-09-08, owner-answered in session): REQ-121..130 drafted from issue #64–#75 intake (v26-gate1-working-notes.md); gates 2..8 not yet run for v26. PRIOR: v24 GATE 8 REVIEW 2026-09-05 (reviewer): NOT PASSED — send_back=["impl","validation"], 3 HIGH; see gates.review.note. Stage stays at validation because Gate 7.5 was never landed (V-1) and must re-run after the Gate 6 fixes. PRIOR: v24 GATE 7.5 ROUND 2 NOT PASSED 2026-09-04 (validator) — 11/12 REQ green at the real tier on the delta build 946b46c (all twelve round-1 defects re-observed fixed; a NEW fresh cold subject passed REQ-117 first try); REQ-116 red on ONE clause: D-14 — trigger-claim refusals (TRIGGER_NOT_FOUND / TRIGGER_ALREADY_CLAIMED) carry see:null (ERROR_CATALOG miss, errors.ts:74-75). Route back to Gate 6 for the one-line fix, then a Gate 7.5 delta re-run of REQ-116. See gates.validation.note. PRIOR: v24 GATE 7.5 ROUND 1 NOT PASSED 2026-09-04 (validator) — 3/12 REQ green at the real tier (REQ-107/108/114), 9 red on the defect register D-1..D-13 (14 rows counting D-1b; four security-relevant: phantom-firing released triggers, cross-principal asset inheritance after deregister, stdio MCP admin-gate bypass via `type`, the mandatory diagram never served). Route back to Gate 6; then a Gate 7.5 delta re-run. See gates.validation.note. PRIOR: v24 GATE 6.5+7 ROUND 2 PASSED 2026-09-04 (verifier) — the round-1 send-back was fixed IN PLACE by the verifier (declared role crossing: no Gate 6 commit landed between the send-back and this round), together with five further defects the fix uncovered; suite 2113/2139 green, coverage 95.53%, solid_check 0 HIGH, determinism 0, TZ time-travel 0 flips, and the first auth-ENABLED real boot (IT-124) as the real-dependency smoke. Next: Gate 7.5 (validator) — flag: authz.ts/scheduler.ts/webhook-registry.ts/call-tool.ts/mcp-facade.ts/tool-specs.ts/server.ts all moved. PRIOR: v24 GATE 6.5+7 ROUND 1 NOT PASSED 2026-09-04 (verifier) — sent back to Gate 6: two authorization defects proven by a rewritten IT-105 against real stores (trigger ownership keyed off the claiming workflow instead of `createdBy`; the moded workspace_* rows' `key: null` turning the ownership check into a no-op). See gates.verification.note. PRIOR: v24 GATE 5 DONE 2026-09-04 (verifier, test-first RED) — 52 new work items (UT-138..162, IT-105..121, E2E-008/009, VAL-118..129) written test-first across every DES-137..162/ARCH-087..108, one VAL per REQ-107..118 (VAL-124/REQ-113 and VAL-128/REQ-117 recorded status:blocked/result:not-run per the design's own Gate-7.5-only ruling, not silently skipped). Every item run once and confirmed red for the stated reason (whole-file import red for 10 brand-new source files: tool-specs/authz/path-verdict/scan-agent-calls/check-mermaid/authoring-guide/call-tool/owner-lookup/audited-read/run-view; genuine behavioural red against existing modules otherwise). 8 files extended in place (compose-config-v2-wiring, params-contract, params-admission, webhook-registry, asset-mcp-tools, asset-skill-materialization-wiring, agent-log-harness-shape, workflow-describe-projection) — pre-existing cases re-run and confirmed still green (regression intact) after each append. trace 1166/65 — 0 broken links / 0 orphans; REQ-107..118's own gaps flipped from 未驗證 (no test at all) to 未實作/未真實驗證 (test exists, mock-only, awaiting Gate 6 impl + Gate 7.5 real:true) — the intended Gate 5 signal, not a new defect. Next: Gate 6 (implementer). PRIOR: v24 GATE 3+4 DONE 2026-09-04 (designer) — TASK-131..153 written to 03-tasks.md (every ARCH-087..108 covered, each card carrying files/des/dod/estimate) and DES-137..162 to 04-design.md, synthesized from the pre-run panel r1+r2 (.panel/design/). The two r2 files CROSSED (each read only the other r1), so three points were ruled by source verification: descriptor keys go ON the decorated descriptor at the ONE decoration site agent-executor.ts:410-450 (not siblings — that site is downstream of both gateways and already merges provenance), adminReads is attached at the facade projection so RunStatusView never carries it PLUS a reduced toPublicRunView stripping the pre-existing principal from ungated /api/runs*, and ERROR_CATALOG drift is locked by constraining four upstream unions to ErrorCode (tsc) because the grep lock is blind to six pass-through codes. O-7b resolved by KEEPING ARCH-103 placement: materializeAssets stays in the SDK gateway client, so a direct-fetch (surfaceType none) dispatch copies nothing and the executor reports missing=declared in one ??. Ordering is binding: TASK-139 (deletion) before 147/148; 140..145 and 146 before 147/148; TASK-152 (113-file rename sweep) is the last code task as ONE mechanical commit; one WIP commit per task. TASK-153 (client plugin) is external and blocks the REQ-117 probe only. trace 1124/65 — the 23 new gaps are exactly the LOW no-IMPL-yet rows for TASK-131..153, the other 42 byte-identical to the Gate 2 baseline, 0 broken links / 0 orphans. Next: Gate 5 (verifier, test-first RED). PRIOR: v24 GATE 2 DONE 2026-09-04 (architect) — ARCH-087..108 + ADR-023..035 appended as the "v24 slice" of 02-architecture.md; synthesized from the pre-run panel (adversarial.r1 + quality-dimensions.r1, complementary → no r2). One addition (TOOL_SPECS array + authorize() once before dispatch), one deletion (analyzer subsystem → pure fixed-grammar checkMermaid in the repurposed diagram-gate.ts), the rest re-shapes existing seams: six workspace_* over one pathVerdict, per-agent params (label required/literal, defaults required, values in agent() refused), workflow-owned assets + pushedBy in one catalog `assets` table (mcp_provision merged into workspace_push kind:mcp), triggers claimedBy on the row + triggers[] on the version row with compensation (no store merge), audit_events read back on run_status.adminReads[], guide built from enforcement constants (AUTHORING.md generated). 35 tools (ch.7 36 − run_trigger − mcp_provision + authoring_guide; TOOL_SPECS.length is the check). 3 owner confirms owed (ADR-030 stdio admin-only + http egress gate; ADR-023 fifth shape for the nested-workflow black box; ADR-035 workflow_register.defaults removed). trace 1075/42, all 42 pre-existing, 0 orphan/broken. ORCHESTRATOR ADJUDICATION v24 #1 (2026-09-04) — the Gate 2 pause is LIFTED, all three clarifications answered (04-design.md 'Orchestrator adjudication (v24) #1'): ADR-023 CONFIRMED (plain rectangle = fifth fixed shape for a nested workflow() black box, EXCLUDED from the REQ-111 label diff; REQ-112's acceptance amended to carry it); ADR-035 CONFIRMED and widened — `defaults` is removed ENTIRELY (not kept for appendPrompt), REQ-110 amended so a `defaults` key is REFUSED with a typed error rather than ignored, REQ-088/092 carry a `superseded-by:` note, and working-notes ch.9's tool table is corrected; ADR-030 ACCEPTED AS THE FAIL-CLOSED DEFAULT (stdio MCP admin-only, http probes gated on isEgressAllowed) — verified at source: mcp-probe.ts:71 spawns an author-supplied command on the engine host and :62 fetches an author-supplied URL with no egress gate, the same vector the hook-asset block already closes; the owner is told it narrows their ch.16.2 ruling and may overrule. GATE 3+ DIRECTIVE from the iteration /goal — the executor tier for `tests` and `implement` is deliberately the LOWER model, so the compensation must be in the SPECIFICATION: task partition finer-grained than usual, and the test set deeper, broader and larger in COUNT than a normal slice, explicitly written to trap the failure modes a mid-tier implementer makes (silent no-op instead of refusal, oracle derived from the code under test, a test left green after the thing it named was retired, a description that stops matching the thing). Every REQ-107..118 needs E2E coverage at Gate 7.5, and every one of the 35 tools needs its interface exercised once including its error path. Prior: v24 GATE 1 DONE 2026-09-04 (orchestrator) — interface consolidation: one prefix per entity (workflow_*/run_*/workspace_*, 40->36 tools, NO deprecation window), three roles with audited admin reads, per-AGENT tunable params (agent() may not carry values), author-supplied Mermaid held to the script BIDIRECTIONALLY (replaces the v23 analyzer, statically decidable), assets owned by their workflow + selective materialization, pushedBy on every upload, triggers created-then-claimed, and workflow_authoring_guide. REQ-107..118 written; Round v24 interview held in session (11 open points, all settled). ACCEPTANCE FOR THE WHOLE ITERATION: a cold model with only tools/list + the guide gets it right FIRST TRY (REQ-117) — a wrong step is a DOC defect, fixed then re-run with another fresh instance. Next: Gate 2 via /sdlc-run. Prior: review# v23 GATE 8 RE-REVIEW #2 PASSED 2026-09-04 (reviewer) - ITERATION CAN CLOSE. send_back=[], arch_consistent=FALSE but 0 HIGH: the RE-REVIEW #1 blocking HIGH (inv 2 closure wrap, R-1/SUS-1) is CLOSED and re-verified on disk at src/graph-analyzer.ts:329-345 (try covers scriptSource + _runJob, finally is the only release site). 11 deviations recorded as v24 tech debt (3 MID / 8 LOW after de-duplicating the two pre-run panels): AC-1 FLOOR 2b not total (getDiagram at :260 outside its own try; both panels graded MED on trigger rarity, not overruled upward), AC-2 B5 early-return swallows every zero-model-call settle silently (ARCH-085 readback seam; inv 5 letter holds since no row is written), AC-3 NEW this pass and raised by neither panel - a stray :memory:/catalog.db (28KB sqlite from a throwaway probe) was swept into commit 9fc4439 and is STILL TRACKED at HEAD, breaking git clone on Windows; AC-4..AC-11 LOW. Traceability 1028 items / 18 gaps (0 high / 1 mid IMPL-082 / 17 low), 0 orphan, 0 broken-link, 0 mock-only, 0 unverified; rtm 106/106 real:true. dashboard_check 0 high (3 MID erDiagram bracket false positives re-verified by reading 02-architecture.md:1645-1680; 1 LOW no offline fallback - refresh trace.py BUT KEEP the local caret-### ITEM_RE hardening, plugin 2.1.3 regresses it). solid_check PASS 23 modules 0 high/0 mid. tsc clean; suite 283 files / 1850 tests / 0 failed, run by the reviewer. README+DEPLOY current-state, history-free, 一鍵部署 ./deploy.sh --background verified-run at Gate 7.5 round 5 (62 references in 08-validation.md). Special-file reviews N/A (no CLAUDE.md/AGENTS.md/SKILL.md touched by v23). No playwright in session - visual dashboard render check declared degraded. .panel/ REMOVED per contract task 6 (send_back empty). PRIOR: v23 GATE 7.5 ROUND 5 PASSED 2026-09-04 (validator) - 13 boots, all via the committed deploy.sh; the 9fc4439 settle rework re-validated for real against real Ollama: 12-key journal line (attempts/cause) observed on ready / gate_refused / model_unmapped / disabled / boot_abandoned / prior_restored, two REAL SIGKILL crashes for the two boot-sweep branches, R-3 proven at the store (disabled register writes no row), ADR-020 warn both rows, and every v23 REQ re-greened (VAL-124..127). One doc defect found and NOT silently fixed: docs/AUTHORING.md rule 1 example is refused by the engine (PARAM_CONTRACT_INVALID). DEPLOY.md rewritten to current state (12-key journal line + cause table, enabled:false precision, ADR-020 warn, prior_restored troubleshooting row). trace 1028/18, residue byte-identical; rtm 106/106 green. Hands to Gate 8 (review). v23 GATE 6.5+7 ROUND 5 PASSED 2026-09-03 (verifier) - simplify pass over 9fc4439's 179 src lines found 0 applicable fixes (3 candidates named+rejected); full regression 283 files / 1850 tests / 0 failed / tsc clean; 9 items flipped red->green (UT-129..134, UT-136, UT-137, IT-104), 0 remaining red; coverage 95.51% overall with the delta at 179/179 lines covered and every graph-analyzer.ts function 100%; trace 1024/18 (0 HIGH), solid_check 0 high, determinism exit 0, TZ-travel 0 flips, seams wired once at the composition root, and a REAL Ollama smoke showing the new 12-key journal line (attempts/cause) plus both ADR-020 mirror-warn rows. Hands to Gate 7.5 (validation round 5) - note that round 4's real evidence pre-dates 9fc4439. v23 GATE 6 RE-ENTRY 2026-09-03 (implementer) — both test_defects from the prior Gate 6 round (UT-125, UT-135) closed by the Gate 5 RE-ENTRY below; re-confirmed directly this round (graph-analyzer.test.ts 43/43 incl. UT-125/UT-135, IT-104 pass, full suite 283/283 files / 1850/1850 tests exit 0, tsc clean); no code change needed — src/graph-analyzer.ts + server.ts were already correct (IMPL-177); IMPL-177 amended in place (greens now 13/13 incl. UT-125/UT-135; commit field corrected to 9fc4439, now committed); gates.impl.passed flips true; hands to Gate 6.5+7 (verification) for the 05-tests.md status-flip/regression-closeout per the verifier's own Mode-A note. PRIOR: v23 GATE 5 RE-ENTRY 2026-09-03 (verifier) — adjudication #8 (04-design.md/commit `9fc4439`) applied: UT-125's assertion (b) re-pointed from `orphanRow?.status==='unavailable'` (structurally unreachable per DES-130 B6's late-write guard) to the journal's `cause:'script_unresolved'` line (the direct evidence the DB-row check was only ever a proxy for); assertions (a)/(c)/(d) unchanged. Confirmed green (`npx vitest run tests/unit/graph-analyzer.test.ts -t 'UT-125'` 1/1 pass; same file pair 51/51; full suite 283/283 files, 1850/1850 pass, closing UT-135's already-fixed fixture defect too — 0 remaining red). `npx tsc --noEmit` clean. `sh .sdlc/trace --check`: 1024/18, byte-identical residue (0 new gaps, 0 broken links). Per Mode A scope, UT-129..137/IT-104 are deliberately NOT flipped green here (that's Gate 6.5+7's regression-closeout job) even though the full suite shows them passing. gates.tests.passed stays true; current_stage stays impl — Gate 6 (implementer) still owns confirming/flipping the rest of the send-back's items at Gate 6.5+7. Flagged for Gate 8 (X-2, unresolved): does `workflow_deregister` delete `workflow_diagrams` rows? PRIOR: v23 GATE 5 RE-RUN PASSED 2026-09-03 (verifier), send-back 41e6382: UT-125 amended in place (O3) + UT-129..137 + IT-103/IT-104, RED-confirmed per 05-tests.md's own 'Gate 5 RE-RUN (v23 Gate 2 RE-RUN #2, send-back 41e6382)' section (concurrent-sibling dispatch on the same scope, reconciled — see gates.tests.note). Next: Gate 6 (impl). PRIOR: v23 GATE 2 RE-RUN #2 PASSED 2026-09-03 (architect) for the Gate 8 RE-REVIEW send-back at 41e6382 (send_back=[architecture,tests,impl], 1 HIGH / 4 MED / 4 LOW). NOTE, honestly, same as the previous re-run: Gates 3/4 were NOT sent back, so the next gate the orchestrator should dispatch is 5 (tests/RED), then 6 (impl); this field is advanced per the standard Gate-2 exit contract, not as a claim that design work is owed. Gate 2 ruled R-3 (build inv 11 choke point), R-2b (hold the settle scope, fuse row-write+emit into one _settle seam), R-2 (cause = closed union + attempts field), R-6 and CONS-1 (delete the counts / name the type), :1708 (strike the contradicting clause), SUS-2 (re-assert ADR-020(c)), R-4 (enqueue total at its front door), and added two new floors on inv 2 (idempotent release; total catch) plus five red-today oracles O1-O5. PRIOR: v23 GATE 7.5 ROUND 4 PASSED 2026-09-03 (validator) — the Gate 6 delta a39c0e7 + the Gate 6.5+7 round-4 closeout (eef7809) were exercised on a real booted engine against real Ollama; every v23 REQ keeps a real:true green, four new VAL items (VAL-120..123) record A1 / V-D+A2+A3+inv5 / A4 / A5. Gate 8 (review) next. PRIOR: v23 GATE 6.5+7 ROUND 4 PASSED 2026-09-03 (verifier) - Gate 6 delta a39c0e7 closed: 2 simplify reuse fixes, A4 shipped by the verifier (the one item a39c0e7's subject claimed and did not land), IMPL-175/176 backfilled, TASK-128/129/130 done, 283 files / 1837 tests green, coverage 95.48% with the per-function bar met on every touched function, trace 1008/18 with the residue byte-identical to round 3. Gate 7.5 (validation) next. PRIOR: v23 GATE 5 RE-RUN PASSED 2026-09-03 (verifier) — see gates.tests.note for the 7 new RED items + concurrency note. PRIOR: v23 GATE 2 RE-RUN PASSED 2026-09-03 (architect) for the Gate 8 send-back at d294880. NOTE, honestly: send_back=[architecture,tests,impl] — Gates 3/4 were NOT sent back, so the next gate the orchestrator should dispatch is 5 (tests/RED), then 6 (impl); this field is advanced per the standard Gate-2 exit contract, not as a claim that design work is owed. Gate 2 closed A1 (ADJ-A1 decided: transport gate, single projection, loopback-exempt), A7, A8, A9, A10, and wrote the testable invariants for A2/A3/A5/A6. PRIOR: v23 GATE 7.5 ROUND 3 PASSED 2026-09-03 (validator) — REQ-103's unbound clause closed for real: a brand-new never-bound workflow now gets its first diagram with the entry node [ workflow_run ], non-vacuity control still refuses an un-allow-listed token, all three bound kinds re-observed, README/DEPLOY rewritten to current state, trace 996/18 byte-identical, rtm.md regenerated 106/106 ✅. Gate 8 (review) next. PRIOR: v23 GATE 6.5+7 ROUND 3 DONE 2026-09-03 (verifier, simplify + regression closeout over the Gate 6 delta c9ea0aa, adjudication #7) — SIMPLIFY: ONE reuse fix applied, the unbound entry label was typed as an independent literal at BOTH halves that disagreed (trigger-bindings.ts's instruction to the model, graph-analyzer.ts's allowlist), so the duplication IS the defect class: extracted to one exported UNBOUND_ENTRY_LABEL in trigger-bindings.ts, consumed by describeTriggerBindings (byte-identical output string) and _buildAllowlist; two candidates rejected and named (server.ts's operator-overridable prompt prose - a constant cannot protect a surface an override replaces, and server.ts:189's workflow_run is the TOOL NAME; and importing the constant into UT-123 - its oracle must stay the engine's instruction as a literal). LEDGER BACKFILL, 6th occurrence: c9ea0aa shipped with no ledger item -> UT-123 + IMPL-174. Regression 281/281 files, 1807/1807 tests, exit 0, read on BOTH summary lines and measured twice (pre/post simplify, identical); tsc clean; zero red. Coverage 95.46% overall, this round's two functions 100%, the 77 pre-existing sub-95% functions did not grow. trace 996 items / 18 gaps, byte-identical baseline set, 0 new gap classes; solid_check 0 high/0 mid/10 low; determinism exit 0; TZ-travel identical 0 flips; no new seam. REAL Ollama smoke: an UNBOUND workflow whose real model output is the engine's own instructed token now settles ready and is served verbatim, and a control label on the same real path still settles GATE_REJECTED_CONTENT - the gate is live, not disabled. VAL-119 stays red: only Gate 7.5 ROUND 3 can flip it on the SHIPPED default prompt. Prior: v23 GATE 7.5 ROUND 2 FAILED 2026-09-03 (validator) — V-1 works (VAL-118 green: bindings reach the prompt; entry node names cron/webhook/chain-upstream live), but NEW VAL-119 is red: the engine tells the model to label an unbound workflow's entry node 'workflow_run' and its own diagram gate then rejects that token (_buildAllowlist never adds it) — every newly registered workflow loses its first diagram, a regression vs round 1. Route: Gate 5 RED (gateDiagram over an obedient unbound output) then Gate 6 one line in _buildAllowlist. V-2/V-3/V-4 all re-verified real; manuals rewritten to current state. Prior: v23 GATE 6.5+7 ROUND 2 DONE 2026-09-03 (verifier, simplify + regression closeout over the Gate 6 delta c3b0c01) — SIMPLIFY: reviewed the 52 changed src lines, NO code change made and the two rejected candidates named in IMPL-173 (13-file fake-ChildProcess consolidation = blast radius; _doStart _startPromise reset = a behaviour change). IMPL-173 backfilled (5th occurrence of the implementer-ships-without-an-IMPL gap). UT-119/120/121 flipped green on a full-suite re-run. THE TWO LONG-CARRIED FAILING TEST FILES ARE FIXED, NOT RE-ACCEPTED: val-023 + hooks-reject-and-timeout-bound-journey were still red (afterAll `Hook timed out in 10000ms`, all 5 assertions passing); diagnosed to a deliberately-hung stub holding every socket it never answered, confirmed pre-existing against a scratch git worktree at 71490c0 (worktree, never `git checkout <sha> -- path`), fixed with one `hungStub.closeAllConnections()` per file — suite is 281/281 FILES green for the first time. Full regression 281 files / 1806 tests / 0 failed, tsc clean. Coverage 95.45% overall; per-function bar clear at 100% on everything this round added or modified, after NEW UT-122 took _doStart from 93.6% to 100%; the same 77 pre-existing >5-line functions stay named debt (unchanged count). NEW IT-100 = the system-level half of V-2 (real createServer + aliases so the server builds the managed-proxy gateway itself, real HTTP register, PATH emptied so the litellm spawn ENOENTs) — verified NON-VACUOUS: exit 1 with an unhandled `spawn litellm ENOENT` against the pre-V-2 worktree, exit 0 here. trace 993 items / 18 gaps: baseline residue byte-identical PLUS exactly one new LOW drift row (DES-064 v11 lags IMPL-173 v23) — DES-064 carried the stale warning literal in its own boundary-conditions and was amended in place with an [AMENDED v23 adjudication #6] marker, iter deliberately left v11 so the three v11 tests that trace it do not each become a drift row of their own. solid_check PASS 0 high/0 mid/10 low; determinism_check exit 0; TZ=Pacific/Kiritimati travel run identical; seams production-wired (describeTriggerBindings has a production caller). REAL-DEPENDENCY SMOKE (real local Ollama qwen2.5:7b through real createServer + real JSON-RPC): unbound registration = promptTokens 526, then a real cron schedule_create + regenerate = promptTokens 536 — the prompt genuinely changes with a live binding, which is the exact oracle Gate 7.5 used to prove REQ-103 was NOT implemented (297/297 then). Both diagrams settled unavailable/GATE_REJECTED_SHAPE on this small local model, so the READY path is NOT re-confirmed at real tier — that is VAL-118, and Gate 7.5 owns it. Next: Gate 7.5 (validator) re-runs VAL-118. Prior: v23 GATE 5 RE-RUN 2026-09-03 (verifier, test-first RED, adjudication #6) — REQ-103 was found half-built at Gate 7.5 (VAL-118, blocked/real:true/fail: the analyzer computes trigger bindings and feeds them to the allowlist but never to the PROMPT itself, graph-analyzer.ts:299); orchestrator adjudication #6 (04-design.md) ruled it back in plus two more items found live: V-2 (managed-LiteLLM spawn has no proc.on('error') handler, newly reachable from workflow_register since v23 — the same defect the Gate 6.5+7 verifier's own note below already flagged as ROUTED NOT FIXED) and V-4 (a live /api/runs/:id/dag warning string still names the retired "skeleton" word, REQ-105); V-3 (an AUTHORING.md shape example) is doc-only, no test. Wrote 3 new RED unit tests — UT-119 (graph-analyzer.test.ts: bound-vs-unbound prompts must differ + name the cron/upstream literal; today byte-identical, matching the validator's own live oracle), UT-120 (graph-layout.test.ts: the unmatched-agent warning must not contain "skeleton"; today it does), UT-121 (litellm-proxy-hardening.test.ts: start() must attach an 'error' listener to the spawned child; today it attaches none) — all confirmed red for the stated reason, all traced to existing DES/ARCH/REQ items (DES-131/DES-128/ARCH-078/079/REQ-103; DES-064/REQ-105; ARCH-005/REQ-102), no new TASK needed (adjudication #6 IS the task-equivalent authorization). Also amended 04-design.md's REQ-103 validation row (line ~4224) and DES-131's allowlist note with `[AMENDED v23 adjudication #6]` markers restoring the analyzer-is-given-the-bindings clause the row had silently dropped. tsc clean; targeted vitest run 4/4 new assertions red, 36/36 pre-existing in the same 4 files still green (no regression); trace 987->990 items (+3, exactly the new UTs), gaps 17->17 byte-identical residue to the pre-edit baseline (captured to a scratch file per CLAUDE.md's git-stash/baseline-file rule, never reconstructed backwards). Next: Gate 6 (implementer) lands the three fixes, then Gate 7.5 re-runs VAL-118 only. Prior: v23 GATE 6.5+7 DONE 2026-09-03 (verifier) — simplify (adopted an in-tree Gate-6.5 diff, verified + checkpointed 36fea54; plus one ANALYZER_SCRATCH_SUBDIR reuse fix), IMPL-159..172 backfilled (4th occurrence of the no-IMPL gap; closed 21 of trace's 41 gaps), full suite 281 files / 1799 tests green + tsc clean, 21 v23 test items flipped green and VAL-115 re-labelled blocked-by-design (Gate 7.5/TASK-124 owns it), coverage 95.42% overall with the ENTIRE v23 slice at 100% per-function (3 genuine offenders found and closed with real tests: _buildAllowlist's live-bindings arm, workflow_regenerate_diagram's ANALYZER_DISABLED arm, SqliteRunStore.getWorkflowName — new UT-118), trace 41 -> 20 gaps (residue byte-identical to baseline + 3 HIGH 未真實驗證 on REQ-103/105/106 that only Gate 7.5 can close, so trace exits 1 by construction), solid_check PASS 0 high (one HIGH fixed as an ARCH-085 deps: declaration, no code moved), determinism clean, TZ-travel identical, seams real-wired (exactly one construction site each), and a REAL local-Ollama smoke of the analyzer end to end — both the ready path and the gate-rejection path. ROUTED NOT FIXED: the default managed-LiteLLM gateway path spawns `litellm` with no proc.on('error') handler, so on a host without the binary v23's registration-time diagram now KILLS the process (pre-existing defect, newly reachable). Next: Gate 7.5 (validator). Prior: v23 GATE 5 TASK-127 RED 2026-09-03 (verifier, test-first RED, targeted) — re-verified the R-3(a)/(b) pass below against HEAD (63bf21d, landed after its numbers were captured: TASK-119 shipped, TASK-127 drafted, val-110's vacuous residual retired) — every delta explained (13/13 not 14/14; 1791/1790/1; trace 970/41), none a defect. Then targeted RED for TASK-127 (dod: Gate 5 writes the RED case first): DES-122's zero-config fail-closed rule (graphAnalyzer.enabled + no resolvable config.workRoot -> tools forced to [], boot line states why) is unbuilt at server.ts:1462-1475 (tools defaults unconditionally, never checks config.workRoot). New case IT-099 in graph-analyzer-composition-root.test.ts (TASK-127's own files list): real createServer, graphAnalyzer.tools:['Bash'], no workRoot, spies console.log, asserts effective-tools boot line reads [] and names workRoot as the reason. Confirmed red for the genuine reason (today prints ["Bash"]). tsc clean. Full suite 1792/1790/2 (UT-111 + IT-099). trace --check: 971 items (+1 IT-099), 41 gaps (unchanged — IT-099 fully traced, TASK-127's own gap just changes shape). Hermetic, no clock literals. gates.tests.passed stays true; current_stage stays impl. Next: Gate 6 continues on UT-111/val-112/TASK-127. Prior: v23 GATE 5 R-3(a)/(b) RETIRE+RE-POINT 2026-09-03 (verifier, test-first RED, targeted) — adjudication #2's remaining test-authoring work (R-3(c)/(d) — UT-111/val-112 — stay implementer-owned draft TASK-117/119, untouched). RETIRE (R-3(a), 3 cases asserting the deleted /skeleton surface, supersession recorded in 01-requirements.md REQ-062/REQ-100 partial-supersession notes, parseWorkflowSkeleton's internal survival preserved): workflow-discovery-http.test.ts's two REQ-062 skeleton cases, val-110-script-masking.test.ts's auth-off skeleton case. One adjacent skeleton case (val-110's auth-ON "omits...") left untouched as a flagged residual — not named by the ruling, now green vacuously not by ruling. RE-POINT (R-3(b), oracle from server.ts read first): dashboard-http.test.ts's malformed-%-path DES-018 case moved from /skeleton to /describe (same outer catch, server.ts:1279-1282), green immediately since TASK-120 already shipped /describe. Confirmed exactly these 4 red before editing, 0 others. Full suite after: 1792 total/1790 pass/2 fail (exactly UT-111+val-112, 0 unrelated regressions; 1792=1795-3 retired). tsc clean. trace --check: 969 items, 40 gaps — unchanged from a pre-edit baseline captured to file first (never via git checkout <sha> --). gates.tests.passed stays true (already closed); current_stage stays impl (Gate 6 mid-flight, test-file-only pass). Next: Gate 6 continues on UT-111/val-112. Prior: v23 GATE 5 TASK-126 RED 2026-09-02 (verifier, test-first RED, targeted) — TASK-126 (adjudication #2's new task: the unowned composition root — GraphAnalyzer/real TriggerPorts never constructed/wired into createServer()'s new McpFacade(...)) had no test-first coverage since it was created after the v23 Gate 5 close below. Added IT-098 (tests/integration/graph-analyzer-composition-root.test.ts, traces TASK-126/DES-125/127/131/134/ARCH-078/079/081): 3 cases (construction-site count, McpFacadeDeps required-not-optional + NO_TRIGGER_PORTS fallback gone, workflow_regenerate_diagram not ANALYZER_DISABLED under enabled:true config), all 3 confirmed red for the genuine unimplemented reason. Full suite 1795 total/1783 pass/12 fail (9 = adjudication #2's own already-triaged reds, untouched by this pass; 3 = this pass's own) — 0 unrelated regressions. trace --check: 969 items (+1), 43 gaps (unchanged from baseline), 0 broken links/orphans. Gate 5 for TASK-113..125 stays closed as-is (gates.tests.passed=true, unchanged); current_stage stays impl (Gate 6 mid-flight, untouched). Prior: v23 GATE 5 DONE 2026-09-02 (verifier, test-first RED) — TASK-113..125/DES-120..136/REQ-101..106(+REQ-100 [AMENDED v23]) all covered by new/extended tests (UT-107..117, IT-096/097, VAL-111..117), all confirmed red for the genuine unimplemented reason, 0 unrelated regressions (full suite 1751 total / 1693 pass / 58 fail, exactly this pass's own reds), trace --check 0 broken links / 0 orphans. gates.tests.passed=true. Next: Gate 6 (implementer). Prior: v23 GATE 3+4 RE-RUN DONE 2026-09-02 (designer) — post-adjudication #1: TASK-125 + DES-136 added (phases public on every surface) and 11 shipped DES amended from the re-run panel; gates.tasks + gates.design stay passed; stage unchanged: Gate 5 test-first is next and now covers TASK-125/DES-136 too. Prior: v23 GATE 3+4 DONE 2026-09-02 (designer, merged) — TASK-113..124 + DES-120..135 written from the pre-run design panel (no second panel spawned); gates.tasks + gates.design passed; next: Gate 5 test-first. Prior: v23 GATE 2 DONE 2026-09-02 (architect) — ARCH-077..086 + ADR-015..022 written for REQ-101..106; next: Gate 3 tasks+design. Prior: v23 GATE 1 DONE 2026-09-02 (orchestrator) — author/user separation part 3: workflow_describe + agent-drawn ASCII diagram + retiring the user-facing skeleton + config-separated analyzer + AUTHORING.md. REQ-101..106 written; Round v23 clarification held in session (A1 honest absence / A2 ASCII everywhere / A3 structure-only diagram) — closes GitHub #32 with two owner-ratified deltas from the issue as filed. Carries 3 hard-won rules (oracle-from-code; a test built on the vulnerability it should catch; a moved check leaves stale descriptions behind — 9 instances) + v22's named debt. Next: Gate 2 via /sdlc-run. Prior: review# v22 GATE 8 RE-REVIEW #3 (reviewer, 2026-09-02) CLOSES v22 — send_back=[]. B1/B2 (H1 residual + the val-107 oracle it broke) independently re-verified genuinely fixed at source (server.ts:812-822/873-890 resolveWritePrincipal, symmetric across register/deregister/publish, gated on authEnabled; IT-095 spoof-refusal + restored val-107 non-owner-publish oracle both read directly) and in DES-117's [AMENDED v22 send-back ROUND 2] block (both wrinkles named, "no accepted debt remains on this row"). Both re-dispatched architecture-expert panels (.panel/review/{adversarial,quality-dimensions}.md) re-verified at HEAD 56116f6: adversarial found 13 open deviations (1 HIGH — an escalation of this document's own prior MEDIUM M1, the dropped runs.requested field, tied to Conflict 2's now-falsified audit-trail rationale — plus a NEW MEDIUM V5, a legacy-cohort resume path that substitutes a different script into a live run); quality-dimensions found 6 (0 HIGH, confirms H4/S-1 resolved, holds the prior "non-blocking" disposition). Adjudicated both against primary sources, not either panel's word: V1/M1 stays MEDIUM recorded debt — the code is unchanged since it was dispositioned non-blocking last pass, and this ledger's own terminating-rule convention (used since v21 RE-REVIEW #3) blocks only on send-back items not fixed, fixes that introduce new defects, or new boundary-crossing findings — not a severity re-argument over unchanged code (the Conflict-2 falsification is real and is now recorded in M1's evidence, not used to force a 3rd send-back loop). V5 read directly against 04-design.md DES-113 (Gate-4-passed): the substitution is a RATIFIED design decision ("Legacy-cohort fallback (option b)", named residual, genuinely recorded via recordLegacySubstitution) — the deviation is that ARCH-072/ADR-010/ADR-014 were never amended to name the consequence explicitly; recorded as new M7, doc-drift debt, not a code violation. 7 MEDIUM (M1-M7) + 12 LOW (L1-L12, incl. 7 new LOW doc-completeness findings from the deeper adversarial pass) recorded in 07-review.md §4.3, each with a named cheap resolution path. Mechanical gates re-run by this reviewer directly: trace 893/17 (byte-identical pre-existing gap set: 1 mid IMPL-082, 15 low drift, 1 low TASK-018; 0 未真實驗證/未驗證/僅mock); rtm.md 100/100 REQ real:true (regenerated, spot-checked); dashboard_check 0 high/2 mid (recorded erDiagram crow's-foot false positive, hand-re-verified)/1 low (trace.py version skew); solid_check PASS 0 high/0 mid/10 low (pre-existing unclaimed files, now 13 modules recognized). Independent re-run: npx tsc --noEmit clean; npx vitest run 261 files/1687 tests, 0 failed (2 documented pre-existing spawn-litellm-ENOENT background artifacts) — matches the ledger's own Gate 6.5+7 ROUND 2/Gate 7.5 ROUND 3 claims exactly. Validation/handover re-confirmed: 08-validation.md's ROUND 3 + ROUND 3 CLOSEOUT evidence covers exactly the register/deregister spoof scenario the prior pass flagged as untested, plus a real deploy.sh LAN-IP healthcheck regression found+fixed before this review; DEPLOY.md §0 leads with ./deploy.sh --background (re-verified); single §1b 設定總表; no new config keys; re-grepped both manuals for history tell-tales — clean (vNN provenance tags on config-key rows are current-state labels, not narrative history, an established pattern in these manuals). Special files: git status/git log --name-only 28d24c7..HEAD show only .panel/review/*.md + dashboard.html touched — no CLAUDE.md/AGENTS.md/SKILL.md. gates.review.passed=true; .panel/ removed (send_back empty, per contract §6). **Iteration v22 (001-remote-workflow-engine) CLOSES.** 19 named debt items (M1-M7/L1-L12) carried forward for a future doc-batch/iteration; no needs_clarification raised. Full record: 07-review.md "v22 GATE 8 RE-REVIEW #3". Prior: v22 GATE 7.5 ROUND 3 (validator, 2026-09-02) DONE — re-confirmed REQ-096/097/100 real-tier evidence for B1 (H1 residual)/B2 after the Gate 8 RE-REVIEW #2 send-back's fix (ea97bc8/7bfdc3c, backfilled IMPL-158, HEAD 982f7f7). Booted twice (RWE_BIND=127.0.0.1 for the real-bearer B2 non-owner-publish probe; RWE_BIND=0.0.0.0 for the B1 D-BIND-exempt register/deregister spoof probe). Found+fixed one real deploy.sh defect this round: the §0 healthcheck curled `${RWE_BIND}` literally, which the server's own Host-header allowlist (net-guard.ts, D-BIND/REQ-056) categorically 403s for `0.0.0.0` — healthcheck now targets `127.0.0.1` unconditionally (always the correct loopback Host regardless of bind); re-ran `./deploy.sh --background` both ways to green, this fixed script IS the round's boot-from-docs-only evidence. B1: live spoof attempt (owner string read off `workflow_get`, replayed as `args.principal`, no real bearer) on `workflow_register`/`workflow_deregister` now refused `PRINCIPAL_REQUIRED` (not `NOT_WORKFLOW_OWNER` — no ownership comparison ran), store-level confirmed untouched (no hijack row, owner/version unchanged). B2: live authenticated non-owner bearer refused `NOT_WORKFLOW_OWNER` on `workflow_publish`, including with a forged `args.principal` alongside the real bearer (real bearer wins). Wrinkle-1 (no-auth attribution) re-confirmed live on a separate no-auth scratch boot plus this round's fresh IT-095/val-107 green re-run. Targeted regression 3 files/20 tests green; full suite 261 files/1687 tests green (2 pre-existing spawn-litellm-ENOENT artifacts unchanged); tsc clean. trace 893/17 gaps, 0 severe/0 未驗證/0 僅mock, byte-identical pre-existing baseline (1 mid IMPL-082, 15 low drift, 1 low TASK-018). No config-key drift (pure logic fix; deploy.sh healthcheck-host fix touches no config file). Full detail: gates.validation note + 08-validation.md's "v22 GATE 7.5 ROUND 3" section. gates.validation.passed=true. Next: Gate 8 re-review #4 (reviewer) — confirm B1/B2 closure holds. Prior: v22 GATE 5 SEND-BACK ROUND 2 (verifier, test-first RED, 2026-09-02) DONE — RED tests for B1 (H1 residual)+B2 (07-review.md §4.2/§8: IT-095 self-asserted-principal closure, VAL-107 restored oracle, VAL-097 B2 re-pin); see gates.tests note + journal.md for full detail. Prior: v22 GATE 8 RE-REVIEW #2 (reviewer, 2026-09-02) SEND BACK: send_back=[tests,impl] — H1 only half-fixed (workflow_register/deregister still accept args.principal; owner disclosed to readers, same attack reopens one read away), H2/H3/H4 fully fixed and re-verified. Full detail: gates.review note + 07-review.md's new top section. Prior (v22 GATE 7.5 ROUND 2 DONE 2026-09-02, validator) — re-confirmed REQ-096/097/100 real-tier evidence after the Gate 8 send-back's H1-H4 fixes (330eefd/2e865e8, on HEAD 28d24c7; working tree also carries the verifier's own H4 dedup, src/errors.ts/scheduler.ts/webhook-registry.ts, behavior-neutral). Full detail in gates.validation note + 08-validation.md's "v22 GATE 7.5 ROUND 2" section. Summary: booted twice (RWE_BIND=127.0.0.1 for the real owner-bearer path; RWE_BIND=0.0.0.0 to reproduce H1's exact D-BIND-exempt vulnerable shape), both g28d24c7-confirmed fresh boots, no undocumented steps. H1 (REQ-096/097): anonymous D-BIND-exempt loopback caller now refused PRINCIPAL_REQUIRED on workflow_register/publish/deregister, including the args.principal forgery attempt; owner/non-owner bearer paths on the 127.0.0.1 boot unaffected (NOT_WORKFLOW_OWNER still fires for a wrong-but-authenticated principal). H3 (REQ-096): direct real-tier repro against a scratch on-disk WorkflowCatalog confirmed MAX-based allocation (v7→v8, not COUNT-based v2; gapped v1/v3→v4, not v3 collision). H4 (REQ-097): schedule_create AND webhook_create both now refuse CHANNEL_UNPUBLISHED against a registered-but-unpublished workflow (both named sites per adjudication #6 §8.1). H2 (REQ-100): GET /api/runs/:id/dag under auth now serves zero script-derived skeleton nodes (only live agent nodes), confirmed live plus by source read of the shipped fix. Regression: 12 targeted files/66 tests green (incl. IT-091/092/093/UT-106/IT-094, the Gate-5 send-back's own regression tests); full suite 261 files/1683 tests/1683 pass, tsc clean; trace 891/17 (0 嚴重/未驗證/未真實驗證, byte-identical to the verifier's baseline). Config sync: no new keys (H1-H4 are pure logic fixes). README.md:159 tightened to name the DAG route explicitly now that H2 makes the claim true again; no other doc changes needed (grepped clean for history tell-tales). No REQ metadata (VAL-106/107/110's real:true) changed — this round re-confirms already-true flags against the post-fix code per the standing-flag precedent (v21 ROUND 2..5); no needs_clarification. Cleanup: 3 test workflows deregistered, both validation boot PIDs killed, scratch scripts removed, git status clean of validation artifacts. gates.validation.passed=true (unchanged); validation→review (Gate 8 re-review next). Prior: v22 GATE 6.5+7 SEND-BACK CLOSEOUT DONE 2026-09-02 (verifier) — full detail in gates.verification/gates.impl notes below and 05-tests.md/06-impl-log.md. Summary: backfilled the Gate 6 ledger gap (H1-H4 fixes were shipped in 330eefd/2e865e8 but never logged, IMPL-157), simplified the H4 error-mapping duplication (errors.ts catalogResolveErrorEnvelope), flipped IT-091/092/093/UT-106 green + added IT-094 (H4 second site) + 3 dashboard-http.test.ts cases (coverage gate), full suite 1683/1683, src/ coverage 95.03%, trace 891/17 (0 severe, 0 new), solid_check/determinism_check/TZ-travel all clean, seam wiring + real-dep smoke confirmed. Prior: v22 Gate 5 SEND-BACK DONE 2026-09-02 (verifier, test-first RED) — the 4HIGH findings from the v22 Gate 8 REVIEW send-back now have RED regression tests (IT-091/092/093, UT-106) + IT-080 amended for H1's fixture-reachability change; DES-117 amended (PRINCIPAL_REQUIRED). See gates.tests note. Gate 6 next: implementer fixes each HIGH per its named fix. Prior: v22 Gate 8 REVIEW DONE 2026-09-02 (reviewer) — SEND BACK to tests+impl (4 HIGH architecture-vs-implementation deviations; see gates.review note + 07-review.md). Prior: v22 Gate 7.5 DONE 2026-09-02 (validator, real-tier REQ-096..100) — see gates.validation note. Prior: v22 Gate 6.5+7 DONE 2026-09-02 (simplify + regression closeout, verifier) — Gate 6 (implementer) had actually shipped TASK-105..112 across commits a54a794/fd19710/ccee745/1ffca37/f4aba7d/23a5fd9 but left NO IMPL-* ledger entries and never updated current_stage/gates.impl (state.yaml still read Gate-5-done); backfilled as IMPL-149..156 in 06-impl-log.md (retroactive, git-show-attributed, per the IMPL-142/145 precedent) — see gates.impl note below. This pass: (1) simplify — removed assertScriptIntegrity/scriptSha256 dead code orphaned by REQ-085's own v22 supersession (retired its sole test UT-091/VAL-094 too, both had been left describing deleted files as green); amended ARCH-074/ADR-013 [AMENDED v22 gate-closeout] to the shipped HarnessDescriptor.mcpUnresolved mechanism per adjudication #5 O-1 (IMPL-148's own "flagged for the gate closeout"); fixed 1 solid_check HIGH (ARCH-073 missing ARCH-069 in deps — server.ts's pre-existing v1 gateway import, first module: declaration ever written for this file). (2) regression — flipped UT-102..105/IT-084..089/VAL-106..110/UT-033/IT-011 to green; full suite 1662/1662 post-simplify, exit 0 (2 documented spawn-litellm-ENOENT background artifacts unaffected). (3) coverage gate — src/ overall 95.02% (was 94.80% at v21 close), 2 genuine new-in-v22 offenders found+closed (WorkflowCatalog.publish's UNKNOWN_VERSION 89.5%→100%, InMemoryRunStore.recordLegacySubstitution 25%→100%, 3 new cases logged in 05-tests.md/UT-010+IT-084); ~27 remaining sub-95%-threshold functions in v22-touched files all git-blame-confirmed pre-existing (v1/v2/v7/v11/v15/v20), same "pre-existing function debt list, not re-scoped" class v21 already carried. Final suite 1665/1665, tsc clean. trace --check: 885 items/22 gaps (was 877/31 pre-pass) — the -9 delta is exactly TASK-105..112's 8 「未實作」+REQ-097/100's 2 「未實作」closed by the IMPL backfill minus +1 new declared-false-positive drift (UT-010←DES-113/TASK-108, iter:v21 test now tracing a v22 design — the DES-088/DES-066/DES-099/DES-100 "iter records origin not last-touch" class, now 5 pairs); remaining 22 = 5 high 未真實驗證 (REQ-096..100, real:false until Gate 7.5, textbook Gate-6.5+7 shape) + 1 mid TDD (IMPL-082, pre-existing) + 14 low 漂移 (pre-existing declared-false-positive class, now +1) + 1 low 未實作 (TASK-018, pre-existing) — 0 severe, 0 new gap CLASSES. solid_check: 0 high (was 1, fixed)/0 mid/10 low (pre-existing unclaimed-file drift, unchanged). determinism_check: clean, exit 0. Time-travel (TZ=Pacific/Kiritimati): 1662/1662, exit 0, no flips. Seam wiring: WorkflowCatalog's aliasNames/mcpLookup ports both wired real at server.ts:1229-1241 (mcpRegistry.get, config aliases); ReadContext/resolvePrincipal wired real at server.ts's /mcp dispatch, proven by IT-089's real-transport test. Real-dependency smoke: the v22 REQs are catalog/masking/channel semantics — VAL-106..110 already run real createServer+real HTTP+real SQLite (no SUT-boundary mock), satisfying this tier for the slice; LLM-tier (VAL-092 REQ-083 echo) stays deferred to Gate 7.5 (litellm binary absent on this host, documented since v1). gates.verification.passed=true; verification→validation. Next: Gate 7.5 (validator), real-tier run for REQ-096..100. Prior: v22 Gate 5 DONE 2026-09-01 (test-first RED — 11 new work items + 2 extended in place, 60 genuinely-red cases, 0 unrelated regressions; full record in gates.tests note). Next: Gate 6 (implementer) via /sdlc-run. Prior: v22 Gate 3+4 DONE 2026-09-01 (tasks TASK-105..112 + design DES-109..119 synthesized from the pre-run panel). # v22 Gate 2 DONE 2026-09-01 (architecture synthesized from the pre-run panel; ARCH-071..076 + ADR-009..014). Prior: v22 Gate 1 DONE 2026-09-01 — author/user separation part 2: version history, beta/release channels, closing inline script, moving submission validation to registration, masking workflow_get for non-owners. REQ-096..100 written by the orchestrator; no new interview needed because the scope IS the owner's Round-v21 answers (Q1 close inline, Q3 mask script, Q4 approved the ordering) — recorded in 01-requirements.md's Clarification log. Carries two v21 rules (assert the property not the shape; never launch over an unstopped run) and two cheap inherited debt items (S-1, P6-2's registration half). Next: Gate 2 via /sdlc-run.   #  v21 CLOSED BY OWNER DECISION 2026-09-01 — after Gate 8 re-review #6 the owner was shown every open finding (what it allows, who triggers it, cost to fix) and chose: land P6-1 + its 5 riders, NO seventh review pass, close, carry the rest as named debt into v22. Full record + debt table in 07-review.md 'GATE 8 — v21 CLOSED BY OWNER DECISION'. State at close (2c08719): suite 1582/1582, tsc clean, coverage 95.31%, trace 824 items/14 gaps with 0 severe / 0 unverified / 0 mock-only, rtm 95/95 real:true, Gate 7.5 ROUND 5 booted from docs alone and confirmed every REQ live incl. all 4 P6-1 delimiter variants refused and P6-2's enforcement point measured. Delivered REQ-090..095: the declared parameter contract, overrides validated with PARAM_LOCKED/PARAM_OUT_OF_RANGE before durable work, repair of two pre-existing defects that made REQ-088 hollow (resolveHarnessParams zero callers; effort a documented no-op), appendPrompt fixed after the author segment, and problem reports bound to workflow+version.   #  v21 Gate 7.5 ROUND 5 PASSED (2026-09-01, validator) — re-confirmed REQ-090/091/094 live after Gate 8 RE-REVIEW #6's P6-1..P6-5 riders (commit efddfd7); full detail + boot log in 08-validation.md's "v21 Gate 7.5 ROUND 5" section; trace 824/14 gaps (0 嚴重/未驗證/未真實驗證/mock-only, identical baseline); rtm.md regenerated 95/95 ✅. current_stage validation→review; gates.review.passed stays false (Gate 8 pass 7 owns that flip). Prior: v21 GATE 6 RE-RUN #7, RIDERS HALF (2026-09-01, implementer) DONE — the LAST code change of iteration v21; the owner has decided v21 closes after this pass, with anything still open carried to v22 as recorded debt. Closes the final two items of 07-review.md GATE 8 RE-REVIEW #6 §T5/§T6 (P6-1 delimiter variants + P6-2/P6-3/P6-4 + QD-REP-1 had already landed at c5e5cf5; pre-pass suite re-verified 1578/1578 before starting, so no earlier rider was left open). Both took the FIX route per adjudication #10 J-2. P6-5: DEFAULT_CEILINGS is now exported ONCE from src/params/contract.ts beside the Ceilings interface it types, and imported by run-manager.ts:42, mcp-facade.ts:14 and server.ts:48 — the production composition root, which previously re-typed all three numbers and imported neither of the other two copies; server.ts keeps its per-key config-or-default fallback mirroring run-manager.ts:230-232; net -4 lines of code before comments; zero behavior change (identical values at HEAD). Two comments that re-typed the same numbers in prose were corrected too (mcp-facade.ts's now-false 'same convention as run-manager.ts own DEFAULT_CEILINGS', and server.ts:155-159's ServerConfig block, which spelled the three values out) — a comment restating a value is the same drift class and no regex over src/ catches it. STRUCTURAL PIN, extending UT-098 in place (tests/unit/params-contract.test.ts, +4 cases, no new ID): (a) exactly one const DEFAULT_CEILINGS declaration under src/ and it is contract.ts's exported one, (b) each of the three consumer sites imports it (matchAll, so a second import cannot hide behind a first — QD-REP-1 precedent), (c) no src/ file except contract.ts assigns a LITERAL to any of the three ceiling keys, (d) a supplementary value assertion whose oracle is the PUBLISHED contract (DEPLOY §1b + ADR-005), not the code under test. THE PIN WAS MUTATION-CHECKED, NOT ASSERTED, and that is why it is correct: three mutations applied to the real tree, run, reverted — (1) re-typing ONE fallback at server.ts's composition root, (2) dropping the import and re-typing all three, (3) a FOURTH declaration in mcp-facade.ts. Mutation (1), the exact shape P6-5 was filed about, PASSED the first draft of (c) silently (that draft only matched a literal directly after the colon, not one at the end of a ?? fallback); the regex now reads the whole value expression. Re-run: (1) → (c) red, (2) → (b)+(c) red, (3) → (a)+(b)+(c) red, tree restored, 75/75 green. Trusting the unmutated pin would have shipped a guard that did not guard its own motivating case — RE-REVIEW #6's own retro lesson, one layer up in the test instead of the comment. QD-CONS-3: re-verified against workflow-catalog.ts:160-194 before writing (the G-1 loop bounds the FINAL effectiveDefaults, caller-supplied defaults.knob and normalized params.knobs.knob.default alike), then corrected 02-architecture.md:967 (v21 interface table row) and DEPLOY.md's maxTimeoutMs row to name BOTH rungs with script per-call agent() opts as the one surviving exclusion (ADR-005). THE REVIEW'S COUNT WAS LOW BY ONE: a THIRD stale surface, 02-architecture.md:976 (the Gate-2 Decision-rationale bullet for ADR-005), carried the identical false 'user-override rung only' claim one screen below :967 and was not on the review's list — fixed and DISCLOSED with the same bracketed AMENDED marker S-2 used, not silently retargeted. DEPLOY.md's maxAppendPromptBytes/maxEffort rows deliberately NOT rewritten: incomplete but not false (they assert no negative); widening them is a v22 doc-completeness item. Recorded as IMPL-147 in 06-impl-log.md. Verification: npx tsc --noEmit clean; full npx vitest run 1582 passed / 0 failed (243 files) = pre-pass 1578 + exactly this pass's 4 cases, 0 regressions (2 pre-existing spawn-litellm-ENOENT background artifacts, documented since IMPL-140); sh .sdlc/trace 824 items (was 823, +1 = IMPL-147) / 14 gaps with the gap set BYTE-IDENTICAL to a scratchpad snapshot taken BEFORE this pass touched the ledger (per CLAUDE.md, never by checking the ledger backwards in place); both traced ids (TASK-100, DES-104) are v21-origin so no new iter-drift pair was minted. No test weakened, deleted or re-scoped. Changes are in the WORKING TREE, deliberately uncommitted. gates.impl.passed stays true; gates.review.passed stays FALSE — pass 7 owns that flip, not the implementer. STANDING FLAG for validation ROUND 5: this changed src/ after Gate 7.5 ROUND 4's evidence, and server.ts's composition root sits on every REQ's path — provably behavior-neutral (same three values to the same three consumers, acceptance tiers that pin timeoutMs.max at the MCP boundary unchanged and green) but ROUND 5 should confirm a fresh boot still reports the documented ceilings rather than assume it. Prior: v21 GATE 5 RE-RUN #7 (2026-09-01, verifier, test-first RED) PASSED — per 07-review.md GATE 8 RE-REVIEW #6 §T6 send-back, scope pinned to P6-1 (BLOCKING) + P6-2/P6-3/P6-4 riders (fix route, not the recorded-decision escape hatch — each is a 1-3 line change on an existing precedent) + QD-REP-1 (green fence). Extended IN PLACE, no new IDs: tests/unit/params-contract.test.ts (UT-098, +9 cases across 3 new describe blocks: P6-1 4 case/whitespace-variant reds against the existing FRAME_CLOSE_FORGERY block's sibling, P6-3 args-default rejection 1 red + 1 green pin, P6-4 non-string/numeric enum-member rejection 2 red + 1 green pin — 71 total, 7 failed/64 passed), tests/integration/params-admission.test.ts (IT-083, +3 cases: P6-1 one resume-side case-variant red appended to the existing F2-durable-half block confirming the shared FRAME_CLOSE_FORGERY constant propagates, P6-2 new describe block — a registered defaults.appendPrompt carrying the forged delimiter dispatched with NO overrides at all, 1 red + 1 green pin — 22 total, 2 failed/20 passed), tests/unit/params-resolve.test.ts (UT-099, +1 green case: QD-REP-1's zero-src/-importer structural fence for this file's mapEffort, mirroring the ADR-006 session-options-builder.ts pin but scoped to named+namespace import forms of the specific binding — 28/28 pass). Full suite npx vitest run: 1578 total, 9 failed/1569 passed (243 files) — exactly this round's 9 genuinely-red cases, 0 unrelated regressions vs the pre-round 1565/1565 baseline (2 pre-existing spawn-litellm-ENOENT background artifacts, documented since IMPL-140). npx tsc --noEmit clean. sh .sdlc/trace --check: 823 items / 14 gaps, IDENTICAL to the RE-REVIEW #6 disclosed set (0 new work-item IDs — only existing UT-098/IT-083/UT-099 cases extended, 0 new gap classes, 0 broken links). Hermetic: no absolute-date-vs-real-clock comparisons in any new case (FixedClock anchors only where a clock is used at all; most new cases are pure/no-clock). P6-3/P6-4/P6-2 each carry a verified-decision escape hatch per 07-review.md §T6 — if Gate 6 takes the recorded-decision route instead of the fix for any of them, the corresponding red case is adjusted by adjudication, not silently (same precedent as the F2-durable-half case's own alternative-encoding note). NOT in this pass's Gate-5 scope (per §T6, impl/doc-only, no dedicated test): P6-5 (ceiling-triple duplication — shared DEFAULT_CEILINGS export or corrected S-1 residual) and QD-CONS-3 (02-architecture.md:967 + DEPLOY.md maxTimeoutMs row doc batch). gates.tests.passed=true (unchanged); current_stage review→impl (Gate 6 re-run #7 next, scope per §T6: P6-1 one-line widen of the shared FRAME_CLOSE_FORGERY constant to /<\s*\/\s*user-instructions/i [linear pattern, no nested quantifiers] + P6-2 3-line run-manager.ts mirror-or-recorded-decision + P6-3/P6-4 validateSpecShape rejections-or-recorded-decisions + P6-5 shared-export-or-corrected-residual + the QD-CONS-3 doc batch, all in the SAME pass per the H-4 precedent). Prior: v21 GATE 8 RE-REVIEW #6 (2026-09-01, reviewer) SENT BACK [tests,impl] — arch_consistent=FALSE over P6-1 (FRAME_CLOSE_FORGERY case/whitespace variant class slips the caller rung — boundary-crossing per §S5's own terminating rule; one-line widen) with riders P6-2..P6-5/QD-REP-1/QD-CONS-3 (each with an escape hatch); every §S7 item verified closed/decided at HEAD; scope pinned 07-review.md §T6; pass-7 terminating rule: P6-1 the only blockable item, close with recorded debt unless a NEW boundary-crossing finding from this batch remains; Gate 7.5 ROUND 5 re-confirms REQ-091/094 via the standing flag; .panel/ retained; full record in gates.review note. Prior: v21 GATE 7.5 ROUND 4 (2026-09-01, validator) PASSED — closes the standing flag GATE 6.5+7 left (2e58d86 + the verifier's own working-tree dedup landed src/ changes after Gate 7.5 ROUND 3's evidence was collected). Re-confirmed REQ-090/091/092/094's real-tier path for the 4 behavior deltas (F2 admission refusal + resume-side mirror, F1 alias-predicate swap, F4 enum min/max rejection) live over MCP HTTP against a fresh deploy.sh boot (g9eae708); REQ-093/095 stand on Round 1/2 evidence per src/ diff-scoping (their dispatch/issue paths untouched). Full suite 1565/1565 pass identical to Gate 6.5+7's own stamp; tsc clean; trace 823/14 identical gap set (0 severe/未驗證/mock-only); rtm.md 95/95 real:true; config sync clean (no new keys); README/DEPLOY needed no edits. Full detail in gates.validation note below. current_stage validation→review (Gate 8 re-review #6 next). Prior: v21 GATE 6.5+7 (2026-09-01, verifier, simplify+regression closeout, re-run after GATE 6 CLOSEOUT #6) — closes the standing flag GATE 6 CLOSEOUT #6 left (2e58d86 landed src/ changes after Gate 7.5 ROUND 3's evidence was collected). 0. Simplify: reviewed the round's diff (2e58d86: contract.ts/harness-defaults.ts/run-manager.ts/server.ts/issue-reporter.ts) against reuse/simplification/efficiency/altitude — found ONE genuine, avoidable duplication: run-manager.ts's resume-side F2 guard re-typed contract.ts's FRAME_CLOSE_FORGERY regex inline solely because the constant was module-private (unlike the deliberate contract.ts/resolve.ts mirror, which exists to dodge a real import cycle — run-manager.ts already imports several names from contract.ts, no cycle risk). Exported FRAME_CLOSE_FORGERY and had run-manager.ts import/reuse it; quality-only, no behavior change, re-ran affected tests green (recorded as an addendum on IMPL-145 in 06-impl-log.md, not a new IMPL entry). Also found and fixed 3 stale ledger entries: UT-098/IT-081/IT-083 in 05-tests.md still carried their Gate-5-RED status:red/result:fail snapshot even though 2e58d86/IMPL-145 had made all three green — flipped to status:green/result:pass with a note, per this gate's own "mark passing green/pass" duty (the Gate 6 closeout passes never touch 05-tests.md metadata). 1. Regression: full suite npx vitest run → 1559 passed / 0 failed (243 files), IDENTICAL count to the pre-edit baseline (the simplify dedup and metadata fixes are non-code/quality-only); npx tsc --noEmit clean; 2 pre-existing spawn-litellm-ENOENT background artifacts (documented since IMPL-140), 0 test failures. 1b. Coverage: overall src/ line coverage 95.18% (≥ 90% whole-tree bar, up from 94.80%); the lines this round's diff actually touched (FRAME_CLOSE_FORGERY check in validateUserOverrides, enum min/max rejection in validateSpecShape, isKnownAlias swap in harness-defaults.ts, resume-side guard in run-manager.ts) sit inside contract.ts (100% whole-file) and outside harness-defaults.ts's/run-manager.ts's uncovered ranges (73,75-76,81-82 and 596-598,649-653 respectively, both pre-existing and unrelated to this diff — confirmed by direct line-range comparison against git show --stat 2e58d86); per-function bar enforced against this round's added/modified code only, per the standing first-coverage-pass precedent; pre-existing 81-function debt list + src/sandbox/child-entry.ts's 0%-instrumented child-process entry point unchanged, not re-audited. 2. No remaining red. 3. sh .sdlc/trace --check: 823 items / 14 gaps, IDENTICAL to IMPL-146's disclosed set (0 severe, 0 未驗證, 0 未真實驗證, 0 orphan/broken-link, 0 new gap classes — cross-checked against dashboard.html's 缺口 tab row-by-row). 3b. solid_check: 0 high / 0 mid / 10 low, byte-identical baseline. 4. determinism_check: pass (new checks are pure regex/string logic, no seam touched). 5. TZ-travel (TZ='Pacific/Kiritimati', no libfaketime on this host): 1559/1559 pass, 0 time bombs. 6. Seam wiring: no new seam this round (pure validation logic, no clock/network/randomness/DI registry); server.ts's single ceilings object still feeds both RunManager and McpFacade unchanged. 7. Real-dependency smoke: F2's durable half (IT-083) already exercises a real RunManager + real SqliteRunStore across two independent manager instances sharing one on-disk store (post-restart-rehydrate shape), SUT boundary not mocked; no new external integration introduced by this round's diff, so no additional smoke target exists. gates.verification.passed=true; current_stage verification→validation (Gate 7.5 re-confirmation next, per the standing flag). Prior: v21 GATE 6 CLOSEOUT #6 (2026-09-01, integrator, cross-task scope) — the 07-review.md GATE 8 RE-REVIEW #5 §S7 send-back is CLOSED end to end; IMPL-145 + IMPL-146 in 06-impl-log.md are the record. Suite 1559 passed / 0 failed (243 files), tsc clean, trace 823 items / 14 gaps. CODE half (F2 both halves, F1 half 1, F4, F5-decided, C-3's schema+drift-lock) rode commit 2e58d86, which landed on HEAD with NO ledger entry — the FIFTH instance of this iteration's race class (35e6994, eb58d96, 244f9f0, 997626d, now 2e58d86); attribution settled from git show --stat, one commit = one entry (IMPL-145), and the two commits either side are deliberately NOT re-logged (976249c is the Gate 5 RED, already in 05-tests.md; c0e6cec is doc-only adjudication #9). §S7's F1 HALF 2 (the workflow-catalog.ts call reorder) is RETIRED as SUPERSEDED-BY-MEASUREMENT, not applied — six declared-knob-default shapes that pass their own spec and would fail validateHarnessDefaults were probed against a real WorkflowCatalog: in the PRODUCTION composition (ceilings passed) 6 of 6 are already refused with nothing stored (5 by the G-1 ceiling loop, which carries the canonical per-knob TYPE and the effort enum via effectiveBounds(canonicalContract(), ceilings), 1 by contract.ts:242's model.default alias check), and the set the reorder would newly cover is empty by construction (effectiveDefaults ⊇ defaults with caller values byte-identical; params-origin keys are confined to TUNABLE_KEYS ⊂ KNOWN_KEYS). The counterfactual was measured too: applying the reorder verbatim changed ZERO accept/reject decisions and kept the suite green, but flipped every params-origin rejection from the origin-keyed PARAM_CONTRACT_INVALID/'params.knobs.effort.default' to HARNESS_DEFAULTS_INVALID/'defaults.effort' — naming back a field the script author never wrote, breaking the very origin-keying §S7 told the implementer to preserve. A strict regression in error attribution for zero security gain, so not applied; 3 green pins added to IT-081 so the retired prescription cannot be silently re-applied. The only residual (a ceilings-less catalog stores 5 of 6) is S-1 verbatim — recorded debt, explicitly on §S7's NOT-in-scope list. DOC BATCH landed this same pass per the H-4 precedent: F3 (the nesting residual was wrong on BOTH halves — it is WIDER than caller overrides, since the run-scoped snapshot carries the PARENT author's defaults.prompt/defaults.tools and the child row's own defaults/params are never read, and it is INTRODUCED by v21, verified at the v20 tip 637b86e where resolveHarnessParams had zero src/ callers; rewritten in 04-design.md ×2 + a new residual line on ARCH-066, v22 candidate re-filed), S-2 (ADR-005's title/decision text and ARCH-066 inv-4 both still said ceilings bound 'the USER override rung ONLY' — false since G-1; corrected to the two rungs, and the lowered-ceiling asymmetry MEASURED on two servers over one workRoot before being declared INTENDED: a row stored under maxEffort:'max' still dispatches under maxEffort:'high' with no overrides, while the identical value as a caller override is refused and a re-register is refused — operator-caused, not caller-reachable, visible on workflow_get, one-call repair, boot-sweep filed as a v22 candidate), C-3's doc half (the review MISATTRIBUTED it to DES-098; 04-design.md:2341 is under DES-099 — corrected the right item plus DES-100:2352 and the v15 class diagram, and recorded the misattribution rather than silently retargeting). GAP SET GREW 11→14, disclosed not smoothed: all 3 are the declared false-positive iter-drift class (DES-099←IMPL-145, DES-099←IMPL-146, DES-100←IMPL-146 — v15 design items traced by v21 IMPL entries; iter: records ORIGIN, not last touch, same as the DES-088/DES-066 pairs IMPL-140 declared). 0 new gap classes, 0 高嚴重度, 0 未驗證, 0 未真實驗證, 0 orphan/broken-link. Changes are in the WORKING TREE, deliberately uncommitted. gates.review.passed stays FALSE: Gate 8 re-review #6 owns that flip, not the integrator. STANDING FLAG for the next gate: 2e58d86 is a behaviour-affecting src/ change landed AFTER Gate 7.5 ROUND 3's evidence was collected (0abba3a scoped itself by git diff 90b5d30..HEAD -- src/, which predates it) — contract.ts, run-manager.ts, harness-defaults.ts and server.ts all moved since, and the F2 admission refusal sits directly on REQ-094's path; current_stage routes to verification, and validation must re-confirm rather than ride Round 3's stamp. Prior: v21 GATE 5 RE-REVIEW #5 re-run (2026-09-01, verifier, test-first RED) PASSED — per 07-review.md GATE 8 RE-REVIEW #5 §S7 send-back, scope pinned to F2 (BLOCKING)/F1/F4; full detail in gates.tests note above. Summary: extended in place, no new IDs, across tests/unit/params-contract.test.ts (+5: F2 forged-delimiter refusal ×2 red + 1 green pin, F4 enum min/max rejection ×2 red), tests/unit/params-resolve.test.ts (+1 green FRAME INTEGRITY PIN, composePrompt deliberately unchanged), tests/integration/harness-defaults-validation.test.ts (+2: F1 both-doors parity, 1 red + 1 green pin), tests/integration/params-admission.test.ts (+1 red: F2 durable-half resume refusal, poisoned row seeded directly per the VAL-100 precedent). Full suite 1555 total, 6 failed/1549 passed (243 files) — exactly the 6 genuinely-new-red cases, 0 unrelated regressions vs the pre-round 1546/1546 baseline. tsc clean. trace 821/11 IDENTICAL to baseline (0 new gap classes). current_stage review→impl (Gate 6 re-run #5 next, scope per §S7: F2 admission refusal in contract.ts + resume-side check in run-manager.ts [or a recorded verified decision] + F1 predicate swap/reordering in harness-defaults.ts+workflow-catalog.ts + F4 fold-in in contract.ts, PLUS the doc batch F3/S-2/C-3 in the SAME pass per the H-4 precedent). Prior: v21 GATE 7.5 ROUND 3 (2026-09-01, validator) PASSED — re-confirmed REQ-090/091's real-tier acceptance path after IMPL-144/997626d landed post-Round-2 (full detail in gates.validation note). validation→review (Gate 8 re-review #5 next). Prior: v21 GATE 6.5+7 (2026-09-01, verifier, simplify+regression closeout, re-run after GATE 6 CLOSEOUT #5) — full detail in gates.verification note below. Summary: found an untraced src fix (997626d, appendPrompt enum-branch content leak, DES-101 row 6) landed on HEAD with no ledger entry — reconciled as IMPL-144 (same class as IMPL-142/143), verified against HEAD per this iteration's established precedent (do not revert a human commit). Simplify: reviewed the full round's diff (43042d3 + 997626d on src/params/contract.ts) against all 4 angles, found already clean, no changes applied. Full suite 1545/1545 pass (243 files, up from 1533 by 997626d's 10 cases + 2 new coverage-gate cases this pass), 0 failures (2 pre-existing spawn-litellm-ENOENT background artifacts, documented since IMPL-140). tsc clean. trace 821 items (+1 = IMPL-144) / 11 gaps, gap set byte-identical to a pre-edit scratchpad snapshot (0 new gap classes). solid_check: 0 high / 0 mid / 10 low (unchanged, pre-existing). determinism_check: pass. TZ-travel (Pacific/Kiritimati, no libfaketime on this host): 1543/1543 pass, 0 time bombs. Seam wiring: no new seam this round (contract.ts stays pure); re-confirmed server.ts's single ceilings object feeds both RunManager and McpFacade. Real-dep smoke: val-103-effort-real 4/4 real Ollama (OLLAMA_BASE_URL + litellm venv on PATH); VAL-100's new real-tier case 7/7 real HTTP+SQLite. Coverage: overall src/ 94.80% (13709 stmts, 12996 covered, up from 94.54%), 90% whole-tree bar cleared. Per-function bar enforced against v21-added/modified code (established precedent from the first coverage pass): found parseParamContract (v21-added, TASK-097) at 94.03% (2 missed statements, the raw.knobs/raw.args shape-guard branches) — WROTE 2 new cases (extending UT-098), re-measured 100%. All functions touched by 43042d3/997626d (validateUserOverrides, checkValueAgainstSpec, boundMax, boundEffort, validateSpecShape, truncatedSupplied) already 100%. Pre-existing 81-function debt list unchanged, not re-audited this pass (same scope precedent). Flag for the next gate: 997626d is a behavior-affecting src change landed AFTER Gate 7.5 ROUND 2's evidence was collected — validation must re-confirm REQ-090's real-tier acceptance path, not trust Round 2's stamp. gates.verification.passed=true; current_stage verification→validation. Prior: v21 GATE 6 CLOSEOUT #5 (2026-09-01, integrator, cross-task scope) — the Gate 8 RE-REVIEW #4 §Q7 send-back is CLOSED end to end. Code half (A1 both halves / A2 / A4 / A5) rode commit 43042d3; this pass landed the cross-partition DOC BATCH that no single TASK owns and that dangled through two prior closeouts, per adjudication #8 H-4 (land it in the SAME pass as the code, not as a follow-up): A3 (ARCH-064 inv 2 + S-2 stop crediting inputSchema additionalProperties:false as a server-side control — nothing evaluates inputSchema; the closed UserOverrides type + validateUserOverrides ARE the control), A6 second half (the `sent, not verified-honoured` definition of effortApplied:true mirrored from ARCH-069 into ARCH-068's tri-state note, the clause that actually defines the field), A7 (IMPL-141's false `P-A6 not done` corrected with evidence — the dedup shipped in 244f9f0 — plus new IMPL-142 recording that untraced commit and eb58d96), A8/O-1 (sequence-diagram harness event drops appendPromptBytes), A9/C-1 (phantom parseUserOverrides renamed to validateUserOverrides in ARCH-064 inv 1 + S-2), A10/O-2 (four sites restated to the post-R-G9 truth: the cap is capPrompt at the onHarness persist site AFTER redact, not inside redactHarness), A11 (DES-105 body stops declaring the B-2-dropped appendPromptBytes/promptTruncated), A12/R-1 (ARCH-065/069 api lines carry {param, restPath, value}; the resolve.ts mapEffort debt entry UPGRADED from duplicate to WRONG — it has no restPath, so a future caller re-emits the P-A1 top-level effort defect; deletion blocked only on re-pointing 5 cases in tests/unit/params-resolve.test.ts, which a doc-only pass may not touch), O-3 (UT-020 note said 6/6, the file holds 5 cases), C-2/F-3 (DES-102 synced to resolve.ts: author-only PAIR not trio — skills is not a RunParams field per B-3 — and SIX folded keys not seven), R-2 (verified ALREADY closed by 43042d3's ARCH-069 deps line, no edit). S-1 stays recorded debt. IMPL-142 + IMPL-143 are the record. Suite 1533 passed / 0 failed (243 files), tsc clean, trace 820 items (was 818: +2 new IMPL entries) / 11 gaps with the gap SET byte-identical to the RE-REVIEW #4 §Q2 list. No file under src/ or tests/ was modified by this pass. current_stage impl→verification. Prior: v21 GATE 5 RE-RUN #4 (2026-09-01, verifier, test-first RED) — Gate 8 RE-REVIEW #4 sent back [tests,impl] over 2 NEW HIGH (A1, A2) + 3 MED (A3..A5, A3/A6 doc-only) findings in src/params/contract.ts (07-review.md §Q7 pin: Gate 5 = A1(both halves)/A2/A4/A5). Extended tests/unit/params-contract.test.ts (+12 cases, 6 new describe blocks) and tests/acceptance/val-100-param-contract.test.ts (+2 cases, real HTTP + direct catalog.db seed) IN PLACE, no new IDs — full detail + Gate-5 semantics decisions (string min/max = byte-length bound, not parse-time rejection) in the gates.tests note below. Full suite 12 failed/1521 passed (1533 total, 243 files), 0 unrelated regressions vs the 1519/1519 pre-re-run baseline; tsc clean; trace 818/11 IDENTICAL to baseline (0 new gap classes). current_stage tests→impl (Gate 6 re-run #4 next). Prior: v21 GATE 7.5 ROUND 2 PASSED (2026-09-01, validator, commit 90b5d30) — re-validated after adjudication #6/#7 F-1/F-2/G-1/G-2 (IMPL-141) landed. Round 1's VAL-102/103 never exercised a REGISTERED `effort`/`appendPrompt` default reaching dispatch (only per-call overrides / the `model` knob) — exactly the P-A3 dispatch-inertness gap and the G-1 ceiling-bypass gap the fix closed. Extended `tests/acceptance/val-103-effort-real.test.ts` with 2 new real-tier cases (G-1 ceiling-bypass refusal; registered `effort` default reaching dispatch) — `npx vitest run tests/acceptance/val-103-effort-real.test.ts` with real OLLAMA_BASE_URL + litellm venv on PATH → 4/4 pass. Independently re-verified over live MCP HTTP against a FRESH `deploy.sh --background` boot (one-command, no undocumented steps; version string `v0.20.0-25-g90b5d30` cross-checked against the clone's own `git rev-parse` before trusting any result — see the environment gotcha below): G-1 confirmed (`defaults.effort:'max'` with NO `params.knobs` block on a `maxEffort:'high'` server → `HARNESS_DEFAULTS_INVALID`, nothing stored; control at-ceiling registers fine); P-A3 confirmed (`defaults.effort:'high'`, no override, no per-call effort → `workflow_agent_log` harness shows `effort:'high'`, `provenance.effort:'default'` while still `running`, run completed for real against local Ollama qwen2.5:7b); P-A4/P-A2 regression pins spot-checked live too. Environment gotcha (not a product defect, recorded honestly): this host has several stale `rwe` server processes left running from past validation rounds (weeks old); the first boot attempt landed on a port with a zombie process already listening, giving a false-healthy result — caught by cross-checking the `/api/status` version string against the fresh clone's commit, then re-run on a verified-free port. Full suite `npx vitest run` → 1519/1519 pass (243 files, up from 1517 by exactly the 2 new cases), 0 failures. `npx tsc --noEmit` clean. `sh .sdlc/trace --check`: 818 items / 11 gaps, IDENTICAL set to the IMPL-141 baseline (all pre-existing 漂移/未實作/TDD; 0 未真實驗證/未驗證/high). Config sync re-confirmed: IMPL-141 added no new config keys (Ceilings threaded from the existing shared object); 32 code keys ↔ 32 DEPLOY.md §1b rows, both directions, unchanged from Round 1. README/DEPLOY needed no edits (quickstart re-run verbatim, still history-free). `rtm.md` regenerated via trace.py's own scan()/build_matrix() (no --rtm flag in this repo's trace.py): 95/95 REQs ✅ real:true-verified, 0 ❌. Full evidence + boot log in 08-validation.md's new "v21 Gate 7.5 ROUND 2" section. Not validated here (explicitly out of scope, routed not dropped): adjudication #6's F-3 (DES-102 prose) and F-4 (P-A6 dedup + doc batch) — doc/consolidation items named for a future integrator/review pass, no independent REQ-level acceptance criterion, do not block any REQ's real-tier status. gates.validation.passed=true; current_stage validation→review (Gate 8 re-review #4 next). Prior: v21 GATE 6 CLOSEOUT #4 (2026-09-01, integrator, cross-task scope) — adjudication #6 F-1/F-2 and adjudication #7 items 1-4 + G-1 + G-2 all CLOSED; FULL SUITE GREEN 1517/1517 (243 files, 0 failures), tsc clean, trace 818 items / 11 gaps with the gap SET UNCHANGED (no new gap, no new class). Recorded as IMPL-141 in 06-impl-log.md, which also records the F-1/F-2 work commit 35e6994 landed without an IMPL entry of its own (the dispatch's 4-failed/1509-passed baseline was stale; the tree was already 1-failed/1512-passed). Landed this pass: G-1 — a caller-supplied defaults.effort/appendPrompt with NO params block bypassed the registration ceiling entirely (the check ran only inside the loop over DECLARED knobs) and admission never re-checks registered defaults, so the value was enforced nowhere; closed by ONE ceiling pass over the FINAL effectiveDefaults using effectiveBounds(canonicalContract(), ceilings) + the shared checkValueAgainstSpec, so registration cannot refuse against a different number than admission enforces by construction. Red case written and confirmed red BEFORE the fix (pre-authorized by adjudication #7). Also: resolve.ts's redundant AuthorDefaults intersection+cast deleted (item 4); G-2 audit — both surviving adjudication-#5 citations read as historical SUPERSEDED notes, KEPT; one Gate 5 test defect fixed by literals only (IT-081 case (e) demanded an over-ceiling effort default on a default-ceiling server, contradicting the dedicated maxEffort:'low' ceiling pair). F-3 and F-4 are named for the integrator closeout by adjudication #6 but were OUTSIDE this dispatch's scope — left routed, not dropped. Changes are in the WORKING TREE, deliberately uncommitted. gates.review.passed stays FALSE: Gate 8 re-review #4 owns that flip, not the integrator. Prior: v21 GATE 5 RELAUNCH (2026-09-01, verifier) re-verified against HEAD post-adjudication-#5/#6 (see `updated` note) — P-A1/P-A2/P-A4 already fixed out-of-band (green), P-A3 still correctly red, IT-081's B4 block retired + UT-101's stale A-7 case corrected per adjudication #6, F-1 ceiling-interaction + UT-099 provenance-matrix gaps added. current_stage stays impl, awaiting Gate 6 re-run #3 (scope unchanged: F-1 widen+ceiling-thread, F-2 dedup, F-3 doc sync, F-4 integrator batch). Prior: v21 GATE 8 RE-REVIEW #3 (2026-09-01, reviewer) sent back [tests,impl] over NEW findings P-A1..P-A4 (07-review.md §P2); Gate 5 RE-RUN #3 (verifier) just wrote/confirmed the pinned-scope RED tests (see gates.tests note) — current_stage now impl, awaiting Gate 6 re-run #3. Prior: v21 GATE 8 RE-REVIEW CLOSEOUT COMPLETE 2026-09-01 (integrator) — all ten §R2 findings R-G1..R-G10 closed; suite 1499/1499 green (vitest exits 1 on the pre-existing spawn-litellm-ENOENT background artifact, verified at the unmodified baseline by stash — 0 test failures), tsc clean, trace 817 items / 11 gaps (+2 declared iter-drift false positives from IMPL-140's honest traces to DES-088/DES-066, both amended this pass; 0 new gap classes, 9 pre-existing untouched). Changes are in the WORKING TREE, deliberately uncommitted. Gate 8 re-review #3 owns the verdict: gates.review.passed stays FALSE and is the next reviewer's flip, not the integrator's. .panel/ retained.
# v17 Gate 8 (review) PASSED 2026-08-18 — ITERATION CAN CLOSE. Self-check: DCR implementation consistent with ARCH-059 v17 (registration_endpoint, POST /register loopback-enforced + metadata-clamped, registered_clients GC'd, port-agnostic /authorize binding, backward-compat unregistered client_id fallthrough, seam-consistent clock+CSPRNG). Trace: 752/9 (0 severe, 0 未驗證, 0 未真實驗證, 0 high; 9 = 1 MID pre-existing + 8 LOW drift, all recorded as known tech debt). No new broken chains. Validation: VAL-095 v17 real:true 9/9; IT-078 27/27; 1342/1342; live curl + restart survival confirmed; README+DEPLOY current-state v17 (no new config keys). 07-review.md v17 section written; journal appended; review→closed. Prior: v17 Gate 7.5 PASSED 2026-08-18 — RFC 7591 DCR (REQ-012 v17); VAL-095 v17 real:true (9/9 acceptance incl 4 new DCR cases 7a-7d); IT-078 27/27; 1342/1342; live curl DCR: registration_endpoint present, POST /register 201, non-loopback 400, port-ignored /authorize 302; restart survival confirmed (registered client persists across kill+restart); no new config keys; trace 752/9 (0 未驗證, 0 未真實驗證, 0 high, all pre-existing low drift); README+DEPLOY updated; gates.validation.passed=true; validation→review. Prior: v17 F4 Gate 7 (regression closeout) PASSED 2026-08-18 — simplify-lite: no code changes (DCR register handler + registerClient/getClient already minimal, all clock/CSPRNG via injected seam); regression 1338/1338; IT-078 flipped green/pass (27/27, 10 new DCR cases + 17 pre-existing); trace 752/11 (0 severe, 0 orphan/broken-link; same pre-existing baseline as Gate 6); det-check pass (production code wall-clock-free per token-store.ts comment — all clock/random through injected seam); TZ-travel 1338/1338 no time bombs (TZ=Pacific/Kiritimati); seam wiring: /register inside if(authHandlers) block (server.ts:1366+1409), authHandlers wired from real authTokenStore (server.ts:1267); real-dep smoke: IT-078 27 cases real SQLite+HTTP; deferred mock-only: Google id_token verify (injected jwksFetch double) + actual MCP-client DCR handshake → Gate 7.5; gates.verification.passed=true; verification→validation. Prior: v17 F2 Gate 6 (impl GREEN) PASSED 2026-08-18 — IMPL-122 bumped v17 (TASK-091, DES-092, DES-093, DES-095): registration_endpoint in buildAuthServerMetadata (DES-092); registered_clients 4th table + registerClient/getClient/gcExpired sweep (DES-093); POST /register handler + port-ignored /authorize binding (DES-095); 1338/1338 pass (10 new DCR cases green + 1328 pre-existing zero regression); tsc clean; trace 752/11 (0 severe, 0 orphan/broken-link; 3 gaps closed vs Gate-5 baseline: TASK-091 impl + DES-092 impl trace; remaining 11 = 6 pre-existing + 5 low TDD-wavefront iter-drift unchanged); gates.impl.passed=true; impl→verification. Prior: v17 F2 Gate 5 (tests RED) PASSED 2026-08-18 — IT-078 updated in place→v17 (red/fail): 10 new failing cases covering RFC 7591 DCR: case 11 (registration_endpoint in AS metadata, field absent→fail), cases 12–15 (POST /register happy path, clamp-don’t-reject, validation 400s, persistence; all 404 pre-fix), cases 16–17 (/authorize port-ignored binding + mismatch, 404-at-register aborts), case 18 (registered_clients GC by sweep, no such table pre-fix→SqliteError); 17 pre-existing cases GREEN (zero regression); 10 fail / 17 pass = 27 total. Traces extended: added DES-092+TASK-091. trace 752/14 (0 severe, 0 orphan/broken-link; 14 = 6 pre-existing + 5 new drift: UT-092/UT-093/UT-095/VAL-095×2, all low). gates.tests.passed=true; tests→impl. Prior: v17 Gate 3+4 (tasks+design MERGED) PASSED 2026-08-18 — RFC 7591 DCR fix: ARCH-059+DES-092/093/095 iter→v17 in place + TASK-091 added; trace 752/13 (0 broken-link/orphan; +4 low iter-drift expected). Prior: v17 fix kicked off 2026-08-18 — real remote-connect failed: Claude Code MCP OAuth client reported "Incompatible auth server: does not support dynamic client registration". Root cause: engine AS advertises no registration_endpoint + has no RFC 7591 DCR handler; a spec-only MCP client with no pre-registered client_id cannot obtain one. REQ-012 acceptance bumped iter v17 with a DCR clause (POST /register issues a public PKCE client_id, rejects non-loopback redirect_uris, metadata advertises registration_endpoint). Running /sdlc-fix (F1-F6). Prior: v16 Gate 8 PASSED 2026-08-18 — ITERATION CLOSED. 07-review.md written; 751 items / 6 gaps (0 severe, 0 未驗證, 0 mock-only; same 6 pre-existing: IMPL-082 MID + DES-088/UT-058/UT-064/IT-057/TASK-018 LOW); arch consistent for v16 scope (HIGH-1 redirect_uri + MED-2 gcExpired + composeConfig workspaceTtlMs all CLOSED; pre-existing H-2/H-3/M-1/L-* unchanged on their own track); validation real-tier all-green (VAL-095 v16 real:true, 8 live curl + IT-078 17/17 + cross-process GC confirmed; 1328/1328); README+DEPLOY current-state; no new drift from fix; review→closed. Prior: v16 Gate 7.5 (validation) PASSED 2026-08-18 — HIGH-1 redirect_uri loopback-only (live curl: evil.example→400, 127.0.0.1→302, [::1]→302, https-scheme→400, garbage→400, empty→400, missing→400) + MED-2 gcExpired GC (IT-078 case 10 real SQLite+real sweep; cross-process live confirmed post-composeConfig-fix: 3 expired rows deleted in <2s); IMPL fix: workspaceTtlMs missing from composeConfig() → _gcTtl=0 → hourly sweep; fix: added workspaceTtlMs:fileConfig.workspaceTtlMs line; no oauth_state row written for rejected redirect_uri; VAL-095 bumped v16 real:true; IT-078 17/17; IT-079 4/4; 1328/1328; UT-093/095 bumped v16; trace 751/6 (3 drift gaps resolved, 6 pre-existing remain); config sync clean (no new keys); README+DEPLOY current-state; gates.validation.passed=true; validation→review. Prior: v16 F4 Gate 7 (regression closeout) PASSED 2026-08-18 — simplify-lite: no code changes (isLoopbackRedirectUri+gcExpired already minimal); regression 1328/1328; IT-078 flipped green/pass (17/17); trace 751/9 (0 severe, 0 orphan/broken-link, all pre-existing); det-check pass; TZ-travel 1328/1328 no time bombs; seam wiring: authTokenStore real (server.ts:1261-1266), auth forwarded main.ts:152, gcExpired in sweep (server.ts:1278); real-dep smoke: IT-078 17 cases real SQLite+HTTP; gates.verification.passed=true; verification→validation. Prior: v16 F2 Gate 6 (impl GREEN) PASSED 2026-08-18 — IMPL-122 bumped v16 (TASK-090, DES-093, DES-095): isLoopbackRedirectUri + gcExpired wiring; 1328/1328 pass; trace 751/9 (0 orphan/broken-link, all pre-existing); gates.impl.passed=true; impl→verification. Prior: v16 F2 Gate 5 (tests RED) PASSED 2026-08-18 — IT-078 updated in place→v16 (red/fail): 7 new failing cases — HIGH-1 cases 8a-8f (/authorize non-loopback redirect_uri → 400, pre-fix 302; state row written on rejected request; https scheme rejected; garbage/empty/missing rejected) + MED-2 case 10 (expired oauth_state rows NOT GC'd by sweep within 3s, pre-fix sweep never calls gcExpired); 3 regression guards (cases 9a-9b-9c) GREEN; 7 pre-existing IT-078 cases remain GREEN; 10 pass / 7 fail (17 total). IT-079 bumped→v16 (stays green, D-BIND cases unchanged). IT-078 traces extended: added DES-093, TASK-090. MED-2 widened-creation-condition (auth-only, no-TTL) NOT asserted here (hourly cadence, no injectable seam); Gate 6 reviewer must confirm + Gate 7 regression. trace 751/9 (0 orphan/broken-link, 1 fewer gap than F1 baseline: TASK-090 now has test trace via IT-078; remaining 9 = pre-existing). gates.tests.passed=true; tests→impl. Prior: v16 Gate 3+4 (design, merged) PASSED 2026-08-18 — F1 impact-design delta done: ARCH-059/DES-093/DES-095 updated in place→iter v16, TASK-090 added (traces ARCH-059); HIGH-1 redirect_uri loopback-only validation before putState + MED-2 gcExpired wired into REQ-026 sweep (interval widened to workspaceTtlMs>0 OR auth-enabled); trace 751/10 (0 new gaps, was 750/11); gates.design.passed=true, architecture stays true; design→tests. Prior: v16 fix kicked off 2026-08-18 — Gate 8 review of v15 returned arch_consistent=false with 2 blocking NEW-v15 violations in the shipped auth code: HIGH-1 (ARCH-059 inv.4 — /authorize redirect_uri not validated → open-redirect → engine bearer theft/account takeover; fix = RFC 8252 loopback-only check before putState) + MED-2 (ARCH-059 note — TokenStore.gcExpired() never called → unbounded oauth_state/auth_codes/bearer_tokens growth; fix = call it from the workspace-GC sweep at server.ts:1264). REQ-012 acceptance bumped iter v16 with 2 explicit clauses. Running /sdlc-fix (F1-F6). Prior: v15 Gate 7.5 PASSED 2026-08-18 — composition-root auth wiring fix (auth: fileConfig.auth in composeConfig); VAL-095..099 real:true pass; 1316/1316; trace 750/11 pre-existing. Prior: v15 Gate 7 PASSED 2026-08-18 — 1316/1316 pass; tsc clean; trace 750/11 pre-existing; simplify IMPL-128 (auth-service wellKnown* de-dup); det-check pass; TZ-travel 1316/1316; seam wiring confirmed; real Google/LLM deferred to 7.5. Prior: v15 Gate 6 (impl GREEN) PASSED 2026-08-18 — IMPL-122..127 integrated (TASK-085..089+doc-drift-lock); TASK-080/081 status reconciled draft→done (orchestrator-authorized, v14 ships already in IMPL-117/118); 1316/1316 pass; tsc clean; trace 749/11 (0 orphan/broken-link, all pre-existing); 3 Gate-5 test defects (UT-092/IT-078/IT-080) fixed in Gate-5 re-run. Prior: v15 Slice B Gate 5 (tests RED) PASSED 2026-08-17 — UT-092..097+IT-078..082+VAL-095..099 (16 new files); 60 fail/1188 pass/233 files; trace 743/19 (0 orphan/broken-link). Prior: Gate 3+4 MERGED PASSED 2026-08-17 — REQ-012+086..089 → TASK-085..089 (ARCH-059→TASK-085 pure-core+TASK-086 route-wiring split; ARCH-060→TASK-087; ARCH-063→TASK-088; ARCH-061+062 MERGED→TASK-089 one workflows row) + DES-092..100 synthesized from CONVERGED panel r1+r2 (adversarial opus + quality-dimensions sonnet — both r2 confirm convergence, all 4 predicted disagreements dissolved, only 3 cheap RDs: RD-1 slot-free check→Gate-7.5 regression tagged REQ-083 not a DES, RD-2 ARCH-061+062 stay merged w/ D-AUTH-5 named assertions, RD-3 Cache-Control:no-store one-liner + X-Frame-Options declined). Synthesizer call A2⟂A4: workflow_run TAKES principal (attribution, no gate) but get/list/status take none (reads open by absence-of-param) — A4's literal wording overruled where it collided w/ REQ-086 attribution acceptance. Seam: token-store constructor-injected clock+csprng, EVERY time/random read through the seam, no Date.now()/randomBytes in module; google-verifier injected now; isLoopbackPeer clock-free. QM → no safety lenses. v15 classDiagram added. gates.tasks/design.passed=true; trace 727 items / 19 gaps / 0 orphan/broken-link (5 new = TASK-085..089 未實作 expected pre-impl; 8 REQ-086..089×{未實作+未驗證} + 6 pre-existing = baseline). current_stage design→tests. Prior — v15 Slice B Gate 2 (architecture) PASSED 2026-08-17 — REQ-012+086..089 → ARCH-059..063 (5 ARCH 1:1); synthesized from panel r1 (adversarial opus scoped-to-slice + quality-dimensions sonnet whole-system), COMPLEMENTARY → r2 NOT run (+task forbids re-spawn); adopted D-AUTH-1..6 wholesale; ARCH-009 OIDC placeholder marked SUPERSEDED; quality whole-system gaps (streaming/metrics/healthz/RunStore-port/openapi/disk-full/SDK-timeout) consciously EXCLUDED as out-of-slice (Karpathy); 2 accepted residual gaps recorded (run-lifecycle mutation ungated for non-owners; /api+dashboard unauthenticated principal exposure — both REQ non-goals); QM → no safety lenses; trace 713 items / 14 gaps (0 orphan/broken-link; 8 new-expected REQ-086..089×{未實作+未驗證} + 6 pre-existing = v14 baseline); gates.architecture.passed=true; current_stage requirements→design. Prior — v15 Slice B kicked off 2026-08-17 — Gate 1 (orchestrator) DONE: REQ-012 un-deferred+rewritten (engine-as-own-AS federating to Google/Gmail per MCP authz spec) + REQ-086 (per-caller principal on /mcp + /assets upload endpoints) + REQ-087 (workflow ownership, creator-only mutation; migrate owner-less→hsuhungjung@gmail.com) + REQ-088 (harness-param binding at registration) + REQ-089 (D-BIND fail-closed, loopback exempt). User rollout decisions recorded in 01 v15 preamble. Next: Gate 2 architecture via sdlc-run full. Prior: v14 Gate 8 (review) PASSED 2026-08-16 — 07-review.md written; 704 items/6 gaps (all pre-existing, all recorded as known tech debt); arch NOT consistent (3 HIGH pre-existing v3 violations recorded; Gate 2 adjudication for H-2/H-3, Gate 6 fix for H-1/M-1); validation real-tier all-green (VAL-089..094 real:true); README+DEPLOY present current-state; .panel/ cleaned. review→closed.   # v14 Gate 7.5 ROUND 2 PASSED (2026-08-16) — REQ-083 key.prompt fix verified via live Ollama run (runId 8cdbed02, qwen2.5:7b, SDK+LiteLLM); VAL-092 green/pass/real:true; 1170/1170 pass; trace 704 items / 6 gaps (all pre-existing); gates.validation.passed=true; current_stage validation→review. v14 Gate 7.5 ROUND 1 OPEN — REQ-083 key.prompt gap — 1170/1170 pass; VAL-092 LLM-gate fixed (HAS_PROVIDER pattern, consistent with suite); all v14 tests green; simplify: no code changes (IMPL-117..121 already minimal); determinism_check: pass; TZ-travel: 1170/1170; seam wiring: secretValueProvider+seedFetcher+putBlobStream.timer all wired; trace: 704 items/12 gaps/all pre-existing; current_stage verification→validation. v14 Gate 6 (impl GREEN) PASSED 2026-08-16 (re-confirmed 2026-08-16 integrator closeout: 1170/1170 pass, 0 test_defects — all 5 prior defects fixed by Gate 5 in same session) — IMPL-117..121 (TASK-080..084) green; tsc clean; trace 704 items / 12 gaps / 0 new orphan/broken-link (12 < 17 baseline: 5 new REQ-081..085 not-yet-real-validated + 7 pre-existing); gates.impl.passed=true; current_stage impl→verification. Prior v14 Gate 5 (tests RED) PASSED 2026-08-15 — REQ-081..085 broken into TASK-080..084 (1:1 with ARCH-054..058) + DES-086..091 synthesized from the pre-run 2-group panel .panel/design/adversarial.r1(opus)+quality-dimensions.r1(sonnet), COMPLEMENTARY → r2 NOT run (none of adversarial's 6 predicted disagreements materialized in quality r1). TASK-080 (ARCH-054 streaming putBlobStream seam + pure hex/ns validators + net-guarded POST /assets/blob route, L) / TASK-081 (ARCH-055 POST /assets/manifest + seedManifestRef run-load + 4-way SEED_SOURCE_CONFLICT ladder + RunStatusView.seedManifestRef, M, seq after 080) / TASK-082 (ARCH-056 redact-at-capture: SecretValueProvider port + redact({name,value}[]) + all 4 persist sinks + sweep-IT-as-DoD, L, ONE task) / TASK-083 (ARCH-057 honest asset_push kind schema + drift-lock, S) / TASK-084 (ARCH-058 pure assertScriptIntegrity + SCRIPT_SHA_MISMATCH rung + scriptSha256 schema, S). DES-086 (putBlobStream {sha256,bytes} + opts.timer idle-seam + isValidSha256Hex/isValidNamespace-before-fd + HTTP 400/413/408/409 taxonomy + BE-2 no-exists-shortcut/BE-3 no-Content-Encoding-decode/BE-4 0-byte-legal + maxBlobBytes 256MiB-min1MiB/blobUploadTimeoutMs 120s-min10s), 087 (manifest-as-CAS-blob seedManifestRef=sha256(bytes) + INVALID_SEED_SPEC parse-fail + run-time MISSING_BLOBS security boundary + 4-way exclusion + TOCTOU/GC-exempt contract), 088 (redact {name,value}[] marker ‹secret:NAME› value-exact + SecretValueProvider.entries() ReadonlyArray + 4 sinks incl redact-before-saveSnapshot + double-redaction-exclusivity/persist-only/replay-divergence invariants + completeness sweep IT), 089 (asset_push kind in-band HOOKS_UNSUPPORTED+mcp_provision drift-lock), 090 (assertScriptIntegrity utf8 + rung BEFORE admission + SCRIPT_SHA_WITHOUT_SCRIPT + full pre-createRun order pinned), 091 (real-tier path per REQ + per-tier mock policy: E2E no SUT-boundary mock, LLM-gated skip for REQ-083). Decisions: error-code twin BLOB_SHA_MISMATCH(new)/BLOB_HASH_MISMATCH(shipped) intentional-no-rename; INVALID_SEED_SPEC over MANIFEST_PARSE_ERROR; idle-timer injected via opts.timer (CasStore has no clock); scriptSha rung before admission; runtime sweep IT over compile-time chokepoint; config defaults named at design time; redact zero-prod-callers verified → test-only blast radius. QM → no safety lenses. gates.tasks/design.passed=true. Prior: v14 Gate 2 (architecture) PASSED — REQ-081..085 → ARCH-054..058 (see gates.architecture note); review→design.
updated: 2026-09-14

tech_stack: >-
  Node.js 22.6+ (real requirement, corrected at Gate 7.5 round 1 — native --experimental-transform-types;
  the "20+" above is historical/inaccurate, kept for traceability) / TypeScript (strict, ESM); tests
  with vitest; SQLite via better-sqlite3 (both RunStore and, as of the D-V2 route-back, WorkflowCatalog
  registrations); MCP server: hand-rolled JSON-RPC-over-HTTP (src/server.ts), NOT @modelcontextprotocol/sdk
  (drift from the original Gate 2/3 sketch, functionally equivalent, noted since round 1); agent
  execution: two GatewayClient implementations — LiteLLMGatewayClient (src/gateway/client.ts,
  default-on managed `litellm[proxy]` Python subprocess routing as of D-R1; config opt-out via
  useLiteLLMProxy:false falls back to direct per-provider fetch) and ClaudeAgentSdkGatewayClient
  (src/gateway/claude-agent-sdk-client.ts, D-F1) backed by the genuine @anthropic-ai/claude-agent-sdk
  query() API (ANTHROPIC_BASE_URL -> the same managed LiteLLM proxy + dummy ANTHROPIC_API_KEY,
  injectable queryImpl) — as of D-F4, src/main.ts (the real product entrypoint) constructs this as
  the DEFAULT gateway with a config opt-out (`"gateway":"sdk"|"direct-fetch"` in rwe.config.json,
  default "sdk"). Gate 7.5 round 2's 2 confirmed real defects on this default path (opts.model never
  forwarded; a real API error misreported as success) were FIXED by the D-F5 route-back and
  RE-VERIFIED FOR REAL at Gate 7.5 round 3 (`ps aux` shows the spawned CLI subprocess's own
  `--model <alias>` flag; a real Ollama/LiteLLM error now resolves to `null`, never smuggled as fake
  success text). NEW at Gate 7.5 round 3: this SAME default path has 2 different new confirmed real
  defects, only reachable now that the 2 old ones are fixed — (a) the SDK session unconditionally
  requests "extended thinking", which LiteLLM forwards to Ollama as `think:true`, rejected by
  non-reasoning Ollama models (e.g. qwen2.5:7b) with a 400, so every real SDK-default+local-Ollama
  agent() call fails after ~4 minutes of the CLI's own retry/backoff before safely resolving to
  `null` (confirmed-working fix isolated but not shipped: `options.thinking={type:'disabled'}`); (b)
  the gateway has no `timeoutMs`/`retries`-bounded race of its own (unlike the direct-fetch path), so
  the configured bound has no effect on this path — see gates.validation.note and 08-validation.md.
  The documented "direct-fetch" opt-out remains fully real-verified across all 3 rounds and is what
  rwe.config.example.json's own committed template still needs to fall back to for a working local-
  Ollama deployment today, even though its own `"gateway"` value stays "sdk" per the binding D-F5/D1
  product decision. Both GatewayClient paths need a real `litellm` binary on PATH (D-R3: requires
  Python 3.11/3.12 specifically — this sandbox's system Python 3.14 lacks prebuilt uvloop/orjson/
  websockets wheels; `uv python install 3.12` + a pinned venv works, see DEPLOY.md §1). agentType
  resolution: composition-root loader (D-F2, src/agent-definitions.ts) populates the agentTypes
  registry from agents/*.md frontmatter (name/model/prompt) at createServer() startup via
  ServerConfig.agentDefinitionsDir — confirmed real at Gate 7.5 round 2, code untouched since. schema
  validation = ajv (D-V4 real JSON-Schema retry-on-mismatch) — confirmed real at Gate 7.5 round 2,
  re-confirmed round 3 (schema-validated agent() success against real Ollama via direct-fetch).
  sandbox = node:child_process + node:vm restricted context (round-2 finding, unchanged/re-confirmed
  round 3: the script-visible budget.spent()/remaining() accessors in src/sandbox/child-entry.ts are
  hard-coded stubs, never reflect live usage — server-side enforcement via RunGuard is correct).
  Ollama used as the real local-model provider throughout all 3 Gate 7.5 rounds. Package manager: npm.

# Locations
sdlc_root: .sdlc/features/001-remote-workflow-engine
layout:
  product_root: .
  src: src
  tests:
    unit: tests/unit
    integration: tests/integration
    e2e: tests/e2e
    acceptance: tests/acceptance
  readme: README.md
  deploy: DEPLOY.md

gates:                   # whether each stage's exit gate passed (TDD order)
  requirements: { passed: true, note: "v27 (2026-09-11, Gate 1 in session): REQ-131..143 — operator dashboard rebuilt to the Claude Design handoff (project 38fc8181-5b00-45aa-a354-bf07994e19ab, design_handoff_workflow_dashboard/). Dark-default theme + vendored Archivo/JetBrains Mono + zh-TW/en + OKLCH accent-hue slider (REQ-131); Workflows home with search/filter/avg-cost + sweep (132); workflow detail + run chips + run history (133); swimlane run graph replacing the flat DAG (134); slide-in agent panel (135); SYSTEM PROMPT MUST NOT REACH THE WIRE on agent detail — verified at Gate 1 that it does today via composePrompt->HarnessDescriptor.prompt->run_agent_log (136); Models tab sortable + slide-in, latency/benchmarks render '-' this iteration (137); System tab stat cards + process table (138); Issues tab RETAINED and restyled, owner's explicit call (139); dag.lanes + agent record wire fixes (140); RunSummary.costUSD sharing ONE fold with /api/runs/:id (141); poll pauses when hidden (142); demo dataset must self-label + retirement condition registered (143). 3 interview rounds / 12 questions, owner-answered in session; Gate 0 as-is map in v27-gate0-asis-map.md. Owner's own fidelity bar, verbatim: 'the UI must be 99% similar to the design'. Constraints carried from Gate 0: C1 page-source literal pins, C2 Chromium selector anchors (#dag-fit/#dag-graph/#dag-zoom/#run-usage/#diagram-img/#diagram-zoom/.card .t), C3 no-skeleton-surface forbids the handoff's i18n 'skeleton' key in dashboard-page.ts, C4 the .dc.html hex is a teal snapshot - the README OKLCH formula is the truth. Won't-have: D1 per-principal dashboard authz, D2 latency/benchmark data sources, D3 configurable poll interval / SSE, D4 mobile responsive. | PRIOR: v26 (2026-09-08, Gate 1 in session): REQ-121..130 — seed refusal, provider fail-fast, three provider paths only (openai+gemini removed, no tool curation), run DAG phase join, resolved provider on terminal record, effort→OpenRouter reasoning + declared capability columns, four-field tokens + per-model cost budget, LR swimlane contract (new registrations only), zoom+pan, guide gaps. Round v26 in 01-requirements.md; six owner rulings in v26-gate1-working-notes.md. PRIOR: v24 (2026-09-04): REQ-107..118 — interface consolidation + roles + per-agent params + author-supplied diagram + workflow-owned assets + pushedBy traceability + trigger claim-at-registration + authoring guide. Owner settled 11 open points at the Gate 1 interview: one iteration not two; NO old-name deprecation window (a tools/list carrying both sets would defeat REQ-117); default role user; admin bypasses ownership AND may read others workspaces but the read is AUDITED; E2E covers v24 REQs + every tool interface + the cold-model experiment; diagram/script agent sets must match bidirectionally; built-in assets can be both added and removed by admin; and the cold-model run must succeed FIRST TRY, with any error treated as a documentation defect. | v23 (2026-09-02): REQ-101..106 added — workflow_describe explain surface, analyzer-drawn ASCII diagram (structure-only, honest absence on failure), trigger bindings fed to the analyzer (the gap in #32 as filed: triggers are not in the script), config-separated analyzer verified by a Gate 7.5 real run not a unit test, skeleton off every user-facing surface but kept internal BEHIND THE AUTH GATE per v22 H2, and AUTHORING.md. Round v23 interview held in session: A1 honest absence (no skeleton fallback, overrides #32), A2 ASCII only (no Mermaid, overrides #32), A3 structure-only diagram (orchestrator-proposed, not overruled). Also fixed REQ-078's stale `draft` status — it shipped in v12 and IS the owner's original per-model-data ask. | v21 (2026-08-31): REQ-090..095 added — author/user separation part 1 (tunable-parameter contract, overrides validation, two wiring-defect repairs: registered defaults never applied + effort a documented no-op). Clarification held in-session: user chose (1) close inline script entirely, (2) appendPrompt fixed after system prompt and skill/MCP, (3) mask script from non-owners AND remove the regex skeleton in favour of an agent-rendered ASCII diagram, (4) approved the v21/v22/v23 ordering. Decisions D12..D16 recorded in 01-requirements.md. | user confirmed 2026-07-03 (REQ-001..015); v3 confirmed 2026-07-10 — added REQ-016..020 (harness/provision/secret/hooks-drop/sdk-timeout), rescoped REQ-009 to skills-only, REQ-012 OIDC stays deferred (hard boundary bind 127.0.0.1+tunnel). v1 slice = REQ-001..007,013,014" }
  architecture: { passed: true, note: "v27h GATE 2 SEND-BACK REPAIR (2026-09-13, architect) — PASSED. The three architecture-owned Gate 8 blocking findings are closed by AMENDING EXISTING ROWS IN PLACE at iter: v27h, zero new ARCH/ADR ids, zero code: AC-2 (ADR-049 title + Consequences, ARCH-124 note) — the root tsconfig DID change at IMPL-229 (ddc4409, DOM/DOM.Iterable/allowJs) and the compile-time server-without-client-and-without-DOM property is restored by an ADDITIVE inverse program tsconfig.server.json run beside the root config in typecheck AND build; the panel CROSS-conceded (each lens adopted the other's shape) so the synthesis decided: additive wins because tsconfig.json is resolved BY NAME by editors/tsx/vitest and because the default program still sees tests/, so DES-192, TASK-197 and ARCH-124's satisfies sentence all stay true (the narrow-root shape flips all three); re-measured by this synthesis on a clean git archive copy — exit 0 / 76 src files / 0 under src/dashboard / ~3.5 s, planted DOM globals go TS2584+TS2304x2, a planted client import goes TS7016, both clean under the root config today. The ADR records BOTH shapes as feasible (the tests-only-config-is-infeasible claim is corrected) and carries the honest limits (editor-unenforced; checkJs off — parse coverage yes, semantics no). AC-3a (ARCH-122 title/api/note) — the shell is head + island + asset refs + one module script and the ENTIRE visible body is client-built (app.js:455 replaceChildren over dashboard-page.ts:92-152); the 'still assertable here' clause is STRUCK as a sentence and replaced by a pin rule (DASHBOARD_HTML is a valid subject only for the shell's own facts; component/CSS pins take clientFile(...)), plus boot-failure honesty (the dead body is a failure mask -> one mount element with static pre-boot text in the existing empty hook + noscript, no id so #app-view stays single-emitter). DASH-1 — FOUR semicolon sites, not two (fixing only the named one moves the parse error to the second in each diagram), and the seven mid-line %% annotations moved to line start VERBATIM (mermaid honours %% only at line start; mid-line it is message text that renders as junk — the zero-meaning-change form was chosen over Notes). Falsified by this synthesis with a real render (mermaid 11.17.2 from node_modules + the repo's cached headless Chromium): control = both blocks fail parse at line 9 / line 14; after the edit both PARSE and RENDER with ZERO literal %% in the SVG; full sweep 40 blocks / 1 failing, and that one is 04-design.md:3306 = design's own DASH-2. ALSO repaired (two drifts the v27g impl repairs themselves induced, in rows already being amended): ARCH-124's api 'any ok -> live immediately' STRUCK for worstOf(perRoute) (the review's §8 item 3 had already chosen that half), and ARCH-123's cache type literal 'immutable' -> the full 'public, max-age=31536000, immutable' directive. OWNER DECISION PENDING on ARCH-124: REQ-131's acceptance says 任一 /api/* success -> Live but the shipped tag requires ALL visible-view routes ok — keep the narrowing (recommended) or revert the live half of the AC-4 repair. TWO FOLLOW-UP IMPL ITEMS OWED (Gate 2 may not make code changes; both DoDs are written verbatim in the rows and both rows carry the 'has NOT landed / UNGUARDED until' sentence): TASK-A (shell body -> mount element + five pin dispositions + UT-240's new positive) and TASK-B (tsconfig.server.json + both script strings + the pinning UT). Trace: 1659 items / 33 gaps — byte-identical gap set to the pre-edit baseline (0 new orphan/broken-link/drift). Synthesised from the 2-round panel .panel/architecture/{adversarial,quality-dimensions}.r{1,2}.md; contested points recorded with who conceded in the file-end 'Decision rationale — v27h'. NOT this round and still open: DOC-1 (validation) and DASH-2 (design). | PRIOR: v27b GATE 2 DELTA PASSED (2026-09-11, architect) — Round v27b owner ruling (ADR-051: 開/撤銷遮罩) folded into the closure REQ-133/134/140 by AMENDING EXISTING ROWS IN PLACE: ADR-051 (decision (a)->(b), owner_decision answered, F-2/B-1/B-2 residuals, Gate 7.5 instruction flipped to prove-visible, K-5 p95 measurement with a BOUNDED pre-approved memo), ADR-055 (sibling surface unconditional), ADR-054 (key-set budget rule), ARCH-125 (the run view renders warning TEXT, not a count), ARCH-126 (deriveLanes loses masked, gains status; the observed-wins join rule; no conflict detector), ARCH-130 (the !authEnabled branch + handleDashboardRequest authEnabled param + !!authCfg argument deleted; version resolution reads the recorded legacySubstitution.resolved so the overlay is CORRECT for the v22 legacy cohort; three-member TOKEN warning vocabulary pushed at the route, never in layoutGraph; dashboard_api_degraded on the two fault arms only) and ARCH-131 (phases[].agents unconditional, maskPredictedOverlay never built, synthetic-principal hazard recorded). New: INV-V27-9 (auth-invariant dashboard read, EXCLUSION-form parity + one positive anchor) — the replacement control for the retired mask. v22 history rows ARCH-073/ARCH-075/ADR-012 carry a superseded_in_part marker with iter left at v22. All amended rows at iter: v27b. Synthesised from the 2-round panel .panel/architecture/{adversarial,quality-dimensions}.r{1,2}.md; contested points (delete-vs-default, label-vs-fix, prose-vs-token, the rejected title-conflict detector, the parity oracle, the sweep boundary) recorded with who conceded in the file-end 'Decision rationale — v27b'. 1591 items / 67 gaps — IDENTICAL to the pre-edit baseline (0 new orphan/broken-link), solid_check PASS (68 modules, 0 mid), dashboard_check unchanged (7 known erDiagram false positives). ORCHESTRATOR ITEM, outside this closure: REQ-105's acceptance clause 「stays behind the auth gate … v23 must not re-open it」 (01-requirements.md:1070) needs a [PARTIALLY SUPERSEDED v27b, Round v27b] marker or Gate 8 fails by construction. | PRIOR(v27): v27 GATE 2 PASSED (2026-09-11, architect) — closure REQ-131..136/140/141 decomposed into ARCH-122..131 + ADR-049..056, every in-closure REQ covered, every ARCH carrying module+deps, 0 new trace gaps, solid_check PASS. One owner_decision pending on ADR-051 (predicted-overlay masking under auth). REQ-137/138/139/142/143 were NOT in this dispatch closure and have no ARCH row yet. | PRIOR(v26): v26 GATE 8 SEND-BACK REPAIR, architecture half (2026-09-11, architect): PASSED — A-1 (ADR-038/ARCH-116/ARCH-117/ARCH-118 + the logical view, both ER rows and two contract-table rows still published the overruled PRICE_UNKNOWN refusal, a nullable costUSD and applied:true for openrouter) and A-2 (ADR-048 Action line amended by adjudication to six members, naming workflow-catalog.ts under the same S-2 argument) closed by in-place amendment; synthesised from .panel/architecture/{adversarial,quality-dimensions}.r1.md (2026-09-11) with the convergence recorded in the file-end 'Decision rationale — v26 Gate 8 send-back, ARCHITECTURE half'. Still 121 ARCH / 130 REQ, 0 orphan REQ, 0 broken links, 24 gaps unchanged. PRIOR: v26 GATE 2 (2026-09-08, architect): PASSED — 12 ARCH (ARCH-110..121) + 11 ADR (ADR-037..047) in 02-architecture.md, each REQ-121..130 covered, every ARCH carrying module:/deps:, plus the v26 4+1 views, the data-architecture delta (runs.price_book, workflow_versions.diagram_contract, four-column tokens + costUSD), the API contract table (three BREAKING client-facing changes in one cross-repo row: budget object, toolUse->toolUseDeclared, seed additionalProperties) and INV-V26-1..7. Synthesized from the pre-run panel (adversarial + quality-dimensions, r1+r2) — no new panel spawned. Contested calls settled with who conceded: unmapped SDK traffic = subtype+count, no payload (QD conceded the payload, held the 64B cap + a dashboard reader); named counters over a warnings[] umbrella (QD withdrew the array); phase join by ORDINAL {title,index} (adversarial conceded the ordinal, QD conceded the second function; agent-before-first-phase = implicit lane 0 with NO warning so REQ-124 stays satisfiable); one providers.ts table with EFFORT_PROFILES folded in under a >=2-readers column rule (QD conceded the split on lifetimes); ADR-038 narrowed to USD-only budgets at admission (both moved) with persisted last-good pricing NOT built and the restart-during-outage consequence recorded + price_book.source provenance; remaining() -> null under a tokens-only budget (QD r2, unrebutted, adopted); ADR-046 records the silent-projection bug class WITHOUT a shared abstraction (both lenses rejected the abstraction). No FLAG-* and no build:/selftest: — scale is undeclared in state.yaml, v26 adds no switchable capability, and the 109 prior ARCH items use the S/M form. archify IS available but no IR was authored: this ledger has 109 pre-existing ARCH nodes and no diagrams/ IR, so a v26-only map would fail dashboard_check's both-ways node cross-check and authoring all 121 nodes is outside this slice (soft dependency, never blocking; the mermaid 4+1 views are the baseline). Housekeeping handed to the orchestrator, not done here: stale tech_stack sentences (openai/gemini GatewayClient, budget.spent() 'stubs'), the D-V2h ledger rows to amend at Gate 4 either way, and four REQ acceptance-text amendments (REQ-121 see:, REQ-122 abort+detail bound, REQ-124 three cohorts + ordinal, REQ-126/130 declaredSource). PRIOR: v24 (2026-09-04, architect): PASSED — ARCH-087..108 (22 module roots: 4 new pure files tool-specs/authz/path-verdict/authoring-guide, 17 surgical edits, 1 acceptance-test module; 3 deletions graph-analyzer/continuation-store/mcp-registry) + ADR-023..035 for REQ-107..118. Panel pre-run (adversarial.r1 + quality-dimensions.r1), headlines complementary → no round 2; referee calls recorded in the v24 Decision rationale (audit read-back: QD wins, no new tool: adversarial wins; refusals coalesce; value-triple compare adopted). Panel gaps G1/G3/G4/G5 were already owner-ruled in v24-gate1-working-notes ch.5/19/15.2/16.1 and are closed by citation. Exit gate: every REQ-107..118 covered by ≥1 ARCH; every ARCH has module:+deps:; every traces:/deps: id exists; sh .sdlc/trace = 1075 items / 42 gaps (was 1040/42), none referencing a new id. needs_clarification (confirm-or-overrule, defaults decided): (1) ADR-030 stdio MCP configs admin-only + http probe through isEgressAllowed — narrows the ch.16.2 author-push ruling on security grounds; (2) ADR-023 the plain rectangle node as the fifth shape for a nested workflow() black box (REQ-112 lists four); (3) ADR-035 workflow_register.defaults removed in favour of required per-agent defaults. | 2026-09-03 (v23 Gate 2 RE-RUN #2 for the Gate 8 RE-REVIEW send-back at 41e6382, send_back=[architecture,tests,impl], arch_consistent:false, 1 HIGH / 4 MED / 4 LOW): PASSED. Panel was pre-run (adversarial.r1 + quality-dimensions.r1, both rewritten for 41e6382; the on-disk *.r2.md belong to the PREVIOUS d294880 round) and NOT re-spawned; round 2 correctly skipped - the two r1 headlines are complementary, not contradictory. No new work items (trace 1012 items / 18 gaps, the same counts as the pre-edit run; all 18 gaps pre-existing - 1 TDD on IMPL-082, 16 old-iteration drift rows, 1 unimplemented TASK-018 - and none references a v23 or ARCH id). Rulings: (1) R-1/HIGH inv 2 RE-ASSERTED VERBATIM - the text is right, the code is wrong (the try wraps only the await of scriptPromise, _runJob is outside it, no .catch() backstop) - plus TWO NEW TESTABLE FLOORS: 2a _release is idempotent per key, _runningCount never below 0 and never above 1, the finally is the ONLY release site and _runJob's tail release is deleted (a double release silently turns concurrency-1 into unbounded parallel gateway.invoke and makes every test greener); 2b the catch body is TOTAL, realized inside the _settle seam, so a failed row write still journals cause=settle_failed, still releases, and leaves the row pending for ADR-017's boot sweep. Blast radius pinned: one orphan pending row terminates the process AT BOOT (no unhandledRejection handler in src/, Node 22 default), bounded to ONE crash by inv 9 alone. (2) R-2b: inv 5's SETTLE scope HELD (referee call R-1 is not re-litigated because the implementation went the other way); one private _settle() is the ONLY caller of putDiagramResult and the ONLY caller of _journal, _attempt returns telemetry - so one-line-per-settle and line-outcome-equals-row-written hold by construction, closing the B5 mismatch (row ready, line unavailable). (3) R-2: cause is KEPT and its TYPE is decided - a CLOSED UNION of engine-authored literals, never string (a string field re-opens ADR-016's channel that can echo the masked script; a union makes the leak a compile error); 12-field wire order pinned (existing ten unchanged, then attempts, then cause); tokens/duration aggregate across attempts; value domain tabled so Gate 5 asserts VALUES not presence. (4) R-3: inv 11's CHOKE POINT IS BUILT, the pre-authorized fallback DECLINED (both panels ruled the same way; the guard lives at 4 sites across 3 modules today and _startJob reads enabled nowhere). Sequencing is a CONDITION: inv 2's fix first, the guard as a SEPARATE RED-then-GREEN step after. IMPL must record which shape was taken. (5) R-6 + CONS-1: counts DELETED not corrected (ARCH-051 said 39, ARCH-082 carried the same stale 39 - a 4th site neither panel caught - while the code declares 40; the derived 29-uncovered went with it); the six-key graphAnalyzer list replaced by the DECLARATION name Partial<GraphAnalyzerConfig> at all three sites, with the doc-only verification recorded (main.ts:184 forwards wholesale - NOT a composeConfig gap). (6) :1708's closing clause struck (4th-of-4 A8 site). (7) SUS-2/ADR-020(c) re-asserted: one conditional console.warn keyed off analyzerEffectiveTools.length>0, naming configured AND effective sets; the unconditional readback log stays. (8) R-4: enqueue() is total at its own front door, not a call-site catch. (9) NEW inv 8 rule: an enumerated oracle is satisfied only in full (UT-125 dropped assertion (b), which is how 33/33 green coexisted with a leaking slot). (10) NEW ARCH-079 preamble rule: an implementation that deviates from an amended invariant must amend it IN THE SAME COMMIT. Gate 5 owes O1-O5 (all red against 41e6382, no new seam), cause value assertions on the five zero-model-call paths, a ts-expect-error that cause is not string, the 12-key set equality, ADR-020's two warn mirror rows, and the two defence-in-depth assertions. Karpathy: one method, three if/try, one union type, one parameter, one warn, one field; two claims deleted, one clause struck; no new module/store/config/migration/seam. needs_clarification EMPTY by finding - nothing this round is owner-shaped (unlike ADJ-A1 last round). | PRIOR: 2026-09-03 (v23 Gate 2 RE-RUN for the Gate 8 send-back at d294880, send_back=[architecture,tests,impl], arch_consistent:false, 3 HIGH / 3 MED / 4 LOW): PASSED. Gate 8 assigned Gate 2 the A1 DECISION plus the A7/A8/A9/A10 amendments; this re-run closed all five and additionally wrote the architectural invariant Gates 5/6 must test A2/A3/A5/A6 against (a RED row with no architectural oracle is how this ledger got here). A1 DECIDED (ADJ-A1, owner-overrulable at Gate 8): GET /api/workflows/:name/describe is admitted only when !authEnabled OR the peer is loopback (REQ-089/D-BIND) OR resolvePrincipal returns a principal, else 401+WWW-Authenticate before any store read; the GATE joins the authHandlers block (server.ts:1722) as the 4th dbindExempt member while the HANDLER stays in the /api/* dispatch (:1918-1930), because authHandlers=authCfg&&authTokenStore and a route living inside it would 404 every no-auth deployment. Rejected: a second masking projection (REQ-101 forbids drift between the two masks). REQ-086 non-goal is slice-scoped ('in this slice', v15) so this adds a protected surface rather than breaking a ratified rule. Amended rows: ARCH-051 (A6 drift-lock = two-sided name-set equality, not a length floor; typed total table recorded as TARGET; 29-tool backfill debt named, budgeted at Gate 3), ARCH-077 + ADR-021 + data-arch lifecycle (A8: the maxWorkflowVersions PRUNE does not exist - the ceiling REFUSES registration; deregister is the single deletion path), ARCH-079 (inv 2 N-1: the try/catch/finally wraps the SCHEDULED CLOSURE in _startJob, not _runJob - corrects BOTH panels' r1 text; inv 4 strike of 'provider HTTP status' at all 4 sites; inv 5 = ONE LINE PER SETTLE at the settle choke point, field list trued up with the shipped gateFail + a new cause field, RETRIES_EXHAUSTED overload named as debt; inv 6 A7 true allowlist membership + per-member ADR-015 re-audit result + the future-widening rule; inv 8 V-C; NEW inv 9 V-A stamp-before-schedule, inv 10 V-B bounds are per-process, inv 11 A2 no gateway call when enabled:false, guard at the _startJob choke point with putDiagramPending moved behind it), ARCH-080 (A5: ONE declaration + interpolation, never transcription, scoped to engine-instructs/engine-validates vocabulary, with the REQ-104 asymmetry), ARCH-081 (QD-O2 phases is emitted; V-D DISABLED takes precedence for any non-ready row when the analyzer is off - prerequisite for inv 11 being honest), ARCH-082 (A6), ARCH-083 (A1 + A9 four-entry allowlist + the call-site deletion rule), ARCH-084 (A4: the mini-preview is DELETED, not re-pointed - 20N req/min/tab), ARCH-085 (A2 + the falsified justification repaired to name its own dependency; noteCode:DISABLED conflation corrected), ADR-016 (A8 strike + journal true-up), ADR-017 (A10: the regenerate->crash->sweep window STATED, with generated_at's double duty as the cause), ADR-022 (A9 + call-site rule), both mermaid views, 4 interface-table rows, + a full Gate-2-re-run Decision rationale. THREE REFEREE CALLS made (the two r2 files were written in parallel and never saw each other): R-1 inv 5 scope = quality's per-SETTLE over adversarial's per-scheduled-job (adversarial's motivating objection dissolves once the emitter is at the choke point; MODEL_UNMAPPED silent is the composeConfig signature made invisible); R-2 A5 vehicle = a note on ARCH-080, NOT a new ADR (crossed concession, decided on quality's surviving argument + no new work item keeps trace byte-identical); R-3 A6 = adopt both shapes (an imported TYPE supplies coverage, not content, so it does not violate adversarial's oracle rule). Panel pre-run by the orchestrator, NOT re-spawned; all four .panel/architecture files read, r2 as baseline. Every load-bearing src/ claim re-verified against the working tree by the synthesizer. Trace 996 items / 18 gaps — BYTE-IDENTICAL to the pre-edit baseline, 0 orphan, 0 broken-link, no new gaps; all 10 v23 ARCH rows still carry module: + deps:. No src/, tests/, README, DEPLOY or 04-design.md touched (Gate 4 was NOT sent back; DES-125/127/129/130/132 stand). Per send_back the next dispatched gate is 5 (tests, RED first), then 6. | PRIOR 2026-09-02 (v23 Gate 2, REQ-101..106 — workflow_describe explain surface + analyzer-drawn ASCII diagram + trigger bindings + config-separated analyzer + skeleton retirement + AUTHORING.md): ARCH-077..086 + ADR-015..022 added, 10 ARCH / 6 REQ, one ARCH per module root, module+deps on every one, 0 orphan / 0 broken-link (trace 917 items, 29 gaps = baseline 29 unchanged; the 12 REQ-101..106 un-impl/un-verified gaps close at Gates 3-7). Synthesized from all FOUR panel files (.panel/architecture/{adversarial,quality-dimensions}.r{1,2}.md), r2 taken as baseline. ALTITUDE (both lenses independently): v23 is the first iteration where an AGENT IS AN INTERNAL ENGINE SUBSYSTEM — the engine calls a model on attacker-influenced input (an author script) and republishes the output to principals forbidden from reading that input; every load-bearing decision follows from that. 077 src/workflow-catalog.ts: workflow_diagrams(name,version,status,diagram,note_code,generated_at,bindings_fp) PK(name,version) in catalog.db, derived+MUTABLE (ADR-021: not on the immutable version row, and NOT a standalone DiagramStore as BOTH panels proposed — the 'derived store must not outlive its source' invariant needs the SAME better-sqlite3 transaction, hence the same Database handle, hence the same module; v22 resolveVersionRequest precedent) (<-REQ-102,101). 078 src/trigger-bindings.ts: ONE getTriggerBindings(name) -> projected snapshot + bindingsFp, TWO call sites (analyzer input; describe-time live field). THREE PRIMARY-SOURCE CORRECTIONS neither panel had: the 3 trigger stores are 3 SEPARATE SQLite FILES (server.ts:1331/1334/1392) so this is an API composition not adversarial R9's 'single batched read'; continuations are keyed by afterRunId = a RUN (continuation-store.ts:75-86) so REQ-103's 'upstream workflow' costs a RunStore join and is legitimately null, and chain bindings are TRANSIENT (status=pending only); and webhooks.secret is a LIVE CREDENTIAL in a table this snapshot feeds into an LLM PROMPT (webhook-registry.ts:76-82) — so the projection is constructed from an explicit field list (no secret, no webhook id), closing the leak at the READ, because the gate only protects the diagram, not the provider call (<-REQ-103,102). 079 src/graph-analyzer.ts: async, single-flight concurrency 1 + queue cap, own timeoutMs/retries, DEDICATED GatewayClient.invoke() path NEVER AgentExecutor [ADR-016] — verified the gateways persist nothing themselves (correlation headers only), so isolation is what we DO NOT pass: synthetic runId/agentId, no workspace, no onEvent/onHarness, no SecretValueProvider, explicit non-CLAUDE.md cwd, tools []. ONE journal line/run {name,version,principal,model,tokens,durationMs,outcome,noteCode} = cost attribution + the effective-config seam. allowedLabels computed here from parseWorkflowSkeleton — the function's SECOND internal consumer, which is what justifies keeping it (<-REQ-102,104,103). 080 src/diagram-gate.ts: PURE gateDiagram(raw, allowedLabels, limits), ALLOWLIST not denylist [ADR-015] — a system prompt is not an access control (a script COMMENT saying 'include the configuration string verbatim in a node label' is a one-line bypass); DIAGRAM_CODEPOINTS constant with exactly 3 consumers closes renderer injection (ANSI/CSI, C0, RTL, zero-width) both ways; reason SPLIT into GATE_REJECTED_CONTENT (security, ~0 expected) vs _SHAPE (replaceability — the 'wired but degraded' signal after a model swap). Pure ⇒ the A3 invariant is unit-testable with ZERO model involvement (<-REQ-102). 081 src/workflow-view.ts: projectWorkflowDescribe — ONE projection for both the MCP tool and the HTTP route (REQ-101's 'the two masks cannot drift'), no script field IN THE TYPE (leak = tsc error), lockedKeys named as locked, LIVE triggers[] + diagramGeneratedAt + diagramStale (one sha256 compare) making REQ-103 machine-checkable [ADR-019] (<-REQ-101,103,102). 082 src/mcp-facade.ts: workflow_describe (ANY principal, required ReadContext, same resolveVersionRequest truth table) + workflow_regenerate_diagram (OWNER-gated via the EXISTING resolveWritePrincipal helper, its 4th call site — verified THERE IS NO ADMIN TIER in this codebase, so both panels' 'owner/admin' reads as owner) (<-REQ-101,102,103). 083 src/server.ts: +2 tool schemas (both join ARCH-051's drift-lock), /api/workflows/:name/describe REPLACES /skeleton (DELETED), and REQ-105 enumerated BY LINE — out: mcp-facade:255/307/329, server:446/1037/1074-1084, workflow-view:24, dashboard-page:203/223; kept internal: workflow-meta:131, dashboard:246, and server:1123-1146 dag BEHIND THE AUTH GATE (v22 H2 stays closed). Guard is MECHANICAL [ADR-022]: CI grep over src/ + the advertised schemas, 3-entry allowlist — the ledger's 9-instance 'deletion isn't done while something still describes it' class becomes a build failure (<-REQ-105,101,102). 084 src/dashboard-page.ts: ASCII diagram via textContent into <pre> (the FIRST model-authored string to reach this renderer; convention KP-12 is not a control when the author is an LLM), skeleton previews removed, no substitute drawing (A1) (<-REQ-105,101,102). 085 src/main.ts: graphAnalyzer{enabled,model,systemPrompt,tools,timeoutMs,retries} forwarded in composeConfig() + a compose-config-v2-wiring.test.ts row + REQ-104's GATE 7.5 REAL RUN — all three, because only the real run proves the value reached the analyzer (<-REQ-104). 086 docs/AUTHORING.md + the workflow_register.script description (the tool schemas are all a cold MCP client has); documented smells, NO new registration rejection (<-REQ-106). CONTESTED CALLS: ADR-018 DELETES inputs_fp/the diagram cache, OVERRIDING a CONVERGED panel position — REQ-104's no-redeploy acceptance becomes true by construction, REQ-102's 'generated fresh' is satisfied literally (closing an owner escalation), and adversarial's own SC-1 cached-failure predicate + the cross-principal oracle disappear with it; the register-loop spend it mitigated is already maxWorkflowVersions' job. ADR-017 unavailable is RECOVERABLE by an explicit owner-gated workflow_regenerate_diagram only — auto-backoff lost (duplicates retries, unbounded spend on a failing provider) and lazy describe-time regeneration lost (every read a potential model call); success state named `ready` after the panels CROSSED concessions in r2 (each adopted the name the other had just abandoned) — decided on the surviving argument, REQ-102 pins only the absence states. ADR-020 tools defaults [] + loud boot warning (requirement mandates the KEY, not a non-empty default; quality endorsed 'without qualification'). ESCALATIONS ALL CLOSED IN-GATE, none sent to the owner: O-1 owner-email/webhook-id — v22 ALREADY ships `owner` to non-owners (ARCH-075 allowlist, ADR-012 reasons about it) so describe opens NO new exposure and masking it there alone would make two surfaces disagree about one object; webhook id simply is not in the projection (no REQ asks for it) — not building a masking mechanism beats building one; O-2 tools default decided as ADR-020; O-3 'generated fresh' dissolved by ADR-018. Owner may overrule any of the three at review. DECLINED (each with the lens that asked): standalone DiagramStore, inputs_fp, cross-workflow script-hash dedupe, auto-retry-with-backoff, lazy regeneration, a 2nd vocabulary-conformance checker (the gate IS one), enrolling the analyzer in generic per-agent observability with masking (a 2nd mask implementation is a drift bug waiting to be filed), an effective-config status surface (folded into the journal line), an admin trust tier (none exists), process-sandboxing the analyzer (it runs no user code), a rate limiter, Mermaid/SVG (A2), a retained structured IR (A2 — and any future one must pass its own gate), any degraded skeleton fallback (A1), extending REQ-083 redact-at-capture, a diagram GC sweep. RESIDUALS: owner visible to all principals on both read surfaces (unchanged from v22); v23 RAISES v22 debt S-1 severity (every registration now costs an LLM call); REQ-083 does not reach script literals (moot only while ADR-016 holds); the shipped systemPrompt assumes a model class (GATE_REJECTED_SHAPE is the signal, DEPLOY.md must say so); chain_create still validates nothing so a diagram may draw a trigger to a non-existent upstream — render VERBATIM, never 'correct' it; dashboard detail has no preview until a diagram is ready; /skeleton disappearing is a breaking change for any scraper. | 2026-09-01 (v22 Gate 2, REQ-096..100 — versioned catalog + beta/release channels + closing inline script + moving the static checks to registration + non-owner script masking): ARCH-071..076 + ADR-009..014 added, 6 ARCH / 5 REQ, module-clean. 071 src/workflow-catalog.ts: append-only workflow_versions(name,version,script,defaults,params,createdAt) immutable rows + release_version/beta_version NULLABLE POINTER COLUMNS on the per-name row (ADR-009: not a channels table — REQ-097 closes the enum AND bars per-user channel state; not in-place overwrite) + owner-gated publish() + THE single resolve(name,{version?,channel?}) accessor with get(name) DELETED (quality REP-1 over adversarial's getVersion-alongside: a surviving legacy newest-row read is how this repo's stored-but-never-wired class recurs 4x; deletion turns the 7 known call sites into tsc errors) + pure total resolveVersionRequest truth table (explicit version > channel > release default; NULL pointer => CHANNEL_UNPUBLISHED naming the channel, NEVER fallback-to-newest) + transactional idempotent boot migration + register/publish each in one db.transaction() (cross-process self-update overlap only — better-sqlite3 is sync so in-process interleaving is nil; the new PK turns yesterday's silent lost update into a raw SQLITE_CONSTRAINT) + per-name version ceiling from the EXISTING Ceilings object (S-1 debt taken) (<-REQ-096,097). 072 src/run-manager.ts: resolve ONCE at admission (:391) and PIN the resolved version — reusing the EXISTING write-once runs.scriptVersion D-V7 field, so the pin costs ZERO schema (both lenses assumed a new column); resume (:635), nested workflow() (:801) and the DAG all read THROUGH the pin, which is the only available fix for a LIVE defect (a named run's resume re-reads catalog.get(name) and can continue a different script than it started); inline script refused at INGRESS ONLY (rehydrate must keep tolerating a pre-v22 persisted spec.script or suspended runs strand); _adhoc left inert; NAME-COLLISION WARNING recorded — RunEntry.scriptVersion is an in-memory resume-generation counter (+= 1, stamped on journal entries as v<n>) and must never write back to the durable pin (<-REQ-096,097,098,099). 073 src/server.ts: script REMOVED from the workflow_run/workflow_resume inputSchemas AND refused typed INLINE_SCRIPT_CLOSED with the register->run migration recipe (schema-level + runtime, asserted separately); new workflow_publish tool; version/channel params; ARCH-051 drift-lock; and the REQUIRED ReadContext threaded into the two read dispatch cases at :808/:823 which thread NO principal today — an optional principal=null would reproduce the composeConfig wiring bug class verbatim (correct impl, unwired call site, all unit tests green, zero protection), a required arg makes it a tsc error; args.principal BARRED from the confidentiality decision (self-asserted, and owner is in the non-owner allowlist so the mask would be bypassable by echoing its own response); /api/* + dashboard serve the non-owner projection whenever auth is enabled (<-REQ-096,097,098,100). 074 src/script-checks.ts (NEW pure): validateScriptEntry lifted verbatim out of submission-validator.ts:92's if(spec.script) block — registration ENFORCES fail-closed (nothing stored), admission OBSERVES and records, workflow_get recomputes and returns validation{ok,errors} (workflow_list deliberately excluded: 3s dashboard poll x per-row script parse); structural guard = ZERO if(spec.script) branches left in SubmissionValidator; P6-2's registration half reuses the SHARED FRAME_CLOSE_FORGERY predicate, never a second copy (<-REQ-099). 075 src/workflow-view.ts (NEW pure): projectWorkflowForRead ALLOWLIST PROJECTION at one choke point — constructed, not deleted, because script is returned TWICE today (top level + result.script) and skeleton/phases are script-derived (a decompiled agent-graph outline) and NOT on REQ-100's allowlist; non-owner branch emits scriptWithheld:true PLUS REQ-095's report affordance and the REQ-090 contract; test oracle pinned as a literal two-sided key allowlist (not.toContain is refused — it passes while result.script leaks) (<-REQ-100). 076 src/mcp-facade.ts: required ReadContext, viewerIsOwner keyed on authEnabled, version selection through the same truth table (<-REQ-100,096). ADRs: 009 append-only rows + 2 pointer columns; 010 the pin (rejects persisting the resolved script into RunSpec — a new UNMASKED sink defeating REQ-100 from another endpoint) and records REQ-006's edited-script-resume clause as SUPERSEDED by REQ-098 (no test pins it; a resume({runId,version}) re-point was rejected — no v22 REQ asks for it and it collides with v21 ADR-002's run-immutable effectiveParams); 011 the boot migration PUBLISHES each migrated workflow's current version to release, atomically — WITHOUT it every pre-v22 named run, schedule, chain and webhook starts failing at its next fire (fleet-wide silent outage bought for nothing; the single highest-value line in the slice); 012 the mask keys on authEnabled NOT principal==null (a null principal also means the D-BIND loopback exemption on an auth-enabled publicly-bound server — exactly the deployment REQ-100 is written for); REQ-100's literal auth-disabled clause preserved byte-for-byte, so this was DECIDED not escalated; 013 REQ-099 disposition split (registration enforces, admission observes, one module — resolves the ONE real panel conflict: adversarial registration-only vs quality admission-recheck, each conceded a half); 014 retention = per-name ceiling at the front door, no GC (a GC deletes silently behind an auditable pin; a ceiling refuses where the author can react), no CAS dedup, deregister keeps today's semantics and pins dangle as recorded strings. Panel: r1 stances COMPLEMENTARY (both independently converge on the pin, the migration-publishes-release, one projection choke point, moving the checks, and taking S-1 + P6-2) => r2 NOT triggered AND the task pre-ran the panel and forbids re-spawning. 4 divergences adjudicated: env-recheck (split by disposition), publish audit trail (DECLINED — quality conceded to simplicity in its own r1; replaced by a structured INFO log line, since the per-run pin already reconstructs pointer moves and publish is owner-gated so who is nearly determined), masking scope (both hold — they address different cells), skeleton/phases masked (adversarial's ruling, uncontested). 2 panel claims corrected against primary source: the cross-process-only race framing, and the pin needing NO new column. Every code claim grep-verified (workflow-catalog.ts:196-220 read-then-write + ON CONFLICT, run-manager.ts:391/635/801, server.ts:808/823/1044-1046, mcp-facade.ts:220/232, submission-validator.ts:92, run-store.ts:81/129/146). QM safety_class => no functional-safety/cybersecurity lenses. Karpathy: complexity budget spent only on 071 (the one genuine schema change) and 072 (the resolution/pin invariants); 074 is code MOVED not written, 075 is one pure function, 076 is a required argument; every heavier option declined (channels table, audit table, version GC, CAS dedup, resume re-point, script-into-RunSpec, lock table, deleting _adhoc, browser-session identity, author-chosen version strings, a schedule lastError column). trace: 841 items / 24 gaps / 0 orphan / 0 broken-link — REQ-096..100 downstream unimplemented/unverified as expected pre-tasks/design, 13 low pre-existing drift + TASK-018 + IMPL-082 unchanged. current_stage requirements->design. Prior: 2026-08-31 (v21 Gate 2, REQ-090..095 — the tunable-parameter contract): ARCH-064..070 + ADR-001..008 added. ARCH-064 pure src/params/contract.ts (LOCKED_KEYS, closed UserOverrides, self-describing PARAM_* errors, additionalProperties:false); ARCH-065 pure src/params/resolve.ts (TWO-moment resolution: mergeRunParams at admission, resolveCallParams 5-rung + provenance at dispatch, composePrompt, mapEffort); ARCH-066 admission rung inserted between run-manager.ts:349 catalog.get and :355 createRun (no existing rung moves) + run-immutable effectiveParams snapshot routed through the ARCH-056 redact sink + 3 ceiling config keys WITH the composeConfig wiring test as DoD; ARCH-067 `params` column normalized at registration (survives v22 script masking); ARCH-068 REQUIRED dispatch argument (tsc-enforced, kills the 4th inert-wiring instance) + descriptor provenance/effortApplied tri-state; ARCH-069 mapEffort applied on the outbound request, session-options-builder.ts stays FENCED (ADR-006); ARCH-070 workflow:<name> label + dedup-fingerprint inclusion. Panel r1 complementary → no r2. Two panel claims corrected against primary source (PROMPT_CAP is descriptor-record-only; the ladder diagram does not match the pinned code order). Trace 777/21 = baseline (0 new gaps). Prior: 2026-08-17 (v15 Slice B Gate 2, REQ-012+086..089 — engine-as-its-own-OAuth-2.0-AS federating identity to Google + per-caller principal on protected surfaces + workflow ownership + harness-param binding at registration + D-BIND fail-closed; slice B of the remote-auth #34-lineage): ARCH-059..063 added, 1:1 per REQ. 059 engine's own AS under src/auth/ (PURE oauth-metadata.ts .well-known builders w/ PKCE S256; token-store.ts SQLite injectable clock+CSPRNG, 3 tables bearer_tokens/single-use auth_codes/single-use oauth_state, issue/verifyByHash/consume/gcExpired; google-verifier.ts iss/aud/exp/JWKS-signature/nonce + email_verified===true BEFORE adopting email claim, injectable JWKS fetcher+Google base URL; auth-service.ts startAuthorize/handleGoogleCallback/tokenExchange/resolvePrincipal) + 4 server.ts routes; OPAQUE sha256-at-rest bearer NOT JWT [D-AUTH-1 kills forgery/alg-none/alg-confusion, revoke=row-delete, leak=hashes], single-use ≤60s atomic-consume codes/state, RFC-8252 loopback redirect for Claude-Code login, Google client_id/secret+pepper in REQ-018 secret store only never sandbox-reachable, weeks-scale bearer TTL (closes consumability clash sans refresh tokens); reject DCR/scopes/refresh-tokens (Karpathy — REQ-012 asks none, Google-no-DCR is WHY engine is own AS); supersedes ARCH-009 no-op OIDC placeholder (←REQ-012). 060 resolvePrincipal gate at protected surfaces ONLY (/mcp + POST /assets/blob/:sha + POST /assets/manifest) → 401+WWW-Authenticate BEFORE any side effect; principal resolved ONCE at edge threaded as EXPLICIT arg not global [D-AUTH-2, unit-testable+concurrency-safe]; recorded on control-plane row (run principal:<email> via workflow_status, CAS namespace first-writer) NEVER into sandbox env/workspace [D-AUTH-4 hermeticity]; CAS attribution record-only not a security control (content-addressed immutable, cross-principal write can't poison); /api+dashboard stay unauth (REQ-086 non-goal) (←REQ-086). 061 workflow ownership: owner TEXT column, register(name,script,defaults,principal) gates MUTATION only (overwrite/deregister→NOT_WORKFLOW_OWNER), run/get/list ungated; idempotent WHERE-owner-IS-NULL backfill→hsuhungjung@gmail.com once per auth-enabled boot; auth-disabled⇒NULL owner not sentinel [D-AUTH-6 keeps backfill uniform] (←REQ-087). 062 defaults TEXT column = first-class {model,tools,skills,timeoutMs,prompt} stored alongside script (workflow_get-queryable not script-parsed = the swappable agent-harness surface); per-param override merge (override timeoutMs keeps registered model); register-time validate shape+model-alias+tool-in-static-allowlist→typed-error+store-nothing (fail-closed), DEFER skill existence to run [D-AUTH-5, skills are mutable per-run assets → register coupling = stale false-failures; REQ says tool not skill]; no-defaults=pre-v15 shape still registers (←REQ-088). 063 EXTENDS existing net-guard.ts chokepoint (not a new module): auth-enabled+non-loopback-bind → non-loopback un-tokened protected-surface call refused fail-closed 401; loopback 127.0.0.1/::1 exempt (admin/curl/self-update rescue); exemption keys on RAW SOCKET remoteAddress ONLY never forwarded header [D-AUTH-3, THE sharpest finding: cloudflared→127.0.0.1 would exempt the entire internet]; deployment hard-rule tunnel-origin-targets-non-loopback-bind; forwarded-for/cf-connecting-ip present⇒never-exempt fail-safe; /github/webhook HMAC-carve-out unaffected; auth-disabled⇒dormant (opt-in switch) (←REQ-089). Synthesized from .panel/architecture/adversarial.r1 (opus, security/scalability/testability, scoped-to-slice) + quality-dimensions.r1 (sonnet, obs/repl/consum/self-sustain, whole-system); r1 stances COMPLEMENTARY → r2 NOT run (no contradictory in-scope recommendation) AND task pre-ran panel+forbids re-spawn. Adopted D-AUTH-1..6 wholesale (baked into item NOTES not only rationale — silently-wrong-if-missed class). Predicted consumability clash (fuller-OAuth DCR/scopes/refresh) never materialized in quality r1 → converged via weeks-scale TTL. Quality whole-system gaps (StreamingSeam/SSE, /metrics, W3C-trace-IDs, RunStore-port, MCP-SDK-migration, @rwe/workflow-types, openapi.json, /healthz, disk-full, SDK-gateway-timeoutMs-race) CONSCIOUSLY EXCLUDED — trace to out-of-slice REQ + mostly pre-existing tracked debt; minting them = the scope creep Karpathy blocks. Where quality intersects slice it converges: principal via existing workflow_status (no new endpoint), harness-defaults = the replaceability/consumability surface REQ-088 already makes first-class. 2 accepted residual gaps recorded (adversarial Risks 7+8, REQ non-goals — record don't architect): run-lifecycle mutation (stop/suspend/resume/purge) ungated for non-owners; /api+dashboard unauth now surface principal:<email>; both = user's 01-preamble rollout decisions, NOT clarifications. QM safety_class → no functional-safety/cybersecurity lenses. Karpathy: 5 ARCH / 5 REQ 1:1, complexity budget ONLY on 059 (the one genuine new subsystem — minimal AS, 4 small pure/injectable files); 060=edge-resolution+explicit-threading (a parameter not machinery), 061=one column+idempotent backfill, 062=one column+register validation reusing alias-table+tool-allowlist, 063=net-guard extension no-new-module; dominant risks all bypass/impersonation (route skipping resolvePrincipal, cloudflared-on-loopback hole, missing email_verified, principal leaking to sandbox) made impossible-by-construction (single edge chokepoint, raw-socket keying, load-bearing verifier gate, control-plane-only attribution); every heavier option (JWT-for-statelessness, DCR/scopes/refresh, namespace-ownership enforcement, separate auth-server file, private-IP connect ban) rejected as speculative; auth opt-in⇒byte-for-byte pre-v15 when disabled, every existing test unchanged. trace: 713 items / 14 gaps (0 orphan/broken-link) all downstream 未實作/未驗證 expected pre-tasks/design (REQ-012+086..089 now trace to ARCH-059..063; 5 new ARCH-not-impl gaps) + pre-existing; 0 new orphan/broken-link. current_stage requirements→design. Prior: 2026-08-15 (v14 Gate 2, REQ-081..085 — raw body-streaming blob upload + server-side seedManifestRef + redact-at-capture + asset_push schema honesty + optional scriptSha256; the remote-seeding-from-a-different-machine fix, slice A of #34): ARCH-054..058 added, 1:1 per REQ. 054 streaming raw-body blob ingest POST /assets/blob/:sha?namespace behind the existing isAllowedHost/isAllowedOrigin net-guard [adversarial R1-HIGH: route exemption demonstrably happens here — github-webhook is Host-exempt]; pure isValidSha256Hex/isValidNamespace validators BEFORE any fd (path-traversal); injectable CasStore.putBlobStream(ns,declaredSha,readable,maxBytes,readTimeout) seam = temp-file + incremental sha256 + mid-stream abort→BLOB_TOO_LARGE + read/idle timeout→BLOB_UPLOAD_TIMEOUT + verify computed==declared→BLOB_SHA_MISMATCH + unlink-on-any-failure + atomic rename→blobs/<sha[0:2]>/<sha> THEN record namespace ref; no-exists-shortcut invariant (bytes fully consumed+verified even if blob exists — possession proof, REQ-064 poisoning); 200→{sha256,bytes,namespace}+one INFO line; disk-fill accumulation = documented residual, no quota/GC (←REQ-081). 055 manifest-as-CAS-blob: seedManifestRef = sha256(manifest bytes) stored as an ordinary immutable blob, NO ManifestStore/table/GC/lifecycle; POST /assets/manifest raw-body validates referenced blobs present→MISSING_BLOBS then stores manifest bytes; run-time MISSING_BLOBS stays the security boundary (register-time = UX), parse-fail→INVALID_SEED_SPEC/MANIFEST_PARSE_ERROR; workflow_run({seedManifestRef}) assembles via EXISTING materializeManifest (inline+ref cannot diverge); 4-way seed-source exclusion (seed/seedManifest/seedRef/seedManifestRef)→SEED_SOURCE_CONFLICT slotted into the v13 ladder; RunStatusView.seedManifestRef provenance; workflow_run desc enumerates 4 sources + cross-refs both dark REST endpoints, drift-locked (←REQ-082). 056 redact-at-capture: enumerate EVERY transcript persist sink [per-agent store workflow_agent_log reads / terminal snapshot / SDK-gateway message|tool_call|tool_result|usage; harness already redactHarness'd — separate unchanged stage] + the journal settled-value sink (run-manager appendJournal JournalEntry.value, a REPLAY source); route each through redact() ON THE PERSIST WRITE ONLY never the live messages array [D-v14-C, silent-breakage if violated]; marker = name-keyed ‹secret:NAME› [D-v14-D, no dictionary oracle, stable no-key, per-secret distinguishable]; redact() signature extended string[]→{name,value}[] via injected SecretValueProvider.entries(), never crosses into sandbox; journal redacted + hermeticity contract 'no secret round-trips through an agent()/workflow() output schema' [D-v14-B option a] (←REQ-083). 057 honest asset_push kind enum — keep hook/mcp-config values, annotate disposition in-band (hook→HOOKS_UNSUPPORTED, mcp-config→mcp_provision), drift-lock, behavior unchanged (←REQ-084,079). 058 pure assertScriptIntegrity(script,sha?) as one rung in the pre-createRun ladder→SCRIPT_SHA_MISMATCH, absent=unchanged; reject any script-signing subsystem framing (←REQ-085,081). Synthesized from .panel/architecture/adversarial.r1+r2 (opus, security/scalability/testability) + quality-dimensions.r1+r2 (sonnet, obs/repl/consum/self-sustain); r2 TRIGGERED (r1 materially conflicted: ManifestStore port, separable asset-server file, fingerprint algorithm, journal scope). The two r2 files CROSSED (each rebuts the other's r1, neither saw the other's r2) → 'favor final round' = UNION of both r2 concessions, leaving ZERO open conflicts (adversarial r2's stale 'remaining disagreements' on ManifestStore/asset-server were both independently CONCEDED by quality r2). 6 synthesizer decisions: D-v14-A manifest-as-CAS-blob OVER dedicated store+ManifestStore port (quality CONCEDED conditionally-on-adoption; dissolves GC/lock/empty-workspace-race by immutability); D-v14-B journal REDACT + hermeticity contract (both lenses recommend option a; journal is a replay source so divergence bites only a secret-exfiltrating misuse); D-v14-C redact at persist-write only never live messages array (both CONVERGE, quality R4); D-v14-D marker ‹secret:NAME› (adversarial r2 synthesis quality never saw — satisfies every quality-held constraint, safer+simpler than both r1 options: keys on handle NAME not value → no offline dictionary oracle, stable no-key; port SecretValueProvider.entries():{name,value}[] = the exact variant quality r2 pre-endorsed; it's an EXTENSION of redact() not pure reuse — quality R10 honored); D-v14-E no separate asset-server.ts, both /assets/* routes in server.ts behind the net-guard (adversarial r1-HIGH; quality r2 CONCEDED R8 — security beats replaceability by asymmetric cost, extraction later is trivial glue move); D-v14-F testability at the unit seam (fake Readable incl maxBytes+1 & stall) not a new HTTP layer, no ManifestStore port (manifest IS a CasStore blob). Converged additive holds folded: BLOB_UPLOAD_TIMEOUT read/idle timeout (quality R5, adversarial r2 conceded), seedManifestRef in RunStatusView (quality R7), 200 {sha256,bytes,namespace}+INFO line (quality obs hold), 4-way conflict + dark-REST cross-ref in MCP descriptions all drift-locked (REQ-079 precedent). REQ-084/085 converged trivial; every expansion (script-signing subsystem, pattern/entropy redaction, resumable/chunked upload, manifest lifecycle table, private-IP ban) rejected as speculative (Karpathy). QM safety_class → no functional-safety/cybersecurity lenses. Karpathy: 5 ARCH / 5 REQ 1:1, complexity budget spent ONLY on 054 (true streaming, the one genuine new I/O path) + 056 (sink-completeness, the load-bearing security REQ); dominant risks are all bypass risks (route skipping net-guard, sink skipping redact, stream skipping verification) made impossible-by-construction not machinery; no change to inline seed/seedManifest/seedRef paths or the existing secret path. trace --check: 674 items / 17 gaps / 0 new orphan/broken-link — REQ-081..085 downstream 未實作/未驗證 expected pre-tasks/design (the 5 previously-undecomposed REQ now trace to ARCH-054..058; 5 new ARCH-not-impl gaps replace them, net gap count unchanged); other pre-existing (REQ-080 in-flight impl, REQ-012 OIDC-deferred, IMPL-082 TDD-label, UT-058/UT-064/IT-057 drift, TASK-018). current_stage review→design. Prior: 2026-08-15 (v13 Gate 2, REQ-080 — engine-pull seedRef:{repoUrl,sha} behind a fail-closed egress allowlist, SSRF-safe): ARCH-052 (pure isEgressAllowed reject-before-network gate [deny-by-default; empty→SEEDREF_DISABLED; non-https/userinfo/parse-fail/non-match→SEEDREF_EGRESS_DENIED; prefix-match on normalized origin+pathname with enforced trailing /] + seed-source mutual-exclusion→SEED_SOURCE_CONFLICT + config-load allowlist normalize/validate, all pre-createRun ←REQ-080) + ARCH-053 (SeedRefFetcher injectable port: hardened git subprocess [GIT_CONFIG_NOSYSTEM/isolated GIT_CONFIG_GLOBAL+HOME/GIT_ALLOW_PROTOCOL=https/http.followRedirects=false/submodule.recurse=false/GIT_TERMINAL_PROMPT=0 — NOT workspace-git's un-isolated env] → --depth1 + wall-clock timeoutMs + total+per-file byte caps from ls-tree BEFORE blob read → unconditional sha-verify [SEEDREF_SHA_MISMATCH] → CAS-assemble via materializeManifest [reuses .claude-strip/.git-reject/realpath/exec-mask/dedupe, ls-tree reject 120000 symlink/160000 gitlink, no worktree checkout-then-copy avoids TOCTOU] → fetch-outcome observability [resolved sha/bytes/latency on run record] → typed SEEDREF_FETCH_FAILED, post-createRun, runId still immediate ←REQ-080). Synthesized from .panel/architecture/adversarial.r1 (opus, security/scalability/testability) + quality-dimensions.r1 (sonnet, obs/repl/consum/self-sustain); r1 stances COMPLEMENTARY → r2 NOT run (quality's REQ-080 asks — fetch timeout R4, fetch observability #4, mutual-exclusion+allowlist consumability #16 — all additive, folded into ARCH-052/053). 6 synthesizer decisions: D-v13-A CAS-assemble OVER direct-worktree-copy (materializeManifest single writer → 4 guards un-forkable + matches docs/seed-sync-architecture.md + REQ-080 traces REQ-065; copy reopens symlink TOCTOU); D-v13-B NO blanket connect-time private-IP ban (would break documented air-gapped/internal-forge use where sanctioned forge lives on a private/LAN IP the operator allowlisted; deny-by-default prefix-match already blocks unsanctioned targets — a blanket ban is a false security win removing a real feature); D-v13-C DNS-rebind-of-allowlisted-host = ACCEPTED RESIDUAL R1, connect-IP-pin NOT built (speculative hardening atop an explicit operator trust grant); D-v13-D NO fetch worker pool (REQ-054 admission counter already bounds concurrent runs→fetches); D-v13-E timeout is a NAMED arch-time commitment not deferred-to-impl (avoids the D-F5 late-timeout recurrence both lenses flagged); D-v13-F secrets/private-repo DEFERRED (K6, public/allowlisted+air-gapped-no-token forges only; GIT_TERMINAL_PROMPT=0 fails a private repo typed). Quality's broader cross-cutting gaps (trace-ID/RunStorePort/CatalogPort/stateful-circuit-breaker/GET-health/journal-SQLite-archival) NOT new ARCH — pre-existing system-wide deferred future-REQ candidates, not seeded speculatively for a single seed-source feature (Karpathy). QM safety_class → no functional-safety/cybersecurity lenses. Karpathy: 2 ARCH / 1 REQ split on the pure-before-network vs network-after-createRun seam (= the testability seam: SSRF matrix = pure unit tests, one real-git IT); feature reduces to one pure fn (isEgressAllowed) + one injected port (SeedRefFetcher) + reuse of submission-validator/seedPathVerdict + CasStore/materializeManifest; every heavier option (private-IP connect ban, connect-IP-pin, fetch pool, in-scope credential helper, direct-worktree-copy) rejected as speculative/guard-diverging; no change to inline/seedManifest seed paths or any secret path. trace 645 items / 8 gaps / 0 orphan/broken-link — REQ-080 downstream 未實作/未驗證 expected pre-tasks/design; other 6 pre-existing (UT-058/UT-064/IT-057 drift, REQ-012 real-tier, TASK-018, IMPL-082 TDD). current_stage review→design. USER DIRECTIVE: PAUSE for user architecture review before dispatching Gate 3. Prior: 2026-08-14 (v12 Gate 2, REQ-076..079 — system_info metrics + enriched models_list + precise schemas): ARCH-048..051 added — 048 SystemProbe injectable port + pure buildSystemInfo shaper + lazy-cached CPU delta (no per-call blocking sleep, no background poller) + sampledAt/windowMs + one snapshot feeds MCP tool AND additive GET /api/system + dashboard panel; no shell-out to ps/df/top; statfs(workRoot) single mount; per-section degrade-to-null-never-throw (←REQ-076). 049 process metrics via same probe: engine-self {pid,uptime,rss,cpuPct,threads,fdCount} + host Top-N {pid,name,cpuPct,memBytes} name=/proc/comm NEVER cmdline/argv (HIGH secret-leak foreclosure) + system-wide state stats, one bounded async /proc pass Promise.race(150ms)→degraded, fixed cpu-desc/mem-tiebreak sort (←REQ-077). 050 enrichModelEntry pure post-buildCatalog layer: capability (200-char cap) + classifyStability + computeCostLevel integer|null over existing maxPricePerMOf fixed bands (free→0/unknown→null/monotonic-by-construction) + explicit modalities; additive/optional/non-breaking, computed-at-call-time-not-persisted, secret-separated, textContent render (←REQ-078). 051 declarative TOOL_DEFS schema precision (type+enum/range+default+unit+effect) + structured drift-lock test (facts not prose), system_info has topN as its ONLY param (←REQ-079). Panel r1 largely complementary; 3 material conflicts I decided as synthesizer: D-v12-A CPU sampling = lazy-cached two-sample delta (adversarial) OVER on-call 100–200ms blocking double-sample (quality) — call still samples OS (never fabricated), no stall on the 3s dashboard poll, cache is the DoS/recon rate-governor, honors standing D-PROBE no-poller, TTL-window per-process delta is a better metric than a noisy micro-window; first-call-null inside REQ degrade contract; quality's sampledAt adopted. D-v12-B system_info param surface = topN ONLY (adversarial D-v12-4) — sortBy rejected (fixed 'stated key' satisfies REQ-077), sections rejected (quality's own G12 justification was skipping the CPU wait, which D-v12-A removes → motivation dissolved; REQ-079 'if present' makes it optional). D-v12-C costLevel scalar = reuse existing maxPricePerMOf (adversarial) OVER a new out-price fn (quality) — surgical reuse; monotonicity + property test PINNED to that same scalar so the verifier doesn't test against out-price and fail spuriously. Converged/ratified: injectable SystemProbe + StubSystemProbe fake, no ps/df/top shell-out, bounded async /proc walk, comm-not-cmdline, statfs(workRoot), pure enrichment layer, structured drift-lock, textContent. Deferred no-speculative-ARCH (both lenses): per-tool config-disable/public-bind suppression (loopback interim control; DEPLOY documents recon surface coupled to 'never public until auth'), cross-platform probes, SSE, metrics/Prometheus framework, GET /health. Karpathy: 4 ARCH / 4 REQ 1:1, one new module (src/system-probe.ts) + one pure enrichment fn + one drift-lock test; every heavier option rejected as speculative. QM safety_class → no functional-safety/cybersecurity lenses. Code citations grep-verified: maxPricePerMOf model-catalog.ts:212, buildCatalog:176/filterCatalog:225, ModelEntry.modalities:16/price:18/location:20/besteffort:29, models_list server.ts:801, tools/list:1291; system_info + /api/system are new. trace --check: 614 items / 14 gaps / 0 new orphan/broken-link — REQ-076..079 downstream 未實作/未驗證 expected pre-design; remaining 6 pre-existing (UT-058/UT-064/IT-057 drift, REQ-012 real-tier, TASK-018, IMPL-082 TDD). current_stage architecture→design. Prior: 2026-08-10 (v11 F1 REQ-074/075, self-decided fix-scale amendment, no panel — appended ARCH-046/047 for the previously-empty --impact closure): ARCH-046 home grouped cards + mini preview (←REQ-074), ARCH-047 per-workflow reliability metrics pure folds (←REQ-075); both reuse shipped seams (ARCH-035 discovery/skeleton, ARCH-042/043 layout+render, ARCH-024/029 listRuns+dashboard). Every in-scope REQ-074/075 now has >=1 ARCH; 0 orphan/broken-link. passed left true (additive, no stage rewind). Prior: 2026-08-09 (v11 Sprint 3 Gate 2, REQ-071..073 — n8n-style Morandi graph dashboard): ARCH-041..045 added along the panel's converged layered pure-function seams — 041 trigger provenance startedBy{type:client|webhook|schedule|chain,id?} at RunManager.start() persisted in runs table (←REQ-071); 042 pure GraphPayload+layoutGraph(explicit x/y+edges)+skeleton-as-topology overlay with never-drop-live-agent fallback, additive /dag envelope no new endpoint, server-authoritative recompute per poll (←REQ-071,072); 043 hand-rolled SVG edge/text renderer + pan/zoom + per-frame tinted nested Morandi frames (stableHash(frame)%palette) + textContent-only invariant + node-cap 'N more', 3s poll no SSE (←REQ-071,072); 044 harness captured at gateway session-build post-curation as a kind:'harness' TranscriptEvent written eagerly via AgentTranscriptSink (journal-replay restart-safe, NO runs column / NO snapshot bloat), pure redactHarness names-only 4KB-cap, deriveAgentRecords fix for harness-without-usage in-flight agents (←REQ-073); 045 clickable box → detail panel, workflow_agent_log gains harness field + ?limit/offset + 50-msg cap, queued canonical/idle display alias, two-tier no-secret proof (pure-mapper unit + headless DOM at Gate 7.5) (←REQ-073). Every in-scope REQ-071..073 has >=1 ARCH; every ARCH traces an existing REQ; 0 orphan/broken-link. Panel BOTH rounds ran, r2 TRIGGERED (materially conflicting r1 stances): D1 derive-from-transcript (adversarial K2) vs capture-at-dispatch (quality G4) → adversarial CONCEDED on code evidence (transcript carries neither prompt nor resolved surface); D6 CSS-flexbox (quality G7) vs explicit-coord/SVG (adversarial K1) → quality CONCEDED (edges+pan/zoom acceptance needs explicit coordinates). Synthesizer-decided (not punted): harness journaled-event mechanism resolves quality's 'design gate must decide' via what-survives-restart discriminator; G15 client-side topology cache REJECTED (a live run's topology grows mid-run → cache-on-load is incorrect, not merely speculative → Karpathy). Design-gate leftovers (both lenses agree, non-blocking): chain source-node display label; non-SDK gateway emits-harness vs harness-absent. Synthesized from .panel/architecture/adversarial.r1+r2 (opus) + quality-dimensions.r1+r2 (sonnet). QM safety_class → no functional-safety/cybersecurity lenses. trace --check: 559 items / 10 gaps / 0 new orphan/broken-link — REQ-071..073 downstream 未實作/未驗證 expected pre-design; remaining gaps pre-existing (REQ-012 real-tier, UT-058 drift, TASK-018, IMPL-082 TDD). current_stage review→design. Prior 2026-07-11 (v3 Gate 2 re-synthesis, REQ-021 gap-close): ARCH-019 added — WorkRoot Project-Isolation Guard (ARCH-019←REQ-021, boot fail-fast WORKROOT_INSIDE_PROJECT + session-init cwd-outside-any-project invariant). Closes the ONE decomposition gap: REQ-021 (added 2026-07-11 after the empirical MEMORY.md-echo reproduction) previously had no ARCH (IMPL-080/UT-051 traced straight to REQ-021, skipping the architecture layer). Now every REQ-001..021 has >=1 ARCH; 0 orphan/broken-link; trace --check gap count unchanged at 8 (7 未真實驗證 REQ-012/016..020/021 pre-real-verify + 1 low TASK-018, all pre-existing, none introduced). Rationale D-WORKROOT: added in synthesis beyond the panel's stated REQ-016..020 scope; separate ARCH not folded into ARCH-016 because the leak is at session-init (CLI project-root resolution) not tool-call reachability, so ARCH-016's realpath/Bash-deny callback never intercepts it; minimal shape = boot ancestor-walk guard + workspace-cwd invariant, no per-run OS jail (consistent with D-SEC). Synthesized from .panel/architecture/adversarial.r1.md (opus) + quality-dimensions.r1.md (sonnet), round-1 headlines complementary, no round 2. current_stage left at validation (feature is mature past Gate 2 — regressing to design would be destructive; this is a surgical decomposition amendment, not a stage rewind). Prior 2026-07-10 (v3 Gate 2): ARCH-015..018 added for the v3 slice — MCP Provisioning Registry (ARCH-015←REQ-017, admin-write/referenced-by-name/strictMcpConfig), Secret Store+Resolver (ARCH-016←REQ-018, parent-only handle indirection + two-layer containment + redaction), SDK Session-Options Builder + outer kill-on-timeout race (ARCH-017←REQ-016,REQ-020, pure builder/ProviderProfile/thinking-disabled/curated allowlist/FailureEnvelope/SessionInitRecord), Asset-Ingestion Policy (ARCH-018←REQ-019, hooks-drop by construction + MCP-config redirect). Every v3 REQ-016..020 has >=1 ARCH; REQ-012 stays ARCH-009. Three fail-closed AMENDMENTS to existing modules, no new module: global RunGuard semaphore (ARCH-002, D-DOS), non-loopback bind guard (ARCH-009, D-BIND), proxy/CLI subprocess lifecycle (ARCH-005+ARCH-017, D-PROC). Synthesized from the real 2-group panel (.panel/architecture/adversarial.r1.md opus-4-8 + quality-dimensions.r1.md sonnet — round-1 headlines complementary, no round 2 needed); 8 contested trade-offs reconciled in '### v3 slice' rationale (D-SEC two-layer not OS-jail; D-DOS global semaphore; D-KILL engine-owned kill; D-PROFILE config table not SPI; D-REDACT clear-to-submitter/redacted-in-telemetry; D-BIND guard-is-not-auth; D-PROC cleanup-in-module/restart-on-supervisor; D-PROBE no background poller). trace --check: 13 gaps, all downstream 未實作/未驗證 (pre-design/impl for v3), 0 NEW orphan/broken-link. Prior 2026-07-03 note: ARCH-001..014 cover REQ-001..015 (0 orphan/broken-link). Grouped panel (opus adversarial + sonnet-5 quality). PAUSE for user architecture review before Gate 3 dispatch (user directive)." }
  tasks:        { passed: true, note: "v27j GATE 3 SEND-BACK REPAIR (2026-09-13, designer): PASSED - the two follow-up impl items the v27h architecture amendments owe an id (02-architecture.md:3835) are minted as TASK-215 (ARCH-122: delete the fossil shell body dashboard-page.ts:92-152, emit ONE mount element, disposition the five dead page-source pins, add UT-241's positive) and TASK-216 (ADR-049: tsconfig.server.json + both script strings + the pinning UT), both status: draft, estimate: S, iter: v27j, each with a verbatim DoD and a red-first clause (this loop has no tests gate). Three existing cards amended in place at iter v27j, 0 other new ids: TASK-204 (the AC-8 header value, one token), TASK-205 (the draggable=false DASHBOARD_HTML clause STRUCK - ui/workflow.js:151-154 sets it and UT-224 has pinned it there since v27g), TASK-206 (the pre-AC-4 'any ok -> live' clause replaced by the shipped worstOf rule). Section header range TASK-196..213 -> ..216. Trace 1661 items / 35 gaps vs the 1659/33 baseline - the delta is EXACTLY the two priced '未實作|low' rows the architecture already costed; TASK-215/216 are deliberately NOT added to their DES rows' traces: lines, because that edge makes an unlanded task reachable from an existing IMPL and the two gaps vanish (measured; the reason is recorded in 04-design.md's v27j rationale item 4). | PRIOR: v27c GATE 3 DELTA (2026-09-12, designer): PASSED — the routed CSS-ownership gap (pending[0]) is closed by ONE new task, TASK-214, which owns src/dashboard/dashboard.css end-to-end (the class contract + every component/view section + the two locks) and is ORDERED BEFORE val-198..202 are judged and before TASK-209/210/211/212 re-run (new ordering rule 6 in the 03-tasks preamble). Seven existing cards amended in place at iter v27c, 0 other new ids: TASK-205 (the two C1 CSS pins re-point to clientFile(dashboard.css) and the accent ramp direction is corrected), TASK-207/209/211/212 (one sentence each: adds no .css, sets only DES-209 STYLE_HOOKS names, carries no colour/typography/transition literal), TASK-208 (DES-206 one-timer made true: five view loops deleted, onTick hook added), TASK-210 (the same sentence + the HTML-cell-layer substrate + the two val-200 selector edits that must travel in its commit). Trace: 1594 items / 59 gaps vs a 1592/58 baseline — the ONE delta is 未實作|TASK-214|low, the identical pre-implementation signal TASK-196..203/207/209..213 already carry; 0 broken links, 0 orphans. | PRIOR: v27b GATE 3 DELTA (2026-09-11, designer): PASSED — no new TASK id and no new ARCH; the Round-v27b closure (REQ-133/134/140) lands entirely on seven EXISTING rows whose dod: is amended in place at iter v27b (197 fixture+DAG_WARNING_EXAMPLES, 201 deriveLanes/label, 202 facade, 203 route, 206 warningText, 209 workflow view, 210 run view); every in-closure ARCH still has >=1 TASK (125->208..212, 126->201, 130->203, 131->202); ordering rule 5 added to the section preamble (197 -> 201 -> 203 -> 202 -> 206 -> 209/210, VAL judged only after 201). | PRIOR: v27 GATE 3 (merged, 2026-09-11, designer): PASSED — TASK-196..213 (18 tasks) for ARCH-120/122..131 + ADR-049..056; every in-closure ARCH has >=1 TASK (122->205, 123->204/196, 124->206/207/196, 125->208..212, 126->201, 127->199, 128->198, 129->200, 130->203, 131->202/197, 120->210), every TASK carries files/des/dod/estimate and an ORDER preamble (196 before any src/dashboard file; 197 owns all v27 types.ts deltas; 213 batches with 205). TASK-212 PORTS the three shipped tabs (Models/System/Issues) so REQ-067/076/077/078 do not regress when ARCH-122 empties the inline script — an in-closure non-regression, NOT a widening to REQ-137/138/139. Trace 1526/49 -> 1544/67: the 18 new gaps are exactly the structural 'low 未實作 TASK-19x..21x' class (a task with no implementation yet); 0 new 斷鏈, 0 孤兒, 0 new mid/high. | PRIOR(v26): v26 GATE 3 (merged, 2026-09-08, designer): PASSED — TASK-170..195 (26 tasks) for ARCH-110..121 + ADR-037..047; every in-scope ARCH has >=1 TASK (110->175, 111->176, 112->171/172/174, 113->184/185, 114->186/187/188, 115->177, 116->178, 117->179, 118->180/181/182/183, 119->189/190/192, 120->191, 121->193; plus 170 doc-only ADR-047, 194 shape pins, 195 tool-surface table). Every card carries files:/des:/dod: with ONE runnable command and its expected outcome, plus estimate. Partition is deliberately finer than usual per the GATE 3+ directive (lower-tier implementers) with six hard ordering edges stated at the section top: providers.ts first; test rewrites before the deletion; the Tokens widening in the SAME commit as dashboard.ts:56; the fixture corpus before both consumers; RULE_CODE before the catalog drift-lock; pin -> cost -> limits/wire/read-path. NOTE: INV-V26-* must never appear in a traces: field (trace.py ID_RE reads 'V26-1' out of 'INV-V26-1' and mints a broken link). PRIOR: v26 NOT YET RUN (Gate 1 closed 2026-09-08; this flag reset so a resume does not skip it). PRIOR: v24 GATE 3 (merged, 2026-09-04, designer): PASSED — TASK-131..153 for ARCH-087..108. Every v24 ARCH has >=1 TASK (checked programmatically); every card carries files:/des:/dod: with ONE runnable command and its expected outcome, plus estimate. Partition follows the panel 18 rows + 14b/15a/19: pure files first (errors/tool-specs/authz/path-verdict/scan/params/checkMermaid), deletion as its own task, one task per store, facade wiring only after every store, acceptance last, rename sweep last of all. Binding order: 139 before 147/148; 140..146 before 147/148; 152 last, one mechanical commit whose message carries the grep -c before/after counts; one WIP commit per task (the 139..148 window is a deliberately red tree). TASK-153 is EXTERNAL (client plugin repo, owner-scheduled) and blocks the REQ-117 probe, not Gate 6. PRIOR: 2026-09-02 (v23 Gate 3 RE-RUN, post-adjudication #1): TASK-125 added — 'phases' joins the public allowlist on every surface (owner ruling 一律公開, ba3db17), traces ARCH-075+ARCH-081, S, files src/workflow-view.ts + its test, des DES-136; lands BEFORE TASK-117/118 so the diagram's allowedLabels point at an already-public field. server.ts deliberately NOT in files: its only other masking site is the /skeleton branch TASK-120 deletes whole (patch-then-delete leaves no artifact; the interim leaves that route stricter, never looser). Landing order now 113→114→115→116→125→117→118→119→120→121. Six existing cards amended from the re-run panel: 114 (+late-write no-op assertion, +putDiagramPending stamp), 117 (+integration late-write test on the REAL setImmediate path — the one test that must not use the schedule seam — +sweepAtBoot three shapes under FixedClock), 118 (+phases in EXPECTED_DESCRIBE_KEYS, +diagramGeneratedAt null unless ready), 120 (parity test context is authEnabled:true/principal:null — the route is UNAUTHENTICATED, an owner call passes for the wrong reason), 122 (six keys → NINE: maxBytes/maxLines/maxQueueDepth lifted out of engine source per REQ-104's literal 'at minimum' + 'no analyzer harness value is hard-coded' clause), 123 (rule 4 post-adjudication wording + the provider-disclosure standing note). Every ARCH-077..086 still has ≥1 TASK. | 2026-09-02 (v23 Gate 3, merged with Gate 4): TASK-113..124 added for ARCH-077..086 — 113 the pure diagram gate FIRST and alone (carries REQ-102/A3's security invariant, green before the analyzer exists), 114 workflow_diagrams table + the ONE deletion path (ARCH-077's maxWorkflowVersions prune does not exist — the ceiling refuses), 115 getTriggerBindings over four narrow ports before both consumers, 116 the curateToolsForProvider empty-set fix STRICTLY BEFORE 117 (falsifies ADR-020: curateToolsForProvider([],'ollama')===['Bash'] on the default sdk+Ollama path), 117 GraphAnalyzer L (queue + per-(name,version) single-flight + own retry loop + 10-value note enum + journal line + isolation-by-omission + the main.ts scratch-cwd repoint + boot alias validation, all in-task), 118 the pure describe projection, 119 the facade's two tools, 120 the server wire + EVERY enumerated skeleton deletion + the grep guard written-first-and-red (the nine-instance defect class), 121 dashboard <pre>/textContent, 122 ARCH-085's four config artifacts in ONE change (composeConfig forward + wiring row + example + DEPLOY), 123 AUTHORING.md deliberately NOT last (its phase-name rule is the sole control over a new disclosure), 124 REQ-104's Gate 7.5 real run as its own line. Every ARCH-077..086 has >=1 TASK; each card carries files/des/dod/estimate. Edges: 113→114→115→116→117→118→119→120→121, 122 after 117, 123 any time before 120, 124 last. | 2026-09-01 (v22 Gate 3, merged with Gate 4): TASK-105..112 added for ARCH-071..076 — 105 versioned catalog + migration + every converted call site in ONE commit (get()/getFull() deleted so tsc finds misses), 106 pure script-checks.ts, 107 registration ENFORCES + version ceiling + main.ts threading + compose-config wiring rows in ONE task, 108 the run pin + resume-through-pin + legacy fallback + SubmissionValidator shrink, 109 the wire surface (script AND scriptSha256 leave together), 110 pure projectWorkflowForRead, 111 required ReadContext + real-transport masking test in ONE task, 112 scheduler markFailed (reopens a falsified ARCH decline). Every ARCH-071..076 has >=1 TASK; each carries files/des/dod/estimate. | 2026-08-31 (v21 Gate 3, merged with Gate 4): TASK-096..104 added for ARCH-064..070 — 096 catalog row widening + params column (lands FIRST, alone: narrow get() makes ARCH-066 no-second-query unimplementable); 097 pure contract.ts; 098 pure resolve.ts; 099 registration stores the normalized contract (ON CONFLICT params = excluded.params is the silent-staleness trap); 100 admission + snapshot + resume + the 3 ceiling keys WITH their composeConfig wiring rows in-task (5th-instance guard); 101 dispatch wiring (tsc lever on AgentReq, not the constructor) + descriptor + 5-segment prompt + observable pre-dispatch rejection; 102 gateway effort with thinkingFor as sole writer; 103 issue binding; 104 delete resolveHarnessParams last. Every ARCH has >=1 TASK; each card carries files/des/dod/estimate. Edges: 096-to-{099,100}, 097-to-098-to-{099,100,101,102}, 100-to-101, 101 parallel 102, 103 independent, 104 last. | 2026-08-19 (v20 refresh-tokens+callback-page FIX F1, self-decided no-panel, ARCH-059 closure): TASK-094 added (traces ARCH-059, M, iter v20) — refresh tokens end-to-end: AS metadata advertises scopes_supported/grant_types refresh/none/iss, client scope threaded oauth_state.scope→auth_codes.scope, refresh_tokens 5th table + issueRefresh/consumeRefresh, /token authorization_code response gains always-present expires_in+scope + refresh_token iff offline_access (space-split), grant_type=refresh_token rotate branch (no PKCE), v17 DCR grant-types clamp widened to include refresh_token, gcExpired 5 tables; TASK-095 added (traces ARCH-059, S, iter v20) — /oauth/google/callback 200 HTML success page (copyable URL + one-click copy + meta/JS auto-forward + id=callback-url anchor) replacing bare 302. Prior: 2026-08-19 (v19 real-client FIX F1): TASK-093 added (traces ARCH-059, S) — echo client OAuth2 state (RFC 6749 §4.1.2) + RFC 9207 iss at the final client redirect; client_state persisted across the Google round-trip. Prior: 2026-08-18 (v17 real-connect FIX F1): TASK-091 added (traces ARCH-059, estimate M, iter v17) — RFC 7591 DCR: registration_endpoint + POST /register (loopback+metadata clamped, persisted+GC'd) + /authorize port-agnostic registered-client binding. Prior: 2026-08-17 (v15 Slice B REQ-012+086..089, merged Gate 3): every in-scope ARCH broken into ≥1 right-sized TASK — ARCH-059 → TASK-085 (M, pure/injectable auth core: oauth-metadata+token-store+google-verifier, UT-only) + TASK-086 (M, route wiring: auth-service+4 routes+resolvePrincipal, seq after 085); ARCH-060 → TASK-087 (M, edge principal threading+attribution+hermeticity-sweep-DoD, seq after 086); ARCH-063 → TASK-088 (S, isLoopbackPeer+fail-closed net-guard, own exhaustive UT); ARCH-061+062 → TASK-089 (L, MERGED — one workflows row: owner+backfill+NOT_WORKFLOW_OWNER gate AND defaults column+D-AUTH-5 validation depth+pure resolveHarnessParams). All traces valid, no ARCH un-tasked. ARCH-059 split (pure-core RED→GREEN before wiring) + ARCH-061+062 merge (same primary key/gate/migration) per converged panel. Prior v14 tasks note superseded (git history)." }
  design:       { passed: true, note: "v27m GATE 3+4 SEND-BACK REPAIR (2026-09-14, designer, the RE-REVIEW #4 send-back, BOTH halves): PASSED. BF-8 (design) - DES-206 gains the POSITIVE degrade rule ARCH-125 only ever stated as a prohibition, amended IN PLACE at iter v27m in the house `amended (...)` style: (V) the disposition is decided by classifyResponse's verdict (res.status, poll.js:51), never re-derived from the body, with the bodies[url] interim named (a positive test of the declared success shape whose key comes from that route's REQUIRED_*_KEYS row; describe has no row, so its interim is a presence test of the degrade envelope); (K)/(U) one question - does this surface already hold a successful paint of the CURRENT subject - with the sentence no row supplied and BF-7 branch (a) presupposed: paint memory is set ONLY by an `ok` route result reaching a paint function, never by render(), never by an empty-shell default; KEEP = no DOM write derived from the non-ok resource, UNAVAILABLE = clear the CHILDREN (never the anchors #dag-fit/#dag-graph/#dag-zoom/[data-legend]/.cell-layer) and paint the ONE component; (O) split where a per-route subtree exists, skip-whole where the composite is rebuilt, never substitute an invented OR remembered value; (N) no view-constructed payload - the workflow.js {cells:[],edges:[],warnings:[],lanes:[],current:null} literal, run.js:502/workflow.js:359's || [], agent-panel.js:234/:238's zeroed record named, predictedPayload(describe) exempt, and NO okBody(res, fallback) helper (IMPL-281's standing recommendation for one is withdrawn in the same edit); (S) one component, one string key `unavailable` in BOTH languages (REQ-138's 無法取樣 / Unavailable pair) - never a per-file literal, which is why system.js:29's zh-only const is disclosed rather than copied; (R) every observed status still reaches the reducer; plus section != route, DES-205 inheriting verbatim (its own v27m amendment), and a 15-row DISCLOSURE table naming every site known non-conforming with an owner and a fix shape. NO new DES id, NO new TASK id, NO trace-link change, NO ARCH edit (D3-6 stays the architect's lane), no DES-207 edit (DES-206 traces TASK-212, so the section!=route sentence is grepped there). BF-7 (impl, commit db12573) - paintSelected now carries paint memory (state.paintedRunId, written only by an ok /dag reaching the painters), bails before any DOM write on an unchanged selection, and paints the explicit t(lang,'unavailable') component on a first paint or a run-switch; strings.js gains the key in zh AND en. BOTH arms are pinned by their own real-Chromium case (commits db12573 code + KEEP cases, 8f6eb2f the UNAVAILABLE-arm case) - a mandatory branch with no case is a branch nobody has run, and .js gets no help from tsc. Falsified by Edit-reverting the guard: 3 failed - one per arm and per symptom: .run-summary [] vs the true `completed - 1 個節點 - 0 tok - $0.00`; [data-node-cell] 1 -> 0; and, on the FROM-LOAD case that pins the UNAVAILABLE arm, TimeoutError waiting for [data-legend] .empty because the pre-fix code paints an empty graph with no marker at all, restored byte-identical -> val-199 8/8 in real Chromium (152.0.7977.75, RWE_REQUIRE_BROWSER=1, not a silent itReal skip), the five dashboard acceptance files 39/39, tsc exit 0, full suite 2840 passed / 26 skipped / 0 failed, trace 1667 items / 35 gaps with the gap SET diffed identical against a pre-edit baseline. CARRIED, disclosed, NOT repaired (out of 'scope is exactly this'): home.js:231's first-paint arm, MEASURED in real Chromium by the quality-dimensions lens - 「全部 (0) - 執行中 (0) - 已註冊 (0)」 for a two-workflow deployment when /api/home is degraded from load, so RE-REVIEW #4 §8's `home.js:231 clean (BF-2)` row is true on the TICK arm only; MID, fix shape written into the disclosure table, the reviewer's to route. Plus QD-O5 (agent panel), QD3-O2 (run.js:502/:512/:513, workflow.js:359), D3-3/D3-4/D3-5, and the OWED tripwire test (measured post-fix at 3 hits, 0 comment lines) named in DES-206 tests: in the 未實作 form, never as if it exists. | PRIOR: v27l GATE 3+4 SEND-BACK REPAIR (2026-09-13, designer, the design half of the RE-REVIEW #2 send-back): PASSED - BF-1/BF-2/BF-3 each verified LANDED at file:line rather than trusted (connection.js:34-40 reports degraded at 1 / offline at >= 2 and reads prev.status nowhere; home.js:231 and run.js:475 carry workflow.js:350 guard shape ahead of any repaint or further fetch; both falsifying cases exist at val-198:262 and val-200:215 with their real-Chromium runs pasted in IMPL-278; DES-202 BF-3 amendment + the DEBT-C flip at :6817/:7274 read SETTLED). What this pass ADDS is the sweep of the row BF-3 owned - three DES-202 clauses the tree contradicts, re-stated in place at iter v27l under ONE consolidated amended line: (1) signature typed worstOf -> live|degraded|fail where the function returns a RANK key (both lenses ranked this the round top design item: an implementer writing to the old union makes connection.js:27 worst===ok permanently false and the tag can never reach live - strictly worse than what BF-1 repaired, from the row FIRST line), and its parenthetical the nav tag reads THIS was false too (the tag reads connectionState.status, app.js:115); (2) tests named an /api/home + /api/runs pair no tick co-fetches (poll.js:16, 20-23) - the case that exists is the workflow view (UT :29-34, val-199:231); (3) boundary said a degraded STRING where classifyResponse tests key presence (connection.js:48) - fail-CLOSED, so the prose moves and the code does not. 0 new DES ids, 0 new TASK rows, 0 trace-link change, 0 lines of code, trace 1663/35 unchanged. NOT WRITTEN, deliberately: the panel converged on 6 further edits plus TASK-217/218, and 04-design.md:7285 (a design clause the tree does not satisfy with no task scheduled is worse than leaving both alone) + the dispatch no-re-decomposition bound force all-or-nothing; both lenses wrote that exact zero-task fallback themselves. Seven deferred items with measured consequences are recorded in 04-design.md Decision rationale - v27l as RE-REVIEW #3 §9 input. TOP ITEM FOR THE NEXT DISPATCH (impl lane, blocking-class, measured in real Chromium): src/dashboard/ui/system.js:74 tests !res.body, so a 200 {degraded} (server.ts:1104) is truthy and :77 calls buildTable whose :49 reads data.cpu.cores - 0 rows, 3 pageerror, tag frozen live, every 3s; contradicts 04-design.md:6857 verbatim. One-token fix: if (res.status !== 'ok') - :72 already holds the classifier answer. Its test val-202:104-117 asserts a selector, not the property. | PRIOR: v27k GATE 8 RE-REVIEW #2 SEND-BACK REPAIR (2026-09-13, implementer, BF-3 — dispatched alongside the impl-owned BF-1/BF-2 rather than a separate designer pass): DES-202's boundary/tests lines re-stated to match the BF-1 reducer fix (a unanimous-fail tick reports degraded at 1, offline at >= 2, never prev.status), and the stale 'inherits ARCH-124 owner_decision: pending' conditional at :6817 plus the matching prose at :7274 (DEBT-C) both closed inline as SETTLED (owner ruled KEEP THE NARROWING, 02-architecture.md:3365 answered since 7604c90). iter v27j -> v27k, 0 new DES ids, 0 trace-link change. See gates.impl.note and 04-design.md's own edits for detail. `passed` stays true (unchanged). | PRIOR: v27j GATE 4 SEND-BACK REPAIR (2026-09-13, designer): PASSED - the design-owned half of the Gate 8 send-back. DASH-2 CLOSED: 04-design.md:3306's v22 classDiagram rendered green out of the file's own bytes under trace.py's own mermaid config (32 rows, 0 verbatim ~, 0 raw entities, 0 literal %%; the control block fails with OPEN_IN_STRUCT and writes no SVG) - three members take #123;/#125;/#44; entity codes, one own-line %% key line explains them, and the one-line Scheduler body is split (it parsed but rendered as ONE member row). Six DES rows amended in place at iter v27j, 0 new DES ids: DES-202 (the pre-AC-4 'any ok -> live' clause struck and its tests: line's inverted case flipped - it was an instruction to write the assertion that re-introduces the repaired bug; the narrowing inherits ARCH-124's still-pending owner_decision and enumerates the four-place flip set), DES-200 (stale caller :1251-1257 -> :1334; 'markup and CSS only' and the C1-pins-STAY clause struck; ARCH-122's amended Layer-1 shape; the pre-boot literal decided - one bilingual sentence in the .empty mount element, naming the asset path only, no cause list, no id, no timer, no <noscript>; UT-241's positive), DES-208 (the v27c 'draggable=false is markup and stays' sentence struck, replaced by the two-half disposition MOVED-at-v27g / RETIRES-under-TASK-215), DES-199 + DES-198 (the AC-8 cache literal is the full header directive and the tests: line now demands the exact value, never a substring; DES-198 also loses a tests: justification the v27g AC-1 repair made false - the seventh row, beyond the panel's six), DES-191 (one sentence: the guard tier gains the inverse tsc program, with its three planted-violation falsifiers and the accepted in-editor limit; the config shape is cited to ADR-049:3444, not transcribed a third time). Every clause describing the post-TASK-215 tree carries an explicit 'pending TASK-215' sentence. One file-end '## Decision rationale - v27j' records who conceded and why, plus four handoffs recorded not taken (DES-111's three stale v22 facts; the .sdlc/mermaid-render-check.mjs oracle spec for the review's §9; Gate 2's four classDiagram authoring rules + ARCH-122's two UT-240 mislabels + the oracle's rename; the floating-CDN version axis). NOT re-run: no re-decomposition, no re-partitioning, no code or test file touched. | PRIOR: v27c GATE 4 DELTA (2026-09-12, designer): PASSED — synthesized from the pre-run two-group lens panel, round 1 only (.panel/design/{adversarial,quality-dimensions}.r1.md; the r1 headlines were COMPLEMENTARY — both open with 'one stylesheet, one new task, ordered before the view acceptance' — so no round 2 was run; the *.r2.md on disk are the earlier v27b delta round). ONE new DES-209 (the declared class contract in tests/fixtures/dashboard-classes.ts, the REQ-headed section format, the swimlane SUBSTRATE, the ui/ style allowlist, <link>-only delivery, SPEC_ROWS); five amendments in place at iter v27c: DES-200 (delete the inline <style> copy + readFileSync), DES-201 (the accent ramp DIRECTION corrected — dark L ascends 100->900, light descends; dashboard.css:22 ships dark accent-100 at L .93, which is REQ-134 running-node fill = near-white on near-white), DES-203 (one geometry, two layers), DES-206 (onTick + 'may not decide anything the stylesheet declares'), DES-208 (the two CSS pins exercise the STAYS option this row already granted). Rulings where the lenses split are in 04-design.md 'Decision rationale — v27c'. ONE owner_decision pending on DES-209 (vendor the handoff README, or scope 99% to the enumerated REQ constants). TWO gates this delta implies but the announced invocation does not run: 02-architecture.md is NOT touched (the one ARCH-125 api: amendment is stated in DES-209 boundary, the house style DES-201/DES-206 already use) and 05-tests.md is NOT touched (no tests gate) — TASK-214 therefore carries the /sdlc-fix F-pattern in its dod: (write the class lock, the no-design-values guard and SPEC_ROWS RED first, then green) and the verifier records them as UT items at the next gate that owns 05-tests.md. NOTE ON current_stage: the generic Gate 4 contract says '-> tests', but this invocation declares gates:[design,impl,verify,validation,review], so current_stage is set to impl (the next gate that actually runs) rather than to a stage the orchestrator did not schedule. | PRIOR: v27b GATE 4 DELTA (2026-09-11, designer): PASSED — synthesized from the two-group lens panel rounds 1 AND 2 (.panel/design/*.r{1,2}.md; the r2 files CROSSED, so seven items were still open and are ruled in the new file-end ## Decision rationale — v27b): pinned=<view.scriptVersion> not requested=; predicted-cell label = labels.join(' / '); PARITY_EXCLUDED=['runId','terminalAt'] with current COMPARED (the adopted stabilization predicate makes it deterministic); one export warningText(lang,raw) in lib/strings.js; server.ts:350 comment amended not deleted; one const for the UNAVAILABLE token; SCRIPT_PHASED as a const in the existing test file. Zero new DES ids, zero new modules/routes/config keys, zero owner_decision deferrals (the owner already ruled at Round v27b). DES-196/197/198/201/206 amended in place at iter v27b, DES-114/115 marked superseded_in_part (iter stays v22), v27 rationale point 11 marked WITHDRAWN, real-tier rows for REQ-133/134/140 re-worded to name the auth-enabled engine. sh .sdlc/trace: 67 gaps = the unchanged baseline, no broken links, no pending owner_decision anywhere in the ledger. | PRIOR: v27 GATE 4 (2026-09-11, designer): PASSED — DES-191..208 in 04-design.md, LEAN (signature/boundary/tests per row; all reasoning in ONE file-end Decision rationale — v27 with who conceded), plus the per-REQ real-tier validation path table, the per-tier mock policy and the seam-consistency statement (no Date.now() under src/dashboard/lib, grep-enforced; listSummaries reads no clock; both stores implement the new read+write). Synthesised from the pre-run lens panel (.panel/design/{adversarial,quality-dimensions}.r1.md — round 1 only, stances complementary). Two ARCH rows AMENDED IN PLACE at iter v27 because their api: line was unimplementable/self-defeating and implementers grep api: lines: ARCH-124 (accentVars -> CSS-native oklch(L C var(--rwe-hue)); a classic pre-paint script cannot ESM-import) and ARCH-125 (setInterval -> self-rescheduling setTimeout after settle). INV-V27-1 NOT amended — its test gains a second (absence) clause instead. 0 owner_decisions raised by this gate; ADR-051 remains the architect's single pending marker and both of its call sites take one !!authCfg boolean. Trace 1544/67 -> 1562/67 with the gap SET byte-identical (0 new, 0 broken links, 0 orphans, 0 drift). | PRIOR(v26): v26 GATE 4 (2026-09-08, designer): PASSED — DES-170..190 in 04-design.md, LEAN (signature/boundary/tests per row, all reasoning in one file-end Decision rationale with who conceded), plus a classDiagram, the per-tier mock policy, a seam-consistency statement (every v26 time read goes through the injected Clock) and a REQ-121..130 real-tier validation table. Synthesized from the pre-run panel (.panel/design adversarial + quality-dimensions, r1+r2 both groups), not re-debated; rule applied: an r2 arbitration beats either r1, because the adversarial r1 on disk was REWRITTEN after its own r2 and regressed on two conceded items (the phantom buildRecordsFromTranscript -> the real deriveAgentRecords, and a top-level catalogFetchedAt -> per-row, verified: models_list returns a bare array at call-tool.ts:255). The design states the owner-corrected money rule ONCE (costUSD:number + unpriced:boolean, no PRICE_UNKNOWN anywhere, pin written in RunManager.start() so trigger runs are tracked, addUsage and the resume fold unconditional, budget:null still accepted) and every money DES descends from it, NOT from ARCH-118 api. ADR-047(b) ledger amendment EXECUTED here (DES-190/TASK-170): the Schedule union comment, DES-017 KP-9/overlap risk and D-V2h now say trigger runs are unbounded and the compensating control is spend recording, not a cap. HOUSEKEEPING FOR THE ARCHITECT (not edited by this gate; 02-architecture.md is the architect s file): 8 stale spots still specify the struck PRICE_UNKNOWN design plus ARCH-114 buildRecordsFromTranscript, ARCH-116 top-level catalogFetchedAt, ARCH-114 nested-lane cohort + REQ-124 shared-timeline clause (nested phase() is now a sub-card fact) — listed in 04-design.md. 0 owner_decision markers minted (the two v26 ones are DECIDED at eaf6546). PRIOR: v26 NOT YET RUN (Gate 1 closed 2026-09-08; this flag reset so a resume does not skip it). PRIOR: v24 GATE 4 (2026-09-04, designer): PASSED — DES-137..162 in 04-design.md, LEAN (signature/boundary/tests per item, all reasoning in one file-end Decision rationale), plus a classDiagram, a REQ-107..118 real-tier validation table and the per-tier mock policy (E2E/acceptance mock no SUT boundary; an unreachable third party is an UNVERIFIED row, never a mock). Contested calls settled with who conceded: mode(args) authz resolver ENDORSED by both (+ detail.mode); LEGACY_REREGISTER for pre-v24 param rows (QD conceded, condition met by DES-156 runnableReason); run_result added to the audited set (extends ADR-027 in the safe direction); outputSchema adopted as a test oracle with publishing deferred; AgentRecord gains label ONLY (provenance stays transcript-only — the smaller wins); adminReads owner-only (adversarial held, QD conceded); guide byte ceiling withdrawn, replaced by one tools/list byte regression guard. Clock seam enumerated (markRefused/markFired in both trigger stores, AssetSync pushedAt, facade audit ts) with the no-Date.now grep guard as the lock. 12 architecture sentences needing amendment are RECORDED BY ID in the rationale (02-architecture.md deliberately not edited by this gate). PRIOR: 2026-09-02 (v23 Gate 4 RE-RUN, synthesized from the post-adjudication .panel/design adversarial.r1/r2 + quality-dimensions.r2 — NO new panel spawned): DES-136 added; DES-122/123/124/125/127/129/130/131/132/134/135 amended in place (still draft, same iteration — no [AMENDED] prose blocks). Blocking defect the re-dispatch existed to catch, fixed: adjudication #1 left the shipped design self-contradictory — EXPECTED_DESCRIBE_KEYS, generated from a type with no phases, would reject phases as a leaked field while the diagram string inside the same response rendered the titles. DES-136 + DES-125 close it; 'phases' is ONE oracle key (the test's deepFlatten does not recurse into arrays, workflow-view.test.ts:29 — verified, not taken from the panel's guess). Other decisions in the file-end 'Decision rationale — v23 Gate 3+4 re-run': the two r2 files crossed AGAIN and the owner ruling overtook both (QD r2 at 19:41 still seconds an escalation ba3db17 resolved at 19:21) — recorded as overtaken, not conceded; QD's only hard condition (the MALFORMED_COMPLETION fold-in ships with gateFail, or the member returns) is satisfied as shipped, so the enum stays at TEN and no lens-vs-lens conflict remains; adversarial's request to amend 02-architecture invariant 5 DECLINED on scope (not merit) — the assent is recorded in the rationale and inside DES-129 where an implementer greps, and invariant 4's 'provider HTTP status' clause is struck because GatewayResult has no such field; three caps lifted into config as a REQ-104 compliance fix; two uncovered boundary states adopted in cheapest correct form — B6 late write (existence guard inside putDiagramResult's transaction, NOT a reaper or a cancellation check; the single-deletion-path rule is exactly what made the orphan immortal) and B7 boot-sweep attempt marker (stamp the already-NULLable generated_at, no new column, FixedClock-testable) with the clause it owes (diagramGeneratedAt only when ready); /describe is UNAUTHENTICATED and pinned to the non-owner projection; two stale justifications corrected (the catalog alias set is no longer 'deliberately empty' since v21 P-A2; the log census is 6 of 17, not 7 of 7); the whole-label gate reading withdrawn by its own author so the r1 recombination fixture is dropped rather than written; the allowlist's model sentinel is the ASCII token model:param because the panel's bracketed form fails the gate's own codepoint pass. REQ-100's amended surface gains a real-tier row. Trace 947 items / 42 gaps = 41 pre-write baseline + 1 new TASK-125-has-no-IMPL, 0 broken links, 0 orphans. | 2026-09-02 (v23 Gate 4, synthesized from the pre-run .panel/design adversarial+quality r1+r2 — NO second panel spawned): DES-120..135 added, panel numbering preserved where content aligns so Gate 8 can re-join the .panel files. Decisions in the file-end Decision rationale: (1) the two r2 files CROSSED (each answered the other's r1) — DiagramNoteCode is TEN not eleven, MALFORMED_COMPLETION deleted per adversarial's r2 self-reversal (gateDiagram takes raw:unknown, so the gate is total over the gateway's actual return type), and QD's diagnostic need is served by the closed journal-only gateFail:'type'|'codepoint'|'size'|'token'; (2) gateFail is an EXPLICIT amendment to ARCH-079 invariant 5's pinned field list, recorded here, 02-architecture.md not edited; (3) journal token fields keep ARCH's promptTokens/completionTokens, mapped one line at the log site from GatewayResult.tokens.{input,output}; (4) the scratch-cwd fight both r1 files predicted DISSOLVED — adversarial withdrew its tie-break on evidence (it had argued against widening invoke(), which QD never proposed), so isolation-by-omission stays primary and main.ts's cwd repoint is the belt, plus a NEW fail-closed rule (zero-config has no resolvable jail root ⇒ tools forced to []); (5) four primary-source corrections to 02-architecture.md are carried INSIDE the DES an implementer greps, not only in the rationale — phantom prune→DES-130, falsified ADR-020→DES-120, false 'phase names already served to non-owners'→DES-135, 'scratch cwd' not expressible through invoke()→DES-122; (6) TASK-116/117 split is a deliberate refinement of the panel's one-task item 5 (fix lands BEFORE the analyzer, analyzer-level built-options assertion retained in 117's DoD); (7) grep guard lands in the deletion task written-first-and-red rather than as a separately-landed red task. Adopted from concessions: no boot backfill + a boot line naming the count and recovery command; MODEL_UNMAPPED with isKnownAlias against the SERVER's alias set (the catalog's is deliberately empty and a check wired to it would be inert); viewerIsOwner dropped. Real-tier path named per REQ-101..106 + the per-tier mock policy + the anti-vacuous-oracle rule (no v23 test computes its expected value with the function under test; no UT may claim to prove REQ-104). ESCALATED non-blocking: the diagram is the FIRST surface to serve meta.phases[].title to a non-owner (ADR-015's 'pre-existing' premise is verified false) — A3 authorises it, default pinned by a secret-in-a-phase-name test so overruling turns a test red. Trace 945 items / 41 gaps = 29 baseline + 12 new TASK-has-no-IMPL (normal pre-implementation), 0 broken links, 0 orphans. | 2026-09-01 (v22 Gate 4, synthesized from the pre-run .panel/design adversarial+quality r1+r2): DES-109..119 added. Four contested calls decided in the file-end Decision rationale: D1 non-owner validation is {ok} not {ok,errors[]} (adversarial N-2: workflow_status is not principal-gated, so errors[] there side-steps REQ-100), D2 NO per-version deregister in v22 (the two r2 files crossed positions; adversarial withdrew its own proposal because it makes two conflated states representable and falsifies the legacy-cohort premise), D3 the scheduler 2 Hz hot loop IS fixed (reopens a recorded ARCH decline whose stated rationale is falsified by primary source), D4 legacy named-run pins take the recorded fallback not strict refusal. EXPECTED_NON_OWNER_KEYS amended deliberately here, not discovered red at Gate 5. | 2026-08-31 (v21 Gate 4, panel-synthesized from .panel/design adversarial+quality r1+r2): DES-101..108 added. Two undecided panel items decided by the synthesizer on primary source: (1) params are RUN-SCOPED across nested workflow() frames — run-manager.ts:652-700 shares runId and wires the nested sandbox onAgentRequest to the PARENT run handler, so containment would be new machinery for a pre-v21 hole (reverses adversarial r1 I-2 and quality r2s endorsement of it); (2) a script-supplied bad param RECORDS then throws, via the existing _sink.capture failure path inside AgentExecutor.run — parallel() swallows every exception into null (guards.ts:143-148), so a bare throw is the exact silent failure this iteration closes. Taken as a pair: B-3 effective bounds at read time ADOPTED, therefore PARAM_OUT_OF_RANGE.source WITHDRAWN. F-2 accept-and-pin (refusal at admission would be a 1-bit secret-value oracle). DES-108 pre-commits REQ-093 real-tier evidence (Ollama no-op branch + injected-seam mapped-value) so Gate 7.5 does not stall a round. Trace 794 items / 30 gaps = 21 baseline + 9 new TASK-has-no-IMPL (normal pre-implementation), 0 broken links, 0 orphans. | 2026-08-19 (v20 refresh-tokens+callback-page FIX F1, self-decided no-panel, ARCH-059 closure — REVERSES the v15..v19 'reject scopes/refresh' stance, same reversal shape as v17 DCR): DES-092 iter→v20 (buildAuthServerMetadata gains scopes_supported[openid,email,offline_access], grant_types_supported[authorization_code,refresh_token], token_endpoint_auth_methods_supported[none], authorization_response_iss_parameter_supported:true — Claude Code auto-appends offline_access only when advertised; issue #26281 = missing expires_in makes it discard the token). DES-093 iter→v20 (refresh_tokens 5th table token_hash PK+principal+scope+nullable client_id+expires_at; nullable scope TEXT added to BOTH oauth_state+auth_codes via idempotent ALTER…catch{} migrations; putState/mintAuthCode carry scope, consumeState/consumeAuthCode return it; issueRefresh(csprng+clock seams, sha256-at-rest, sliding REFRESH_TTL_MS ~90d)+consumeRefresh(single-use atomic DELETE-RETURNING); gcExpired sweeps all 5 tables; fully seam-consistent — scope client-supplied, refresh reuses issue's seams). DES-095 iter→v20 (authorize captures client scope SEPARATE from hard-coded Google-leg openid email, never forwarded to Google; tokenExchange authorization_code response={access_token,token_type,expires_in ALWAYS,scope ALWAYS echoed(\"\" when none),refresh_token? iff offline_access via space-split membership not substring}; new grant_type=refresh_token branch no-PKCE rotate → new bearer+rotated refresh, client_id binding enforce-if-stored, 400 invalid_grant on unknown/expired/consumed/mismatch; v17 register grant-types clamp WIDENED to [authorization_code,refresh_token] — the load-bearing 4th edit site, else a registered client never attempts refresh; handleGoogleCallback 200 text/html success page(copyable raw URL+one-click copy+meta http-equiv refresh+JS location.replace+id=callback-url anchor, attribute &→&amp; escape) replacing bare 302, catch/relative branch untouched). Decisions A(scope rides oauth_state→auth_codes both, unlike v19 client_state) B(widened DCR clamp is load-bearing) C(refresh client_id enforce-if-stored skip-if-null) D(single-use rotation, NO RFC9700 family-revocation — Karpathy) E(sliding ~90d via exported REFRESH_TTL_MS, no config knob) F(id=callback-url stable anchor protects IT, sanctioned 302→200 fallout). Closes v19's out-of-closure exclusion (iss metadata flag now advertised). No new module/route/seam/config-schema; auth-disabled ⇒ byte-for-byte pre-v15. Real-tier: real @modelcontextprotocol/sdk client stays connected across access-token expiry with no browser re-auth (VAL-095 @7.5); IT-078 v20 real server+SQLite, Google doubled via injected jwksFetch. trace 756/17 (0 broken-link, 0 orphan; +5 low iter-drift = IT-078/VAL-095 v19-behind DES-092/093/095 v20 — expected TDD wavefront, verifier/impl catch up). REQ-012 already v20. Verifier owns IT-078/VAL-095/UT-092..095, implementer owns IMPL-122. gates.architecture stays true. current_stage design→tests. | Prior: 2026-08-19 (v19 real-client FIX F1): client OAuth2 state round-trip — DES-093 (oauth_state.client_state nullable col + putState/consumeState carry clientState) + DES-095 (authorize captures client state, googleCallback echoes state+RFC9207 iss at final client redirect, iss=raw effectiveIssuer per oauth-metadata:38); no new clock/CSPRNG seam (client-supplied). self-decided F1, no panel. | 2026-08-18 (v18 real-consent FIX F1, self-decided no-panel, ARCH-059 closure): Google's 3 OAuth endpoints on 3 distinct hosts (was one conflated googleBase → real consent OK but /oauth/google/callback 502'd because accounts.google.com/token + /oauth2/v3/certs do not exist; token lives at oauth2.googleapis.com/token, JWKS at www.googleapis.com/oauth2/v3/certs; invisible because the fake IdP served all 3 off one base). ARCH-059 iter→v18 (three separately-injectable URL fields, each defaulting to an exported production constant, static UT regression guard pins the 2 previously-wrong defaults — the only tier that catches this fake-double-collapses-hosts class; verifier drops misnamed googleBase for a full jwksUri). DES-094 iter→v18 (VerifyIdTokenDeps googleBase→jwksUri + JwksPort arg googleBase→jwksUri + call site deps.jwksFetch(deps.jwksUri); RENAME-ONLY zero behavior change; UT-094 fixture rename is sanctioned mechanical fallout). DES-095 iter→v18 (export GOOGLE_AUTHORIZE_URL='https://accounts.google.com/o/oauth2/v2/auth'/GOOGLE_TOKEN_URL='https://oauth2.googleapis.com/token'/GOOGLE_JWKS_URL='https://www.googleapis.com/oauth2/v3/certs'; AuthConfig drops googleBase?, gains googleAuthorizeUrl?/googleTokenUrl?/googleJwksUrl? test-injectable defaulting via ??; createAuthRouteHandlers uses the 3 resolved URLs, default jwksFetch fetches the full jwksUrl, verifyIdToken deps pass jwksUri:jwksUrl; server.ts:149 stale comment updated. Per-tier policy Exit-Gate-4: unit=import+assert the exact prod token/JWKS constants (ONLY tier catching the conflation); integration IT-078 v18=fake Google via injected googleAuthorizeUrl+googleTokenUrl (distinct hosts)+jwksFetch as today; acceptance/E2E VAL-095=REAL Google consent completes callback→302 loopback+engine auth-code, the 502 repro now green. Sanctioned compile-break fallout: removing AuthConfig.googleBase breaks VAL-095 fake-Google setup + UT-094 fixtures — mechanical renames owned by Gate 5/7.5). TASK-092 added (traces ARCH-059, estimate S, iter v18). trace 753/12 (0 broken-link, 0 orphan; +3 low iter-drift = TASK-092 未實作 + DES-094/095 v18 ahead of tests — expected TDD wavefront, matches v16/v17). REQ-012 already v18. Verifier owns IT-078/VAL-095/UT-094, implementer owns IMPL-122. gates.architecture stays true. current_stage design→tests. Prior: 2026-08-18 (v17 real-connect FIX F1, self-decided no-panel, ARCH-059 closure): RFC 7591 Dynamic Client Registration — closes live Claude-Code connect failure 'Incompatible auth server: does not support dynamic client registration'. REVERSES v15 ARCH-059 'reject DCR' stance (wrong for a spec-only MCP client with no pre-registered client_id). ARCH-059 iter→v17 (DCR implemented: registration_endpoint advertised, public POST /register, registered_clients table, /authorize registered-client redirect binding; scopes/refresh still rejected via clamp-not-error). DES-092 iter→v17 (buildAuthServerMetadata adds registration_endpoint). DES-093 iter→v17 (registered_clients 4th table + registerClient/getClient, client_id from csprng seam, issued-at from clock seam in seconds; gcExpired extended 3→4 tables; weeks-scale expires_at bounds the unauth tunnel-reachable write path). DES-095 iter→v17 (register(req,res) handler: JSON body, every redirect_uri loopback-clamped else 400 invalid_redirect_uri, bad JSON→invalid_client_metadata, clamp grant/response/auth-method, 201 public client no secret; /authorize registered-client binding scheme+host+PATH port-AGNOSTIC per RFC 8252 §7.3 — cached client_id + fresh ephemeral port must pass, exact-match would re-break live connect; absent/unregistered client_id → existing loopback-only path, IT-078 9a-9c stay 302). TASK-091 added (traces ARCH-059, estimate M, iter v17). Decisions A(port-agnostic binding) B(clamp-not-reject requested metadata) C(bound new table, binding is a reader not a security boundary — unregistered path open by design, security rests on loopback+PKCE+single-use state). Real-tier: real @modelcontextprotocol/sdk auth register→authorize→token vs live engine (VAL-095 @7.5). trace 752/13 (0 broken-link, 0 orphan; +4 low iter-drift = UT-092/093/095+IT-078+VAL-095 awaiting verifier/impl catch-up — expected TDD wavefront). REQ-012 already v17. Verifier owns IT-078/VAL-095 extensions + UT bumps, implementer owns IMPL-122+metadata/token-store/server edits. gates.architecture stays true. current_stage design→tests. Prior: 2026-08-18 (v16 Gate-8 FIX F1, self-decided no-panel, ARCH-059 closure): HIGH-1 open-redirect + MED-2 unbounded auth tables. ARCH-059 iter→v16 (invariant 4 upgraded descriptive→ENFORCED: /authorize validates redirect_uri RFC-8252 loopback-only BEFORE putState/Google-redirect, non-loopback→400 invalid_request no state row; gcExpired now explicitly invoked from REQ-026 maintenance sweep, interval created when workspaceTtlMs>0 OR auth-enabled). DES-095 iter→v16 (new PURE exported isLoopbackRedirectUri(uri): http:+{127.0.0.1,localhost,[::1]} any port, parse-in-try/catch→false, [::1] named-UT for WHATWG bracket serialization; https/127.8 NOT generalized; relative-URI callback branch flagged-unreachable not-removed; gcExpired try/catch→log+continue wired at server.ts sweep, condition widened to fully close MED-2). DES-093 iter→v16 (gcExpired contract pinned: delete expires_at<=clock() across all 3 tables, return count, retain live, idempotent). TASK-090 added (traces ARCH-059, estimate S, iter v16, both fixes). trace 751/10 (0 new gaps, was 750/11 pre-existing — TASK-090 closed one). REQ-012 already v16 (2 acceptance clauses). Verifier owns IT-078/079 extensions, implementer owns IMPL-122. gates.architecture stays true. current_stage design→tests. Prior: 2026-08-17 (v15 Slice B REQ-012+086..089, Gate 3+4 merged, SYNTHESIZED from the CONVERGED 2-group panel .panel/design/adversarial.r1+r2(opus interface-contract/boundary-error/testability) + quality-dimensions.r1+r2(sonnet observability/replaceability/consumability/self-sustainability) — both r2 confirm convergence, all 4 predicted disagreements dissolved): DES-092..100 for TASK-085..089. 092 oauth-metadata PURE builders (S256, wwwAuthenticate spec-check at wiring); 093 token-store opaque sha256-at-rest bearer + single-use ≤60s atomic auth-code/state, CONSTRUCTOR-injected clock+csprng (Exit-Gate-5: every time/random read through seam, no Date.now/randomBytes), gcExpired try/catch; 094 google-verifier iss/aud/exp(injected now)/JWKS-sig/nonce + email_verified===true BEFORE email, injected JWKS+base zero-network UT; 095 auth-service startAuthorize/handleGoogleCallback/tokenExchange + resolvePrincipal DISCRIMINATED UNION (no throw) + 4 routes + uniform 401 wire + internal DEBUG auth.resolve log; 096 principal resolve-once-at-edge before side-effect + append nullable principal to workflow_run/register/deregister+asset handlers (get/list/status take NONE — reads open by absence) + control-plane attribution + NEVER into sandbox [D-AUTH-4] + paired presence+absence hermeticity IT as DoD; 097 isLoopbackPeer pure (::ffff:127 exempt, forwarded-header⇒never-exempt, raw socket only) + fail-closed guard; 098 owner column + mutation-only gate + idempotent WHERE-owner-IS-NULL boot backfill + boot log; 099 shared HarnessDefaults type (3 consumers) + D-AUTH-5 validation depth (model-alias+tool static, skill deferred) + fail-closed no-partial-write + pure resolveHarnessParams per-param merge; 100 real-tier path per REQ (real MCP client discovery, Google doubled via injected googleBase; REQ-089 0.0.0.0+LAN-IP 401/200 no 2nd host) + per-tier mock policy (E2E no SUT-boundary mock) + I-3 hung-provider slot-free→Gate-7.5 REQ-083 + I-4 Cache-Control:no-store note. Decisions: A2⟂A4 reconciled (workflow_run takes principal for attribution, reads don't); C-1 append-param over CallContext; C-2 uniform 401+internal log; RD-1 slot-free→validation not DES; RD-2 ARCH-061+062 merged w/ named assertions; RD-3 no-store yes/X-Frame-Options declined. Code-anchored (callTool server.ts:730/arg-cast:747, net-guard.ts isLoopback, workflow-catalog.ts:47 register). QM → no safety/cyber lenses. v15 classDiagram added. gates.tasks/design.passed=true; current_stage design→tests. Prior v14 design note superseded (git history)." }
  tests:        { passed: true,    note: "v27d GATE 5 RE-RUN (2026-09-13, verifier) — PASSED, zero new RED: dispatch's impact closure (REQ-131..136/140/141) was already fully implemented at HEAD 1a1746a; two ledger-lane corrections made (UT-244 stale header noted not flipped; dashboard-spec.ts run-chip SPEC_ROW narrowed to .is-selected, re-measured green) — full detail in current_stage's own v27d note and 05-tests.md. | PRIOR: v27 GATE 5 DEFECT-QUEUE REPAIR (2026-09-12, verifier) — PASSED: closed the two still-open items of the v27 GATE 5 DEFECT QUEUE (pending: block) — (1) UT-249's clientCorpus() anti-vacuity case had gone stale once Gate 6 built the real client tree (src/dashboard/ is no longer empty); clientCorpus() gained an optional root param (default unchanged, its 4 other callers untouched) and the case now injects a real disposable empty temp dir instead of relying on the real tree staying unbuilt. (6) static-assets-route.test.ts's traversal case used fetch(), which collapses ../ dot-segments CLIENT-SIDE via the WHATWG URL parser before the request line is built, so ui/../lib/theme.js silently resolved to the already-legitimate lib/theme.js before it ever reached the server — not a real vulnerability (src/static-assets.ts's lookupStaticAsset is a bare Map.get over a fixed allowlist, re-verified by reading it) but a defective test transport. Rewritten to send each TRAVERSAL_TABLE string over node:http's raw path option; verified empirically that this puts the exact byte string on the wire unnormalized (a throwaway echo server), and that a deliberately-vulnerable path.join/normalize variant IS caught by the same probe (non-vacuity check). Items (2)/(3)/(4)/(5) were already resolved at cb895d0/e8232ea — re-confirmed here by direct measurement (dashboard-no-external-host.test.ts 2/2, dashboard-no-design-values.test.ts 7/7, run-store-parity.test.ts 2/2 green), not re-worked. val-193 case 3's C2 re-point (orchestrator authorization) was independently verified already correctly applied at ddc4409 — #dag-graph anchor still asserted, .cell-usage is the new text-descendant selector, VAL id unchanged — no action needed. status/result on UT-249/IT-170 left as their original Gate-5 red/fail measurement (the bookkeeping flip is Gate 6.5+7's job, per the IMPL-240/241 precedent on UT-233/235/236); 05-tests.md gained a dated Amended note on each instead. sh .sdlc/trace --check: 1617 items / 41 gaps, byte-identical gap SET to the pre-edit baseline (verified by diffing the dashboard's own embedded gap array, not just the count — 0 new, 0 broken links, 0 orphans). Full regression: npx vitest run → 399/402 files passed, 2798/2830 tests passed; the 2 failing files (val-018-dashboard-browser-ui.test.ts's viewBox assertion, val-199-workflow-detail.test.ts's SPEC_ROWS history-table/accent-token/mono-font-family rows) are pre-existing, confirmed independent of this pass's files by dependency inspection and an isolated re-run — NOT part of the routed defect queue. Flagged, not fixed: val-199's `.mono font-family` row compares a single-quoted CSS literal against getComputedStyle(), which browsers ALWAYS return double-quoted regardless of source quote style — this reads as a 7th SPEC_ROWS oracle bug of the same class the v27c Gate 5 delta already fixed six of, but is outside this pass's routed scope; the width/accent-token rows on the same test read as genuine unimplemented CSS. npx tsc --noEmit clean. Next: Gate 6 (implementer) continues the Sprint A closure (TASK-208/209/211/212 remaining per the v27c journal note), and should look at the val-018/val-199 findings above. PRIOR: v27c GATE 5 DELTA (2026-09-12, verifier, test-first RED) — PASSED: recorded TASK-214/DES-209's deferred F-pattern test half (no Gate 5 of its own in the v27c design invocation) — UT-255 (dashboard-class-contract.test.ts, green 13/13, TASK-214's own commit 09c6089) + UT-256 (dashboard-no-design-values.test.ts, red 3/7, the SLICE's final green per ordering rule 4, blocked on TASK-208/209/210/211/212) — 2 new UT ids. Wired DES-209's promised SPEC_ROWS real-tier oracle (tests/helpers/spec-rows.ts) into VAL-206/207/208/209 (one new case each, amended in place, iter v27c, status stays red): probe-with-same-property + pseudo-element (::before) + self-referential-root-token support in the helper, and 6 dashboard-spec.ts rows fixed where the ORACLE was wrong (a repeat()/minmax() literal getComputedStyle can never echo, an em literal it always resolves to px, a --rwe-hue default row a hue-swap pass always fails, three anchors narrower than DES-209's own table assigns). Remaining reds are unlanded TASK-208/209 emitters (data-section/ data-run-chip/data-history-table/.mono) plus two flagged run-scenario gaps (val-200 state cells, val-201 .detail-block) — Gate 6/7 work, not re-engineered here. sh .sdlc/trace: 1601/50, byte-identical gap set to the 1599/50 pre-edit baseline (0 broken links, 0 orphans, +2 items only, 0 new gaps). PRIOR: v27b GATE 5 DELTA (2026-09-12, verifier, test-first RED) — PASSED: the TESTS half of the scoped delta (impactIds REQ-133/134/140, closing the same round v27b Gate 2/3/4 delta) folded ADR-051's owner-ruled reversal into 7 EXISTING items, no new UT/IT/VAL id, no new ledger namespace. UT-238 (dashboard-derive-lanes.test.ts + layout-graph-phase.test.ts) REWRITTEN: masked axis deleted (28->14+4+1=19 cases), the :34 cast gone, lanes unconditional, dense/non-clamped/derivation-failure join rows, a robustness @ts-expect-error row, plus the layoutGraph inert-cell label case (1 red, 1 Mode-C green). UT-244 (dashboard-lib-strings.test.js) gains warningText(lang,raw) (6 cases) off a new DAG_WARNING_EXAMPLES fixture export, and its own header ID typo (UT-243->244) fixed in the same edit. IT-168 (dag-masking-auth.test.ts) FLIPPED from masked-negative to unconditional-positive: dag.lanes+__skel_ under auth, describe.phases[].agents on both servers, a phases-scoped describe-parity case, and the INV-V27-9 stabilized-parity sub-test (SCRIPT_PHASED const, a named stabilization predicate with a named stabilization-deadline error with both servers polled CONCURRENTLY via Promise.all (not sequentially) under a 30s test timeout above the 15s per-poll deadline, live key-set check, PARITY_EXCLUDED=[runId,terminalAt] with current COMPARED, positive __skel_/lane-title anchors on the auth server). Mid-gate test-first finding, fixed in the same pass (not an owner_decision): describe.phases is the author-declared meta.phases (workflow-meta.ts parseMeta), a THIRD phases concept separate from the script's own phase() call structure and from RunStatusView.phases; registerPublishedVia's synthesizeMeta never emits phases, so SCRIPT/SCRIPT_PHASED now declare their own export const meta = {phases,params.agents} via a metaBlock() test helper, or cases 2/3/4 would stay red forever for a fixture reason, not the intended one — case 4 (describe-parity) now measures Mode-C green. IT-169 (dashboard-http.test.ts) extends the EXISTING deregister case with the UNAVAILABLE warning + a scoped console.warn spy (filtered to event:'dashboard_api_degraded'), and adds the register-x2/deregister/register-x1 FALLBACK recipe (pinned=v2 resolved=v1, no log line). IT-092 (same file) RE-TRACED to REQ-100 alone (ARCH-073/075/ADR-012/DES-114/115 kept as lineage prose only, all already superseded_in_part elsewhere and still cited by other items — 0 new orphans measured) and REWRITTEN as a Mode-C guard: the ['__trigger__']-only assertion this v22 test used to make is DELETED (it pinned the exact mask ADR-051 reverses); the surviving invariant is a planted comment sentinel (RWE-IT092-SCRIPT-BYTES-DO-NOT-LEAK) asserted absent from the raw DAG response text on both servers — confirmed GREEN now and structurally unaffected by the reversal (REQ-100's script-text masking stands). VAL-207 gains a THIRD case: a second auth-ENABLED server (mintBearer pattern), a distinctive agent-label marker, and an anonymous page GET asserting the label renders VISIBLY — the Gate 7.5 instruction this proves at acceptance tier FLIPPED from 'record what degrades' to 'PROVE the overlay IS visible' (ADR-051). VAL-212's own text amended to state the p95 measurement (GET .../dag, auth on, 200 sequential GETs) is a Gate-7.5-only VAL-side number, same non-test-first pattern as VAL-205's bench script — not built here. Every touched item run this gate and confirmed red for the stated reason except IT-092 (Mode C, green throughout) and the one Mode-C layoutGraph empty-labels case. sh .sdlc/trace --check: 1591 items / 67 gaps, BYTE-IDENTICAL to the pre-delta baseline (0 broken links, 0 orphans, the 21 drift rows and other 66 gaps unrelated to any touched ID); REQ-133/134/140 read 未真實驗證 (test exists, not yet real — the intended Gate 5 signal). Full regression on touched files: dashboard-derive-lanes.test.ts 19/19 red (whole-function TypeError); layout-graph-phase.test.ts 26 total (25 green incl. 24 pre-existing unchanged, 1 red); dashboard-lib-strings.test.js whole-file red (unchanged reason, now covering warningText too); dag-masking-auth.test.ts 8 total (4 green: 3 IT-092 + 1 IT-168 describe-parity case now Mode-C green post-fixture-fix, 4 red IT-168); dashboard-http.test.ts 13 total (8 green pre-existing unchanged, 5 red incl. the 2 v27b additions); val-199-workflow-detail.test.ts 3/3 red (real Chromium, Chrome present). tsc --noEmit: 41 project-wide errors, all pre-existing-class (missing v27 exports) or self-correcting (@ts-expect-error TS2578) — 0 new error CLASSES from this delta. No owner_decision opened; ADR-051's is already answered. Next: Gate 6 (implementer) turns TASK-196..213 green over the FULL Sprint A closure (REQ-131..136/140/141), this delta's red items included. PRIOR: v27 (2026-09-11, verifier, test-first RED): PASSED — 34 new work items (UT-230..249/252..254, IT-165..170, VAL-206..212/205) written test-first across every in-closure DES-191..208/REQ-131..136/140/141; every key DES has a UT/IT and every REQ has a VAL; each run once and confirmed red for the stated reason (a minority Mode-C green-by-construction, disclosed per item in 05-tests.md, not forced red). Test infra: vitest.config.ts include widened to `.test.{ts,js}`, tests/helpers/{require-browser,client-corpus}.ts, tests/fixtures/dashboard-wire.ts. Known compile-time consequence (disclosed, DES-192's own framing): `npx tsc --noEmit` is RED repo-wide on the not-yet-existing `AgentLogView` export until Gate 6 — vitest itself unaffected. Scope reduction disclosed: DES-208's full ~41-assertion disposition sweep is NOT completed here, only its helper + anti-vacuity test + one anchor per file — flagged as Gate-6-adjacent follow-up, not silently dropped. Filename correction: val-197-diagram-pan.test.ts -> the real val-197-diagram-drag-pan.test.ts. sh .sdlc/trace: 1562/67 -> 1591/67, gap COUNT unchanged, REQ-131..136/140/141 flipped 未實作+未驗證 -> 未實作+未真實驗證 (the intended signal); 0 new broken links/orphans. Next: Gate 6 (implementer). See 05-tests.md's \"v27 — REQ-131..136/140/141...\" section for the full item list. | PRIOR(v26): v26 GATE 5 (2026-09-08, verifier): PASSED — 59 new work items (33 UT + 15 IT + 1 E2E + 10 VAL) written test-first across DES-170..189/REQ-121..130; every DES has a UT, every REQ has a VAL; all run once and confirmed red for the stated reason (or held green as a Mode-C invariant); trace 1413 items/65 gaps = the unchanged Gate-3+4 baseline, 0 broken links, 0 orphans. See 05-tests.md \"v26 — REQ-121..130...\" section for the full item list and Gate-5-specific obligations (real pre-v26 snapshot fixture, installed-SDK-version-pinned error taxonomy, LiteLLM-version-pinned REASONING_BUDGET note). Next: Gate 6 (implementer). PRIOR: v26 NOT YET RUN (Gate 1 closed 2026-09-08; flag reset). PRIOR: 2026-09-04 v24 GATE 5 (verifier, test-first RED, Mode A) — wrote 52 new work items test-first BEFORE any v24 implementation: UT-138..162 (22, one per key DES-137..162 boundary rule), IT-105..121 (16, real SQLite stores / real booted createServer() HTTP, no mock of the SUT boundary), E2E-008/009 (register-crash-window in-process crash-window floor per DES-149's own Gate-7.5 scope note; admin-cross-read S-4), VAL-118..129 (one per REQ-107..118, mapped from 04-design.md's own Real-tier validation table; VAL-124/REQ-113 and VAL-128/REQ-117 are status:blocked/result:not-run because the design itself rules both provable ONLY by a Gate 7.5 real run / a disqualified-reviewer cold-model protocol, never a mock or an automated stand-in — recorded honestly, not silently skipped). Every item measured red once: whole-file import failure for 10 brand-new source files (tool-specs.ts, authz.ts, path-verdict.ts, scan-agent-calls.ts, check-mermaid.ts, authoring-guide.ts, call-tool.ts, owner-lookup.ts, audited-read.ts, run-view.ts — none exist yet) or a genuine behavioural red against an EXISTING module/real engine (params/contract.ts, params/resolve.ts, scheduler.ts, webhook-registry.ts, asset-sync.ts, workflow-catalog.ts, run-store.ts, dashboard-page.ts, workflow-view.ts, claude-agent-sdk-client.ts, server.ts still exhibit pre-v24 behaviour). 8 files extended in place rather than rewritten wholesale (surgical discipline — DES-159 names four of them as REWRITE targets whose OLD assertions test retired v21-v23 behaviour; flagged in 05-tests.md per item, not silently deleted, left for the implementer task DES-159 itself names as owner); every append re-ran the full file afterward and confirmed pre-existing cases still pass (regression intact, counts recorded per 05-tests.md item). Two authoring mistakes caught and fixed during this pass (both instructive, recorded in 05-tests.md): a namespace-derivation fixture used a malformed sha256 that was already 400-refused for an unrelated reason (false-green risk, fixed to a valid-format hash isolating the ?namespace= check); a catalog row-count-unchanged assertion didn't pin WHICH refusal caused the count to stay flat (fixed to assert the specific MERMAID_REQUIRED throw first). No test_defects filed — no case was judged 'the test itself is wrong, not the code' this round (that judgment call belongs to a later implementation-stage report). Hermetic throughout: every clock-dependent case uses FixedClock/an injected Clock, zero bare Date.now()/new Date()/absolute-future-literal comparisons in any new test. trace 1166/65, 0 broken links / 0 orphans (REQ-107..118's own gap type flipped from 未驗證 no-test-at-all to 未實作 + 未真實驗證 mock-only-pending-real, the intended Gate 5 signal). gates.tests.passed=true; current_stage -> impl. Next: Gate 6 (implementer) works TASK-131..153 in the pinned order (03-tasks.md), each RED item above going green. PRIOR: 2026-09-03 v23 GATE 5 RE-ENTRY (verifier, adjudication #8 applied, commit `9fc4439`'s 04-design.md ruling) — the two Gate 6-reported test defects are closed: UT-125's assertion (b) re-pointed from the (structurally-unreachable-per-DES-130-B6) DB row status to the journal's cause:'script_unresolved' line, confirmed green (1/1, then 51/51 for the file pair, then 283/283 files / 1850/1850 full suite, 0 remaining red); UT-135's fixture defect was already fixed at Gate 6.5+7 round 4 and is re-confirmed green here. tsc clean. trace --check 1024/18, byte-identical residue (0 new gaps/broken-links). UT-129..137/IT-104 deliberately left as Gate 6 last set them (their green flip belongs to Gate 6.5+7's regression closeout, not this Mode-A re-entry) even though they measure green in the full-suite run above. gates.tests.passed stays true; current_stage stays impl — Gate 6 continues per its own handoff. Full detail in 05-tests.md's UT-125 entry ('RESOLVED — Gate 5 RE-ENTRY' note). PRIOR: 2026-09-03 v23 GATE 5 RE-RUN (v23 Gate 2 RE-RUN #2, send-back 41e6382) — UT-125 amended in place (O3's dropped assertion (b) restored, flipped red) + 8 new items UT-129..137 in tests/unit/graph-analyzer.test.ts (O1/O2/O4/O5 + retries/attempts + 12-key set equality + cause value domain + settle_failed carve-out + AnalyzerCause type-level union — all red for the measured reason except UT-137, vitest-green/tsc-red by design) + IT-103/IT-104 in tests/integration/graph-analyzer-composition-root.test.ts (IT-103 a legitimate immediate green defence-in-depth pin per IT-102 precedent, IT-104 one red case + one green pin, ADR-020 mirror rows). CONCURRENCY (CLAUDE.md / retro sdlc-agent-ledger-clobber): a sibling Gate 5 dispatch was running in this same working tree on this same send-back scope; it authored UT-129..137 (this instance's own independent, functionally-equivalent attempt at O1/O2/O4/O5 was removed as a duplicate, ceded as the more complete/organized file per this ledger's established IT-101/IT-102/UT-128 precedent) and this instance authored IT-103/IT-104 plus verified UT-125's amendment and the sibling's UT-129..137 by direct test run. Full detail in 05-tests.md's own 'Gate 5 RE-RUN (v23 Gate 2 RE-RUN #2, send-back 41e6382)' section. sh .sdlc/trace baseline unchanged this round (no new DES/TASK; Gates 3/4 not sent back). Next: Gate 6 (impl), per Gate 2's own handoff. PRIOR: 2026-09-03 v23 GATE 6.5+7 ROUND 4 (verifier): UT-124/125/126/127/128 + IT-101 flipped red->green on the full 283/1837 suite; UT-128 gained 2 cases (A2's two settle paths, the debt its own docblock named) and IT-101 gained ROW 4 (A1's ADMIT line was executed by no test - a gate refusing every authenticated caller passed the whole oracle); UT-116's case 4 re-pointed to ABSENCE per the ARCH-084 A4 amendment. Zero remaining red; IT-015 the only blocked/not-run. PRIOR: 2026-09-03 v23 GATE 5 RE-RUN (verifier, test-first RED) for the Gate 2 re-run send-back d294880 — 7 new items across A1/A2/A3/V-D/A5/A6 (6 RED + 1 green drift-lock hardening, IT-102; + sibling's inv-5 UT-128): UT-124 (A2 enabled:false guard across enqueue/sweepAtBoot/regenerate, 4/4 red — 4th case added by this instance per inv 11's latent-clobber text: a PRIOR ready row must survive a disabled enqueue()), UT-125 (A3/N-1 orphan-pending row must not wedge the queue, red), UT-126 (V-D diagramNote precedence table, 2/6 red cells + 4 green pins), UT-127 (A5 diagram-vocabulary export + membership, 2/2 red). CONCURRENCY, recorded honestly (CLAUDE.md / retro sdlc-agent-ledger-clobber): a SIBLING Gate 5 dispatch was found executing in this SAME working tree during this run, same send-back scope — full detail in 05-tests.md's own process note. This instance independently verified 3 sibling-authored items rather than re-writing them: IT-101 (A1 transport gate, tests/integration/workflow-describe-auth-gate.test.ts, verified red 3/6 — this instance's own duplicate attempt at the same oracle was removed, ceded as the more complete file, real-LAN-peer case included), IT-102 (A6 tools/list drift-lock, verified GREEN — legitimate hardening per 02-architecture.md's own 'this is all v23 owes' text, production already conforms; this instance's duplicate block removed), UT-128 (R-1/inv-5 one-journal-line-per-settle, verified red 3/3). Dedup of both dispatches' ledger claims is owed to the orchestrator, not resolved here. Primary-source correction for Gate 8: 02-architecture.md:555's '39 tools' is stale — direct count of server.ts:188-242 is 40 (both this instance and the sibling transcribed 40 independently, in agreement). trace 1003 items / 18 gaps — unchanged from the 996/18 baseline (7 new items, 0 new gaps, 0 broken links on new IDs), tsc clean. A7/A8/A9/A10 were Gate-2-only doc closures — verified no new tests owed. Next: Gate 6 (impl) — A1's gate + README/DEPLOY edits, A2's guard + moved durable write, A3's try/catch/finally around the scheduled closure in _startJob, A5's VOCAB_GLYPHS/DEFAULT_GRAPH_ANALYZER_SYSTEM_PROMPT export + interpolation + deleting the false server.ts:299-300 comment, V-D's one-line note-precedence widening in workflow-view.ts. PRIOR: 2026-09-03 v23 Gate 5 RE-RUN (verifier, test-first RED, adjudication #6) — 3 new RED unit tests for the Gate 7.5 REQ-103 failure: UT-119 (analyzer prompt must carry live trigger bindings, not just the allowlist — DES-131/DES-128/ARCH-078/079/REQ-103), UT-120 (live /api/runs/:id/dag warning must not say 'skeleton' — DES-064/REQ-105), UT-121 (LiteLLMProxyManager.start() must attach an 'error' listener — ARCH-005/REQ-102, the same crash the Gate 6.5+7 note below flagged ROUTED NOT FIXED). All confirmed red for the stated reason; tsc clean; trace 987->990 items, 17->17 gaps byte-identical. gates.tests.passed stays true (the rest of v23's slice was already green and stays so). Next: Gate 6 lands the 3 fixes + V-3's doc-only AUTHORING.md addition, then Gate 7.5 re-runs VAL-118 only. Prior: 2026-09-03 v23 Gate 6.5+7 (verifier) flipped this iterations items on a full-suite re-run: UT-107..117, IT-096..099, VAL-111..114, VAL-116/117 -> green/pass (21 items); VAL-115 (REQ-104) -> blocked/not-run, a deliberate deferral to TASK-124s Gate 7.5 real run, not a failing red. NEW this gate from the coverage bar: UT-118 (SqliteRunStore.getWorkflowName, 3 cases on real sqlite) plus 3 cases extending UT-111 (the live-bindings allowlist arm) and 1 extending UT-114 (ANALYZER_DISABLED). gates.tests.passed stays true. Prior: 2026-09-02 (v23 Gate 5, verifier, test-first RED) DONE — TASK-113..125/DES-120..136/ARCH-077..086/REQ-101..106(+REQ-100 [AMENDED v23]) all covered: UT-107..117 (11), IT-096/097 (2), VAL-111..117 (7, VAL-115/REQ-104 deliberately not-run — no unit test may claim to prove REQ-104 per its own acceptance text; only Gate 7.5's real run is proof), plus 2 existing files extended in place (UT-024's allowed-tools file gains the curateToolsForProvider empty-set case; UT-033 compose-config-v2-wiring gains the graphAnalyzer forwarding rows; UT-104 workflow-view.test.ts corrected for phases-now-public). Brand-new modules (diagram-gate.ts, trigger-bindings.ts, graph-analyzer.ts) red at collect time (module-not-found, same precedent as v13's seedref batch); existing-module gaps red via TypeError/not-a-function (same (obj as any) convention as put-blob-stream.test.ts). Targeted run: 93 total, 58 failed / 35 passed (all fails confirmed genuine-unimplemented, all passes documented green pins). Full suite: 1751 total, 1693 passed / 58 failed (279 files) — exactly this pass's 58 reds, 0 unrelated regressions against the v22 close baseline (258/279 files unchanged, 2 pre-existing spawn-litellm-ENOENT artifacts unaffected). sh .sdlc/trace --check: 967 items / 42 gaps — 0 broken links, 0 orphans; every DES-120..136 has a direct UT/IT/VAL trace; the 25 new gap rows (REQ-101..106 mid-未實作+high-未真實驗證 ×2 each, TASK-113..125 low-未實作 ×13) are the CORRECT expected pre-Gate-6/pre-Gate-7.5 shape, not a defect. Next: Gate 6 (implementer). Prior (v22 Gate 5 SEND-BACK ROUND 2, verifier, test-first RED) DONE — closes 07-review.md v22 GATE 8 RE-REVIEW #2's B1 (HIGH, H1 residual: workflow_register/deregister still accept a self-asserted args.principal under authEnabled) + B2 (MEDIUM, the val-107 oracle round-1's fix broke). IT-095 (tests/integration/catalog-write-auth-dbind.test.ts, new describe block, same fixtures as IT-091): 2 red cases — a D-BIND-exempt caller replays the REAL owner string as args.principal on workflow_register/workflow_deregister, expects PRINCIPAL_REQUIRED, observes undefined (spoofed write silently succeeded) both times; 1 green pin — wrinkle-1 no-auth attribution preserved (args.principal still attributes ownership with authEnabled:false). val-107-release-channels.test.ts: restored the NOT_WORKFLOW_OWNER assertion round 1 wrongly rewrote to \"succeeds\" (itself B2's regression) — now red, since workflow_publish still drops args.principal unconditionally rather than gated on authEnabled. val-097-workflow-ownership.test.ts: +1 green-pin case (bob, real bearer, tries workflow_publish on alice's workflow -> NOT_WORKFLOW_OWNER), re-pinning coverage B2 found missing. IT-091's case-1 setup amended (fixture-reachability only, seedExtraVersion() replaces a D-BIND register-with-principal call the coming fix will refuse) — its own 5 cases stay green. Full suite npx vitest run: 1687 total, 1684 passed / 3 failed (261 files) — exactly this round's 3 genuinely-red cases, 0 unrelated regressions vs the prior round's close baseline 1683/1683 (2 pre-existing spawn-litellm-ENOENT artifacts unaffected). npx tsc --noEmit clean. sh .sdlc/trace --check: 892 items / 17 gaps (was 891/17) — gap count/classes unchanged (1 mid IMPL-082, 15 low iter-drift, 1 low TASK-018), 0 broken links, 0 new gap classes. current_stage: review -> impl. Next: Gate 6 (implementer) — gate the args.principal-drop on authEnabled for ALL THREE catalog writes (register/deregister/publish), not an unconditional drop, per the two named wrinkles in 07-review.md §4.2. Prior (v22 Gate 5 SEND-BACK, verifier, test-first RED) DONE — 07-review.md v22 GATE 8's 4 HIGH findings (H1 anonymous D-BIND catalog-write auth bypass on workflow_publish/register/deregister; H2 unmasked GET /api/runs/:id/dag script-derived skeleton leak under auth; H3 COUNT(*)-based version allocator bricking re-registration for migrated multi-registration workflows; H4 Scheduler.create() missing the resolves-release check) — 4 new work items (IT-091 catalog-write-auth-dbind.test.ts, IT-092 dag-masking-auth.test.ts, IT-093 scheduler-create-channel-check.test.ts, UT-106 extended into catalog-versions.test.ts) + workflow-ownership.test.ts (IT-080) amended in place for H1's fixture-reachability change (10/10 pass unchanged against pre-fix HEAD — pure setup migration hand-seeding the release pointer instead of routing through workflow_publish, plus case 6 re-sited onto its own auth-disabled server; carries no red case of its own). 04-design.md DES-117 amended: new PRINCIPAL_REQUIRED error-code row [AMENDED v22 send-back] (authEnabled && effectivePrincipal===null refuses on all three mutations before the ownership check; workflow_publish drops the args.principal fallback entirely, register/deregister keep it as recorded accepted debt). Full suite npx vitest run: 1677 total, 1670 passed/7 failed (260 files) — exactly this pass's 7 genuinely-red cases (IT-091 x3, IT-092 x1, UT-106 x2, IT-093 x1), 0 unrelated regressions vs the v22 Gate 6.5+7 close baseline 1665/1665 (2 pre-existing spawn-litellm-ENOENT background artifacts unaffected). npx tsc --noEmit clean. sh .sdlc/trace: 889 items/17 gaps (was 885/17) — gap count unchanged, 0 new gap classes, 0 broken links (confirmed via dashboard.html cross-reference). Send-back scope: H1-H4 only per 07-review.md §8 (6 MEDIUM + 4 LOW recorded as non-blocking debt, no new tests written for them this pass). gates.tests.passed stays true (Gate 5 was already closed for v22's original REQ-096..100 scope; this is the send-back's targeted RED subset re-entering the same gate). current_stage -> impl (Gate 6 next: implementer fixes the 4 HIGH per each finding's named fix, reconfirms 06-impl-log.md, flips these items green). | Prior: 2026-09-01 (v22 Gate 5, verifier, test-first RED) DONE — REQ-096..100 (version history/beta-release channels/inline-script closure/registration-time validation/non-owner script masking): 11 new work items (UT-102..105, IT-084..089 covering ARCH-071..076/DES-109..119, VAL-106..110 one per REQ) + 2 extended in place with no new ID (UT-033 compose-config-v2-wiring +maxWorkflowVersions case; IT-011 scriptversion-fidelity rewritten to v22 Rule 1's literal 'v1'/'v2' oracle, replacing the relative 'v2 !== v1' oracle that passes when both are wrong). Full suite npx vitest run: 1654 total, 60 failed/1594 passed (258 files) — exactly this pass's 60 genuinely-red cases across the 17 touched files, 0 unrelated regressions vs the v21-close baseline 1582/1582 (1594 = 1582 + 12 legitimate green pins — e.g. plain workflow_resume unaffected by the inline ban, a migrated legacy workflow still runs, /api/workflows never carried script; 2 pre-existing spawn-litellm-ENOENT background artifacts, documented since IMPL-140, unaffected). npx tsc --noEmit: every new error is exactly the expected unimplemented-v22-surface shape (missing exports/modules/properties on WorkflowCatalog/SqliteSchedulerPort, maxWorkflowVersions not yet on FileConfig) — 0 errors outside the 17 touched files. sh .sdlc/trace --check: 875 items / 33 gaps (was 824/14) — 0 orphan/broken-link, 0 new gap CLASSES; the +19 delta is exactly the standard pre-impl shape: 5 mid 未實作 (REQ-096..100, not yet implemented) + 5 high 未真實驗證 (same 5 REQs, real:false until Gate 7.5 — textbook Gate-5 shape, identical class to every prior iteration's pre-validation baseline) + 8 low 未實作 (TASK-105..112) + 1 low 漂移 (IT-011, iter:v1 now tracing v22-iter DES-113/TASK-108 — the DES-088/DES-066/DES-099/DES-100 declared-false-positive iter-drift class); the pre-existing 14 (6-item recorded-debt baseline + 8 accrued low/mid drift) are untouched byte-for-byte. Hermetic: every new clock use is FixedClock or a local mutable fake anchored to a fixed instant; no absolute-date-vs-real-clock comparisons. Flagship RED: tests/integration/run-version-pin.test.ts's marker test (start v1 blocked mid-agent()-call → suspend → register+publish v2 with an entirely different script → resume → result is v1's) reproduces the real resume-determinism bug live (run-manager.ts:632-636 still re-reads catalog.get(spec.name) on resume) as a durable regression test, not merely a stubbed red. gates.tests.passed=true; current_stage tests→impl. Prior: 2026-09-01 (v21 GATE 5 RE-RUN #7, verifier, test-first RED — 07-review.md GATE 8 RE-REVIEW #6 §T6 send-back scope P6-1 BLOCKING + P6-2/P6-3/P6-4 riders taken as fixes + QD-REP-1 green fence; P6-5/QD-CONS-3 correctly out of Gate-5 scope, impl/doc-only): extended in place, no new IDs, 3 files touched. P6-1 (BLOCKING, case/whitespace variant class slips the FRAME_CLOSE_FORGERY caller rung): tests/unit/params-contract.test.ts new describe block sibling to the existing F2 block, 4 red cases (</USER-INSTRUCTIONS>, </User-Instructions>, </ user-instructions>, < /user-instructions> all admitted today); tests/integration/params-admission.test.ts +1 red case appended to the existing F2-durable-half block (one case-variant on the resume side suffices — shared constant, per §T6). P6-2 (MED, appendPrompt's author-origin default frame-checked nowhere before dispatch): tests/integration/params-admission.test.ts new describe block, 1 red (registered defaults.appendPrompt with the forged delimiter dispatches with NO overrides at all today) + 1 green pin (benign default still dispatches). P6-3 (LOW, args.<k>.default never applied/enforced): tests/unit/params-contract.test.ts new describe block, 1 red (a declared default on an args spec registers as-is today) + 1 green pin. P6-4 (LOW, type:'enum' is string-only by construction, a numeric enum is a fail-closed brick): tests/unit/params-contract.test.ts new describe block, 2 red (mixed-type enum, fully-numeric motivating case) + 1 green pin. QD-REP-1 (LOW, resolve.ts's mapEffort duplicate has no zero-importer fence unlike the parallel ADR-006 one): tests/unit/params-resolve.test.ts new describe block, 1 green case (structural filesystem check, no red needed — pins current truth; checks both named and namespace import forms so a second import statement or a `* as x` alias can't hide from the fence). Full suite npx vitest run: 1578 total, 9 failed/1569 passed (243 files) — exactly the 9 genuinely-red cases (7 in params-contract.test.ts, 2 in params-admission.test.ts), 0 unrelated regressions vs the pre-round 1565/1565 baseline (2 pre-existing spawn-litellm-ENOENT background artifacts, documented since IMPL-140). npx tsc --noEmit clean. sh .sdlc/trace --check: 823 items / 14 gaps, IDENTICAL to the RE-REVIEW #6 disclosed set (0 new IDs, 0 new gap classes, 0 broken links — only existing UT-098/IT-083/UT-099 entries extended). Hermetic: no absolute-date-vs-real-clock comparisons in any new case. gates.tests.passed=true; current_stage review→impl (Gate 6 re-run #7 next, scope per §T6: P6-1 one-line widen of the shared FRAME_CLOSE_FORGERY constant [linear pattern] + P6-2/P6-3/P6-4 fix-or-recorded-decision + P6-5 shared-DEFAULT_CEILINGS-export-or-corrected-S-1-residual + the QD-CONS-3 doc batch, same pass per H-4). Prior: 2026-09-01 (v21 GATE 5 RE-REVIEW #5 re-run, verifier, test-first RED — 07-review.md GATE 8 RE-REVIEW #5 §S7 send-back scope F2/F1/F4): extended in place, no new IDs, 4 files touched. F2 (BLOCKING, cross-principal attribution forgery via the `</user-instructions>` close-delimiter): tests/unit/params-contract.test.ts new `validateUserOverrides() — appendPrompt cannot forge the <user-instructions> frame close-delimiter` describe block (3 cases: forged appendPrompt containing the literal close tag refused PARAM_OUT_OF_RANGE [RED]; rejection never echoes forged/surrounding content, DES-101 row 6 discipline [RED]; benign appendPrompt unaffected [GREEN regression pin]); tests/unit/params-resolve.test.ts new FRAME INTEGRITY PIN case in the composePrompt() describe block — composePrompt stays a pure concatenation (exactly one true OPEN/CLOSE per call regardless of appendPrompt content); the integrity guarantee lives entirely in validateUserOverrides' admission-time refusal, not here [GREEN on write, deliberately not red — composePrompt is not changing]; tests/integration/params-admission.test.ts new `F2 durable half` describe block — a pre-fix-admitted run's persisted effectiveParams seeded directly (same seed-the-column-directly precedent as VAL-100) with the forged delimiter must be refused at resume(), never silently re-dispatched to the spawner [RED]. F1 (MED, riding the batch — two alias predicates give the SAME openrouter/<id> passthrough string OPPOSITE answers depending on registration door): tests/integration/harness-defaults-validation.test.ts new `meta.params.model.default vs top-level defaults.model` describe block — the string as a declared params.knobs.model.default already registers fine [GREEN regression pin, contract.ts's isKnownAlias already carries the passthrough]; the SAME string as a caller-supplied top-level defaults.model is rejected HARNESS_DEFAULTS_INVALID today [RED, harness-defaults.ts's own aliasNames.has() has no passthrough carve-out]. F4 (LOW, folded into the batch — min/max on an enum spec are NaN-inert/advertised-not-enforced): tests/unit/params-contract.test.ts new `parseParamContract() — min/max on a type:'enum' spec is rejected` describe block, 2 cases (min and max both RED — validateSpecShape accepts them today). Confirmed via direct re-run per file (params-contract.test.ts 62/62, 4 failed/58 passed; params-resolve.test.ts 27/27 pass; harness-defaults-validation.test.ts 29/29, 1 failed/28 passed; params-admission.test.ts 19/19, 1 failed/18 passed). Full suite (`npx vitest run`): 1555 total, 6 failed/1549 passed (243 files) — exactly the 6 genuinely-new-red cases this round, 0 unrelated regressions vs the pre-round 1546/1546 (v21 Gate 7.5 ROUND 3) baseline (2 pre-existing spawn-litellm-ENOENT background artifacts, documented since IMPL-140). `npx tsc --noEmit` clean. `sh .sdlc/trace --check`: 821 items/11 gaps, IDENTICAL to the pre-round baseline (0 new gap classes — only existing UT-098/UT-099/IT-081/IT-083 extended in place, no new work-item IDs). Hermetic: no absolute date literals in any new case (params-admission.test.ts's new case reuses the file's existing FixedClock anchor). Not in this pass's scope (07-review.md §S7 NOT-in-scope list): §S1's closed items, R-1/S-1 recorded debt, the 11 trace gaps, solid_check's 10 unclaimed files, trace.py version sync, state.yaml strict-YAML defect. gates.tests.passed=true; tests→impl (Gate 6 re-run #5 next, scope per §S7: F2 admission refusal + resume-side check [or a recorded verified decision] in contract.ts/run-manager.ts + F1 predicate swap + validateHarnessDefaults reordering in harness-defaults.ts/workflow-catalog.ts + F4 fold-in in contract.ts, PLUS the doc batch F3/S-2/C-3 in the SAME pass per the H-4 precedent). Prior: 2026-09-01 (v21 GATE 5 RE-RUN #4, verifier, test-first RED — 07-review.md GATE 8 RE-REVIEW #4 §Q7 send-back scope A1(both halves)/A2/A4/A5): extended in place, no new IDs. tests/unit/params-contract.test.ts (+12 cases across 6 new describe blocks — A1 half 1 `parseParamContract() malformed ParamSpec shape guard` [4 red: type outside the 3 literals, non-array enum, non-number min, non-number max, all currently registered with ok:true]; A1 half 2 `effectiveBounds() total over an already-poisoned stored contract` [2 red: a stored effort spec with a non-array enum throws `TypeError: authorEnum.filter is not a function` inside boundEffort today — pinned that the pure function must never throw, fix MECHANISM left to Gate 6]; A4 `checkValueAgainstSpec() string min/max byte-length bounds` [2 red + 1 green pin: min/max on a type:'string' spec are NaN-inert today (`(value as number)` comparison), pinned semantics = UTF-8 byte length, enforced in checkValueAgainstSpec itself (NOT rejected at parse) so A5 can reuse the SAME predicate per F-2's one-bounds-predicate discipline]; A5 `effectiveBounds() appendPrompt advertises maxAppendPromptBytes` [1 red + 1 green pin: effectiveBounds narrows only timeoutMs/effort today, appendPrompt.max stays undefined though the ceiling is enforced at admission and named in the tool description — pinned fix shape = same min(author,ceiling) precedent as boundTimeoutMs]; A2 `checkValueAgainstSpec() rejection cost must not scale quadratically` [1 red, per-test timeout raised to 90s: reviewer's pinned red-test shape exactly — ~1MB out-of-enum model value must reject in <1s with echo ≤64 bytes; reconfirmed O(n^2) on this host, 602/2401/9614/29274/59770ms @ 100k/200k/400k/700k/1M chars, actual ~59.8s at 1M ≫ 1s today] — 10 failed/36 passed, 46 total in file). tests/acceptance/val-100-param-contract.test.ts (+2 cases, new describe block `REQ-090 real-tier: a malformed/poisoned params contract must not durably break workflow discovery` — real createServer HTTP + a direct better-sqlite3 connection to the SAME catalog.db seeding a pre-existing poisoned row, per the review's explicit 'seed the column directly' instruction: (1) red — a params.knobs.effort with a non-array enum registers successfully today instead of being refused typed with nothing stored, same assertion shape as the file's existing LOCKED-key case; (2) red — a directly-seeded poisoned row (bypassing workflow_register entirely, simulating data written before the registration guard existed) makes workflow_get return a JSON-RPC error.code:-32000 ('authorEnum.filter is not a function') via server.ts's generic tools/call catch, instead of a normal tool-call envelope; asserted resolution-agnostically via a new callToolRaw helper so either a canonical-fallback OR a typed-error fix satisfies it — Gate 6 picks which; a sibling healthy workflow registered before the poison must stay listed in workflow_list — 2 failed/4 passed, 6 total in file). Full suite: 12 failed/1521 passed (1533 total, 243 files) — exactly the 12 new red cases, 0 unrelated regressions vs the pre-re-run 1519/1519 (v21 Gate 7.5 ROUND 2) baseline. `npx tsc --noEmit` clean. `sh .sdlc/trace --check`: 818 items/11 gaps, IDENTICAL to the pre-re-run baseline (0 new gap classes — only UT-098/VAL-100 extended in place, no new work-item IDs). Hermetic: no clock/date literal is ever compared as future/past in any new case (a `new Date().toISOString()` is used once only to satisfy the seeded row's NOT NULL createdAt column). gates.tests.passed=true; tests→impl (Gate 6 re-run #4 next, scope per 07-review.md §Q7: A1 both halves + A2's O(n) fix + A4/A5 same-module fold-in, plus the review's deduped doc batch — A3/A6/A7..S-1). Prior: 2026-09-01 (v21 GATE 5 RELAUNCH, verifier, post-adjudication #6 — 04-design.md 'Orchestrator adjudication #6' F-5 'the relaunch starts at tests, not impl'): a second workflow was launched by the human against the same tree while the Gate 5 RE-RUN #3 below was still in flight (self-diagnosed root cause in commit ce171e3) — P-A1/P-A2/P-A4 code fixes landed out-of-band (244f9f0), then two doc-only adjudication commits (9d86075 #5, ce171e3 #6) reconciled the race, #6 reversing #5's E-3 call ('the Gate 5 tests were right'). Re-verified against HEAD per advisor guidance (do not revert the human's own commits — the RED moment for P-A1/P-A2/P-A4 genuinely happened at eb58d96 before the race landed fixes): P-A1/P-A2/P-A4 confirmed GREEN (now regression pins); P-A3's 2 cases confirmed still correctly RED. Test-side corrections: (a) RETIRED IT-081's B4 describe block (adjudication #6 names it explicitly — it asserted the OPPOSITE of IT-083's newer P-A2 block for an identical scenario, replaced with a comment pointing at the surviving P-A2 case); (b) fixed UT-101's older 'A-7' case in place (asserted top-level `effort`, contradicting the now-adopted `output_config.effort` nesting the P-A1 shape-pin block proves correct — no `src/` change, test-only correction to match the adjudicated contract). Per adjudication #6's F-5 'Add' list: new `F-1 ceiling interaction` describe block in IT-081 (2 cases, 1 genuinely red — `WorkflowCatalog.register()` never receives `Ceilings` today, so an author `effort` default isn't bounded by `maxEffort` like every other declared value) + 3 new UT-099 cases (1 genuinely red — `defaultRunParams` must read an author-declared `effort`/`appendPrompt` default with `'default'` rung provenance once F-1 widens `HarnessDefaults`; pinned via an `as HarnessDefaults` cast ahead of that interface change, confirmed clean under `npx tsc --noEmit --strict`; 2 green coverage-completion pins for already-correct `overrides.effort` merge and `opts.timeoutMs` call-rung behavior). No `src/` implementation changes this pass — F-1/F-2/F-3/F-4 stay Gate 6 scope. Full suite: 4 failed/1509 passed (1513 total, 243 files) — exactly the 4 genuinely-red cases (IT-081 case (e) round-trip, IT-081 ceiling-at-or-below, IT-083 P-A3 dispatch-inertness, UT-099 default-rung), 0 unrelated regressions (2 pre-existing spawn-litellm-ENOENT + 2 pre-existing EADDRINUSE background-process artifacts, same documented environment limitation as prior rounds, not counted in the 4). `npx tsc --noEmit` clean. `sh .sdlc/trace --check`: 817 items/11 gaps, IDENTICAL to the pre-relaunch baseline (0 new gap classes). Hermetic: no clock/date literals in any new case. gates.tests.passed=true (unchanged); current_stage stays impl (Gate 6 re-run #3 next, scope confirmed unchanged: F-1 widen+ceiling-thread, F-2 dedup, F-3 doc sync, F-4 integrator batch). Prior: 2026-09-01 (v21 GATE 8 RE-REVIEW #3 send-back, Gate 5 RE-RUN #3 per 07-review.md §P2 P-A1/P-A2/P-A3/P-A4, re-run scope (a)/(b)/(c)/(d)): extended UT-101 (tests/unit/gateway-effort.test.ts, +2 red REST output_config.effort transport-contract shape-pin cases — direct-fetch + LiteLLM-proxy branches), UT-020 (tests/unit/claude-agent-sdk-gateway-thinking.test.ts, +1 green SDK-side Options.effort companion contract pin — already correct), IT-081 (tests/integration/harness-defaults-validation.test.ts, +1 red discover→edit→re-register round-trip case [P-A3 registration half] + new P-A4 describe block [1 red bad-model-default + 1 green regression pin]), IT-083 (tests/integration/params-admission.test.ts, +new P-A2 describe block [1 red registration-parity case + 2 green regression pins] + new P-A3 describe block [1 red dispatch-inertness case via injected spawner capturing req.runParams]) — all IN PLACE, no new IDs (same precedent as prior re-review rounds). Full suite 6 failed/1503 passed (1509 total, 243 files) — exactly the 6 new genuinely-red cases, 0 unrelated regressions vs the 1499/1499 pre-re-run baseline (2 pre-existing spawn-litellm-ENOENT background-process artifacts, same documented environment limitation present on the unmodified baseline). `npx tsc --noEmit` clean. `sh .sdlc/trace --check`: 817 items/11 gaps, IDENTICAL to the pre-re-run baseline (0 new gap classes — only existing IDs extended, no new work-item IDs). Hermetic: FixedClock anchor only in the new P-A3 case, no absolute-date-vs-real-clock comparisons. Batched into Gate 6 ONLY per the review's own pinned scope (deliberately NOT Gate-5 test work): P-A5 (duplicate value/bounds-checker consolidation) + P-A6 (secret-marker-grammar duplication, export hasSecretMarker()) + 4 doc amendments (ARCH-066 inv-2 legacy-NULL exception, appendPromptBytes process-view drop, workflow_agent_log description, parseUserOverrides→validateUserOverrides rename) — same precedent as the prior round's R-G6..G10 doc/consolidation batch (no dedicated new tests, verified by inspection at Gate 6/review time). gates.tests.passed=true; tests→impl (Gate 6 re-run #3 owns the fix). Prior: 2026-09-01 (v21 GATE 8 RE-REVIEW send-back, Gate 5 RE-RUN #2 per 07-review.md §R2 (a) R-G1 adversarial marker-literal-on-resume / (b) R-G2 effective post-merge model zero-durable-work / (c) R-G3 default-deployment alias table non-empty): extended IT-083 in place (tests/integration/params-admission.test.ts), no new IDs — 3 genuinely-red + 1 green pin; full suite 1493/1496 (3 fail = exactly the new cases, 0 unrelated regressions vs 1492/1492 baseline); tsc clean; trace 816/9 identical to baseline. See 05-tests.md IT-083 'v21 GATE 8 RE-REVIEW send-back' note for full detail. Prior: 2026-09-01 (v21 Gate 8 send-back, Gate 5 RE-RUN per 07-review.md §4 B1..B4 — B1 HIGH ≡ adversarial F1 ≡ quality QD-1, B2/B3 MED ≡ adversarial F2/F3, B4 MED ≡ quality QD-3): extended in place, no new IDs — tests/unit/params-contract.test.ts (+4: B1 validateUserOverrides canonical-contract alias check [RED] + openrouter passthrough [GREEN pin]; B4 parseParamContract empty-aliasNames skip [RED] + openrouter passthrough [RED]), tests/integration/params-admission.test.ts (+3: B1 UNKNOWN_ALIAS + zero-durable-work [RED] + openrouter passthrough [GREEN pin]; B2 new describe block — spawner override + 2 RunManager instances sharing 1 SqliteRunStore, assertion accepts EITHER sanctioned outcome per review §4[b] [byte-identical dispatch OR a typed refusal on resume; mechanism choice left to Gate 6] — today's actual behavior [a successful resume silently dispatching the ‹secret:NAME› marker] satisfies neither branch [RED]), tests/integration/redact-sweep.test.ts (+1: new sink (6) kind:'harness' descriptor.prompt — fake gateway calls req.onHarness() with a secret-bearing prompt, persisted transcript carries it raw [RED]), tests/integration/harness-defaults-validation.test.ts (+1: new describe block, own no-aliases server — a declared model-enum wrongly PARAM_CONTRACT_INVALID on a default-alias server [RED]). 7 genuinely-red + 3 GREEN regression pins. Full suite: 7 fail/1485 pass (1492 total, 243 files) — all 7 failures are exactly the new B1/B2/B3/B4 red cases, 0 unrelated regressions vs the pre-change 1483/1483 baseline (1 pre-existing sandboxed-env spawn-litellm-ENOENT warning, unrelated, present pre-change too). tsc clean; trace 815/9 (0 new gap classes, identical to the pre-re-run baseline). No time bombs (FixedClock anchors only, no absolute-date-vs-real-clock comparisons). gates.tests.passed stays true; current_stage: review→impl (Gate 6 resumes, owns B1/B2/B3/B4 red→green + B5 doc amendments + full regression). | 2026-08-31 (v21 Gate 5 ADDENDUM, second send-back per 04-design.md 'Orchestrator adjudication #2' + 05-tests.md 'Gate 5 addendum'): Part 1 (5 enumerated tests, extended in place, no new IDs) — (1) enum>32-members cap in params-contract.test.ts: GENUINELY RED (no per-spec enum-length check in src/params/contract.ts); (2) redact-sweep.test.ts sink(5) effectiveParams snapshot: GREEN on write (run-manager.ts:411-413 already redacts before createRun); (3) v15-v2-workspace-transport.test.ts purge-preserves-effectiveParams assertion: GREEN on write (workspace_purge is filesystem-only); (4) catalog-persistence.test.ts genuine pre-v21 raw-sqlite migration fixture: GREEN on write (idempotent ALTER + get() already handle it); (5) claude-agent-sdk-gateway-thinking.test.ts SDK-side low-vs-max Options diff: GREEN on write (claude-agent-sdk-client.ts:509/581 already writes mapEffort's value). Part 2 (DES-101..108 clause-coverage sweep, full table in 05-tests.md): 6 additional GAP rows found and closed — unknown-knob registration rejection (green), suppliedTruncated:true for >64-byte rejected string values (GENUINELY RED — no truncation logic exists), defaults.tools dispatch-ladder (green), CallKey byte-identical regardless of overrides/ADR-002 (green), session-options-builder.ts zero-importer fence/ADR-006 (green); several clauses recorded as covered-by-construction or explicitly out-of-sweep-scope (pre-existing/dropped-by-adjudication) with rationale in the table, none silently skipped. Full suite: 3 fail/1471 pass (1474 total, 243 files) = 1 pre-existing (UT-100/A-8) + 2 new genuinely-red (enum-cap, suppliedTruncated); 0 unrelated regressions vs the 1/1463 pre-addendum baseline. tsc clean; trace 805/30 (0 new gap classes — no new work-item IDs added, only cases extended into existing UT/IT files). No time bombs (FixedClock anchors only, no absolute-date-vs-real-clock comparisons). gates.tests.passed stays true; current_stage stays impl (Gate 6 resumes, owns the 2 new red + the pre-existing UT-100 red). | 2026-08-31 (v21 Gate 5 RE-RUN, send-back A-2/A-3/A-5/A-7 per 04-design.md 'Orchestrator adjudication — v21 Gate 6 send-back'): extended in place, no new IDs, existing suites not rewritten — IT-081 +3 red cases (A-2 cross-validated meta.params defaults, spec.default read nowhere in src/params/contract.ts); IT-083 +1 case, GREEN on write (A-3 advertised==enforced bound — ceiling wiring already landed, only the behavioral test was missing, kept as a deliberate regression pin); UT-057 +2 red cases (A-5 workflow label sanitize, DES-107 amended — no general name predicate, label-scoped transform only, test defines the spec); UT-101 +2 cases, GREEN on write (A-7 mapEffort on the LiteLLM-proxy branch — effortBodyFields(applied) already spread into both callProvider and callViaLiteLLMProxy from one upstream mapEffort call); UT-097 doc-drift fixed (A-9, repointed to mergeRunParams/UT-099). Full suite 6 fail/1458 pass (1464 total, 243 files) = 1 pre-existing (UT-100/A-8, TASK-101's to fix) + 5 new genuinely-red; 0 regressions vs the 1/1455 pre-re-run baseline. tsc clean; trace 805/30 (0 new gap classes). gates.tests.passed stays true; current_stage stays impl (Gate 6 resumes). | 2026-08-31 (v21 Gate 5 RED, REQ-090..095 tunable-parameter contract): 4 new UT (UT-098 params-contract.test.ts, UT-099 params-resolve.test.ts, UT-100 agent-executor-params.test.ts, UT-101 gateway-effort.test.ts — all MODULE NOT FOUND for src/params/{contract,resolve}.ts, or genuine behavioral red), 1 new IT (IT-083 params-admission.test.ts), 6 new VAL (VAL-100..105, one per REQ-090..095, tier acceptance real:false); extended in place: UT-020/UT-033/UT-057/IT-004/IT-012/IT-043/IT-066/IT-081 (new v21 cases red, all pre-existing cases stay green — UT-020's v21 case and UT-057's fingerprint compat-pin case are deliberate GREEN regression guards, same precedent as prior iterations). Full suite: 40 failed/1378 passed (1418 total, 244 files) — all 18 failing files are exactly the v21-touched ones, 0 unrelated regressions (226 pre-existing files stay green). Debugging note: 3 acceptance/IT files initially hung/mis-asserted because (a) `workflow_status` nests `agents` under `.result.agents` not top-level (fixed by using the fixed `'agent-1'` single-agent-script convention, matching the pre-existing `runAndGetAgentId` helper) and (b) `useLiteLLMProxy:true`-default + `aliases` spawns a real litellm subprocess unavailable in this sandbox (fixed via `useLiteLLMProxy:false` + an `ollama` alias, which reaches `onHarness` without a live backend). trace: 805 items / 30 gaps (0 new gap classes vs the 794/30 pre-Gate5 baseline — only expected TDD-wavefront: REQ-090..095 未實作+未真實驗證, TASK-096..104 未實作). No time bombs (no absolute date literals anywhere; FixedClock anchors only). gates.tests.passed=true; tests→impl. Prior: 2026-08-19 (v20 F2 Gate 5 RED, F2-impact closure: REQ-012/ARCH-059/DES-092/DES-093/DES-095/IMPL-122/IT-078): UT-092→v20 red/fail (4 fail/13 pass=17 total: grant_types_supported expects [authorization_code,refresh_token]→[authorization_code] mismatch; scopes_supported/token_endpoint_auth_methods_supported/authorization_response_iss_parameter_supported undefined≠expected). UT-093→v20 red/fail (11 fail/16 pass=27 total: issueRefresh/consumeRefresh is not a function; scope col absent in consumeAuthCode/consumeState). IT-078→v20 red/fail (11 fail/26 pass=37 total: cases 12/13 grant_types mismatch; cases 20/21/22 callback returns 302≠200; cases 23-28 v20a/v20b unimplemented). VAL-095→v20 red/fail (2 fail/7 pass=9 total: cases 3+4/7d callback 302≠200). All RED for right reason (features not yet implemented). No time bombs (all relative clock assertions). trace: 756/12 (0 broken-link/orphan; 5 TDD wavefront gaps from Gate-3+4 closed by bumping IT-078/VAL-095 to v20; 12 pre-existing LOW/MID tech-debt remain). gates.tests.passed=true; tests→impl. // Prior: 2026-08-19 (v19 F2 Gate 5 RED, F2-impact closure: REQ-012/ARCH-059/DES-093/DES-095/IMPL-122/IT-078): IT-078 updated in place→v19 red/fail (2 fail/29 pass=31 total): case 21 (/authorize?...&state=CLIENT_STATE_ABC123 → full authorize→callback flow → final client Location must carry state=CLIENT_STATE_ABC123 AND iss=metadata.issuer; pre-impl state absent→null≠expected→FAIL; iss absent→null≠expected→FAIL) + case 22 (/authorize no state → final Location NO state param AND iss=metadata.issuer; state-absent half passes pre-impl; iss-absent→null≠expected→FAIL). 29 pre-existing cases GREEN (zero regression). No time bombs (all relative assertions). trace: 754/13 (0 broken-link/0 orphan; all LOW). gates.tests.passed=true; tests→impl. // Prior: 2026-08-18 (v18 F2 Gate 5 RED, F2-impact closure: REQ-012/ARCH-059/DES-094/DES-095/IMPL-122/IT-078): UT-094 updated in place→v18 red/fail (4 fail/11 pass=15 total): spy test (deps.jwksUri forwarded not undefined) + 3 static-pin tests (GOOGLE_AUTHORIZE_URL/TOKEN_URL/JWKS_URL exports absent→undefined). IT-078 updated→v18 red/fail (2 fail/27 pass=29 total): case 19 (/authorize 302 Location origin=googleAuthorizeUrl not googleBase→59990≠59099) + case 20 (full callback→502 ECONNREFUSED on dead googleBase). VAL-095 updated→v18 red/fail (2 fail/7 pass=9 total): cases 3+4+7d callback hits dead googleBase→ECONNREFUSED→502. All 27 existing IT-078 cases GREEN. Hermetic (dead port 59990, no real Google calls). trace: 753/13 (0 broken-link/0 orphan; +1 LOW VAL-095 now red=expected Gate 5). gates.tests.passed=true; tests→impl. // Prior: v17 F2 Gate 5 RED 2026-08-18, F2-impact closure IT-078): IT-078 updated in place→v17 red/fail (17 pass + 10 fail = 27 total): 10 new DCR cases — case 11 (registration_endpoint absent→fail), cases 12-13 (POST /register 201 + clamp-dont-reject), cases 14a-14c (400 invalid_redirect_uri/invalid_client_metadata), case 15 (registered_clients persistence), cases 16-17 (port-ignored /authorize binding + mismatch), case 18 (registered_clients GC sweep). Red reasons: route absent (404), field absent (typeof-undefined), table absent (SqliteError). 17 pre-existing cases remain GREEN (zero regression). Traces extended: added DES-092+TASK-091. No time bombs (expires_at all relative). trace: 752/14 (0 orphan/broken-link; 14 = 6 pre-existing + 5 new low TDD-wavefront drift: UT-092/093/095/VAL-095×2). gates.tests.passed=true; tests→impl. // Prior: v16 F2 Gate 5 RED 2026-08-18, F2-impact closure IT-078/IT-079): IT-078 updated in place→v16 red/fail (10 pass + 7 fail = 17 total): 6 new HIGH-1 cases (cases 8a-8f — /authorize non-loopback redirect_uri → 400, each variant: https://evil.example/cb, https://127.0.0.1/cb, unparseable garbage, empty, missing — all currently return 302 pre-fix → RED) + 1 new MED-2 case (case 10 — expired oauth_state row NOT GC-ed within 3s, pre-fix sweep never calls gcExpired → RED) + 3 regression guards (9a-9c, loopback 127.0.0.1/localhost/[::1] → 302, GREEN) + 7 pre-existing v15 cases GREEN. IT-079→v16 (stays green, D-BIND cases unchanged). No time bombs (all expires_at relative to Date.now()). trace: 751 items / 9 gaps (TASK-090 gap closed via IT-078→TASK-090 trace) / 0 orphan/broken-link. gates.tests.passed=true; tests→impl. Prior: 2026-08-17 (v15 Gate 5 RED, REQ-012+086..089): 16 new test files — UT-092 (oauth-metadata pure builders 12 cases), UT-093 (TokenStore seam injected clock+csprng bearer/code/state lifecycle 17 cases), UT-094 (verifyIdToken RS256 accept+9 reject branches 11 cases), UT-095 (resolvePrincipal union never-throws uniform-401 8 cases), UT-096 (isLoopbackPeer truth-table 19 cases: all 127.0.0.0/8+::1+::ffff:127.x+forwarded-header fail-safe), UT-097 (resolveHarnessParams pure per-param merge 10 cases), IT-078 (auth routes + I-2 hermeticity integration), IT-079 (D-BIND 0.0.0.0+LAN-IP real socket), IT-080 (ownership gate+boot backfill 10 cases), IT-081 (D-AUTH-5 named assertions 10 cases), IT-082 (schema drift-lock v15 7 cases), VAL-095 (REQ-012 discovery+PKCE+auth-disabled), VAL-096 (REQ-086 protected surfaces+principal attribution), VAL-097 (REQ-087 ownership gate+backfill), VAL-098 (REQ-088 harness defaults), VAL-099 (REQ-089 D-BIND LAN-IP+loopback-exempt). RED confirmed: 60 fail / 1188 pass / 233 files (1170 pre-existing + 18 pre-impl-compatible new). No time bombs (no absolute date literals; FakeClock anchors only). trace: 743 items / 19 gaps / 0 orphan/broken-link (same gap count as v15 baseline). current_stage tests→impl. Prior: 2026-08-15 (v14 Gate 5 RED, REQ-081..085): 14 new test files written before implementation — UT-086 (isValidSha256Hex+isValidNamespace 19 cases), UT-087 (CasStore.putBlobStream 7 cases: fake-Readable+fake-timer), UT-088 (seedManifestRef 4-way SEED_SOURCE_CONFLICT+MISSING_BLOBS+INVALID_SEED_SPEC ladder 8 cases), UT-089 (redact({name,value}[]) ‹secret:NAME› marker 9 cases), UT-090 (persist-only+double-redaction-exclusivity 6 cases), UT-091 (assertScriptIntegrity+SCRIPT_SHA_MISMATCH+SCRIPT_SHA_WITHOUT_SCRIPT ladder 9 cases), IT-075 (redact-sweep completeness: appendTranscript sink 3 cases), IT-076 (POST /assets/blob/:sha + POST /assets/manifest 9 cases), IT-077 (v14 schema drift-lock 11 cases), VAL-090 (REQ-081 blob-stream 5 cases), VAL-091 (REQ-082 seedManifestRef round-trip 5 cases), VAL-092 (REQ-083 redact-capture 3 cases LLM-gated), VAL-093 (REQ-084 asset_push schema honesty 4 cases), VAL-094 (REQ-085 scriptSha256 integrity 5 cases). RED confirmed: 50/50 unit fail; 21/23 integration fail (2 trivially pass); 15/24 acceptance fail (9 trivially pass). No broken-link gaps; no time bombs (FixedClock anchors only). trace: 699 items / 25 gaps / 0 orphan/broken-link (7 未真實驗證: REQ-012 pre-existing + REQ-080..085 real:false expected Gate 5; 8 未實作: REQ-081..085+TASK-080..084 expected Gate 5; 1 TDD: IMPL-082 pre-existing; 3 drift: UT-058/UT-064/IT-057 pre-existing; 5 task-IMPL: TASK-018+080..084 expected Gate 5). current_stage tests→impl. Prior: 2026-08-14 (v12 Gate 5 RED, REQ-073..080)." }
  impl:         { passed: true, note: "v27m Gate 8 RE-REVIEW #4 SEND-BACK REPAIR (2026-09-14, implementer, BF-7 — closed alongside the design-owned BF-8 in the same prior pass, attributed there as `designer` rather than dispatched separately): `passed` stays true (unchanged by this repair). Full detail is in gates.design.note and 06-impl-log.md IMPL-282 — `workflow.js`'s `paintSelected` (commit db12573: code + the KEEP-arm and first UNAVAILABLE-arm cases; 8f6eb2f: the second UNAVAILABLE-arm case) now applies DES-206's v27m (V)/(K)/(U)/(N)/(S)/(R) clause: paint memory (`state.paintedRunId`) written ONLY by an `ok` `/dag` reaching the painters; (K) bails before any DOM write when `state.selectedRunId` is unchanged since the last successful paint; (U) clears the svg/cell-layer/legend CHILDREN and paints the ONE `t(lang,'unavailable')` component (`strings.js` gains the key in BOTH `zh` and `en`) on a first paint or a run-switch; no `okBody(res, fallback)` helper introduced. IMPL-281's stale 「the sweep is clean now, all 16 sites are guarded」 sentence corrected in the same edit (d7aa533) — that IS the claim BF-7 falsifies. Re-run independently by THIS dispatch, not merely read from the log: `npx tsc --noEmit` exit 0; `RWE_REQUIRE_BROWSER=1 PUPPETEER_EXECUTABLE_PATH=.../linux-152.0.7977.75/chrome-linux64/chrome npx vitest run tests/acceptance/val-199-workflow-detail.test.ts` → 8/8; the five dashboard acceptance files (val-198..202) → 39/39; full `npx vitest run` → 403 files / 1 skipped, 2840 passed / 26 skipped / 0 failed, exit 0; `sh .sdlc/trace .sdlc/features/001-remote-workflow-engine --check` → 1667 items / 35 gaps, matching the pre-existing baseline (0 new gaps/orphans/broken links). `current_stage` stays `review`, NOT `verification` — this is a Gate 8 send-back repair, which routes back to the reviewer (gates.design.note's own routing rationale, and the v27l precedent), not forward through Gate 6.5+7. owner_decision sweep: 0 live `- **owner_decision:** pending` markers anywhere in the ledger. | PRIOR: v27k Gate 8 RE-REVIEW #2 SEND-BACK REPAIR (2026-09-13, implementer): the two impl-owned blocking findings (BF-1/BF-2) fixed, plus BF-3 (design-owned, dispatched to this same run rather than routed to a separate designer pass). BF-1 — src/dashboard/lib/connection.js:35's nextConnection returned prev.status on the FIRST unanimous-fail tick, so a page that was live kept claiming it for a full 3s interval where every route failed; fixed to consecutiveFails >= 2 ? 'offline' : 'degraded' (the counter still advances unchanged), UT-245's matching case renamed/flipped. BF-2 — ui/home.js:227 and ui/run.js:495 rendered a degraded {runs:[],degraded:'...'} body as data (empty card grid / empty swimlane over a live one) in the two views AC-4 (v27g) had not reached; both onTicks now bail at the top (same guard shape as ui/workflow.js:350) when the body is absent, carries degraded, or fails an Array.isArray shape check — one falsifying real-Chromium case added per view (val-198/val-200), both run for real (RWE_REQUIRE_BROWSER=1, cached Chromium), both green. BF-3 — 04-design.md:6815-6816 (DES-202 boundary/tests) described the reducer BF-1 replaces; re-stated in place, plus the now-stale 'inherits ARCH-124 owner_decision: pending' conditionals at :6817/:7274 (DEBT-C) closed inline (SETTLED — owner ruled KEEP THE NARROWING, 02-architecture.md:3365 answered since 7604c90); iter bumped to v27k, no new DES id, no trace-link change. See 06-impl-log.md IMPL-277/278 and journal.md's matching entry. `passed` stays true (unchanged by this repair). Full regression 2835/26/0 (+2 new cases over the RE-REVIEW #2 baseline of 2833), tsc clean, trace 1663/35 (+2 items for IMPL-277/278, 0 new gaps — same 35-gap SET as the RE-REVIEW #2 baseline: 0 high/orphan/broken-link/mock-only, 10 mid = the recorded REQ-137/138/139/142/143 out-of-closure pair, 25 low = 21 drift + 4 TASK 未實作). | PRIOR: v27g Gate 8 SEND-BACK REPAIR (2026-09-13, implementer): the 8 impl-owned findings (AC-1/AC-3b/AC-4/AC-5/AC-6/AC-7/AC-8/AC-9) fixed — see 06-impl-log.md IMPL-269..276 and journal.md's matching entry. `passed` stays true (it already was, from Gate 6.5+7); this repair does not re-derive it, per the same v26 GATE 8 SEND-BACK REPAIR precedent below. Full regression 2833/26/0 (+4 new cases), tsc clean, trace no new gaps (1659/33, same gap count as baseline). | PRIOR: v27: Gate 6 CLOSED ON EVIDENCE at Gate 6.5+7 (2026-09-12, verifier) — TASK-196..214 all status:done, IMPL-221..249 land the full REQ-131..141 dashboard rebuild; the verifier flips this bit (the implementer's own six Gate 6 passes never did) because every closure REQ now has a green VAL (05-tests.md) and the full regression + coverage gate both pass — see gates.verification.note for the complete Gate 6.5+7 record. | PRIOR: v27: NOT YET RUN for REQ-131..143 (Gate 1 passed 2026-09-11; awaiting /sdlc-run). | PRIOR(v26): v26 GATE 8 SEND-BACK REPAIR (2026-09-11, implementer): 11 impl-tagged blocking findings fixed and IMPL-198/201/203/205/212/213/216 amended in place (no new IMPL IDs, per this ledger's own IMPL-205 amendment precedent) — see 06-impl-log.md for the per-finding detail and state.yaml's top-level current_stage note for the summary. New tests: UT-228/229, IT-161..164, plus amendments to UT-173/IT-075/UT-202 (no new IDs there — extends the same describe blocks). Full regression 2641 passed / 0 failed / 26 pre-existing skips over 376 files; tsc clean; trace 1492/24 (0 new gaps vs Gate 8's own 1486/24 baseline). PRIOR: v26 GATE 6 CLOSED 2026-09-09 by the SINGLE FULL-SCOPE INTEGRATOR after the 22-way parallel phase (dcfc3a5). Handover state: 34 failed / 2453 passed, 18 failing files, 27 tsc errors, 45 clarifications. Closing state: npx vitest run -> 364 files / 2498 tests passed, 26 skipped (every skip a pre-existing env gate; none added), 0 failed; npx tsc --noEmit clean; sh .sdlc/trace -> 1422 items / 29 gaps (was 65 at Gate 5), 0 broken links, 0 orphans. IMPL-197..205 are the record (IMPL-205 and the IMPL-199/202/203/204 amendments were added by the integrator's own post-closeout review of section A3: the v1 fallback was doubling every COMPLETED grandfathered agent, the executor caps thread had no test that would go red if it broke, and five landed TASKs (175/184/185/187/188) had no IMPL row - trace did not flag them because it computes 'implemented' by REACHABILITY, so trace silence on a TASK is not evidence of implementation. Re-measured on a CLEAN tree at HEAD: 2503 passed / 0 failed / 26 pre-existing skips.) FOUR SEAMS that left REQ-125/126/127 INERT IN PRODUCTION with every unit test green (the composeConfig() wiring-bug class): (1) the admission price pin never reached AgentExecutor at either construction site, so every real call was unpriced; the pin was also keyed only over models the AUTHOR named, so a script with no explicit model pinned NOTHING - the implicit default alias is now covered. (2) guard.addUsage had no production caller, so assertBudget()s USD arm was dead code and the two cache columns never counted; setSpent widened to {usd,tokens} and the resume path folds with foldUsage, so a resumed run no longer enforces a different number than an uninterrupted one. GatewayResult.unmapped, transport and proxyModel now ride the PERSISTED usage event and deriveAgentRecords derives them back, so the live fold and the at-rest fold agree and DES-188s derived-equals-snapshot lock is true on a real-gateway run. (3) supported_parameters never reached ModelEntry, and - one hop further than reported - nothing ever filled GatewayClient.invokes caps? field, so wireEffort saw UNKNOWN_CAPS on every call. (4) the harness descriptor carried the rwe-proxy-* cloak in `model`, so record.model === record.proxyModel on every proxied call. RULINGS APPLIED: budget is USD (the five v25 tests rewritten against budget.tokens(), none deleted); ADR-048 (allowlist 4->6, workflow-catalog.ts joins on ADR-048s own argument - FLAGGED for Gate 8 to amend its Action line); registration-time v2 gating WIRED (diagram_contract=v2 is now a verified fact, not a stamp) with the blast radius MEASURED at every step 104->52->22->10->3->0 files; REQ-128s unowned prose written in the guide and on workflow_register.mermaid, AUTHORING.md regenerated. THREE REAL DEFECTS the v2 sweep exposed, each fixed in src/ not appeased in tests: checkMermaid could not express one label in two lanes (the guides own draft/critique/revise example); phase(fork:+tier) recorded the TRUNCATED literal as a lane title; switch was missing from DYNAMIC_OPENERS. REQ-124s v1FallbackGraph added - the dag route collapsed a derive refusal to an EMPTY overlay, which would have deleted every predicted cell from every pre-v26 workflows graph. REAL SMOKE (scratch engine, port 8911, workRoot /home/user/.local/share/rwe-v26-smoke, production rwe.service on 8899 untouched): a two-agent two-phase workflow through the managed LiteLLM proxy to ollama - agents carry phase/phaseIndex set, provider=ollama, model=qwen2.5:7b, proxyModel=rwe-proxy-local, transport=claude-agent-sdk, costUSD=0 with unpriced:false (a KNOWN zero, the discriminator that the pin reached the capture site); and a second anthropic-direct run priced NON-ZERO at costUSD=0.003011 for 2796 in + 43 out on claude-haiku-4-5, arithmetically exact, with the scripts own budget.spent() returning the same number. CARRY FORWARD: (a) REQ-128 as accepted makes every phase-less script UNREGISTRABLE from v26 on - existing VERSIONS are grandfathered but the next re-registration is refused AGENT_BEFORE_PHASE; (b) the deployed rwe.config.json still declares four openai aliases, which REQ-123/validateAliases now refuses at boot - a config migration, not a code gap; (c) UNDECIDABLE_SHAPE is an arm of DeriveResult with no producer; (d) additionalProperties:false on run_start.seed.items is deliberately NOT applied (it would replace IT-141s typed INVALID_SEED_SPEC with a schema INVALID_ARGUMENT). PRIOR: 2026-09-04 v24 GATE 6 CLOSED BY THE ROUND-2 VERIFIER (role crossing, declared) -- gates.impl.passed flips back TRUE. Round 1's send-back was never picked up by an implementer (no commit landed between a3b5d0a and this round), so rather than loop, the verifier fixed both send-back defects plus five more it found on the way, all mutation-checked: (a) ownerOf reads createdBy on both trigger stores + the webhooks.createdBy column TASK-142 owed; (b) authorize()'s subject fallback for the moded key:null rows; (c) workflowRegister's ownerless-trigger escape; (d) the unwired resolveMcp seam; (e) resolveScheduleTarget reading ownerOf as the claim; (f) schedule_create born disabled from its own advertised shape; (g) run_result's missing adminCrossRead. IMPL-184 is the record, IMPL-185 backfills TASK-134 (NINTH occurrence of the ledger-honesty gap -- src/path-verdict.ts shipped at 2912c05 with no entry, and its dod's >=30-row floor was 15; both closed), IMPL-186 is the test-only coverage pass. TASK-133's dod (>=40 generated rows + the completeness pin) was met by round 1; 28 v24 TASK rows now read done, TASK-153 stays open by design (external client plugin). Full suite 2113/2139 green, 0 fail, tsc clean. PRIOR: 2026-09-04 v24 GATE 6.5+7 ROUND 1 (verifier) SENDS GATE 6 BACK — gates.impl.passed flips FALSE. TWO AUTHORIZATION DEFECTS, both proven RED by a rewritten IT-105 against REAL stores (tests/integration/authz-owner-lookup.test.ts, 6 of 11 cases red, 5 green): (a) TRIGGER OWNERSHIP READS THE WRONG COLUMN — `SqliteSchedulerPort.ownerOf` (scheduler.ts:440) returns `claimedBy` and `WebhookRegistry.ownerOf` (webhook-registry.ts:211) returns `workflow`, i.e. the CLAIMING WORKFLOW NAME, where DES-139 (`schedules.createdBy`/`webhooks.createdBy`) and DES-149 step 2 (`createdBy === p.id`) require the CREATING PRINCIPAL; `owner-lookup.ts`'s own header already says `createdBy`. Consequence with auth on: an unclaimed schedule reads `null` -> admin-only, so its creator cannot schedule_delete/schedule_setEnabled it; a claimed one reads 'wf-a' compared against 'alice@x.com' -> NOT_TRIGGER_OWNER. `webhooks` has NO createdBy column at all (TASK-142 half-done; call-tool.ts's own comment records it), so every webhook is ownerless. The SAME `_triggerOwner` shape is used by mcp-facade.ts:251 for workflowRegister step 2, and `grep -rn 'TRIGGER_ALREADY_CLAIMED|NOT_TRIGGER_OWNER' tests/` returns NOTHING — that refusal arm (mcp-facade.ts:293-296) is reached by no test at all, which is why the defect survived Gate 6. (b) THE MODED workspace_* ROWS NEVER RUN THEIR OWNERSHIP CHECK — `workspace_list`/`workspace_delete`/`workspace_push` carry `key: null` in TOOL_SPECS while their resolved rows declare ownership:'run'/'workflow'; authorize() computes `subject = spec.key !== null ? args[spec.key] : undefined`, the real lookup answers `undefined` ('does not exist'), and DES-139's own non-leak rule then returns OK — a non-owner passes. Confirmed against the REAL TOOL_SPECS rows and a real SQLite store (better-sqlite3 binds undefined as NULL and returns no row, so this is a silent BYPASS, not a crash). Invisible to every existing test because they all run auth-disabled, which short-circuits before the lookup. `authz.ts` itself is NOT at fault: UT-140 now carries a 50-row generated matrix over kind x role x ownership x mode plus the `cases.length` completeness pin TASK-133's dod always required, and all 50 expected verdicts — written from DES-139's text BEFORE running the code — matched (59/59 green). ALSO OWED BY GATE 6: TASK-133's dod was not met as shipped (8 hand-written unit cases, not >=40 generated; IT-105's three cases asserted only `typeof lookup.runOwner === 'function'` — the verifier filled both this round); TASK-142's webhook `createdBy` column; the per-function coverage bar on the v24 delta (28 long offenders remain, list in gates.verification.note); solid_check's 1 HIGH (ARCH-103's file-level `module:` nested inside ARCH-069's `src/gateway` reads as an undeclared cross-module dep — an architecture-declaration fix); `asset-sync.ts`'s exported `resolveMcp` (DES-153) has ZERO production callers while server.ts:1363 re-implements the same rule inline; and all 29 v24 TASK rows still read `status: draft`. FIVE IMPL entries were BACKFILLED by this verifier (EIGHTH occurrence of the ledger-honesty gap): IMPL-179..183 claim TASK-133/139/140/154/156, which shipped at 2912c05/ba46e3b/80f9c65 with no entry and read `未實作` in trace. TASK-153 is external (client plugin) and correctly unclaimed. PRIOR: 2026-09-03 v23 GATE 6 RE-ENTRY (send-back 41e6382, implementer, this round): both test_defects reported last round are now closed by the Gate 5 RE-ENTRY (verifier, adjudication #8, commit 9fc4439) — UT-125's assertion (b) re-pointed from the DB row to the journal's cause:'script_unresolved' line (a test-oracle fix, not a code gap; the code's behaviour was already correct), UT-135's queue_full fixture (two distinct names) already fixed at Gate 6.5+7 round 4, both re-confirmed directly this round: npx vitest run tests/unit/graph-analyzer.test.ts -> 43/43 pass (UT-124..137 all green); npx vitest run tests/integration/graph-analyzer-composition-root.test.ts -t IT-104 -> pass; full suite npx vitest run -> 283 files / 1850 tests, 0 failed, exit 0; npx tsc --noEmit clean. No code change needed this round — src/graph-analyzer.ts and server.ts's mirror-row warn were already correct (IMPL-177, landed at 9fc4439 despite that commit's docs(v23) subject); only the ledger needed closing: IMPL-177 amended in place (greens extended to UT-125/UT-135, 13/13 in-scope items now green; the 'commit: uncommitted at report time' field corrected to 9fc4439, now committed; the 'two reported test defects block full green' closing paragraph replaced with a RESOLVED note citing this confirmation). gates.impl.passed flips true; current_stage -> verification (Gate 6.5+7 owns the 05-tests.md status-flip/regression-closeout pass, per the verifier's own Mode-A note — this round did not touch 05-tests.md's UT-129..137/IT-104 status lines for that reason). PRIOR: 2026-09-03 v23 GATE 6 (send-back 41e6382, this round, implementer): IMPL-177 lands the settle choke point (_settle), the closure-level try/catch/finally (inv 2 FLOOR 2a/2b, oracles O1/O2/O4), the AnalyzerCause closed union + cause value domain (R-2(d)), O5's relational fix (R-2b), the attempts/12-key journal fields, and R-3's _startJob choke point (guard-consolidation, done as the required separate step AFTER inv 2). 11/13 in-scope items green (UT-126/127/128/129/130/131/132/133/134/136/137, IT-103/104); full regression 1848/1850 green, 0 newly broken, tsc clean. TWO items NOT green, reported as test_defects (not fudged): UT-125's restored assertion (b) — putDiagramResult silently no-ops per DES-130 B6's late-write guard whenever no workflow_versions row exists for (name,version), which is unconditionally true for ANY script_unresolved/orphan fixture (that is the only way resolve() throws CatalogNotFoundError), so 'the row settles to unavailable' is structurally unsatisfiable under B6 as currently specified — a Gate 2 conflict between ARCH-079 R-1's oracle enumeration and DES-130 B6, not a Gate 6 code gap (assertions 1/3/4 hold; the journal line with cause:'script_unresolved' does fire). UT-135's queue_full case reuses the SAME name for both enqueue() calls, so the second call is silently absorbed by the single-flight _pendingKeys guard (DES-127 B4) before ever reaching the queue-full check — confirmed against the already-green UT-128 QUEUE_FULL fixture (lines 533-548), which correctly uses two distinct names. gates.impl stays false and current_stage stays impl pending the Gate 5 fix + re-run. PRIOR: 2026-09-03 v23 Gate 6 CLOSEOUT ROUND 4 (backfilled by the Gate 6.5+7 verifier - SEVENTH occurrence of this ledger-honesty gap, after IMPL-149..156/157/158/159..172/173/174): the implementer shipped TASK-128/129/130's code at a39c0e7 with ZERO IMPL entries and left all three tasks reading draft. Backfilled as IMPL-175 (git-show-attributed, verified against source). AND the subject overstated the delta: it claims 'A1/A2/A3/A4/A5/A10 landed' but A4 (delete renderMiniPreviewAsync + its call site, re-point UT-116) had not shipped at all - closed by the verifier as IMPL-176 and declared, not absorbed. gates.impl.passed stays true. Prior: 2026-09-03 v23 Gate 6 CLOSEOUT (backfilled by the Gate 6.5+7 verifier - FOURTH occurrence of this same ledger-honesty gap, after IMPL-149..156/157/158): the implementer shipped TASK-113..123/125/126/127 across ebd530d (the bulk), 277a8d9 (subject-labelled docs(v23) but carrying continuation-store/scheduler/sqlite-run-store/mcp-facade/server - TASK-126s composition root), 6447aa2 (allowlist widened to four files, mini-preview anchor, regenerate principal) and 3185c39 (TASK-127s zero-config fail-closed guard + the UT-111 FIXTURE correction), leaving the tree 1792/1792 green and tsc clean - but wrote ZERO IMPL entries and left current_stage/05-tests.md reading impl/red. Backfilled as IMPL-159..172 in 06-impl-log.md (git-show-attributed, verified against source, never from a commit subject). TASK-124 deliberately gets no IMPL entry - it is REQ-104s Gate 7.5 real run, the validators work. gates.impl.passed stays true. Prior: 2026-09-02 v22 Gate 6 SEND-BACK CLOSEOUT ROUND 2 (backfilled by the Gate 6.5+7 verifier, third occurrence of this same ledger-honesty gap) — the implementer shipped B1 (H1 residual: workflow_register/deregister still accepted a self-asserted args.principal under authEnabled) + B2 (workflow_publish's round-1 over-fix, dropping args.principal unconditionally instead of gated on authEnabled) across ea97bc8 (fix, self-labeled 'wip'/'TREE IS RED' this time — an accurate checkpoint, but still not a completed-gate record) and 7bfdc3c (IT-080 test migration off the H1 vulnerability its own topology relied on), verified against source directly, but again wrote no IMPL-* entry and left current_stage/05-tests.md's IT-095/VAL-107 reading red/Gate-6-next. Backfilled as IMPL-158 in 06-impl-log.md (git-show-attributed). Full detail: 06-impl-log.md IMPL-158, 05-tests.md's IT-095/VAL-107/VAL-097 entries, gates.verification note below. gates.impl.passed stays true. Prior: 2026-09-02 v22 Gate 6 SEND-BACK CLOSEOUT (backfilled by the Gate 6.5+7 verifier, same ledger-honesty gap as below) — the implementer actually shipped all 4 HIGH fixes (H1 PRINCIPAL_REQUIRED, H2 DAG masking, H3 MAX-based version allocator, H4 both sites) across commits 330eefd (mislabeled 'docs' in its subject — see that commit's own trailer) and 2e865e8, verified against source directly, but again wrote no IMPL-* entry and left current_stage/05-tests.md's 4 send-back items reading red/Gate-6-next. Backfilled as IMPL-157 in 06-impl-log.md (git-show-attributed). Full detail: 06-impl-log.md IMPL-157, 05-tests.md's IT-091/092/093/UT-106/IT-094 (new) entries. gates.impl.passed stays true. Prior: 2026-09-02 v22 Gate 6 (TASK-105..112) — ACTUALLY SHIPPED 2026-09-02 across a54a794/fd19710/ccee745/1ffca37 (+f4aba7d/23a5fd9 for the N-1 MCP-surfacing follow-up), full suite 1660/1660 per 1ffca37's own subject line, but the implementer wrote ZERO IMPL-* entries and never flipped current_stage/gates.impl — this note itself was still the stale v21 one until the Gate 6.5+7 verifier pass backfilled IMPL-149..156 (git-show-attributed, per the IMPL-142/145 retroactive-recording precedent) and corrected this field. Full record: 06-impl-log.md IMPL-149..156, gates.verification note below, 05-tests.md's 'Gate 6.5+7 regression confirmation'. Prior (v21, superseded but preserved for history): 2026-09-01 v21 GATE 6 RE-RUN #7 RIDERS HALF (implementer) — the LAST code change of iteration v21, closing the final two items of 07-review.md GATE 8 RE-REVIEW #6 §T5/§T6 after P6-1/P6-2/P6-3/P6-4/QD-REP-1 landed at c5e5cf5 (pre-pass suite re-verified 1578/1578 first). Both took the FIX route per adjudication #10 J-2. P6-5: ONE exported DEFAULT_CEILINGS in src/params/contract.ts, imported by run-manager.ts, mcp-facade.ts and server.ts's production composition root (which used to re-type all three numbers and import neither other copy); per-key fallback preserved; net -4 lines of code; zero behavior change; two prose comments that re-typed the same values also corrected. Pinned STRUCTURALLY by 4 cases extending UT-098 in place (one-declaration, three-importers, no-literal-in-src, plus a value case whose oracle is DEPLOY §1b + ADR-005 rather than the code). The pin was MUTATION-CHECKED, and the check mattered: re-typing one fallback at server.ts (?? 600_000 — the exact shape P6-5 was filed about) silently PASSED the first draft, whose regex only looked directly after the colon; widened to read the whole value expression, after which all three mutations go red (literal → (c); dropped import + three literals → (b)+(c); a fourth declaration → (a)+(b)+(c)) and the restored tree is 75/75 green. QD-CONS-3: claims re-verified against workflow-catalog.ts:160-194 before writing; 02-architecture.md:967 and DEPLOY.md's maxTimeoutMs row now name BOTH rungs with script per-call agent() opts as the one surviving exclusion; a THIRD stale surface the review did not list (02-architecture.md:976, the Gate-2 ADR-005 rationale bullet, same false 'user-override rung only' claim) was found, fixed and DISCLOSED with the S-2 bracketed AMENDED marker. DEPLOY.md's other two ceiling rows left alone — incomplete but not false; v22 doc-completeness. IMPL-147 is the record. Suite 1582 passed / 0 failed (243 files, +4 = exactly this pass's cases); tsc clean; trace 824 items (+1) / 14 gaps, gap set byte-identical to a pre-edit scratchpad snapshot; no new iter-drift pair (TASK-100/DES-104 are both v21-origin). No test weakened, deleted or re-scoped. Working tree, deliberately uncommitted; gates.review.passed stays FALSE — pass 7 owns that flip. Standing flag for validation ROUND 5: src/ moved after ROUND 4's evidence and server.ts's composition root is on every REQ's path; provably behavior-neutral, but a fresh boot should still be confirmed to report the documented ceilings. Prior: 2026-09-01 v21 GATE 6 CLOSEOUT #6 (integrator, cross-task scope) —Gate 8 RE-REVIEW #5's §S7 send-back CLOSED end to end; IMPL-145 + IMPL-146 in 06-impl-log.md are the record. Suite 1559 passed / 0 failed (243 files), tsc clean, trace 823 items / 14 gaps. CODE half (F2 admission refusal + resume-side durable half, F1 half 1, F4, F5 decided-and-documented, C-3 schema + drift-lock) rode commit 2e58d86, recorded retroactively as IMPL-145 — it reached HEAD with no ledger entry, the FIFTH instance of this iteration's race class; attribution settled from git show --stat (one commit carrying every ownerless chunk the dispatch listed: run-manager.ts, contract.ts, harness-defaults.ts, server.ts, github/issue-reporter.ts, schema-drift-v15.test.ts), so ONE entry claims it and the neighbouring 976249c (Gate 5 RED, already recorded in 05-tests.md) and c0e6cec (doc-only) are NOT re-logged. §S7's F1 half 2 (workflow-catalog.ts call reorder) RETIRED as superseded-by-measurement rather than applied: measured against a real WorkflowCatalog, the production composition already refuses 6 of 6 probe shapes with nothing stored, the set the reorder would newly cover is empty by construction, and applying it verbatim changed zero accept/reject decisions while flipping every params-origin rejection off the origin-keyed code §S7 itself said to preserve. 3 green pins added to IT-081 so the decision is test-locked. The ceilings-less-catalog residual is S-1 verbatim (recorded debt, on §S7's NOT-in-scope list). DOC BATCH in the same pass (H-4 precedent): F3 nesting-residual rewrite (wider AND introduced-by-v21, both verified in code and at the v20 tip; 04-design.md ×2 + a new ARCH-066 residual line), S-2 (ADR-005 + ARCH-066 inv-4 corrected to the two rungs; lowered-ceiling asymmetry measured on two servers over one workRoot, then declared INTENDED with its repair path and a v22 boot-sweep candidate), C-3 doc half (review misattributed it to DES-098 — the anchor is under DES-099; corrected DES-099 + DES-100 + the v15 class diagram, misattribution recorded). Gap set grew 11→14 and is disclosed: all 3 are the declared false-positive iter-drift class (v15 DES items traced by v21 IMPL entries — iter: records origin, not last touch), 0 new classes, 0 high/未驗證/未真實驗證/orphan. No test weakened, deleted or re-scoped; no src/ file touched by this pass. Changes in the working tree, uncommitted. Prior: 2026-09-01 v21 GATE 6 CLOSEOUT #5 (integrator, cross-task scope) — Gate 8 RE-REVIEW #4's §Q7 send-back CLOSED end to end; IMPL-142 + IMPL-143 in 06-impl-log.md are the record. Suite 1533 passed / 0 failed (243 files), tsc clean, trace 820 items / 11 gaps with the gap SET byte-identical to §Q2's list (the 2 new items are the 2 new IMPL entries; both trace only v21 TASK/DES ids so neither adds an iter-drift pair). CODE half (A1 both halves, A2, A4, A5) rode commit 43042d3 — attribution settled from git log rather than from the two implementers who both reported a concurrent writer: 43042d3 is the only src commit after the Gate 5 RED commit 92667d7, a single 99-line contract.ts diff carrying all four fixes, so ONE entry (IMPL-143) claims it. A1: parseParamContract now rejects a non-literal type / non-array enum / non-numeric min-max typed with nothing stored, AND the read path is made TOTAL over an already-poisoned stored row (boundEffort falls back to ALL_EFFORTS, boundMax to Infinity — propagating NaN would have served null on workflow_get and been inert at admission, a ceiling bypass dressed as a display bug); adjudication #8 H-1's escape of verifying no deployed row is poisoned was DECLINED as instructed. A2: truncatedSupplied is one O(n) Buffer slice instead of a per-char trim-and-remeasure loop (O(n squared): 611ms at 100k, 2396ms at 200k, ~70min event-loop block from one 8 MiB overrides.model upstream of every admission limit) — with the UTF-8 continuation-byte back-off preserved, since a naive subarray(0,64) overshoots the 64-byte cap on multibyte input. A4: string min/max are a UTF-8 BYTE-LENGTH bound (H-3's pinned semantics), replacing NaN-inert comparisons; appendPrompt keeps its by-size-never-by-content carve-out. A5: effectiveBounds now narrows all THREE ceilings — maxAppendPromptBytes was enforced at admission and advertised nowhere. DOC BATCH landed in the SAME pass per H-4 (unpinned scope is how F-3/F-4 dangled through two closeouts): A3, A6 second half (ARCH-068's tri-state note, flagged by an implementer as outside their partition), A7, A8/O-1, A9/C-1, A10/O-2, A11, A12/R-1 incl. the debt-entry upgrade, O-3, C-2/F-3, R-2 (verified already closed by 43042d3, no edit) — every §Q7 item closed; S-1 stays recorded debt by design. LEDGER HONESTY, the class this review caught three times: IMPL-141 declared P-A6 not done when it had shipped ~2h earlier in 244f9f0 (MARKER_PREFIX + exported hasSecretMarker, called at run-manager.ts:538) — corrected in place with the evidence rather than quietly rewritten, and IMPL-142 now records 244f9f0 and eb58d96, two more commits that landed src with no IMPL entry (third and fourth instances after 35e6994). A12 could not take the cheaper true fix: deleting the wrong resolve.ts mapEffort copy needs tests/unit/params-resolve.test.ts re-pointed, and a doc-only pass may not weaken a test — the debt entry is upgraded from duplicate to WRONG instead, on both ARCH-065 and DES-102. No file under src/ or tests/ was modified by this pass. Prior: 2026-09-01 v21 GATE 6 CLOSEOUT #4 (integrator, cross-task scope) — adjudication #6 F-1/F-2 + adjudication #7 items 1-4, G-1 and G-2 CLOSED; suite 1517/1517 green (243 files, 0 failures), tsc clean, trace 818 items / 11 gaps with the gap SET UNCHANGED. IMPL-141 in 06-impl-log.md is the record, and it also covers the F-1/F-2 half that commit 35e6994 landed with no IMPL entry of its own (KNOWN_KEYS + shape checks for effort/appendPrompt reusing EFFORT_RANK; the adjudication-#5 unappliable-knob rejection removed while A-2(b)'s violatesOwnSpec self-consistency check stays; Ceilings threaded server.ts -> WorkflowCatalogOpts -> register() from the SAME object RunManager and McpFacade get; F-2's one shared bounds predicate, checkValueAgainstSpec, with no pinned error shape touched). Landed this pass: G-1's red case AND fix in one pre-authorized pass — a caller-supplied defaults.effort/appendPrompt with NO params block was ceiling-checked nowhere (register-time check ran only over DECLARED knobs; validateHarnessDefaults holds no ceilings; admission's validateUserOverrides walks the caller's overrides, never the registered defaults), closed by ONE pass over the FINAL effectiveDefaults bounded by effectiveBounds(canonicalContract(), ceilings) through the shared predicate — deliberately NOT a third hand-rolled EFFORT_RANK comparison — with the rejection code following the value's origin (caller defaults -> HARNESS_DEFAULTS_INVALID, declared knob default -> PARAM_CONTRACT_INVALID, neither pinned by any existing assertion); item 4's redundant AuthorDefaults intersection+cast deleted from resolve.ts; G-2 audited — both surviving adjudication-#5 citations are historical SUPERSEDED notes describing the live widen behaviour, so both KEPT. One Gate 5 test defect fixed by literals only and declared in 05-tests.md + IMPL-141 (IT-081 case (e) demanded an over-ceiling effort default on the default-ceiling shared fixture, contradicting the dedicated maxEffort:'low' ceiling pair; no assertion weakened, removed or re-scoped). F-3 (DES-102 prose) and F-4 (P-A6 secret-marker grammar + the doc batch) are adjudication #6 integrator items OUTSIDE this dispatch's scope — routed, not dropped. Prior: 2026-09-01 v21 GATE 8 RE-REVIEW CLOSEOUT — Gate 6 re-run #2 GREEN, all ten findings R-G1..R-G10 of 07-review.md §R2 closed (integrator, cross-task scope again: the five remaining items each lived in a file no single TASK owned, so every parallel implementer correctly refused to cross its partition). IMPL-140 in 06-impl-log.md is the record; IMPL-139 (the FIRST send-back closeout, findings B1..B5) is retained above it with two supersession markers, because R-G1 deleted the very mechanism its B2 shipped. Inherited green from commit f8bb366: R-G1 (HIGH security regression — unredactBestEffort DELETED, not fixed; it could not tell an engine-written ‹secret:NAME› marker from a caller who merely typed the public marker grammar into appendPrompt, so resume dereferenced a secret for an attacker who never held it; resume is now REFUSAL-ONLY via typed PARAM_SECRET_UNAVAILABLE, which subsumes R-G4 rotated-secret drift and R-G5 duplicated marker grammar), R-G2 (HIGH — the EFFECTIVE post-merge model is now alias-checked after mergeRunParams and before createRun, closing the stale registered-default hole B1 only half-closed) and R-G3 (MED — admission and dispatch now read the same alias table; the control was inert on the documented default deployment). Landed in this pass: R-G9 — the 4KB harness prompt cap RELOCATED out of redactHarness into the exported capPrompt and applied at the one onHarness persist site AFTER redact(), because capping first could cut a secret across a 2048-char seam and redact() is a value-EXACT substring match, so neither fragment matched and partial credential bytes reached disk; the gateways hold no SecretValueProvider (verified) so redaction cannot move upstream, and capPrompt is applied UNCONDITIONALLY (outside the provider branch) since the cap is a size bound, not a security control. R-G10 — both kind!=='harness' redaction-skip guards deleted after checking they cannot violate DES-088 invariant (a): a harness descriptor persists through onHarness, a DIFFERENT site with its own single redact(), so no event can receive two passes; deletion is strictly fail-safe and strictly less code. R-G6/G7/G8 doc amendments written to the END state (R-G6 was authored against the restore mechanism R-G1 then deleted, so the docs describe refusal-only resume and never the intermediate): ARCH-064's api line now carries the real five-code Err union from contract.ts:47 (adding PARAM_CONTRACT_INVALID and UNKNOWN_ALIAS — both previously ZERO-hit in the ARCH/DES layer) plus isKnownAlias; ARCH-056's sink enumeration retracts the false 'kind:harness is already redacted' premise and adds the harness sink; ARCH-066 inv-2/inv-5 and the workflow_resume interface row gain the refusal-only read direction and PARAM_SECRET_UNAVAILABLE; DES-088 invariant (a) is RESTATED from kind-based 'double-redaction exclusivity' (false — redactHarness was never a redactor) to positional 'one redaction pass per persisted event'; DES-104's resume clause and DES-066's cap location follow. ONE test relocated with its assertions intact (UT-070's cap case retargeted at capPrompt at unchanged strength — declared, not pre-authorized by any adjudication) and THREE added, including a direct R-G9 seam pin that fails on the pre-fix ordering; no test was weakened, deleted or re-scoped. NOT done, declared: the three quality-lens LOW doc-drift items (QD-OBS-1/QD-CONS-1/QD-CONS-2) are outside §R2's pinned scope, which names only R-G6/G7/G8 for docs — left for the next reviewer to route rather than silently widened. Verification: npx tsc --noEmit clean; full suite 1499/1499, 243 files, 0 test failures (baseline 1496/1496; +3 = the new cases). vitest EXITS 1 despite every test passing, on the single uncaught spawn-litellm-ENOENT background error — verified PRE-EXISTING, not assumed: with this pass's changes stashed, params-admission.test.ts alone already reports 14 passed / 1 error / exit 1 at the unmodified baseline; no litellm binary is on PATH here and nothing in this pass touches proxy spawning. An earlier reading of this run said 'exit 0' — that was a | tail pipeline's status, not vitest's. sh .sdlc/trace: 817 items (was 816, +1 = IMPL-140) and 11 gaps (was 9). THE GAP SET CHANGED BY EXACTLY TWO, both declared: DES-088(v14) and DES-066(v11) now read as 'lagging' IMPL-140(v21) under the tool's cross-iteration iter-drift heuristic. Both are false positives in substance — those two designs were amended IN THIS PASS (that is R-G8/R-G9's whole content); what lags is the iter: field, which by house convention records ORIGIN iteration, not last-touched. Same accepted class as the baseline's DES-088(v14)-lags-IMPL-127(v15) pair, whose iter: was likewise not bumped. Rejected: dropping the two DES ids from IMPL-140's traces: (a lie told to silence a warning) and bumping their iter: to v21 (falsifies v14/v11 origin history). No new gap CLASS; the 9 pre-existing gaps are untouched." }
  verification: { passed: true, note: "v27 GATE 6.5+7 (2026-09-12, verifier) — PASSED. Full record (simplify/regression/coverage/trace/solid_check/determinism_check/TZ-travel/seam-wiring/smoke) is in current_stage's own note above (this gate's summary line) and in 06-impl-log.md IMPL-247/248/249. | PRIOR: v27: NOT YET RUN for REQ-131..143 (Gate 1 passed 2026-09-11; awaiting /sdlc-run). | PRIOR(v26): v26 GATE 6.5+7 (2026-09-09, verifier) PASSED. (0) SIMPLIFY (merged Gate 6.5), scope `git diff ce2b10a..HEAD -- src` (35 files, +3501/-595): the Skill was RUN; the Agent fan-out is unavailable in this context so it was a SINGLE-PASS inline review across all four angles, stated not glossed. TWO fixes, quality-only, zero behaviour change, suite green after. (a) `ExpectedGraph` had THREE declarations of ONE contract (`skeleton-graph.ts`, `dashboard.ts`, `check-mermaid.ts`); `dashboard.ts`'s copy carried its OWN note 'replace with a real import once skeleton-graph.ts ships' and it had shipped, so dashboard now imports + re-exports the canonical types (type-only, every existing importer unaffected). ARCH-113's whole point is 'one derivation, two consumers' -- a second declaration of the shape they JOIN ON is the same drift risk one layer down, hidden by structural typing until a field diverges. `check-mermaid.ts`'s copy is DELIBERATELY kept: UT-115/ADR-022's `skeleton` allowlist excludes that file and even a type import would put the word in its source. (b) `server.ts`'s /dag route dropped the dynamic-import + optional-member-cast + presence-guard shim whose own comment justified it as 'TASK-185 not landed yet'; a sibling module already imported it statically. The try/catch is KEPT -- it is what makes the never-500-over-a-dashboard-read contract hold for a parse fault, not merely for a missing module. FIVE candidates NAMED AND REJECTED: the duplicated `UNKNOWN_CAPS` literal (a new cross-module edge for one three-field literal tsc already keeps in sync); `foldUsage` vs `foldUsageFromRecords` (a documented live/at-rest PAIR over different inputs with different unpriced rules -- the design's lock is that they AGREE, not that they are one function); run-manager's two three-line validateSeedSpec blocks; `v1FallbackGraph`'s unused `nodes` parameter (signature-parallel with deriveExpectedGraph at the one call site -- FLAGGED, not churned); authoring-guide's example corpus (already factored behind stadiumNode/agentSpec). IMPL-206 is the record. (1) REGRESSION: `npx vitest run` 367 files / 2534 passed / 0 failed / 26 pre-existing skips, exit 0; `npx tsc --noEmit` clean. All 52 v26 red items flipped green in 05-tests.md, each matched against its own named `File:` in the run's per-file result -- none flipped on faith; the dated Gate-5 red-reason prose is left VERBATIM. The SIX blocked items are UNCHANGED and named: VAL-172 (needs a real revoked OpenRouter key + `ps aux` on a real CLI child), VAL-173 (three provider runs on the DEPLOYED box + the ADR-042 deploy-order check), VAL-175 (a real OpenRouter key), VAL-176 (a real --detailed_debug LiteLLM capture), VAL-178/VAL-180 (REQ-117's cold-model protocol, which DISQUALIFIES this verifier by its own text) -- the v24 VAL-124/128 precedent, six instead of two because this iteration's REQs are external-dependency-heavy. VAL-174/177/178 gained an `amended` bullet each carrying this gate's REAL evidence, explicitly marked NOT the acceptance clause. (1b) COVERAGE (@vitest/coverage-v8@1.6.1, --no-save, coverage/ gitignored), measured AFTER the simplify pass and the fills: overall lines 96.15% (20902/21738), functions 95.84%, branches 89.23% -- above the 90% bar and above v24 round 2's 95.53%. The PER-FUNCTION bar over the v26 delta (160 of 771 functions) went 17 long offenders -> 6, with 0 short offenders throughout. EIGHT new items (UT-205/206/208..212, IT-156; no UT-207 -- that draft became IT-156 and the number is left unused) took eleven functions to the bar by WRITING the tests, several load-bearing rather than merely uncovered: the `unmappedMessages` counting loop was unreached in BOTH Sigma-folds, i.e. the ONE column IMPL-198's reconciliation exists for had no test and either fold could have returned {} forever; `checkLanes`' lane-COUNT and lane-TITLE refusals were dead, so a diagram with the wrong number of swimlanes could have registered as conformant; `materializeSeed`'s defence-in-depth throw is the exact line that replaced issue #64's `contentB64 ?? ''`. The SIX that remain each carry a one-line rationale in 05-tests.md and none is 'we chose not to test it': child-entry.ts (0/153) and main.ts's `main`/`runCheckConfig` (0%) run in a SPAWNED CHILD PROCESS the parent's v8 instrumentation cannot see -- --check-config IS covered, by IT-143, which drives a real `tsx src/main.ts --check-config` child, so they are tested at a HIGHER tier than coverage can observe; server.ts `createServer` 86.1% and main.ts `composeConfig` 88.9% need real external deps (unchanged v24 disposition); workflow-catalog's member initializer 92.5% is still mostly the three ZERO-caller workflow_diagrams accessors -- a DELETION, not a test gap. Whole tree for context: 72 long + 7 short offenders, none of the 7 short inside the v26 delta. No bar was lowered and no test was weakened, deleted or re-scoped. (1c) MODULE GATE: DORMANT -- `module_check --selftest --matrix` reports no ARCH declares `build:` (0 of 58 module rows). (2) NO REMAINING RED. (3) `sh .sdlc/trace --check` exit 1 (measured with output redirected, NOT read off a pipeline's status -- the v21 mistake): 1432 items / 29 gaps, the gap SET byte-identical to Gate 6's. 10 HIGH are all `not-real-verified` for REQ-121..130, STRUCTURAL before Gate 7.5 flips real:true and unavoidable by construction at any verification round preceding this iteration's first validation run; 1 MID (IMPL-082 TDD, pre-existing since v11); 18 LOW (16 iter-drift + TASK-018 + TASK-153-external). 0 broken links, 0 orphans, 0 NEW gaps, 0 new gap classes. (3b) solid_check: 58 modules, 0 HIGH / 0 mid / 10 low (unclaimed-file warnings), unchanged by the static import -- its JS import regex already matched the dynamic form, so the declared dependency set did not move. (4) DETERMINISM: exit 0 -- and the honest version is that it did NOT reproduce v24 round 2's claimed 'exit 0'. At plugin 2.4.3 it exits 1 with 18 hits, and 14 of those are PRE-EXISTING at ce2b10a (verified by running it against a `git archive` extract of that commit, never a checkout); the 4 NEW ones are string literals in `guards.ts`'s own guard TABLE, i.e. in the file whose job is BLOCKING those APIs. Thirteen comments/refusal-message literals now carry `det:allow` with the reason they are not calls; three are deliberate real time (the audit row's `ts`, the litellm subprocess READINESS POLL an injected clock would never advance, and the legacy-temp-dir sweep compared against filesystem mtimes). The remaining TWO were NOT allowed, because the contract forbids det:allow on time used for a DECISION: auth-service computed the OAuth `expires_in` as `(expiresAt - Date.now())/1000` where `expiresAt` came from TokenStore's INJECTED clock -- two different clocks, nonsense under any fake clock and a second short under the real one -- and the subtraction was redundant besides, since `issue()` stamps `expiresAt = now + bearerTtlMs`. Both sites now say `bearerTtlMs / 1000`, with no clock read at all. DECLARED: that is a verifier-applied CODE change in a module the v26 delta never touched, taken because the alternative was to det:allow a decision the contract forbids. (5) TIME-TRAVEL: `TZ='Pacific/Kiritimati' npx vitest run` -> 367 files / 2534 passed / 0 failed / 26 skipped, exit 0 -- compared test-by-test against the normal-clock run: ZERO tests flipped in either direction. (No faketime binary in this sandbox; the documented install-free fallback.) (6) SEAM WIRING: PASS. The four seams Gate 6 named as having left REQ-125/126/127 inert were re-verified to have PRODUCTION callers, not only tests: `priceBook` reaches AgentExecutor at BOTH construction sites (run-manager 674 start / 965 resume); `guard.addUsage` is called at agent-executor:330; `caps` is filled by `_pinnedCapsFor` and threaded onto `gateway.invoke` at agent-executor:657; the harness descriptor carries `modelName: resolvedModel` with the cloak beside it as `proxyModel`. NEW v26 seam checked the same way: `ModelBook` is built in the composition root over the REAL `buildModelCatalog` (server.ts:724) and passed to RunManager (725) -- run-manager's own `async () => []` default is the test-only fallback. Every other seam is production-defaulted (`queryImpl ?? sdkQuery`, `gateway ?? new LiteLLMGatewayClient`, `authorize ?? realAuthorize`, `proxyManager ?? new LiteLLMProxyManager`). (7) REAL-DEPENDENCY SMOKE: RUN. A scratch engine on 127.0.0.1:8917 with its own workRoot (production rwe.service on 8899 untouched -- `NRestarts=0`, `ExecMainStartTimestamp` unchanged, verified after the fact) booted the REAL main.ts, registered a v2 phase+LR-swimlane workflow, published it, and ran two agents against REAL Ollama (qwen2.5:7b) over direct-fetch. Every v26 record field is real: provider 'ollama', model 'qwen2.5:7b' (the RESOLVED id, not the alias), transport 'direct-fetch', phase/phaseIndex per agent, four token columns, costUSD 0 with unpriced FALSE (the known-zero discriminator that the pin reached the capture site), unmappedMessages {}. `GET /api/runs/<id>/dag` -- the exact route this gate's simplify change touched -- returned warnings [], one cell per agent in its own column, ZERO `__skel_` duplicates. A phase-less script was REFUSED AGENT_BEFORE_PHASE with line and label. `models_list` served the real catalog with the declared-capability columns. STILL MOCK-ONLY and left to Gate 7.5, listed as unverified risk: the LLM gateway through the MANAGED LiteLLM proxy and the claude-agent-sdk transport (no litellm on PATH in this shell; it lives in a pinned venv), GitHub issue_* (no credential), and the MCP stdio probe. FLAGGED FOR GATE 7.5: (i) ten src files moved after Gate 6's evidence -- dashboard.ts, server.ts, auth/auth-service.ts, auth/token-store.ts, audited-read.ts, scheduler.ts, scheduler-engine.ts, tool-specs.ts, sandbox/guards.ts, gateway/litellm-proxy.ts -- and the OAuth token-exchange path plus the /dag route are the surfaces that moved; (ii) commit ad0d803 landed MID-GATE from a concurrent committer on this shared working tree and SWEPT UP this gate's dashboard.ts edit into its own commit (this gate's own closeout is 0697679) (nothing lost, the HEAD content is byte-for-byte what this pass wrote, but git log does not name it -- recorded in IMPL-206 and as an amendment on IMPL-205); (iii) the six blocked VALs above are Gate 7.5's whole opening agenda. PRIOR: 2026-09-04 v24 GATE 6.5+7 ROUND 2 (verifier) PASSED. CONTEXT, DECLARED FIRST: this gate was re-dispatched with NO Gate 6 commit between round 1's send-back (a3b5d0a, 19:57) and this round -- HEAD was still the verifier's own commit, the tree clean, ~1 minute elapsed -- so an identical second send-back would have been a loop with zero progress. The verifier applied the send-back fix DIRECTLY, as a normal red->green pass over tests that were already written and already red (IT-105's six rows), and logged it as IMPL-184 rather than folding it into simplify, because it CHANGES BEHAVIOUR and Gate 6.5 is quality-only by contract. ROLE CROSSING, on the record. (0) SIMPLIFY (merged Gate 6.5): the Skill was RUN, twice-scoped. Re-running round 1's range (`git diff c9c6592..HEAD -- src`) would have been idle -- zero src commits landed between the rounds -- so after this round's seven src files changed it was run over `git diff a3b5d0a..HEAD -- src` (single-pass inline; the Agent fan-out is unavailable in this context, stated rather than glossed). TWO fixes, quality-only, zero behaviour change, full suite green after: one `scopeToActor(rows, principal, actor)` declaration replacing the same operator-sees-everything-else-sees-own-createdBy predicate typed twice across schedule_list/webhook_list (a security-relevant rule that must not drift between the two trigger stores; net -3 lines), and the two `subject as string` casts in authorize() that this round's own change orphaned. THREE candidates NAMED AND REJECTED: the `?? true` rewrite of the enabled default (`??` also fires on null, which the schema refuses and the explicit form documents -- churn with a hypothetical semantic delta for zero lines); resolveMcp through assetCatalogPort walking every workflow per dispatch (real, accepted -- once per agent dispatch at human-rate row counts, against keeping two implementations of one security-relevant rule); scheduler.get()'s full projection where ownerOf did a one-column SELECT (once per due firing; get is the existing public claim reader). Altitude reviewed and found right at both new decision points. Item (d) below (the unwired resolveMcp seam) was found by the SEAM-WIRING check, not by simplify, and is recorded as such. Round 1's two named-and-rejected candidates (the TOOL_SPECS row tails; the ~90-line auth-gated/fallback duplication in the /mcp and /assets handlers) are unchanged and stay v25 debt. (1) SEVEN DEFECTS FIXED, all mutation-checked. (a) trigger ownership read the WRONG COLUMN -- both stores' `ownerOf` now read `createdBy`, not `claimedBy`/`workflow`; `webhooks` gained the column (CREATE TABLE + the v24 rebuild + an idempotent additive ALTER), `webhook_create` records the caller, `WebhookView` carries it, `webhook_list` is principal-scoped exactly as `schedule_list` is (which made that row's own advertised description true), and server.ts's ownerless-trigger boot count loses the cast that existed only because the column did not. (b) the MODED workspace_* rows never ran their check -- `authorize()`'s subject now falls back, for a `key: null` spec, to the argument the RESOLVED row's ownership names (`runId` for 'run', `workflow` for 'workflow'), which each tool's own `mode()` predicate already required to be present, so the fallback is total by construction; rejected alternative was a per-row `key` override field. (c) NEW, found by writing the test round 1 said no test reached: `McpFacade.workflowRegister`'s step-2 check had an `owner !== null` escape letting ANY caller adopt an OWNERLESS trigger, contradicting DES-139's stated operator consequence and authorize()'s own ownerless rule -- removed; IT-123 (6 cases, real stores, real facade) is the pin. (d) the unwired `resolveMcp` seam, above. (e) A CONSUMER OF THE OLD SEMANTICS, caught before it shipped: server.ts's fire-path gate `resolveScheduleTarget` read `ownerOf` as \"the workflow holding this trigger\", so after (a) it would have resolved a PRINCIPAL ID as a workflow name and refused EVERY authenticated user's schedule CLAIMED_WORKFLOW_MISSING -- invisible to the whole suite, which is auth-disabled; now `get(id).claimedBy`. DES-149's signature line is amended in place (bracketed) to say which reader is which. (f) `schedule_create` was undriveable from its own advertised schema: `enabled` is optional and the store writes `s.enabled ? 1 : 0`, so the row's OWN happy fixture created a schedule born DISABLED that `tick()` never selects -- defaulted to true beside the existing `kind` default. (g) `run_result`'s audited cross-read was unreachable: DES-151 says in so many words that run_result is in the audited set and `AuditAction` names it, but the TOOL_SPECS row carried no `adminCrossRead`, so an admin's cross-read happened UNAUDITED and the owner's `adminReads[]` never showed it -- one literal added, plus an IT-105 case pinning against TOOL_SPECS itself which rows carry the flag. (2) REGRESSION: 301 files / 2139 tests, 2113 pass, 0 fail, 26 skip, exit 0; `npx tsc --noEmit` clean. Baseline at round start was 2065 with SIX failing (IT-105, byte-identical to round 1). IT-105/VAL-120/VAL-126 flipped red->green; IT-123, IT-124, UT-163 are new; IT-122 is BACKFILLED (the resume-legacy-params test file shipped at Gate 6 with no ledger entry -- TENTH occurrence of the ledger-honesty gap). EVERY REQ HAS A GREEN VAL except the two named exceptions carried forward unchanged: VAL-124 (REQ-113) and VAL-128 (REQ-117) stay status:blocked/result:not-run per their own Gate 5 entries and the design's Gate-7.5-only ruling -- the same two round 1 named, no third appeared. FOUR test files had an ORACLE corrected, each declared in its own entry and none weakened: `ownerOf` now means the creator, so trigger-claims/webhook-registry/register-crash-window re-point their CLAIM assertions at `get(id).claimedBy`/`get(id).workflow` at unchanged strength (webhook-registry GAINS an assertion that ownerOf keeps answering the creator while the claim moves), and IT-105's webhook fixture creates with a `createdBy` instead of asserting an owner it never set. (2b) COVERAGE: overall src line coverage 94.91% -> 95.53% (17010/17805), functions 95.48% (655/686), re-measured AFTER the simplify pass so the figures match the committed tree -- above the 90% bar. The PER-FUNCTION bar over the v24 delta went from 26 long offenders + 2 short (round 1) to 8 long + 0 short; whole-tree long offenders 91 -> 72. Nineteen functions taken to the bar by WRITING the tests (IMPL-186, test-only), several of them load-bearing rather than merely uncovered: parseMetaParams 46.2% (neither guard arm), rearmAtBoot 41.2% (the method server.ts calls at every boot ran nowhere but its early return), WebhookRegistry.deliver 86.2% (only ONE of RefusalReason's four members was produced anywhere in the suite), validateOneAgentSpec 85.9% (three engine-ceiling refusals unreached), composeConfig (ADR-028's boot REFUSAL on a malformed role unreached), checkMermaid (a catch-all whose own comment admitted it was untested). The EIGHT that remain each carry a one-line Decision rationale in 05-tests.md's round-2 coverage block -- runDiagnostics (needs a real GitHub credential; Gate 7.5's tier), createServer (composition root; the uncovered remainder needs a real external dep), sweep (two console.error catch bodies, no store injection point), sendBlobUploadError (the `: 500` default of a four-way map; needs a disk fault), reclaimStaleWorkspaces 94.9% (the two rmSync permission-fault catches), _resolveMcpConfigs (a bare rethrow for a third failure mode resolveConfig does not have today), composeConfig 94.1% (the litellm onSupervisionEvent closure -- no litellm binary on PATH here), and workflow-catalog's member initializer, where 47 of 51 missing lines are the three DEAD workflow_diagrams accessors round 1 found have ZERO callers (a v25 deletion, not a test gap). No bar was lowered. (3) `sh .sdlc/trace --check`: 1184 items / 31 gaps, exit NON-ZERO -- and the gap SET is byte-identical to round 1's. 12 HIGH are all `not-real-verified` for REQ-107..118, which is STRUCTURAL before Gate 7.5 flips real:true and unavoidable by construction at any verification round preceding this iteration's first validation run (the Gate 5 contract itself assigns that flip to the validator); 1 MID (IMPL-082 TDD, pre-existing since v11); 18 LOW (16 iter-drift + TASK-018 + TASK-153-external). 0 broken links, 0 orphans, 0 NEW gaps. (3b) solid_check: 45 modules, 0 HIGH / 0 mid / 10 low -- round 1's single HIGH is CLOSED by declaring ARCH-069 in ARCH-103's `deps:` (ARCH-103's `module:` is one FILE nested inside ARCH-069's directory module, so the sibling import is a real, intended dependency that was simply undeclared; the repair is the declaration, not moving production code, and it is recorded with a bracketed AMENDED marker). (4) determinism_check src --check: exit 0. (5) TIME-TRAVEL: `TZ='Pacific/Kiritimati' npx vitest run` -> 301 files / 2139 tests, 2113 pass, 0 fail -- identical to the normal-clock run, ZERO tests flipped. (No faketime binary in this sandbox; the documented install-free fallback.) (6) SEAM WIRING: round 1's ONE hole is closed -- see (d). Re-enumerated this round: `deps.authorize ?? realAuthorize` is production-defaulted; `ownerLookup`/`audit` are built in createServer() and passed through buildToolDeps; `facade.bindAssetSync` and `gateway.bindResolveMcp` both have real composition-root callers, and bindResolveMcp now injects the same implementation the unit tests cover. `WorkflowCatalog.assetsOf` is left orphaned-in-production by (d) and recorded as v25 debt rather than deleted, because deleting it would weaken catalog-v24.test.ts. (7) REAL-DEPENDENCY SMOKE: RUN this round (round 1 declared it not run). IT-124 boots the REAL createServer() on 127.0.0.1 with auth ENABLED, three real TokenStore bearers and configured roles, over real MCP HTTP against real SQLite stores -- the seam every other test short-circuits, and the reason two authorization defects shipped past ~2000 green tests. It proves trigger ownership on BOTH stores, the moded workspace_* checks, an audited admin cross-read surfacing in the owner's adminReads[], a fire-path refusal RECORDED on the row, and a real schedule firing end to end. Still mock-only / unverified at this tier and left to Gate 7.5: the LLM gateway (no litellm binary on PATH), GitHub issue_* (no credential), and the MCP stdio probe. gates.verification.passed=true; gates.impl.passed=true; current_stage -> validation. FLAGGED FOR GATE 7.5: seven src files changed after round 1's evidence -- authz.ts, scheduler.ts, webhook-registry.ts, call-tool.ts, mcp-facade.ts, tool-specs.ts, server.ts -- and the auth-enabled path plus the schedule fire path are the surfaces that moved. PRIOR: 2026-09-04 v24 GATE 6.5+7 ROUND 1 (verifier) NOT PASSED — see gates.impl.note for the two authorization defects this round sends back. (0) SIMPLIFY (merged Gate 6.5), scope = the v24 delta `git diff c9c6592..HEAD -- src` (5159 insertions / 42 files; the tree was clean so a diff-vs-HEAD would have been EMPTY — the range is the honest scope): THREE fixes applied, quality-only, zero behaviour change. server.ts's `?namespace=` retirement guard was copy-pasted at FOUR upload routes with its 150-char message re-typed verbatim each time -> one `refuseNamespaceParam(req,res)`; the CAS blob-upload failure->HTTP-status mapping was duplicated verbatim in both copies of the blob route -> `sendBlobUploadError(res,err)`; tool-specs.ts's `resolveFixture` resolved a FixtureRef twice (scalar arm + array arm, duplicating the throw) -> one `fill` closure. Net -24 src lines, tsc clean, 10 touched test files re-run green. TWO candidates NAMED AND REJECTED: the repeated row tails in the 35-row TOOL_SPECS table (declarative data whose value is that each row is complete and greppable in place) and the ~90-line auth-gated/fallback duplication of the /mcp and /assets/manifest handlers (real, but unifying it means restructuring the request pipeline — neither quality-only nor surgical; recorded as v25 debt). IMPL-178 amended in place with this record. (1) REGRESSION post-simplify: `npx vitest run` 297 files / 1947 tests pass, 26 honest skips, 0 failed, exit 0; `npx tsc --noEmit` clean. 47 of the 52 v24 items flipped red->green in 05-tests.md (dated red-reason prose left verbatim as record). IT-105 stays red; VAL-120 (REQ-109) and VAL-126 (REQ-115) are deliberately NOT flipped because IT-105 is what proves them — flipping VAL-120 would certify an enforcement path that is provably open, and VAL-126 would certify DES-149's \"the creator deletes the trigger\" clause, which is exactly what is closed. VAL-124/VAL-128 stay `blocked` per their own Gate 5 entries (Gate 7.5-owned). VAL-123 and VAL-129 flipped green WITH their deferrals recorded in-entry: 1 of 3 cases in val-mermaid-renders.test.ts is `it.runIf(HAS_BROWSER_TOOLING)` (no headless browser in this repo; the sibling `it.skipIf` asserts UNVERIFIED(no browser) explicitly), and 5 of 77 rows in v24-tool-surface.test.ts are the GitHub-credential-gated issue_* happy paths, recorded UNVERIFIED in the conformance artifact rather than as passes. (1b) COVERAGE (@vitest/coverage-v8@1.6.1, npm install --no-save, --coverage.reportOnFailure so the red file does not suppress the report, coverage/ gitignored): overall src line coverage 94.91% (16863/17766), functions 95.32% (653/685) — ABOVE the 90% bar, up from 94.70% at the start of this round. The PER-FUNCTION bar is NOT met on the v24-touched delta: 32 long offenders (>5 lines, <95%) + 2 short at round start, 26 long + 0 short after this round's fills. SIX long and BOTH short offenders were closed this round by writing the tests the dods asked for — asset-sync.ts `resolveMcp` was at 0 of 17 lines (the entire replacement for the deleted mcp-registry.ts, with ZERO tests) and now has 6; tool-specs.ts `listMode`/`deleteMode` (66.7% each) now have a 9-row mode/authz-row table plus a hostile-shape row; params/contract.ts `validateRequiredKeySpec` (10/14) and `validateNameArray` (4/6) now have their whole refusal side; authz.ts `authorize` (92.2%) is at the 50-row matrix. THE 26 REMAINING v24-touched long offenders are Gate 6's, worst first: server.ts `runDiagnostics` 3.7% (1/27), scheduler.ts `rearmAtBoot` 41.2% (7/17), workflow-meta.ts `parseMetaParams` 46.2% (12/26), workflow-catalog.ts `insertVersion` 70.5% (31/44), mcp-facade.ts `runList` 75% (6/8), workflow-catalog.ts `_parseParams` 76.5%, server.ts `resolveScheduleTarget` 77.8% (14/18), workflow-catalog.ts `listAssets` 83.3%, mcp-facade.ts `workflowRegister` 83.8% (31/37), mcp-facade.ts `workflowDeregister` 84.2%, server.ts `createServer` 84.7% (777/917), params/contract.ts `validateOneAgentSpec` 85.9%, webhook-registry.ts `deliver` 86.2% (50/58), server.ts `sweep` 86.7%, workspace-gc.ts `reclaimStaleWorkspaces` 87.3%, claude-agent-sdk-client.ts `_resolveMcpConfigs` 88.2%, workflow-catalog.ts member initializers 88.4% (526/595), mcp-facade.ts `workflowPublish` 89.5%, main.ts `composeConfig` 91.4%, mcp-facade.ts `runResult` 91.7%, workflow-catalog.ts `deregister` 92.3%, mcp-facade.ts `workspaceDelete` 92.5%, claude-agent-sdk-client.ts `materializeAssets` 92.6%, scheduler.ts member initializers 94.3%, check-mermaid.ts `checkMermaid` 94.7% (125/132), plus this round's own `sendBlobUploadError` 88.9% (8/9 — its 500 fallback arm). Pre-existing non-v24 debt (65 long / 7 short) is unchanged and stays inside the v21 Decision-rationale scope. DEAD CODE FOUND BY THE MEASUREMENT: `workflow-catalog.ts`'s `putDiagramPending` (1/10), `putDiagramResult` (1/25) and `getDiagram` (1/12) have ZERO callers anywhere in src/ or tests/ — orphaned when TASK-139 deleted graph-analyzer.ts, and TASK-139's dod did not list them. Reported for the Gate 6 round rather than deleted here (their `workflow_diagrams` table is v23 schema). (2) REMAINING RED: IT-105 only, 6 cases, both defects above — reported, never ignored, no test weakened, deleted or re-scoped. (3) `sh .sdlc/trace --check`: 1178 items / 31 gaps (down from 36). 12 HIGH are all `未真實驗證` for REQ-107..118 — STRUCTURAL before Gate 7.5 flips real:true, and by construction unavoidable at a first-round verification of a new iteration (the Gate 5 contract itself says the validator owns that flip); 1 MID (IMPL-082 TDD, pre-existing since v11); 18 LOW (16 iter-drift + TASK-018 + TASK-153-external). Five `未實作` TASK gaps CLOSED by the IMPL-179..183 backfill. 0 broken links, 0 orphans. (3b) solid_check: 45 modules, 1 HIGH / 0 mid / 10 low. The HIGH is NEW in v24 — ARCH-103's `module:` is a single FILE (src/gateway/claude-agent-sdk-client.ts) nested inside ARCH-069's directory module (src/gateway), so its import of a sibling reads as an undeclared cross-module dep. Reported, not papered over: the fix is an architecture declaration, not a test change. Two of the 10 low are new v24 unclaimed files (owner-lookup.ts, scan-agent-calls.ts). (4) determinism_check src --check: exit 0. (5) TIME-TRAVEL: TZ=Pacific/Kiritimati full suite — same result set as the normal-clock run, no test flipped (no faketime binary in this sandbox; the documented install-free fallback). (6) SEAM WIRING: `deps.authorize ?? realAuthorize` in call-tool.ts is production-defaulted (tests inject the spy); `ownerLookup`/`audit` are built in createServer() and passed through buildToolDeps; `facade.bindAssetSync` and `gateway.bindResolveMcp` both have real composition-root callers. ONE HOLE FOUND: asset-sync.ts's exported `resolveMcp` (DES-153, the pure catalog-port helper) has ZERO production callers — server.ts:1363 re-implements the same workflow-wins-over-global rule INLINE over `catalog.assetsOf`. Two implementations of one rule, only one of them reachable from production. (7) REAL-DEPENDENCY SMOKE: NOT run this round and declared as such rather than fabricated — the gate is failing on an authorization-wiring defect that a real boot cannot fix, and the acceptance tier already drives a real booted createServer() over real MCP HTTP for all 35 tools. Gate 7.5 owns the real-provider run. gates.verification.passed=false; gates.impl.passed=false; current_stage -> impl. PRIOR: 2026-09-03 v23 GATE 6.5+7 ROUND 5 (verifier) DONE - closes the Gate 6 RE-ENTRY over commit `9fc4439`, whose `docs(v23)` subject actually carried 179 lines of src (graph-analyzer.ts's `_settle` choke point, the TOTAL closure try/catch/finally, the `AnalyzerCause` closed union, R-3's `_startJob` enabled-guard + server.ts's ADR-020 mirror warn); that code landed AFTER round 4's simplify pass, so it IS this round's simplify scope, not already-cleaned ground. (0) SIMPLIFY: ZERO fixes applied - the honest outcome, not a skipped step. All 179 changed lines read; three candidates found, all NAMED AND REJECTED with reasons: (a) collapsing `_settle`'s six hand-forwarded telemetry fields into one `...telemetry` spread on the `_journal` call - rejected, the forwarding is fully tsc-checked, so it is NOT the silent-drift class the repo's one-declaration rule targets, and a spread would silently swallow a future telemetry field instead of erroring on it; (b) `_attempt`'s `let outcome ... return outcome` collapsed into direct returns - rejected as churn of a decision a PRIOR Gate 6.5 already made and documented in place (both shapes avoid the `as` casts that motivated it); (c) extracting the zero-model-call telemetry literal (promptTokens null / completionTokens null / durationMs 0 / gateFail null / attempts 0), typed twice inside `_settleUnavailable` - rejected, the two copies sit 8 lines apart inside ONE 20-line function and deliberately differ in `cause`/`noteCode`; a shared const is abstraction for two adjacent uses. No 06-impl-log.md edit follows, because no code changed (IMPL-177 stands as the Gate 6 RE-ENTRY left it). (1) REGRESSION: `npx vitest run` 283 files / 1850 tests, 0 failed, exit 0 (read off the process exit status); `npx tsc --noEmit` clean. NINE items flipped red->green/pass: UT-129, UT-130, UT-131, UT-132, UT-133, UT-134, UT-136, UT-137, IT-104 (metadata only; dated red-reason prose left verbatim as record, round-4 precedent). UT-125/UT-135/IT-103 were already green from the Gate 5 RE-ENTRY. ZERO remaining red (IT-015 the single labelled deferral; UT-091/VAL-094 retired). (1b) COVERAGE (@vitest/coverage-v8@1.6.1, npm install --no-save, coverage/ gitignored): overall src line coverage 95.51% (15004/15709), up from round 4's 95.48%, well above the 90% bar. The DELTA itself: 179 changed src lines, 179 instrumented, 0 missed; every function in graph-analyzer.ts at 100% including `causeForAttemptFailure` (the one function whose default arm is documented unreachable) and server.ts's new mirror-warn line. Pre-existing debt UNCHANGED at 77 long offenders (>5 lines, <95%) + 8 short (<=5 lines, >1 missed) - same counts as round 4, all inside the v21 Decision-rationale scope (the per-function bar is enforced against iteration-touched code; worst offenders are process-entrypoint/diagnostic surfaces no in-vitest test can move and Gate 7.5 real-verifies: main.ts `onSupervisionEvent` 0% (0/6), main.ts `loadFileConfig` 0% (0/9), server.ts `checkMcpConfigTransport` 0% (0/16), server.ts `runDiagnostics` 3.8% (1/26), server.ts `createServer` 84.3% (717/851)). (2) NO REMAINING RED, see above. (3) `sh .sdlc/trace --check`: 1024 items / 18 gaps, 0 HIGH (17 low = iter-drift + TASK-018 未實作; 1 mid = IMPL-082 TDD), residue unchanged from the Gate 6 RE-ENTRY, 0 broken links. (3b) solid_check: 0 high / 0 mid / 10 low (未認領檔案, pre-existing). (4) determinism_check src --check: exit 0. (5) TIME-TRAVEL: TZ=Pacific/Kiritimati full suite 283 files / 1850 tests pass, 0 flips (no faketime binary in this sandbox; the documented install-free fallback). (6) SEAM WIRING: server.ts:1499 is the ONE `new GraphAnalyzer(` site (IT-098 pins the count) and passes REAL gateway/catalog/ports/clock with NO `schedule` override, so production runs the real setImmediate path while only tests inject `runInline`. (7) REAL SMOKE (real engine boot via tsx, real Ollama qwen2.5:7b, direct-fetch gateway, no mocks): a real workflow_register produced exactly ONE journal line carrying TWELVE keys in wire order, with the delta's own two new fields present at REAL tier - attempts:1, cause:'gate_refused' (promptTokens 476, completionTokens 120, durationMs 47482, outcome 'unavailable', noteCode 'GATE_REJECTED_SHAPE', gateFail 'codepoint'); and BOTH IT-104 rows confirmed on real boots - effective tools [] emits NO warn, configured tools ['Read'] curated to ['Bash'] emits the ADR-020 mirror warn naming the EFFECTIVE set (exactly why it must key off effective, not configured). FLAGGED FOR GATE 7.5: gates.validation's round-4 evidence was collected against a39c0e7/eef7809, which PRE-DATES 9fc4439's settle rework - validation round 5 should re-run the v23 REQ surfaces against THIS tree. gates.verification.passed=true; current_stage verification->validation." }
  validation:   { passed: true, note: "v27i GATE 7.5 SEND-BACK REPAIR (2026-09-13, validator) — PASSED. DOC-1 (HIGH, the sole finding routed to validation by the v27 GATE 8 REVIEW's send_back) is CLOSED: README.md:112/113/114/120/139/140 and DEPLOY.md:788 rewritten to current-state — Models tab is a plain unsortable/unfilterable directory table (src/dashboard/ui/models.js:6), System tab is a six-row resource table with no cards/process table (src/dashboard/ui/system.js:2-6,40-64), the tab-hidden poll-pause claim deleted (grep for visibilitychange/document.hidden/visibilityState across src/ = 0 hits), and the Issues-tab quoted degrade string corrected from a fabricated 「資料無法取得」 to the real 'GitHub not configured' (src/dashboard/ui/issues.js:5). No code/test/config touched (doc-only finding); no test pinned the removed strings (grep confirmed). Full detail: 08-validation.md's 'v27i GATE 7.5 SEND-BACK REPAIR' section. sh .sdlc/trace --check: 1659 items / 33 gaps, byte-identical gap SET to the pre-edit baseline (0 new orphan/broken-link/未真實驗證/未驗證); rtm.md unchanged (no REQ's verification status is affected by a doc-prose fix) — all 8 closure REQs (131-136/140/141) remain ✅. current_stage DELIBERATELY LEFT AT review (unchanged) — Gate 8 owns this send-back loop; the one remaining named finding before re-review is design's DASH-2 (04-design.md:3306), out of this gate's scope. | PRIOR: v27 GATE 7.5 round 3 (2026-09-13, validator) — PASSED. REQ-135 (VAL-209) FIX RE-CONFIRMED FOR REAL: Gate 6 landed commit 0673f22 (IMPL-267/268) wiring onSelectAgent into ui/workflow.js's two paintSwimlane calls and fixing agent-panel.js's window.event timing, exactly per this note's own prior diagnosis. Booted a FRESH deploy.sh --background scratch instance (RWE_PORT=8937, auth off, gateway:sdk default, workRoot under this session's own scratch dir — production rwe.service PID 2713463 on port 8899 untouched throughout); version banner 0.1.0 (v0.20.0-313-g0673f22) confirmed the fix commit was on disk. Independent validator harness (not the implementer's own fix-verification script) registered a NEW real 4-phase/4-lane workflow, ran it to completion with four real local-Ollama qwen2.5:7b agent calls through the real gateway:sdk wiring, then drove real Chromium against /dashboard/workflow/<name> (the PRIMARY route): .cell-layer.onSelectAgent is now a function (was undefined); clicking the real leftmost and rightmost node cells each fired a real GET /api/runs/:id/agents/:agentId request and opened [data-agent-panel] with 6 stat cards (was: no request, no panel); the slide side now genuinely follows click position — agent-panel (no from-left) on the left-half click, agent-panel from-left on the right-half click (was: always right/no from-left). REQ-136 re-checked on the SAME newly-reachable route: the /agents/:id response the panel now fetches there carries no systemPrompt-ish key. Regression spot-check of REQ-133/REQ-134 (the two sibling REQs rendered by the same files 0673f22 touched) on the same instance found no regression: 1 run chip + 1 history row + a version tag (REQ-133); .cell still 3 direct children, clip ratios 0.999/0.909 (REQ-134). REQ-140/REQ-141 are server-side and untouched by this commit's client-only diff; their 2026-09-13 re-confirmations (VAL-212/VAL-205) already post-date every fidelity-sweep commit and stand unchanged. Config: no drift, 0673f22 touched no config/settings file. Docs: README.md's dashboard section + 已知限制 list and DEPLOY.md §6 rewritten current-state — the REQ-135 known-limitation text (click doesn't open panel on the primary page) is now FALSE and removed; no changelog/history text added to either manual (current-state rule). trace: 1651 items / 33 gaps, gap SET byte-identical to every prior v27 measurement (10 MID REQ-137/138/139/142/143 out of closure, 23 LOW pre-existing drift) — exit 1, not chased to 0. Full record: 08-validation.md VAL-209 (amended in place) + its new 'Gate self-check (v27 Gate 7.5 round 3...)' section. rtm.md regenerated: REQ-135 flips back to ✅. current_stage -> review; Gate 8 next. | PRIOR: v27 GATE 7.5 RE-RUN (2026-09-13, validator) — NOT PASSED, REQ-134 FIXED but a NEW real defect found on REQ-135: re-ran the WHOLE closure (not only REQ-134) because commits 23a909a..1a1746a (IMPL-250..266, same session) touched shared dashboard surface (dashboard.css/ui/app.js/ui/home.js/ui/run.js/dashboard.ts) after the last validation. Fresh deploy.sh-booted scratch instances (B=8935 auth off, A=8936 auth on, gateway:\"direct-fetch\"; production rwe.service PID 2713463 untouched throughout, confirmed before/after). REQ-131/132/133/136/140/141 RE-CONFIRMED real:true GREEN on fresh real runs (VAL-206/207/211/212/205 amended in place with new evidence). REQ-134 (VAL-208) FIXED AND RE-CONFIRMED FOR REAL: 3 direct `.cell` children (was 5), clip ratios 0.999/0.909 (was 0.33/0.30), all four dot states measured on genuinely distinct real runs (done/failed/predicted-queued/running), duration now in row 3 — flips to real:true/pass. REQ-135 (VAL-209) NEW REAL FAILURE, unrelated to REQ-134s fix: works on the legacy `/dashboard/<runId>` route (what the acceptance suite tests) but NOT on `/dashboard/workflow/:name` (the product's actual primary page per the design handoff's own single workflow-detail view — `ui/workflow.js`s two `paintSwimlane` calls never wire `onSelectAgent`, confirmed `layer.onSelectAgent===undefined`, zero `/agents/:id` fetch on click, no panel ever opens); a SECOND independent defect on the working route too — `agent-panel.js`s `window.event` read happens after an `await`, so the from-left-when-right-half clause never fires. Full record: 08-validation.md VAL-209. MECHANICAL CAVEAT (same shape as REQ-134s last round, verified by reading trace.pys own is_real_test): `sh .sdlc/trace --check` will NOT show REQ-135 as a gap (VAL-209 is real:true, so verified_real includes REQ-135 regardless of its result:fail) — read this note, the pending item, 08-validation.md VAL-209 and rtm.mds REQ-135 warning row, not the gap count. `current_stage` stays `impl` (routed back), NOT `review`. Config: no drift, no new key this round (checked against the fidelity-sweep diff). Docs: README.md/DEPLOY.md rewritten current-state for the fidelity-sweep-visible changes (tabs now underlined `<a aria-current>`, hue slider degrees readout, footer, nav brand, theme segment order, home cards LAST RUN timestamp); REQ-135s defect is NOT described in the human manuals, per the manuals history-free/current-state-only rule — it lives in 08-validation.md/pending:/this note. | PRIOR: v27 GATE 7.5 (2026-09-12, validator) — NOT PASSED, sent back to Gate 6: 7 of 8 closure REQs (131/132/133/135/136/140/141) are real:true GREEN against two SEPARATE deploy.sh-booted scratch instances (B=8935 auth off, A=8936 auth on; production rwe.service PID 2713463 untouched throughout). REQ-134 FAILS FOR REAL: a genuine, reproducible rendering defect — `.cell-label`/`.cell-model` inside every swimlane node render clipped to ~30% of their declared line-height (flex-column shrink + overflow:hidden resets automatic min-size to 0 per CSS Flexbox 4.5; root cause is structural — 5 flex-column siblings where REQ-134 specifies 3 grouped rows, not a missing flex:none), measured via real Chromium getBoundingClientRect() against a real completed run — see 08-validation.md VAL-208 for the full defect record, screenshots, and the bounded diagnostic. IMPORTANT MECHANICAL CAVEAT: `sh .sdlc/trace --check` CANNOT show this as a REQ-134 gap — VAL-207/VAL-212 (siblings tracing the same REQ-134) are legitimately real:true for THEIR OWN clauses, and trace.py's `verified_real` only requires ANY one real:true item per REQ, not all — so REQ-134 mechanically reads 'verified_real' despite VAL-208's real fail. Do not read the trace dashboard as green for REQ-134; read 08-validation.md and this note. A GENUINE LEDGER DEFECT was also found and fixed this pass: v27 Gate 5's own VAL-198..204 IDs collided with SEVEN pre-existing v26 08-validation.md entries (D9-D14, REQ-127) under the SAME numbers — trace.py's scan() silently let the older (08-validation.md, later-sorted) entries win, so REQ-131/132/133/134/135/136/140 had ZERO visible acceptance evidence in the trace graph before the fix (empirically confirmed: `--impact REQ-131` listed no VAL-* item at all). Fixed by renumbering only the v27-era occurrences to VAL-206..212 across every v27-era file (05-tests.md/02/03/04/06-impl-log/journal/state.yaml); the old v26 VAL-198..204 entries are untouched and still correctly serve REQ-127. Full record: 08-validation.md's own 'Ledger defect found and fixed' section. REQ-137/138/139/142/143 remain explicitly OUT of this closure (a later slice, per the Gate 6.5+7 verifier's own note) — untouched, not silently passed. `current_stage` stays at `impl` (routed back), NOT `review` — Gate 8 must not run until REQ-134 is fixed and re-validated. | PRIOR(v27, orchestrator/verifier): NOT YET RUN for REQ-131..143 (Gate 1 passed 2026-09-11; awaiting /sdlc-run). | PRIOR(v26): 2026-09-09 v26 GATE 7.5 ROUND 6 PASSED (validator) — delta re-run of the REQ-127/REQ-129 impact closure on the D9/D10 + D11/D12 fix tree (d1c453b). Booted from documented steps only: DEPLOY.md §0's own second-instance form (`set -a; . ~/.config/rwe.env; set +a` then `RWE_CONFIG_PATH=… RWE_BIND=127.0.0.1 RWE_PORT=89xx ./deploy.sh --background`) for TWO scratch engines — A(8935) production alias table verbatim + a copy of the production workRoot, secrets blanked and its own updateFlagPath (round-5 hygiene rule); B(8936) same config, FRESH workRoot. Production rwe.service never restarted (NRestarts=0, MainPID 1188044, ActiveEnterTimestamp unchanged). REQ-127 GREEN (VAL-187 red->green): on the PRODUCTION alias table a real Haiku call now records costUSD 0.031974 / unpriced:false (round 3: 0 / true) and meta.budgetEnforceable {usd:true,tokens:true,unpricedModels:[]}; hand-recomputed exact incl. cacheWrite at 2x and, on the repeat call, cacheRead 15272 at 0.1x (costUSD 0.0037472); the USD ceiling binds on that same table over two priced calls (spent 0.000644 >= 0.000001, second dispatch refused) and the token ceiling binds (164 >= 100); models_list/GET /api/models serve FOUR rows for four models with no unknown price and fable pinned; a NEW arm no earlier round ever ran — a real OpenRouter call (openai/gpt-4.1-nano) priced at costUSD 0.000014, cross-checked against OpenRouter's own published /models pricing, deltaAbs 0; and a real scheduler-fired run carries budget.limits {usd:null,tokens:null} while still recording four columns + costUSD 0.000396. REQ-129 GREEN (VAL-189 red->green): real Chromium 25, 1100x900, real mouse input against real production run 77f74018 — run DAG viewBox/width:100%/preserveAspectRatio with 10 cells legible, real pan then a REAL trusted click on Fit resets, poll does not undo it, D8's four columns readable in #run-usage; the author figure now pans the FULL (-180,-90) gesture with mouseup delivered and no sticky follow (D10 closed, measured with round 3's own harnesses unmodified); wheel zoom green on BOTH figures; the agent-cell transcript click still works after the fix's preventDefault; and the grandfathered TD figure (gp-runner v3, graph TD, 9 nodes) pans, zooms and resets too. MEASUREMENT TRAP recorded so round 7 does not chase it: page.mouse.wheel() is validated against the browser WINDOW (headless default 800x600), not the emulated viewport, so any wheel below y=600 reaches nothing — --window-size fixes it and Input.synthesizeScrollGesture confirms independently. NEW DEFECT, OUTSIDE THE CLOSURE (recorded, not fixed): D13 — an upgraded workRoot keeps `schedules.workflow NOT NULL` (SQLite cannot ALTER a column and no rebuild migration exists), so schedule_create fails on this box's production-shaped database with 'NOT NULL constraint failed: schedules.workflow' while a fresh workRoot succeeds; production holds 0 schedules so nothing is firing, written into DEPLOY §6 + README and routed to the orchestrator. Docs: §1b round-trips 43 code keys + 10 env names both ways, §0's expected `tools: 35` matches the running engine, history/dead-field greps clean; three current-state gaps filled (README's minimum-env named only ANTHROPIC_API_KEY although this box runs anthropicAuth:'subscription'; the dashboard's zoom/pan was documented nowhere; D13). Config sync: no code change this round, no new/changed key; rwe.config.example.json unchanged and still correct. trace: 1479 items / 22 gaps — 0 未驗證, 0 未真實驗證 (the 22 are 19 low-severity 漂移 + 2 未實作 + 1 TDD, all pre-existing; DES-076's ModelEntry alias->aliases drift is a designer amendment, not visible to the tool). rtm.md regenerated: 0 ❌, 1 ⚠️ (REQ-126, owner-held). Gate 8 remains blocked by TWO owner_decision markers: VAL-186 (REQ-126 effort never reaching the OpenRouter wire) and VAL-187 (the deployed alias claude-sonnet-4-6 -> model claude-sonnet-5). PRIOR: 2026-09-09 v26 GATE 7.5 ROUND 3 NOT PASSED (validator) — delta re-run of the impact closure REQ-127/128/129 on the fix pass's tree (242b5a0). Booted from documented steps only: three scratch engines, each `RWE_CONFIG_PATH=… RWE_BIND=127.0.0.1 RWE_PORT=89xx ./deploy.sh --background` (DEPLOY.md §0's own second-instance form) — A(8930) production alias table + fresh workRoot, B(8931) production alias table + a byte copy of the production workRoot, C(8932) ONE alias per model. Production rwe.service never restarted (NRestarts=0, ActiveEnterTimestamp unchanged). RESULT: REQ-128 GREEN — VAL-188 flipped red->green by the validator's own cold-model re-run with a THIRD subject family, x-ai/grok-4.6 over the OpenRouter API given only tools/list + the guide: registerAttempts=1, refusals=0, 42 tool calls, a conformant v2 LR swimlane drawn before ever seeing a refusal, run 2a7b8039 completed (evidence/v26/req128-round3-grok46.json). REQ-127 RED (new defect D9) — VAL-187 flipped green->red: with the PRODUCTION alias table (two aliases on one anthropic model) every Anthropic call records costUSD 0 / unpriced:true and budgetEnforceable.usd:false, because overlayAliases appends a ratesPerM:null duplicate row and ModelBook's index build lets it overwrite the priced static row; the SAME workflow on a one-alias engine records costUSD 0.0023872, exact to the corrected D3/D4 table. Everything else in REQ-127 is green and was re-measured: the four columns incl. the first REAL cacheWrite 15272 / cacheRead 15272 ever seen through the engine, the token budget (BUDGET_EXCEEDED spent 164 >= 100), the USD budget over two priced calls (spent 0.000399 >= 0.000001), and D8's four dashboard columns on real production run 77f74018. REQ-129 RED (new defect D10) — VAL-189 stays red for a DIFFERENT clause: round 1's pan-covered Fit is genuinely fixed (real drag-pan then a real trusted mouse click resets, hitAfterPan is button#dag-fit), but the author figure cannot be drag-panned at all — a (-180,-90) gesture moves it (-18,-9) because the browser's native image drag takes over (dragstart on #diagram-img, no mouseup), and the figure then follows the cursor with no button held; counterfactual draggable=false delivers the full (-180,-90). REQ-126 remains with the owner: VAL-186 now carries the owner_decision:pending marker that blocks Gate 8. Docs rewritten to current state: DEPLOY §0 gained the env-file line this pass actually ran, §1b round-trips 43 code keys both ways (one combined row for the injection-only ports), the aliases row + README + §6 carry D9's measured before/after and D10's workaround, and the last four history phrasings were removed from both manuals. Config sync: no new key, no changed default; rwe.config.example.json itself ships sonnet+default on one model, i.e. it reproduces D9 — documented, deliberately not patched. trace: 1463 items / 22 gaps, 0 未驗證 / 0 未真實驗證 (trace counts a real:true red as verified — read 08-validation, not the count); 3 drift gaps are new since round 1 (DES-112/DES-157 behind IMPL-208, DES-141 behind IMPL-210) and are left for the designer's amendment bullet, since an iter bump alone would push their own ten v22/v24 tests into the mirror-image drift. rtm.md regenerated: 0 ❌, 3 ⚠️ (REQ-126 owner-held, REQ-127 D9, REQ-129 D10). SEND BACK to impl for D9 and D10. PRIOR: 2026-09-09 v26 GATE 7.5 ROUND 1 NOT PASSED (validator) — booted from documented steps only (./deploy.sh --background as DEPLOY.md §0, plus four scratch instances; production rwe.service NEVER restarted, NRestarts=0). All 10 v26 REQs were exercised against real wiring (no SUT-boundary mock): 7 real:true GREEN (VAL-181 REQ-121, VAL-182 REQ-122, VAL-183 REQ-123, VAL-184 REQ-124, VAL-185 REQ-125, VAL-187 REQ-127, VAL-190 REQ-130) and 3 real:true RED — VAL-186/REQ-126 (effort never reaches OpenRouter: a transparent recording pass-through in front of the REAL OpenRouter API shows NO reasoning/reasoning_effort field and low-vs-high requests identical, while harness.effortApplied claims applied; chain isolated: SDK maps budgetTokens to --max-thinking-tokens, the CLI then sends thinking:{type:'adaptive'} (budget lost), LiteLLM maps that to reasoning_effort and DROPS it for openrouter — and the explicit shape 400s with UnsupportedParamsError under generateLiteLLMConfig's own config); VAL-188/REQ-128 (the v2 swimlane contract itself is real-green — all five refusal codes with line+expected+see, v1 grandfathering, harness table — but the cold-model clause FAILED: a fresh openai/gpt-5.6-luna given only tools/list + the guide needed FOUR workflow_register attempts, first refused PARSE_ERROR 'Unexpected token export' for wrapping the body in export default async function; the diagram was never the problem, the guide never states the script-body form); VAL-189/REQ-129 (viewBox+width=100%+preserveAspectRatio, wheel zoom, drag pan and 3s-poll survival all real-green in a real 1100px Chromium against production data, but after a pan the translated svg#dag-graph covers #dag-fit — elementFromPoint proves it — so the documented fit reset is unreachable by mouse). Seven defects recorded in 08-validation.md 'Defects found this pass': D1 three tools/list arrays with no items (Gemini clients reject the whole surface, observed), D2 gateway:'direct-fetch' without useLiteLLMProxy:false breaks every agent() call (alias vs rwe-proxy- name mismatch; terminal failure recorded as state:'done'), D3 claude-sonnet-5 static price row is $3/$15 vs the real $2/$10, D4 cache read priced at the input rate, D5 scripts/smoke.sh registered graph TD (FIXED here, re-run green), D6 the guide's missing script-body sentence, D7 agentSlots declared in KNOWN_FILE_CONFIG_KEYS but never forwarded by composeConfig (agentSemaphore stays 32). Docs rewritten to current state and re-run verbatim: README.md (both agent() examples rebuilt for the v2 contract + timeoutMs 300000 after a real timeout, schedule example rebuilt for create-then-claim) and DEPLOY.md (all v26-conditional/migration prose deleted, §6 given the five measured limitations). Config sync: no new config key this iteration; §1b round-trips against KNOWN_FILE_CONFIG_KEYS except D7. trace: 1442 items / 19 gaps — all ten 未真實驗證 gaps for REQ-121..130 closed, remainder = the unchanged pre-existing 16 漂移 + 2 未實作 + 1 TDD. rtm.md regenerated: 0 ❌, 3 ⚠️ (REQ-126/128/129). SEND BACK to impl for REQ-126, REQ-128 (fix then re-run the cold-model protocol with ANOTHER fresh subject), REQ-129." }
  review:       { passed: false, note: "v27 GATE 8 RE-REVIEW #4 (2026-09-13, reviewer) - NOT PASSED / SEND BACK: send_back=[design,impl], 2 blocking findings (0 HIGH / 2 MID), full record in 07-review.md section 'v27 GATE 8 RE-REVIEW #4'. CLOSED AND RE-VERIFIED: BF-4 (system.js:78 is `if (res.status !== 'ok')`, the one-token form; val-202:130-166 is the whole-route interception case and :104-121 now waits on .sys-table tr), plus BF-5 (run.js:512) and BF-6 (workflow.js:329) - two further sites of the same class the implementer found on its own sweep, reported first in IMPL-279 and then fixed in separate commits with separate falsifying tests. All three IMPL rows (IMPL-279 2552230 / IMPL-280 86617d7 / IMPL-281 35e00fc) carry their commit and a POST-COMMIT falsification. Reviewer ran the evidence itself: 6 files / 49 tests passed real-Chromium 152, 0 failed; tsc --noEmit exit 0; trace 1666 items / 35 gaps with a gap SET byte-identical to RE-REVIEW #3 (0 HIGH, no 斷鏈/孤兒/未真實驗證/待業主決策, no v27 IMPL lagged by any DES/UT/VAL row); dashboard_check 0 high / 7 mid (the same erDiagram bracket false positives) / 1 low, all SoT links resolve, headless render 43/43 svg, 0 err, 0 blank, 8 tabs, 0 pageerror; solid_check 0 mid / 10 low (the same 未認領檔案 set); module_check dormant; owner_decision sweep 0 pending on the fixed metadata key; Gate 7.5 confirmed unchanged (no mock-only REQ, 08-validation.md present, README+DEPLOY current-state with DEPLOY §0 一鍵部署 leading and a single 設定總表, and none of the three repairs makes a manual sentence stale). BLOCKING: (1) BF-8, DESIGN - DES-206 must state the POSITIVE rule for res.status !== 'ok' over the whole enumerated getJSON-consumer population (poll tick = no DOM write; first paint / selection change = clear + the explicit Unavailable component; NEVER a view-constructed payload), amended in place at iter v27m, no new DES/TASK id, no ARCH edit, no code. Both panels name its absence (A4-4 / QD3-R1) as the root cause of six rounds - ARCH-125 states only the negative, so four incompatible behaviours ship. (2) BF-7, IMPL - workflow.js:326 synthesizes an empty DAG payload on a degraded /dag; :328 paints it after paintSwimlane erases (run.js:219/:222) and :329 hands it to renderLegend, which with a healthy /api/runs/:id computes nodeCount from the fabricated cells (run.js:361) and prints 'completed - 0 個節點' (:367). Fix per BF-8's clause INCLUDING the run-switch branch (a naive bail would leave the previous run's graph under a newly selected chip), plus a val-199 case degrading /dag ALONE that asserts the [data-node-cell] count and the .run-summary node count are UNCHANGED and pageerror is empty over two ticks; correct IMPL-281's 'all 16 sites are guarded' sentence in the same edit. ARCHITECTURE CONSISTENCY: NO - adversarial 4 deviations (0 HIGH / 2 MED / 2 LOW), quality-dimensions 13 (0 HIGH / 3 MED / 10 LOW); the one live in-closure violation is BF-7. RECORDED AS DEBT, NOT BLOCKING: A4-2 (MID - server.ts:616 puts the raw exception message on an unauthenticated wire; NOT blocking because no ARCH clause pins the wire degraded VALUE - ARCH-130's closed-set/detail clause is explicitly about the LOG line, which server.ts:615 implements correctly - and the wire assignment is v2-era code b6f5659, not this iteration's; architect-lane note owed), QD-O5/A4-3 (MID, upgraded from LOW on corrected evidence - agent-panel.js:234-241 never reads res.status and paints in 0 / out 0 / cache read 0 / cache write 0 plus the false '無 system prompt 紀錄'; the two lenses split LOW vs MEDIUM and the split is recorded; routing is debt because the repair changes the panel's rendering shape, DES-205's lane), QD3-O2 (LOW - run.js:513/:502), DOC-R3-185 (LOW - RE-REVIEW #3 §5's 'system.js is the only view...' sentence was false when written and IMPL-281 inherited it). send_back does NOT include validation or architecture. .panel/ deliberately LEFT IN PLACE - this pass sends back. | PRIOR: v27 GATE 8 RE-REVIEW #3 (2026-09-13, reviewer) - NOT PASSED / SEND BACK: send_back=[impl], 1 blocking finding (0 HIGH / 1 MID), full record in 07-review.md section 'v27 GATE 8 RE-REVIEW #3'. THE THREE BLOCKERS RE-REVIEW #2 RAISED ARE ALL CLOSED and were re-opened on disk at file:line, not taken from the repair gates' notes: BF-1 at connection.js:39-40 (status = consecutiveFails >= 2 ? offline : degraded; prev.status survives only in the comment at :36) with UT :54-59 flipped; BF-2 at home.js:231 and run.js:475 (the workflow.js:350 guard shape, ahead of any repaint or further fetch) with one falsifying real-Chromium case per view at val-198:262 and val-200:215; BF-3 at 04-design.md:6815/:6816 re-stated and the DEBT-C owner_decision inheritance closed at :6817 and :7274. Executed here, not read: tsc --noEmit exit 0, and RWE_REQUIRE_BROWSER=1 vitest over the three closure files = 3 files / 31 tests passed / 0 failed (12/13/6, exactly IMPL-277/278's claim). Trace 1663 items / 35 gaps, composition identical to the RE-REVIEW #2 baseline, ZERO new drift rows from the v27k+v27l repairs. Dashboard renders 43/43 mermaid to <svg>, 0 pageerror, 8 tabs, all SoT links resolve. solid_check 0 mid / 10 low; module_check dormant. owner_decision sweep on the fixed key = 0 pending anywhere including journal.md. Gate 7.5 unchanged: no mock-only REQ, 08-validation.md passed, README+DEPLOY current-state with a verified one-command deploy. THE ONE BLOCKER (BF-4, impl lane): src/dashboard/ui/system.js:74 tests !res.body, but server.ts:611-616 answers HTTP 200 {degraded:'...'} which is truthy, so :77 calls buildTable and :49 reads data.cpu.cores -> TypeError. Measured in real Chromium by the v27l design panel: 0 rows, 3 pageerror, nav tag frozen live (the tag freezes because app.js:379's await view.onTick is unguarded, so the tick never reaches :383-384 - that unguarded await is DEBT D3-3, NOT in scope). Same ARCH-125 api clause BF-2 was raised on, in the one view BF-2's required shape did not name; in-closure code (TASK-212). Fix = ONE token, if (res.status !== 'ok') at :74 (system.js:72 already holds classifyResponse's answer; do NOT re-derive !res.body || res.body.degraded at the call site), PLUS replacing val-202-ported-tabs.test.ts:104-117 (form without the property: clicks the tab, asserts nothing) with the val-199:231 setRequestInterception recipe for /api/system. Grepped the siblings: issues.js:102 and models.js:55 are clean, so scope is exactly this. arch_consistent = NO (one live ARCH-125 violation at HEAD). Auto re-run is already spent, so this hands back to the orchestrator. .panel/ is deliberately LEFT IN PLACE (cleanup only on close). Non-blocking: 10 MID (the five parked REQs x2 rows) + 60 LOW recorded as debt in 07-review.md section 9, including seven new rows (D3-1..D3-6 adopted from the v27l design panel's measured-and-recorded list so they are not lost with .panel/, plus DOC-UT245). DEGRADED MODE: no playwright browser tool in session, so the dashboard render was confirmed with headless Chromium via puppeteer instead (43/43 svg, 0 errors, 8 tabs clicked). TOOLING NOTE: the plugin trace.py overwrites dashboard.html with a fork-incompatible render and drops metrics.jsonl with 536-gap numbers - both were undone; the deliverable is the fork's 1663/35 output. | PRIOR: v27 GATE 8 RE-REVIEW #1 (2026-09-13, reviewer) - NOT PASSED / SEND BACK: send_back=[architecture], 1 blocking finding (1 HIGH), full record in 07-review.md section 'v27 GATE 8 RE-REVIEW #1'. ALL 13 first-pass blocking findings are CLOSED and were re-verified on disk at file:line at 29eb8a0, four of them by execution: full suite 403 files / 2833 tests passed, 0 failed, exit 0; tsc --noEmit exit 0; a real headless-Chromium render of dashboard.html is 43/43 mermaid blocks to <svg> (was 40/43 - DASH-1a/1b/DASH-2 all render); analyze() enumerates 1661 items / 35 gaps and the 33->35 delta is EXACTLY the two newly minted follow-up tasks TASK-215/216, with zero new drift rows from four gates editing the ledger. arch_consistent flips NO -> YES; arch_violations empty. THE ONE BLOCKER (OWN-1, issue #15): 02-architecture.md:3365 still carries a live '- **owner_decision:** pending' on ARCH-124. The owner ALREADY RULED on 2026-09-13 in commit 29eb8a0 (keep the narrowing; REQ-131 acceptance amended at 01-requirements.md:1729 with an [AMENDED v27h] block) - only the marker was never flipped, and that commit body says so itself ('Still owed before Gate 8 can pass'). Repair = ONE bullet rewritten to answered(2026-09-13) - <the ruling>, plus two same-edit companions that the ruling makes stale: ARCH-124's amendment conditional at 02-architecture.md:3364 ('if the literal reading wins...') and DES-202's prose at 04-design.md:7274. No code change, no new ARCH/ADR/TASK id, no trace link touched. Auto re-run is already spent, so this hands back to the orchestrator. .panel/ is deliberately LEFT IN PLACE (cleanup only on close). Non-blocking: 10 MID (the five parked REQs x2 rows) + 51 LOW recorded as debt in 07-review.md section 9, including three new rows (DEBT-A the second DASHBOARD_HTML draggable pin at dashboard-page-source.test.ts:95, dispositioned by TASK-215; DEBT-B REQ-131's owner-amended mixed-state clause has unit-tier evidence only; DEBT-C DES-202's pending-inheriting prose). | PRIOR: v27 GATE 8 REVIEW (2026-09-13, reviewer) — NOT PASSED / SEND BACK: send_back=[impl, architecture, validation, design], 13 blocking findings (2 HIGH, 11 MID), full record in 07-review.md section 'v27 GATE 8 REVIEW' sections 2/3/6/8. HIGH-1 (AC-1, both architecture panels independently): ADR-054/INV-V27-7's disclosure key-set control asserts a hand-written fixture against itself (dashboard-disclosure.test.ts:36-43 over dashboard-wire.ts:125-131) instead of a served body, and the two endpoints this delta widened (/api/home, HTTP GET /api/runs/:id/agents/:agentId) carry no row at all. HIGH-2 (DOC-1, found by this reviewer): README.md:112/113/120/139/140 + DEPLOY.md:788 document Models sorting/filtering/slide-in, System stat cards + process table, and hidden-tab poll pause — none exists (models.js:6 'No sorting, no filtering'; system.js ports six table rows; grep visibilitychange|document.hidden over src/ = 0 hits); REQ-137/138/142 are recorded out-of-closure, so the manuals promise unbuilt features. MID: AC-2 tsconfig DOM+allowJs contradicts ADR-049/ARCH-124 by name; AC-3a/b ARCH-122's shell is replaceChildren'd away by app.js:427 and the C1 page-source pin guards deleted bytes; AC-4 a degraded /api/runs body crashes workflow.js:227-229 and worstOf() has no caller so the tag stays 'live' (reproduced with node); AC-5 three tabs poll once at mount while the footer clock keeps claiming freshness; AC-6 INV-V27-5's lock never touches the rendered update panel; AC-7 lib/clock.js breaks ARCH-124 purity; AC-8 woff2 ships bare 'Cache-Control: immutable' vs ARCH-123's full directive; AC-9 INV-V27-1's oracle substitutes a priced call for the terminally-failed one; DASH-1/DASH-2 three mermaid diagrams (02-architecture.md:3561 and :2531 — a ';' splitting the statement; 04-design.md:3306 — '{' inside a classDiagram member) FAIL TO RENDER in a real headless Chromium, verified by loading dashboard.html and root-caused with mermaid.parse; :2531 was mis-recorded as a checker false positive at v26 Gate 8. CLEAN THIS PASS: trace 1651 items/33 gaps (0 HIGH, 0 broken links, 0 orphans, 0 mock-only, 21 drift + 2 TASK all pre-v27 and byte-identical to v26's set, 10 MID = the recorded out-of-closure REQ-137/138/139/142/143); ZERO v27 doc-code iteration drift; solid_check 68 modules 0 mid (0 cycles, 0 undeclared deps); module_check dormant (no build: declared); owner_decision sweep = 0 pending on the metadata key (22 bullets: 16 '—', 6 answered/DECIDED); full regression npx vitest run = 403 files / 2829 tests passed, 0 failed, exit 0; Gate 7.5 real-tier evidence and the deploy.sh one-command path both verified genuine. .panel/ LEFT IN PLACE for the re-run. | PRIOR: v27: NOT YET RUN for REQ-131..143 (Gate 1 passed 2026-09-11; awaiting /sdlc-run). | PRIOR(v26): 2026-09-11 v26 GATE 8 RE-REVIEW #1 (reviewer) — PASSED / CLOSE: send_back=[], blocking_findings=[], 0 HIGH. Re-review after the workflow's ONE automatic re-run of [impl, architecture]; scope was the previously-blocking items only, per the dispatch. ALL 13 CLOSED, each re-verified by this reviewer at file:line, none taken from a repair note: H-1 resolveEffortApplied retired, direct-fetch now calls the SAME wireEffort and really imports PROVIDER_CAPS, caps?: Caps declared on LiteLLMGatewayClient.invoke, openrouter branch spreads effortBodyFields (INV-V26-2/ADR-045); H-2 terminalHttpFailure sets detail=`<status> <statusText>` on every HTTP failure and retryable:false on 401/403/404, retry loop gained the terminal break (ARCH-111/ADR-040); H-3 capDetail runs AFTER redact at the persist site, AgentRecord.detail exists and is written, redact-sweep gained sink (7) with the straddling-the-1024-byte-cap case (INV-V26-5); H-4 models_list and GET /api/models both read ModelBook.snapshot() and stamp the real fetchedAt per row (ARCH-116, api amended to the shipped per-ROW shape); M-1 numField/gaps name every dropped usage field into the existing unmapped counter that has a dashboard reader (INV-V26-6); M-2 failed branch writes both the record field and the usage-event field, IT-162 proves run_result.meta.unmappedMessages end-to-end through a real facade; M-3 v1FallbackGraph deleted, deriveExpectedGraph gained contract:'v1'|'v2' (INV-V26-3); M-4 LayoutCell carries tokens/costUSD/unpriced and the agent cell renders the per-call line (ARCH-118); M-5 UNDECIDABLE_SHAPE deleted from all three src sites AND docs/AUTHORING.md, both directions locked by test; M-6 ipc/protocol.ts declares spent?: {usd, tokens}; M-7 the guard's prose says SIX and the assertion is untouched; A-1 ten sites amended incl. ADR-038's HEADING, the PU node relabelled not deleted, owner_decision markers normalised — a PRICE_UNKNOWN sweep leaves only hits inside the prose arguing why it must not return; A-2 ADR-048's Action amended BY ARGUMENT to six, each member adjudicated by name against S-2, the toBe(6) pin and the seventh-entry case untouched. Also CLOSED this pass: review LOWs D-6 (caps 'unknown') and D-7 (additionalProperties:false). REMAINING, all recorded as known tech debt in 07-review.md, none blocking: MID R-1 (the M-2 residual — one-line seam at run-guard.ts:45, four named false sentences in types.ts/run-manager.ts/06-impl-log.md, DES-180 carve-out, and a specified falsifying two-folds-agree test) and MID IMPL-082 (pre-v26 TDD gap); 49 LOW = 23 trace gaps + 10 solid_check unclaimed files + 7 dashboard_check + 6 carried panel LOWs (D-1..D-5, D-8) + 3 ledger/tooling hygiene (dashboard_check mermaid false positives; the 2026-08-01 project-local trace.py; check-mermaid.ts:12 still says 'exactly-four-file allowlist' where ADR-048 now says six, and the 35-file uncommitted working tree whose IMPL amendments cite no sha — the orchestrator should land a checkpoint commit). Gate 7.5 re-confirmed untouched: 0 未真實驗證, 08-validation.md present, README.md + DEPLOY.md present and not touched by either repair half. Browser tools unavailable — dashboard QA ran in stated degraded mode (static checks in full). PRIOR: 2026-09-10 v26 GATE 8 REVIEW (reviewer) — NOT PASSED / SEND BACK: send_back=['impl','architecture'], arch_consistent=FALSE, gaps high=4 mid=9 low=51. Both architecture-expert groups were pre-run and consolidated (not re-spawned); every blocking claim re-verified on disk at file:line. Routing rule: blocking = (a) code violates a declared v26 INV-V26-*/ARCH-* api clause, or (b) the architecture body contradicts an owner ruling already in this ledger. IMPL (11): H-1 resolveEffortApplied is a 4th effort table and gateway/client.ts never reads PROVIDER_CAPS (INV-V26-2/ADR-045; plus invoke() drops caps?: Caps and the openrouter direct branch omits effortBodyFields); H-2 direct-fetch sets no retryable:false, no terminal break, no detail/status on any !res.ok arm (ARCH-111/ADR-040/REQ-122); H-3 detail is capped BEFORE redact(), AgentRecord.detail was never implemented, and redact-sweep gained no case (INV-V26-5); H-4 models_list.catalogFetchedAt is a hardcoded null and models_list bypasses ModelBook TTL/single-flight (ARCH-116); M-1 silent ?? 0 usage projections with no named counter (ADR-046 self-designates this a Gate 8 finding); M-2 capture() failed branch drops result.unmapped (INV-V26-6); M-3 v1FallbackGraph is a 2nd shape derivation that chains a parallel() (INV-V26-3/REQ-124); M-4 per-agent DAG cell shows no tokens/cost, LayoutCell lacks the fields (ARCH-118); M-5 UNDECIDABLE_SHAPE advertised with no producer (ARCH-119/121); M-6 ipc/protocol.ts still declares spent?: number (ARCH-118); M-7 no-skeleton-surface.test.ts header comments still say EXACTLY-FOUR while the allowlist pins six (impl-side half of A-2). ARCHITECTURE (2): A-1 the v26 body still publishes PRICE_UNKNOWN + nullable costUSD (02-architecture.md:2957/:3118/:3091/:3102) and ARCH-117 openrouter applied:true, contradicting ADR-038 (2026-09-08) and VAL-186 (2026-09-10) — a repeat miss, both Gate 4 design panels flagged it; A-2 ADR-048 Action names one file and 4->5 but the allowlist ships six with toBe(6) (ADR-022) — Gate 8 adjudicated workflow-catalog.ts as passing S-2, so amend the ADR. CLEAN: Gate 7.5 validation+handover PASS (0 未真實驗證/0 未驗證, VAL-200/201/202 real-green, v26 live on production, DEPLOY.md §0 一鍵部署 actually run), traceability 1486 items/24 gaps all recorded (0 斷鏈/0 孤兒, trend 65->24), solid_check PASS (0 high/0 mid), module_check dormant, dashboard SoT links all resolve, owner_decisions=[] (all four markers answered). Dashboard mermaid mids (6) are dashboard_check FALSE POSITIVES (erDiagram cardinality tokens + one apostrophe) — diagnosed individually, no doc change. No playwright in session: dashboard render QA ran in documented degraded mode. See 07-review.md v26 section. PRIOR: 2026-09-05 v24 GATE 8 REVIEW (reviewer) — NOT PASSED / SEND BACK: send_back=['impl','validation'], arch_consistent=FALSE, 3 HIGH. HIGH (all re-verified on disk, none descoped by any adjudication): (1) AF-1 ARCH-098/102 boot migration of legacy assets + mcp_provisions was never written AND workspace-gc.ts:66-88 now deletes the pre-v24 global asset tree under <workRoot>/assets (grep mcp_provisions|'legacy' over src/ is empty; server.ts:847 wires the sweep unconditionally) — data loss on upgrade; (2) AF-2 workflow-catalog.ts:472 writes triggers:[] as NULL so ADR-026 NOT_IN_RELEASE is unreachable (server.ts:775, webhook-registry.ts:279) — a released trigger keeps firing after it is un-declared; (3) AF-3 PRINCIPAL_REQUIRED is a live wire code (authz.ts:37,89) absent from the closed ERROR_CATALOG that ARCH-087/DES-137 make the single source (errors.ts) — same class as D-14, one function away. Also to impl: SF-1 CLAUDE.md both forbids and recommends git stash after 3c80cbd amended only the first hunk (claude-md-improver audit, 46/100 grade D); DR-1 three post-IMPL-187 src commits (d43d6d7/caf15c1/e9db0c4) carry no IMPL entry — TENTH occurrence of this ledger-honesty gap. To validation: V-1 Gate 7.5 was never landed — gates.validation.passed is still false with a ROUND 2 note while round 3 ran, passed and wrote 08-validation.md (front matter status: passed; state.yaml is OLDER than it); V-2 VAL-161 (REQ-116) still reads status: red / result: fail with no SUPERSEDED marker, and this repo trace.py is_real_test() (:186-187) never looks at status, so a red row silently counts as verification. After the impl fixes land, REQ-113/115/116 need their real-tier evidence re-observed (precedent: 08-validation.md:4966). CLEAN and checked: trace 1229 items / 19 gaps (1 mid TDD IMPL-082 + 16 low iter-drift + 2 low unimplemented TASK-018/153), 0 orphan / 0 broken-link / 0 未驗證 / 0 未真實驗證; solid_check PASS 45 modules 0 mid (10 low unclaimed files); dashboard_check 0 high — every SoT file:line link resolves, the 5 mid mermaid rows are checker false positives (erDiagram ||--o{ cardinality braces; the apostrophe in 'U's run'), re-derived by hand; README.md + DEPLOY.md present, current-state, history-free, single 設定總表, DEPLOY leads with 一鍵部署 ./deploy.sh --background which Gate 7.5 round 3 actually ran; git diff e9db0c4..HEAD -- src/ is EMPTY so round 3 evidence does cover HEAD; 35 tool specs = 35 advertised. 11 MID + 14 LOW architecture deviations (AF-4..AF-28) recorded as v25 debt in 07-review.md §4/§8. Panels consolidated, NOT re-spawned; .panel/ RETAINED because send_back is non-empty. No playwright in session (degraded mode declared). PRIOR: 2026-09-04 v23 GATE 8 RE-REVIEW #2 (reviewer) — CLOSE: send_back=[], arch_consistent=FALSE (0 HIGH; 3 MID + 8 LOW recorded as v24 debt), iteration can close. 07-review.md new top section written; prior v23 GATE 8 RE-REVIEW #1 marked superseded. Consolidated the two PRE-RUN panels (.panel/review/adversarial.md + quality-dimensions.md, both rewritten against HEAD 46a0c38) — NOT re-spawned, per dispatch. Both returned 0 HIGH independently and both closed the RE-REVIEW #1 blocking HIGH; this reviewer re-verified every load-bearing claim at file:line rather than accepting a panel summary. CLOSED+VERIFIED: R-1 inv 2 closure wrap (graph-analyzer.ts:329-345), R-2 settle seam (:231-248, one putDiagramResult caller at :241, 12 keys at :289-293), R-3 enabled choke point (:323 first statement of _startJob), R-6 TOOL_NAMES integer deleted, adjudication #9 AUTHORING.md params example (docs/AUTHORING.md:20-32). RECORDED DEBT: AC-1 MID FLOOR 2b not total (getDiagram :260 outside the try; unhandled rejection reachable at boot via sweepAtBoot/server.ts:1507; both panels graded MED on trigger rarity = disk-level read failure, and this reviewer found no fact they lacked to overrule upward; UT-136 fixtures only the write direction) — Gate 5 then 6 in v24. AC-2 MID zero-model-call settle silently swallowed when a prior ready row exists (:259-261 returns before the try; regenerate reports queued:true and nothing happens); consolidation correction — inv 5 letter holds (no row written), ARCH-085 readback-seam intent is what is violated. AC-3 MID NEW, raised by NEITHER panel and no prior gate: a stray directory literally named :memory: containing a 28KB catalog.db was swept into commit 9fc4439 by an over-broad git add and is still tracked at HEAD (git ls-files confirms); no src/ or tests/ code does this (every :memory: in src/ is the guarded sentinel; every test WorkflowCatalog uses tmpdir) so it is repo hygiene not a code defect, but a colon is illegal on NTFS so git clone fails on Windows — MID, Gate 6 in v24 (git rm -r --cached + .gitignore). AC-4 LOW enqueue not total and the decline unrecorded, AC-5 LOW FLOOR 2b guard placed outside _settle so _journal has two callers plus an unpinned cause drift on the ready branch, AC-6 LOW sweepAtBoot bypasses maxQueueDepth, AC-7 LOW server.ts:955 preempts the choke point (fix = amend inv 11, do NOT write the row: it would hide the version from missingDiagramCount), AC-8 LOW R-3 sequencing condition not met (9fc4439 landed 249 src lines + 556 test lines + nine RED oracles in one docs-labelled commit; no commit holds those oracles failing), AC-9 LOW three-consumers comment at diagram-gate.ts:22-24 unfixed a second round, AC-10 LOW HTTP describe route drops version/channel and the typed error code, AC-11 LOW jail= boot line names a directory nothing uses. TRACE: 1028 items / 18 gaps = 0 high, 1 mid (IMPL-082 TDD, since v14), 17 low (16 iter-drift pairs unchanged + TASK-018 since v3); 0 orphan, 0 broken-link, 0 mock-only, 0 unverified; rtm.md 106/106 real:true. DASHBOARD QA: 0 dead links across 1028 items; 3 MID bracket-imbalance are re-verified erDiagram crow-foot false positives; 1 LOW no offline fallback = recorded debt, and the debt item says port the fallback forward WITHOUT losing this repo copy of the caret-### ITEM_RE hardening (plugin 2.1.3 is caret-#{2,4} and would re-open the v7 false-gap incident). MODULE BOUNDARY: solid_check PASS, 23 modules, 0 high / 0 mid, 10 LOW unclaimed files carried. VALIDATION+HANDOVER: PASS — VAL-124..127 all tier acceptance / real:true / pass with real-boot stdout evidence; 08-validation.md present; README.md + DEPLOY.md present at layout.readme/layout.deploy, step-by-step 淺白繁中 with ASCII diagrams, history-free banner, single 設定總表 at DEPLOY.md:332 (the other 4 mentions are cross-references), and DEPLOY §0 leads with ./deploy.sh --background which Gate 7.5 round 5 actually ran for all 13 boots. Carried LOW manual debt unchanged: README iteration tags at :18/:62-74/:155/:171 incl. two version-diff sentences (:64, :74) plus DEPLOY :426/:813 — not stale, not duplicated, not false, so not escalated. SPECIAL-FILE REVIEWS: N/A (no CLAUDE.md/AGENTS.md/SKILL.md on any v23 IMPL files: line or in the v23 git range; the two 06-impl-log CLAUDE.md mentions are v21 prose citing the no-git-checkout rule). REVIEWER-RUN EVIDENCE: tsc --noEmit clean; npx vitest run 283 files / 1850 tests / 0 failed. DEGRADED MODE: no playwright browser tool in session, so no visual svg/tab/link-click confirmation — static link + lexical check substituted. .panel/ REMOVED per reviewer contract task 6 (send_back empty)." }
pending:
  - "v27h OWNER RULING (2026-09-13) — ARCH-124 connection-tag semantics: the owner ruled KEEP THE NARROWING. Live shows only when EVERY route the visible tab depends on is ok; a mixed state shows degraded. Rationale adopted from the architect: a tag reading Live while the table the user is looking at is fed by a degraded route lies to the operator, which contradicts this repo own degrade-never-pretend stance. CONSEQUENCE ALREADY APPLIED BY THE ORCHESTRATOR: REQ-131 acceptance amended in 01-requirements.md (Gate 1 artifact, and the architect correctly refused to edit an acceptance clause itself) - the text changed, NOT the implementation, which shipped at v27g in src/dashboard/lib/connection.js:24-37. A three-state option (live / partly-degraded / offline) was considered and declined: the handoff defines only Live / Offline / Demo data, so a fourth source tag would exceed the fidelity oracle DES-209 established. STILL OWED: ARCH-124 own owner_decision marker in 02-architecture.md must be flipped from pending to answered citing this ruling - it BLOCKS Gate 8 until it is."
  - "RESOLVED (2026-09-13, v27 Gate 7.5 RE-RUN, validator, VAL-208) — was: v27 GATE 7.5 BLOCKING FINDING (2026-09-12, validator), NOT WAIVED, routed to Gate 6 — REQ-134
    FAILS at the real tier: `src/dashboard/dashboard.css`'s `.cell` (the swimlane node) lays out
    `cell-dot`/`cell-label`/`cell-model`/`cell-effort`/`cell-usage` as FIVE `flex-direction:column`
    siblings; REQ-134 specifies THREE rows (①dot+label together ②model+effort together ③usage).
    `cell-label`/`cell-model` both carry `overflow:hidden`, which resets a flex item's automatic
    minimum size to 0 (CSS Flexbox §4.5), so the column's shrink algorithm compresses them to ~30%
    of their declared line-height — measured on a REAL completed run via real Chromium
    `getBoundingClientRect()`: `.cell-label` 5.08px rendered vs 15.5px expected, `.cell-model` 3.92px
    vs 13.2px expected. The agent label and model name render as an illegible sliver of glyph tops
    in EVERY node, in both themes. A bounded diagnostic (`flex:none` override, no `src/` touched)
    confirms the fix needs the missing two-row grouping wrapper, not a one-line CSS patch — the
    74px cell has no room for 5 full-height rows. Full record, screenshots, and a swept blast-radius
    check (every OTHER v27 text surface is clean) in `08-validation.md` VAL-208. **MECHANICAL
    CAVEAT for whoever next reads `sh .sdlc/trace --check`: this does NOT show as a REQ-134 gap** —
    VAL-207/VAL-212 (siblings tracing the same REQ) are legitimately `real:true` for their own
    clauses, and trace.py's `verified_real` only requires ANY ONE real:true item per REQ. Read this
    pending item and 08-validation.md, not the dashboard's gap count, to know REQ-134 is open.
    OWNER OF THE FIX: Gate 6 (implementer), `src/dashboard/ui/run.js` + `dashboard.css`'s `.cell`
    row structure. Also recommend (not built here): one new `SPEC_ROWS` kind asserting
    `getBoundingClientRect().height >= k * lineHeight` for text anchors — the entire real-Chromium
    tier currently checks `getComputedStyle` property values only, which cannot see a flex-shrink
    clip. A secondary, UNDIAGNOSED visual glitch was observed in the same screenshots (the run
    page's top-left usage banner text overlaps: `410 tok` runs into `in 384 · out 26 · cache read 0
    · cache write 0< $0.01`) — flagged for the same fix pass to look at, not analysed further.
    RESOLVED: Gate 6 (commits 23a909a/1ec82b5, IMPL-250..252) added the missing `.cell-head`/
    `.cell-meta` grouping wrappers; re-measured for real at Gate 7.5 RE-RUN (2026-09-13, validator)
    against a NEW real completed run on a fresh `deploy.sh` scratch instance: `.cell` now has 3
    direct children, `.cell-label` clip ratio 0.999 (was 0.33), `.cell-model` 0.909 (was 0.30), all
    four `.cell-dot` states (done/failed/predicted-queued/running) measured on genuinely distinct
    real runs. Full record: `08-validation.md` VAL-208 (amended in place). The secondary usage-
    banner overlap glitch was independently fixed too (`f5ee006`'s `.usage-row` class hook)."
  - "RESOLVED (2026-09-13, v27 Gate 7.5 round 3, validator, VAL-209) — was: v27 GATE 7.5 RE-RUN
    BLOCKING FINDING (2026-09-13, validator), NOT WAIVED, routed to Gate 6 —
    REQ-135 FAILS on the product's real PRIMARY page. The design handoff
    (`.sdlc/design-handoff/README.md` §2/§3) describes ONE view: the workflow-detail page's own
    swimlane, click a node to open the agent panel — there is no separate 'run view' in the design.
    The real user path (Home → click a workflow card → `/dashboard/workflow/:name`, `app.js:399`)
    renders its swimlane via `ui/workflow.js`'s own two direct `paintSwimlane(...)` calls
    (`workflow.js:292`,`:306`), NEITHER of which passes `onSelectAgent` — measured on a real loaded
    page: `document.querySelector('.cell-layer').onSelectAgent` is `undefined`, clicking a real
    `[data-node-cell][data-agent-id]` fires no `/api/runs/:id/agents/:agentId` request, no
    `[data-agent-panel]` ever appears. `openAgentPanel()` itself works fine (verified by calling it
    directly) — this is a wiring gap in `workflow.js` only. The ONLY route where the panel opens is
    the legacy `/dashboard/<runId>` route (`ui/run.js`'s own `render()`, whose default
    `onSelectAgent` — IMPL-224 — is correct), which is also the ONLY route
    `tests/acceptance/val-201-agent-panel.test.ts` navigates to, so no test currently catches this.
    A SECOND, independent REQ-135 defect found on that same working route: `agent-panel.js`'s
    `openAgentPanel()` reads `window.event` AFTER `await getJSON(...)` has already resolved, so it
    is always `undefined` by then — REQ-135's 'clicked node in the right half → panel slides from
    the left' clause never fires (measured: clicking the rightmost real cell on a real completed run
    still opens `class=\"agent-panel\"`, never `from-left`). Full record, commands and measured
    output: `08-validation.md` VAL-209 (amended in place). **MECHANICAL CAVEAT, same shape as
    REQ-134's above**: `sh .sdlc/trace --check` will NOT show REQ-135 as a gap — VAL-209 is
    `real:true` (a genuine real-tier run; its FAILURE is the real evidence) and `trace.py`'s
    `is_real_test`/`verified_real` only checks `real`, never `result`
    (confirmed empirically: `REQ-135 in verified_real` reads `True`). Read this pending item,
    `08-validation.md` VAL-209, and `rtm.md`'s REQ-135 ⚠️ row — never the trace gap count — to know
    REQ-135 is open. OWNER OF THE FIX: Gate 6 (implementer), `src/dashboard/ui/workflow.js`'s two
    `paintSwimlane` call sites (thread `onSelectAgent: (id,lbl) => openAgentPanel(runId,id,lbl,
    {lang})`, the same default `run.js:430` already implements) and `src/dashboard/ui/
    agent-panel.js`'s `window.event` timing (capture the click coordinate synchronously, before the
    `await`, and pass it through the already-accepted `opts.nodeCenterX`/`opts.graphWidth`). Also
    recommend (not built here): `val-201-agent-panel.test.ts` should add a case against
    `/dashboard/workflow/:name`, not only the legacy route.
    RESOLVED: Gate 6 (commit `0673f22`, IMPL-267/268) wired `onSelectAgent` into `ui/workflow.js`'s
    two `paintSwimlane` calls and fixed `agent-panel.js`'s `window.event` timing exactly as
    diagnosed above; re-measured for real at Gate 7.5 round 3 (2026-09-13, validator) against a
    NEW real 4-lane run on a fresh `deploy.sh` scratch instance: `.cell-layer.onSelectAgent` is now
    a function, clicking the primary route's real leftmost/rightmost node cells fires real
    `/api/runs/:id/agents/:agentId` requests and opens `[data-agent-panel]` with 6 stat cards, and
    the slide side now genuinely follows click position (`agent-panel` on the left-half click,
    `agent-panel from-left` on the right-half click). REQ-133/REQ-134 regression spot-checked clean
    on the same instance. Full record: `08-validation.md` VAL-209 (amended in place)."
  - "RESOLVED (2026-09-12, commit 9812645) — was: v27 UNLANDED CLAUSE, NO WAIVER — TASK-200 DoD requires the tool-surface tables run_agent_log row to be regenerated; it was NOT, and v24-tool-surface.md is untouched since v26. Unlike TASK-199s equivalent sub-clause this one carries NO orchestrator waiver and is NOT known to be structurally unsatisfiable - it simply did not happen. IMPL-241 records it plainly rather than implying it landed. Gate 7.5 or a follow-up must either regenerate the row (RWE_TOOL_SURFACE_REPORT=1 over the real-server acceptance suite) or the orchestrator must waive it on stated grounds. The cross-repo harness.prompt grep the same DoD requires WAS run this pass against /home/user/Documents/remote-workflow-plugin: zero hits. RESOLVED: an implementer ran RWE_TOOL_SURFACE_REPORT=1 over the real-server acceptance suite for real (commit 9812645) — the artifact is genuinely fresh (new run ids, createdAt moved 2026-09-08 -> 2026-09-12); the diff is identifier/timestamp churn only, the run_agent_log shape is byte-for-byte unchanged. Verified at Gate 6.5+7 (verifier, 2026-09-12): file mtime and commit both confirmed on disk."
  - "v27 ORCHESTRATOR CORRECTION (2026-09-12) — a Gate 6 implementer reported that REQ-134 row 2 (model short name + effort tag) needs a NEW public wire field because `effort` is absent from EXPECTED_AGENT_RECORD_KEYS, and recommended a separate larger task touching agent-executor.ts/types.ts. THAT IS WRONG and must not be actioned. The design does not read effort off the run record at all: the handoff Workflow Dashboard.dc.html:637-638 reads `const p = pAgents[c.label]; const effort = p.effort?.default` - i.e. the DECLARED effort from GET /api/workflows/:name/describe params.agents, not the applied effort from AgentRecord. That field is GUARANTEED to exist: src/params/contract.ts:259-311 enforces model/effort/timeoutMs as REQUIRED with a .default since v24 (DES-144), and validates the value. model follows the same line: `rec?.model || p.model?.default` - run record first, describe as fallback. src/dashboard/ui/run.js already fetches per-run data on tick; it needs the describe join, not a wire change. Declared-vs-applied is the right distinction for a lane cell anyway."
  - "v27 ORCHESTRATOR WAIVER (2026-09-12) — TASK-199 DoD sub-clause 'the regenerated tool-surface table must name the four optional fields (costUSD/unpricedCalls/tokensTotal/agentCount)' is WAIVED. An implementer regenerated the artifact for real (RWE_TOOL_SURFACE_REPORT=1) and demonstrated the clause is structurally unsatisfiable as the artifact is built: RunSummary declares the four fields last (src/types.ts:372-375) so they serialize at the tail of each run object, while tests/acceptance/v24-tool-surface.test.ts:162 truncates every observed value at 200 chars - a budget already spent inside the FIRST run object. The DoD's intent (the four fields are documented) is already met: README.md names all four. Waived rather than met by raising the truncation limit inside TASK-195s test file, which would change an unrelated task's artifact to satisfy this one. Recorded so Gate 8 sees a decision, not an unexplained miss."
  - "v27 GATE 5 DEFECT QUEUE (2026-09-12, accumulated across three Gate 6 passes, all REPORTED not worked around) — (1) RESOLVED (2026-09-12, verifier) - UT-249 in dashboard-client-corpus.test.ts asserted src/dashboard/**/*.js does not exist; the client tree legitimately exists now, so the premise was stale. clientCorpus() gained an optional root param and the case now injects a real empty temp dir; see gates.tests.note for detail. (2) RESOLVED at cb895d0 - dashboard-no-external-host flagged http://www.w3.org/2000/svg, an XML namespace identifier not a network fetch, via a bare https?:// regex; fixed with a narrow stripNamespaceUris() carve-out (re-verified 2026-09-12, verifier: 2/2 green, and the hunk read directly from cb895d0's own diff). (3) RESOLVED at cb895d0 - dashboard-no-design-values.test.ts:74 stripped comments from the corpus ONCE for the whole describe, so the .style check at :101,107 could never see its own `// rwe-allow-style:` exemption; fixed by having that one check read the raw (unstripped) clientCorpus() instead of the module-level stripped corpus (re-verified 2026-09-12, verifier: 7/7 green, and the hunk read directly from cb895d0's own diff). (4) RESOLVED at cb895d0 - the composePrompt empty-segment fix closes the s-empty property row; no designer ruling was needed after all. (5) RESOLVED at cb895d0 - the AgentRecord literal type-checks and tsc is at 0 errors. NOTE: items (4) and (5) of this very queue were STALE and were corrected by a ledger reviewer that CHECKED the note instead of trusting it - the orchestrator's own list carried the exact defect this ledger keeps asking everyone else to avoid. (6) RESOLVED (2026-09-12, verifier) - static-assets-route.test.ts's traversal case used fetch(), which collapses the segments per WHATWG URL before the request is sent, so the case could never exercise what it claimed - the server is structurally safe (bare Map.get over a fixed allowlist, no path ever built from caller input), the test transport was the defect. Rewritten to send the raw byte string over node:http's path option; see gates.tests.note for detail."
  - "v27 ORCHESTRATOR AUTHORIZATION (2026-09-12) — val-193-dag-fit-and-columns.test.ts case 3 MAY be re-pointed from the `#dag-graph text` selector to `.cell-usage`. This is NOT a new decision: Gate 1 recorded constraint C2 (01-requirements.md, Round v27) as 'retain the Chromium selector anchors as compatibility anchors; where the design genuinely forbids it, RE-AUTHOR the test under the SAME VAL id and re-run REQ-129 real validation, never silently remove it'. This is that exception: DES-209 moves the per-cell token/cost line to an HTML .cell-usage element, so no SVG <text> remains under #dag-graph for case 3 to find. The anchor #dag-graph ITSELF survives and must keep working - only the assertion about its text descendant moves. TASK-210s implementer measured the SVG-text alternative and rejected it on evidence (.cell-layer paints above the SVG behind an opaque .cell background, and .cell-usage sets no fill so the SVG default black is invisible on #18191b). OWNER OF THE EDIT: the tests/verifier gate, not an implementer - and the VAL id stays val-193, with REQ-129s real-run validation repeated at Gate 7.5."
  - "RESOLVED (v27c Gate 3+4 design delta, 2026-09-12) — closed by TASK-214 (new, owns src/dashboard/dashboard.css end-to-end: the declared class contract, every component/view section, the two locks) + DES-209 (new) + 6 TASK / 5 DES amendments in place at iter v27c, with ordering rule 6 putting TASK-214 before val-198..202 are judged. Three things the panel measured that the routed note did not know: the gap is a VOCABULARY gap (29 of 40 class names ui/*.js sets have no rule at all), REQ-134 node is not deliverable on the pure-SVG painter on disk (ellipsis, an in-node .tag-neutral and --shadow-md have no SVG form) so DES-209 decides the substrate, and the committed dark accent ramp runs the wrong DIRECTION for REQ-134 running-node fill (dashboard.css:22 accent-100 = L .93, near-white under near-white ink) so DES-201 corrects it. One owner_decision is OPEN on DES-209 (vendor the handoff README, or scope 99% to the enumerated REQ constants) and BLOCKS Gate 8 until answered. WAS: v27 GATE 6 GAP FOUND BY AN IMPLEMENTER, ROUTED TO DESIGN (2026-09-12, orchestrator) — src/dashboard/dashboard.css is listed in TASK-205 ONLY, whose scope was porting the PRE-v27 component CSS onto the new token set. The four tasks that build the new surfaces (TASK-207 swimlane/runlist/agent models, TASK-210 ui/run.js, TASK-211 ui/agent-panel.js, TASK-212 ported tabs) each require pixel-precise CSS in their DoD but none lists a .css file, and TASK-205 is already done. Verified on disk: dashboard.css is 126 lines and matches the rweGlow/rweRing/rweSweep/slide/backdrop/swimlane/lane- class family exactly ONCE - the handoff spec requires seven keyframe sets plus node-state, agent-panel slide-in + backdrop, lane-header and ported-tab classes. Left unassigned, REQ-132/134/135 visual acceptance and the owner's 99%-fidelity bar cannot pass, and an implementer would have to invent the CSS against no DES - the exact drift the implementer refused to risk and flagged instead. OWNER OF THE FIX: the design gate (task cards are its artifact), NOT the orchestrator and NOT an implementer improvising. Routed as a design delta ahead of the impl re-run."
  - "v27 OWNER DECISION ANSWERED (2026-09-11, orchestrator) — ADR-051/ADR-055 predicted-overlay mask under auth: owner ruled OPEN (reverse the mask). Recorded in 01-requirements.md Round v27b + the acceptance of REQ-133/REQ-134/REQ-140. REVERSES v22 send-back H2 / DES-114 / ARCH-073 / ARCH-075 / ADR-012 for the predicted-overlay predicate ONLY; Won't-have D1 (no per-principal dashboard permission tier) is NOT reopened. Gate 6 was stopped mid-implementation so the reversal lands before any product code was written against the masked decision; Gates 2/3+4/5 outputs stay on disk and are amended in place by a scoped delta run (gates architecture+design+tests, impactIds REQ-133/134/140), after which impl->review runs over the full Sprint A closure. Gate 5 artifact that must flip: tests/integration/dag-masking-auth.test.ts."
  - "v26 GATE 7.5 ROUND 7 (2026-09-09, fixer) — OUT-OF-CLOSURE FIX PASS, ruled in by the orchestrator and recorded so Gate 8 sees a decision, not creep. D13: `schedules.workflow` stayed NOT NULL on every UPGRADED workRoot (the v24 relaxation only reached the fresh CREATE TABLE), so `schedule_create` failed on every upgraded deployment since v24 while passing on every test's fresh db; fixed with the transactional, PRAGMA-guarded table rebuild (IMPL-218 / VAL-200 / IT-159, commit 5f5742b), verified against a byte copy of this box's production schedules.db. Sibling table `webhooks` checked and already correct (v24 TASK-156). D14: deploy.sh step 2 reported on the repo-root rwe.config.json even when RWE_CONFIG_PATH pointed elsewhere; now resolves before step 2 and uses it throughout (IMPL-219 / VAL-201 / IT-160). No gate flag flipped: gates.validation.passed stays as round 6 set it, and Gate 8 remains blocked by VAL-186 and VAL-187 owner_decision markers, which are the owner's and untouched."
  - "RESOLVED (closed at Gate 8 v23 RE-REVIEW #2, 2026-09-04) — was: OPEN DOC DEFECT found at Gate 7.5 v23 ROUND 5 (docs/AUTHORING.md rule 1 illustrative meta.params example refused by the running engine with PARAM_CONTRACT_INVALID). FIXED at commit 46a0c38 (adjudication #9) and verified on disk this pass at docs/AUTHORING.md:20-32: the example now teaches knobs:{effort:{type:enum,enum:[low,medium,high],default:low}} + args:{topic:{type:string}}, states that type is REQUIRED and the vocabulary is string|number|enum, and correctly scopes the ignored-not-rejected fallback to a WRONGLY-NESTED block only. REQ-106 clauses and VAL-117/VAL-126 remain green. No further action."
  - "GATE 7.5 (v14 ROUND 2) PASSED (2026-08-16): REQ-083 key.prompt fix verified via live Ollama run. Production rwe.service (port 8899, qwen2.5:7b via SDK+LiteLLM proxy) restarted with RWE_SECRET_VAL092_SECRET wired; workflow_run dispatched agent('Echo...val092-secret-tok-abc9981xyz...') → runId 8cdbed02; journal.jsonl JournalEntry key.prompt='...‹secret:VAL092_SECRET›...' (REDACTED, not raw); workflow_agent_log message event text='...‹secret:VAL092_SECRET›...' (marker, not raw); IT-075 extended sink-4 case: 5/5 pass (getJournal() entries clean). VAL-092 flipped green/pass/real:true. Trace 704 items / 6 pre-existing gaps (REQ-012 OIDC deferred D5; IMPL-082 mid; UT-058/UT-064/IT-057 low; TASK-018 low). 1170/1170 pass. No new config keys. gates.validation.passed=true; current_stage→review."
  - "GATE 7.5 (v7 ROUND 1) PASSED (2026-07-24): REQ-037..040 all real-validated. REQ-037 real (routing+security-invariant; non-Anthropic live; Anthropic-direct honest partial). REQ-038 real (workflow_run openrouter/nex-agi/nex-n2-pro → PONG). REQ-039 real (models_list{} → 100 entries, live Ollama+OpenRouter federated). REQ-040 real (filtered result 5/100). VAL-046..049 added. 4 v7 未真實驗證 gaps cleared; residual: REQ-012/TASK-018/IMPL-082."
  - "GATE 7.5 (v5 ROUND 1) PASSED (2026-07-19): REQ-027..030 all real-validated. REQ-027 real (issue_report → issueNumber:1, url:https://github.com/HsuJavis/remote-workflow-engine/issues/1, issue genuinely created in private repo). REQ-028 real (live call succeeded only with RWE_SECRET_GITHUB_TOKEN; GITHUB_TOKEN_MISSING path via IT-043). REQ-029 real (authenticated GET confirmed labels [agent-reported,severity:low] + all body sections present). REQ-030 real (live success path no-hang; error-bound paths via UT-057). VAL-036..039 in 08-validation.md. Live engine: systemd user service 127.0.0.1:8787. Full suite 535 green (2026-07-19). current_stage -> review; Gate 8 next."
  - "GATE 7.5 (v3 ROUND 1) PASSED (2026-07-18): REQ-016..021 all real-validated. REQ-016 capability-limited-real (SDK gateway end-to-end, D-F11 accepted). REQ-017 real (MCP_NOT_PROVISIONED error + probe + handle-stored-not-value). REQ-018 real (SECRET_MISSING + workspace clean). REQ-019 real (HOOKS_UNSUPPORTED). REQ-020 real (val-023 2/2 8s). REQ-021 real (boot guard WORKROOT_INSIDE_PROJECT + clean boot). VAL-025..030 in 08-validation.md. Live engine systemd user service 0.0.0.0:8787 confirmed. v3 acceptance tests 17/17."
  - "GATE 7.5 (v2 ROUND 2) PASSED (2026-07-04): D-V2V-1 (REQ-009 asset wiring) and D-V2V-2 (REQ-008 browser dashboard) both re-verified for real — a real spawned claude CLI subprocess's own --mcp-config/--setting-sources=project argv (read via ps aux, not source-reading) confirms the pushed mcp-config asset reaches a real agent() call; a real materialized SKILL.md on disk inside the run's own workspace confirms the pushed skill reaches it too; the recursion guard (D4) re-confirmed excluding this system's own plugin skill + a self-referential mcp-config end-to-end against this round's own real instance; the /dashboard HTML page re-confirmed real (curl+DOM-content-assertion tier, no headless browser available in this sandbox) with a live-update-without-reload demonstration. REQ-010/011/015 re-confirmed via documented smoke check (real Claude Code CLI recheck, real scripts/smoke.sh pass, real SIGTERM orphan-reap) with no regression, not re-litigated in full per the binding CONVERGENCE RULE. VAL-017 flips red->green; new canonical VAL-018 green. Full evidence: 08-validation.md 'v2 ROUND 2' section + gates.validation.note above. gates.validation.passed=true, current_stage -> review (Gate 8 next)."
  - "GATE 7.5 (v2 ROUND 1) NOT PASSED (superseded, preserved for history) — REQ-009 FAILED at the real tier: asset_push/asset_list/asset_delete/recursion-guard/mcp-config live-probe were all real-confirmed green (VAL-009), but NO code path wired a pushed skill or MCP server config into any agent() call (ClaudeAgentSdkGatewayClient hard-coded settingSources:[]+strictMcpConfig:true with mcpServers never populated; composeConfig()/createServer() never threaded AssetSyncService's stored assets into the gateway at all — confirmed by direct source reading, zero grep matches for 'asset' in agent-executor.ts/run-manager.ts/gateway/*.ts). REQ-009 clauses 1/2 ('agents can invoke that skill'/'agents can call its tools') were unmet. Item VAL-017 (08-validation.md) recorded this red/fail. REQ-008/010/011/015 were ALL real:true green that round (VAL-008/010/011/016). Routed REQ-009 back to Gate 5/6 (D-V2V-1) and REQ-008's JSON-only dashboard back to Gate 5/6 (D-V2V-2) — both now CLOSED, see the ROUND 2 entry above."
  - "GATE 8 CLOSED (2026-07-03 22:05/22:10): all 6 D-G8-1..6 binding closing-fixes independently re-verified against the current source tree, tests, and Gate 7.5 round-7's real-process evidence — see 07-review.md's 'GATE 8 CLOSING RE-REVIEW' callout and gates.review.note. 0 remaining unfixed HIGH architecture-consistency findings. 7 MEDIUM/LOW findings (v1.1 backlog) remain deferred by binding decision, correctly recorded, not silently dropped. gates.review.passed=true. Iteration v1 closes; no further gate action required unless a v1.1 pass is chartered for the backlog items."
  - "Gate 8 closing-fixes round (D-G8-1..6, IMPL-051, 2026-07-03 21:50): all 4 HIGH review findings (V3 nested-workflow callSeq collision, O-1 transcript black-box, C-1 placeholder tools/list, S-1 no default timeoutMs on the zero-config default gateway path) plus 2 MEDIUM (V2 stale pre-dispatch budget check, V5 full process.env forwarded to spawned CLI) are now FIXED at the code/test tier — full suite 217 tests, 215 green (2 pre-existing unrelated reds, see gates.impl.note), tsc clean, trace 18 gaps (unchanged baseline). The remaining 9 MEDIUM/LOW findings are recorded as a v1.1 backlog in 07-review.md per this round's binding user decision (NOT fixed this round, not silently dropped). CLOSED — see the GATE 8 CLOSED entry above for the re-review outcome."
  - "GATE 7.5 PASSED at round 6 — CONVERGENCE RULE satisfied: every v1 REQ acceptance clause now has real:true green evidence (VAL-001..007/013/014/015 all green in 08-validation.md), or is covered by a binding accepted-gap decision (D-V3 paid-provider gap; D-F11 model-capability-tier tool-use gap, new this round). Next stage: review (Gate 8)."
  - "CONFIRMED RESOLVED FOR REAL (Gate 7.5 round 6, D-F12): real 3-agent parallel() against real Ollama, polled via workflow_status while status:'running' — all 3 agents showed state:'running' (was agents:[] at round 5). Correct transition to 'done' on completion. No further action needed."
  - "CONFIRMED RESOLVED FOR REAL (Gate 7.5 round 6, D-F13): suspended a real ~57s essay-generation call, resumed it — a NEW subprocess spawned (not a cache replay), 27s wall-clock to a real 940-output-token completion (was an instant null at round 5). Also freshly confirmed workflow_stop + cached-prefix resume with an EDITED script (unchanged call replays from cache, changed call re-runs live with new content) — first real re-verification of this REQ-006 3rd-clause behavior since round 1. No further action needed."
  - "RE-CLASSIFIED as an accepted model-capability-tier gap, NOT a code defect, NOT a blocker (Gate 7.5 round 6, D-F11, per D-F11's own binding two-stage instruction): ClaudeAgentSdkGatewayClient.invoke()'s curated allowedTools/tools/settingSources/strictMcpConfig fix is confirmed genuinely on the wire (ps aux shows --allowedTools Read,Write,Bash --tools Read,Write,Bash --setting-sources= --strict-mcp-config; real token usage dropped ~4095->~1550-2016) — but re-running round-5's exact write/read tool-use probes with this fix live, qwen2.5:7b STILL never emits a tool_use message (direct SDK query() probe with the same curated options: num_turns:1, zero tool_use messages). This environment has no local Ollama model larger than 7B to try instead (confirmed: qwen2.5:7b/qwen2.5vl:7b/bge-m3-embedder only). Recommendation for future work: try a larger/more tool-calling-capable local model (e.g. qwen2.5:32b, qwen2.5-coder:32b) or a paid provider with sandbox credentials — filed to 08-validation.md's v1.1 backlog, not a gate blocker."
  - "New minor cosmetic finding, non-REQ-blocking (Gate 7.5 round 6, v1.1 backlog): an AgentRecord whose call was cut short by workflow_suspend/workflow_stop never transitions out of state:'running' — stays stuck forever in workflow_status.agents[] alongside the new record a live re-run creates. The run's own final result is correct (D-F13 above); no REQ acceptance clause requires an 'aborted' state value; purely a display nit. Filed to 08-validation.md's v1.1 backlog."
  - "Operational hazard, not REQ-blocking (first found Gate 7.5 round 5, RE-CONFIRMED STILL OPEN round 6, reproduced live twice this round): LiteLLMProxyManager hard-codes port 4000 (not derived from ServerConfig/FileConfig). Combined with the pre-existing known orphan-litellm-on-shutdown limitation, a second server instance started while an orphan from a prior instance still holds port 4000 gets a false-positive healthy boot against the STALE orphan instead of its own freshly-generated proxy (round-6 repro: ss -tlnp confirmed port 4000 held by the FIRST instance's orphan the entire time a 2nd/3rd instance, each with a different alias config, reported '[remote-workflow-engine] ready'). Documented as a pre-launch-cleanup step in DEPLOY.md §1/§2/§4/§5 and in 08-validation.md's v1.1 backlog (suggested fix: derive the port or pick one dynamically)."
  - "Minor operational cleanup (not REQ-blocking, documented as known limitations in DEPLOY.md, unchanged since round 2, re-confirmed still open round 6): main.ts's SIGTERM/SIGINT shutdown never stops the managed LiteLLMProxyManager subprocess (orphaned litellm process after every graceful restart, re-confirmed this round across all 5 server instances); LiteLLMProxyManager.start()'s mkdtemp() temp config dirs are never cleaned up (cosmetic /tmp accumulation). Both filed to 08-validation.md's v1.1 backlog."
  - "User decision still open (carried from round 1, D-V3 already accepts Ollama-only v1 real coverage, not re-raised as a new ask): no ANTHROPIC_API_KEY/OPENAI_API_KEY/GEMINI_API_KEY in this environment — REQ-004's paid-provider success path beyond the terminal/missing-credentials branch remains real-unverified. local Ollama + gateway:\"sdk\" (default) is fully real-verified for REQ-004 (all 4 clauses green). Whether paid providers' models correctly perform tool-use through this same SDK gateway is ALSO unverified (only Ollama's tool-use non-firing has been confirmed this round; a paid provider might behave differently) — this is the D-F11 model-capability-tier gap's own open question, not a new ask."
  - "tech_stack drift note (not blocking, for awareness, unchanged from round 1): the actual delivered architecture is a hand-rolled JSON-RPC-over-HTTP server (src/server.ts) with two GatewayClient implementations (direct-fetch/LiteLLM-proxy-fronted, and a real @anthropic-ai/claude-agent-sdk session) rather than the @modelcontextprotocol/sdk-based design originally sketched at Gate 2/3 — functionally meets the REQs. Real Node requirement is 22.6+ (native --experimental-transform-types), not the documented 20+. Real Python requirement is 3.11/3.12 for the litellm[proxy] subprocess dependency, not previously documented at all."
  - "Config-drift found and fixed at Gate 7.5 round 6: rwe.config.example.json already shipped defaultAllowedTools (from a prior round's D-F11 implementation) but DEPLOY.md §1b never documented this key — fixed in round 6's DEPLOY.md rewrite (JSON example + prose explanation of purpose/default/precedence added). No further action needed."
  - "v1.1 backlog (non-REQ improvement ideas, explicitly NOT gate blockers per the CONVERGENCE RULE — full detail in 08-validation.md's own 'v1.1 backlog' section): (1) give an aborted AgentRecord its own terminal-ish state instead of 'running' forever; (2) fix the litellm port-4000 collision hazard (derive port or pick dynamically) and/or stop the managed litellm subprocess on shutdown; (3) re-run VAL-003's tool-use clause against a larger/more capable local model or a paid provider with sandbox credentials; (4) once tool-use is resolved on some model, re-check the latent ClaudeAgentSdkGatewayClient cwd-not-per-run-workspace gap (currently masked); (5) clean up LiteLLMProxyManager's mkdtemp() temp config dirs."
notes: >-
  Gate 1 clarification done via 2 AskUserQuestion rounds; decisions D1–D9 recorded in 01-requirements.md.
  v1 = core engine (REQ-001..007, no auth, localhost); v2 = dashboard/sync/plugin/deploy (REQ-008..011);
  v3 = OAuth/OIDC (REQ-012). Tech stack: Node.js/TypeScript + Claude Agent SDK + embedded LiteLLM.
  USER DIRECTIVE: after Gate 2 passes, PAUSE for user architecture review before dispatching Gate 3.
